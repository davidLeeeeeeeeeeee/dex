/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import * as $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const pb = $root.pb = (() => {

    /**
     * Namespace pb.
     * @exports pb
     * @namespace
     */
    const pb = {};

    pb.Token = (function() {

        /**
         * Properties of a Token.
         * @memberof pb
         * @interface IToken
         * @property {string|null} [address] Token address
         * @property {string|null} [symbol] Token symbol
         * @property {string|null} [name] Token name
         * @property {string|null} [owner] Token owner
         * @property {string|null} [totalSupply] Token totalSupply
         * @property {boolean|null} [canMint] Token canMint
         */

        /**
         * Constructs a new Token.
         * @memberof pb
         * @classdesc Represents a Token.
         * @implements IToken
         * @constructor
         * @param {pb.IToken=} [properties] Properties to set
         */
        function Token(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Token address.
         * @member {string} address
         * @memberof pb.Token
         * @instance
         */
        Token.prototype.address = "";

        /**
         * Token symbol.
         * @member {string} symbol
         * @memberof pb.Token
         * @instance
         */
        Token.prototype.symbol = "";

        /**
         * Token name.
         * @member {string} name
         * @memberof pb.Token
         * @instance
         */
        Token.prototype.name = "";

        /**
         * Token owner.
         * @member {string} owner
         * @memberof pb.Token
         * @instance
         */
        Token.prototype.owner = "";

        /**
         * Token totalSupply.
         * @member {string} totalSupply
         * @memberof pb.Token
         * @instance
         */
        Token.prototype.totalSupply = "";

        /**
         * Token canMint.
         * @member {boolean} canMint
         * @memberof pb.Token
         * @instance
         */
        Token.prototype.canMint = false;

        /**
         * Creates a new Token instance using the specified properties.
         * @function create
         * @memberof pb.Token
         * @static
         * @param {pb.IToken=} [properties] Properties to set
         * @returns {pb.Token} Token instance
         */
        Token.create = function create(properties) {
            return new Token(properties);
        };

        /**
         * Encodes the specified Token message. Does not implicitly {@link pb.Token.verify|verify} messages.
         * @function encode
         * @memberof pb.Token
         * @static
         * @param {pb.IToken} message Token message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Token.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            if (message.symbol != null && Object.hasOwnProperty.call(message, "symbol"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.symbol);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.owner != null && Object.hasOwnProperty.call(message, "owner"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.owner);
            if (message.totalSupply != null && Object.hasOwnProperty.call(message, "totalSupply"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.totalSupply);
            if (message.canMint != null && Object.hasOwnProperty.call(message, "canMint"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.canMint);
            return writer;
        };

        /**
         * Encodes the specified Token message, length delimited. Does not implicitly {@link pb.Token.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.Token
         * @static
         * @param {pb.IToken} message Token message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Token.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Token message from the specified reader or buffer.
         * @function decode
         * @memberof pb.Token
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.Token} Token
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Token.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.Token();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.string();
                        break;
                    }
                case 2: {
                        message.symbol = reader.string();
                        break;
                    }
                case 3: {
                        message.name = reader.string();
                        break;
                    }
                case 4: {
                        message.owner = reader.string();
                        break;
                    }
                case 5: {
                        message.totalSupply = reader.string();
                        break;
                    }
                case 6: {
                        message.canMint = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Token message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.Token
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.Token} Token
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Token.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Token message.
         * @function verify
         * @memberof pb.Token
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Token.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                if (!$util.isString(message.symbol))
                    return "symbol: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.owner != null && message.hasOwnProperty("owner"))
                if (!$util.isString(message.owner))
                    return "owner: string expected";
            if (message.totalSupply != null && message.hasOwnProperty("totalSupply"))
                if (!$util.isString(message.totalSupply))
                    return "totalSupply: string expected";
            if (message.canMint != null && message.hasOwnProperty("canMint"))
                if (typeof message.canMint !== "boolean")
                    return "canMint: boolean expected";
            return null;
        };

        /**
         * Creates a Token message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.Token
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.Token} Token
         */
        Token.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.Token)
                return object;
            let message = new $root.pb.Token();
            if (object.address != null)
                message.address = String(object.address);
            if (object.symbol != null)
                message.symbol = String(object.symbol);
            if (object.name != null)
                message.name = String(object.name);
            if (object.owner != null)
                message.owner = String(object.owner);
            if (object.totalSupply != null)
                message.totalSupply = String(object.totalSupply);
            if (object.canMint != null)
                message.canMint = Boolean(object.canMint);
            return message;
        };

        /**
         * Creates a plain object from a Token message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.Token
         * @static
         * @param {pb.Token} message Token
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Token.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.address = "";
                object.symbol = "";
                object.name = "";
                object.owner = "";
                object.totalSupply = "";
                object.canMint = false;
            }
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.symbol != null && message.hasOwnProperty("symbol"))
                object.symbol = message.symbol;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.owner != null && message.hasOwnProperty("owner"))
                object.owner = message.owner;
            if (message.totalSupply != null && message.hasOwnProperty("totalSupply"))
                object.totalSupply = message.totalSupply;
            if (message.canMint != null && message.hasOwnProperty("canMint"))
                object.canMint = message.canMint;
            return object;
        };

        /**
         * Converts this Token to JSON.
         * @function toJSON
         * @memberof pb.Token
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Token.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Token
         * @function getTypeUrl
         * @memberof pb.Token
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Token.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.Token";
        };

        return Token;
    })();

    pb.TokenRegistry = (function() {

        /**
         * Properties of a TokenRegistry.
         * @memberof pb
         * @interface ITokenRegistry
         * @property {number|Long|null} [tokenCount] TokenRegistry tokenCount
         */

        /**
         * Constructs a new TokenRegistry.
         * @memberof pb
         * @classdesc Represents a TokenRegistry.
         * @implements ITokenRegistry
         * @constructor
         * @param {pb.ITokenRegistry=} [properties] Properties to set
         */
        function TokenRegistry(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TokenRegistry tokenCount.
         * @member {number|Long} tokenCount
         * @memberof pb.TokenRegistry
         * @instance
         */
        TokenRegistry.prototype.tokenCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new TokenRegistry instance using the specified properties.
         * @function create
         * @memberof pb.TokenRegistry
         * @static
         * @param {pb.ITokenRegistry=} [properties] Properties to set
         * @returns {pb.TokenRegistry} TokenRegistry instance
         */
        TokenRegistry.create = function create(properties) {
            return new TokenRegistry(properties);
        };

        /**
         * Encodes the specified TokenRegistry message. Does not implicitly {@link pb.TokenRegistry.verify|verify} messages.
         * @function encode
         * @memberof pb.TokenRegistry
         * @static
         * @param {pb.ITokenRegistry} message TokenRegistry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRegistry.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tokenCount != null && Object.hasOwnProperty.call(message, "tokenCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.tokenCount);
            return writer;
        };

        /**
         * Encodes the specified TokenRegistry message, length delimited. Does not implicitly {@link pb.TokenRegistry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.TokenRegistry
         * @static
         * @param {pb.ITokenRegistry} message TokenRegistry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRegistry.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TokenRegistry message from the specified reader or buffer.
         * @function decode
         * @memberof pb.TokenRegistry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.TokenRegistry} TokenRegistry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRegistry.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.TokenRegistry();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 2: {
                        message.tokenCount = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TokenRegistry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.TokenRegistry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.TokenRegistry} TokenRegistry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRegistry.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenRegistry message.
         * @function verify
         * @memberof pb.TokenRegistry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenRegistry.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.tokenCount != null && message.hasOwnProperty("tokenCount"))
                if (!$util.isInteger(message.tokenCount) && !(message.tokenCount && $util.isInteger(message.tokenCount.low) && $util.isInteger(message.tokenCount.high)))
                    return "tokenCount: integer|Long expected";
            return null;
        };

        /**
         * Creates a TokenRegistry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.TokenRegistry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.TokenRegistry} TokenRegistry
         */
        TokenRegistry.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.TokenRegistry)
                return object;
            let message = new $root.pb.TokenRegistry();
            if (object.tokenCount != null)
                if ($util.Long)
                    (message.tokenCount = $util.Long.fromValue(object.tokenCount)).unsigned = true;
                else if (typeof object.tokenCount === "string")
                    message.tokenCount = parseInt(object.tokenCount, 10);
                else if (typeof object.tokenCount === "number")
                    message.tokenCount = object.tokenCount;
                else if (typeof object.tokenCount === "object")
                    message.tokenCount = new $util.LongBits(object.tokenCount.low >>> 0, object.tokenCount.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a TokenRegistry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.TokenRegistry
         * @static
         * @param {pb.TokenRegistry} message TokenRegistry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenRegistry.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.tokenCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.tokenCount = options.longs === String ? "0" : 0;
            if (message.tokenCount != null && message.hasOwnProperty("tokenCount"))
                if (typeof message.tokenCount === "number")
                    object.tokenCount = options.longs === String ? String(message.tokenCount) : message.tokenCount;
                else
                    object.tokenCount = options.longs === String ? $util.Long.prototype.toString.call(message.tokenCount) : options.longs === Number ? new $util.LongBits(message.tokenCount.low >>> 0, message.tokenCount.high >>> 0).toNumber(true) : message.tokenCount;
            return object;
        };

        /**
         * Converts this TokenRegistry to JSON.
         * @function toJSON
         * @memberof pb.TokenRegistry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenRegistry.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TokenRegistry
         * @function getTypeUrl
         * @memberof pb.TokenRegistry
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TokenRegistry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.TokenRegistry";
        };

        return TokenRegistry;
    })();

    pb.TokenRecord = (function() {

        /**
         * Properties of a TokenRecord.
         * @memberof pb
         * @interface ITokenRecord
         * @property {pb.IToken|null} [token] TokenRecord token
         */

        /**
         * Constructs a new TokenRecord.
         * @memberof pb
         * @classdesc Represents a TokenRecord.
         * @implements ITokenRecord
         * @constructor
         * @param {pb.ITokenRecord=} [properties] Properties to set
         */
        function TokenRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TokenRecord token.
         * @member {pb.IToken|null|undefined} token
         * @memberof pb.TokenRecord
         * @instance
         */
        TokenRecord.prototype.token = null;

        /**
         * Creates a new TokenRecord instance using the specified properties.
         * @function create
         * @memberof pb.TokenRecord
         * @static
         * @param {pb.ITokenRecord=} [properties] Properties to set
         * @returns {pb.TokenRecord} TokenRecord instance
         */
        TokenRecord.create = function create(properties) {
            return new TokenRecord(properties);
        };

        /**
         * Encodes the specified TokenRecord message. Does not implicitly {@link pb.TokenRecord.verify|verify} messages.
         * @function encode
         * @memberof pb.TokenRecord
         * @static
         * @param {pb.ITokenRecord} message TokenRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                $root.pb.Token.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TokenRecord message, length delimited. Does not implicitly {@link pb.TokenRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.TokenRecord
         * @static
         * @param {pb.ITokenRecord} message TokenRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TokenRecord message from the specified reader or buffer.
         * @function decode
         * @memberof pb.TokenRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.TokenRecord} TokenRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRecord.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.TokenRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.token = $root.pb.Token.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TokenRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.TokenRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.TokenRecord} TokenRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenRecord message.
         * @function verify
         * @memberof pb.TokenRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenRecord.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.token != null && message.hasOwnProperty("token")) {
                let error = $root.pb.Token.verify(message.token);
                if (error)
                    return "token." + error;
            }
            return null;
        };

        /**
         * Creates a TokenRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.TokenRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.TokenRecord} TokenRecord
         */
        TokenRecord.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.TokenRecord)
                return object;
            let message = new $root.pb.TokenRecord();
            if (object.token != null) {
                if (typeof object.token !== "object")
                    throw TypeError(".pb.TokenRecord.token: object expected");
                message.token = $root.pb.Token.fromObject(object.token);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.TokenRecord
         * @static
         * @param {pb.TokenRecord} message TokenRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.token = null;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = $root.pb.Token.toObject(message.token, options);
            return object;
        };

        /**
         * Converts this TokenRecord to JSON.
         * @function toJSON
         * @memberof pb.TokenRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TokenRecord
         * @function getTypeUrl
         * @memberof pb.TokenRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TokenRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.TokenRecord";
        };

        return TokenRecord;
    })();

    /**
     * SignAlgo enum.
     * @name pb.SignAlgo
     * @enum {number}
     * @property {number} SIGN_ALGO_UNSPECIFIED=0 SIGN_ALGO_UNSPECIFIED value
     * @property {number} SIGN_ALGO_ECDSA_P256=1 SIGN_ALGO_ECDSA_P256 value
     * @property {number} SIGN_ALGO_BLS_BN256_G2=2 SIGN_ALGO_BLS_BN256_G2 value
     * @property {number} SIGN_ALGO_SCHNORR_SECP256K1_BIP340=3 SIGN_ALGO_SCHNORR_SECP256K1_BIP340 value
     * @property {number} SIGN_ALGO_SCHNORR_ALT_BN128=4 SIGN_ALGO_SCHNORR_ALT_BN128 value
     * @property {number} SIGN_ALGO_ED25519=5 SIGN_ALGO_ED25519 value
     * @property {number} SIGN_ALGO_ECDSA_SECP256K1=6 SIGN_ALGO_ECDSA_SECP256K1 value
     */
    pb.SignAlgo = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SIGN_ALGO_UNSPECIFIED"] = 0;
        values[valuesById[1] = "SIGN_ALGO_ECDSA_P256"] = 1;
        values[valuesById[2] = "SIGN_ALGO_BLS_BN256_G2"] = 2;
        values[valuesById[3] = "SIGN_ALGO_SCHNORR_SECP256K1_BIP340"] = 3;
        values[valuesById[4] = "SIGN_ALGO_SCHNORR_ALT_BN128"] = 4;
        values[valuesById[5] = "SIGN_ALGO_ED25519"] = 5;
        values[valuesById[6] = "SIGN_ALGO_ECDSA_SECP256K1"] = 6;
        return values;
    })();

    pb.PublicKeys = (function() {

        /**
         * Properties of a PublicKeys.
         * @memberof pb
         * @interface IPublicKeys
         * @property {Object.<string,Uint8Array>|null} [keys] PublicKeys keys
         */

        /**
         * Constructs a new PublicKeys.
         * @memberof pb
         * @classdesc Represents a PublicKeys.
         * @implements IPublicKeys
         * @constructor
         * @param {pb.IPublicKeys=} [properties] Properties to set
         */
        function PublicKeys(properties) {
            this.keys = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PublicKeys keys.
         * @member {Object.<string,Uint8Array>} keys
         * @memberof pb.PublicKeys
         * @instance
         */
        PublicKeys.prototype.keys = $util.emptyObject;

        /**
         * Creates a new PublicKeys instance using the specified properties.
         * @function create
         * @memberof pb.PublicKeys
         * @static
         * @param {pb.IPublicKeys=} [properties] Properties to set
         * @returns {pb.PublicKeys} PublicKeys instance
         */
        PublicKeys.create = function create(properties) {
            return new PublicKeys(properties);
        };

        /**
         * Encodes the specified PublicKeys message. Does not implicitly {@link pb.PublicKeys.verify|verify} messages.
         * @function encode
         * @memberof pb.PublicKeys
         * @static
         * @param {pb.IPublicKeys} message PublicKeys message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublicKeys.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.keys != null && Object.hasOwnProperty.call(message, "keys"))
                for (let keys = Object.keys(message.keys), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]).uint32(/* id 2, wireType 2 =*/18).bytes(message.keys[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified PublicKeys message, length delimited. Does not implicitly {@link pb.PublicKeys.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.PublicKeys
         * @static
         * @param {pb.IPublicKeys} message PublicKeys message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublicKeys.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PublicKeys message from the specified reader or buffer.
         * @function decode
         * @memberof pb.PublicKeys
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.PublicKeys} PublicKeys
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublicKeys.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.PublicKeys(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.keys === $util.emptyObject)
                            message.keys = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = 0;
                        value = [];
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.int32();
                                break;
                            case 2:
                                value = reader.bytes();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.keys[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PublicKeys message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.PublicKeys
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.PublicKeys} PublicKeys
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublicKeys.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PublicKeys message.
         * @function verify
         * @memberof pb.PublicKeys
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PublicKeys.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.keys != null && message.hasOwnProperty("keys")) {
                if (!$util.isObject(message.keys))
                    return "keys: object expected";
                let key = Object.keys(message.keys);
                for (let i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "keys: integer key{k:int32} expected";
                    if (!(message.keys[key[i]] && typeof message.keys[key[i]].length === "number" || $util.isString(message.keys[key[i]])))
                        return "keys: buffer{k:int32} expected";
                }
            }
            return null;
        };

        /**
         * Creates a PublicKeys message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.PublicKeys
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.PublicKeys} PublicKeys
         */
        PublicKeys.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.PublicKeys)
                return object;
            let message = new $root.pb.PublicKeys();
            if (object.keys) {
                if (typeof object.keys !== "object")
                    throw TypeError(".pb.PublicKeys.keys: object expected");
                message.keys = {};
                for (let keys = Object.keys(object.keys), i = 0; i < keys.length; ++i)
                    if (typeof object.keys[keys[i]] === "string")
                        $util.base64.decode(object.keys[keys[i]], message.keys[keys[i]] = $util.newBuffer($util.base64.length(object.keys[keys[i]])), 0);
                    else if (object.keys[keys[i]].length >= 0)
                        message.keys[keys[i]] = object.keys[keys[i]];
            }
            return message;
        };

        /**
         * Creates a plain object from a PublicKeys message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.PublicKeys
         * @static
         * @param {pb.PublicKeys} message PublicKeys
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PublicKeys.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.objects || options.defaults)
                object.keys = {};
            let keys2;
            if (message.keys && (keys2 = Object.keys(message.keys)).length) {
                object.keys = {};
                for (let j = 0; j < keys2.length; ++j)
                    object.keys[keys2[j]] = options.bytes === String ? $util.base64.encode(message.keys[keys2[j]], 0, message.keys[keys2[j]].length) : options.bytes === Array ? Array.prototype.slice.call(message.keys[keys2[j]]) : message.keys[keys2[j]];
            }
            return object;
        };

        /**
         * Converts this PublicKeys to JSON.
         * @function toJSON
         * @memberof pb.PublicKeys
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PublicKeys.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PublicKeys
         * @function getTypeUrl
         * @memberof pb.PublicKeys
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PublicKeys.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.PublicKeys";
        };

        return PublicKeys;
    })();

    pb.BaseMessage = (function() {

        /**
         * Properties of a BaseMessage.
         * @memberof pb
         * @interface IBaseMessage
         * @property {string|null} [txId] BaseMessage txId
         * @property {string|null} [fromAddress] BaseMessage fromAddress
         * @property {number|Long|null} [executedHeight] BaseMessage executedHeight
         * @property {pb.IPublicKeys|null} [publicKeys] BaseMessage publicKeys
         * @property {Uint8Array|null} [signature] BaseMessage signature
         * @property {string|null} [fee] BaseMessage fee
         * @property {pb.Status|null} [status] BaseMessage status
         * @property {number|Long|null} [nonce] BaseMessage nonce
         * @property {string|null} [statusText] BaseMessage statusText
         */

        /**
         * Constructs a new BaseMessage.
         * @memberof pb
         * @classdesc Represents a BaseMessage.
         * @implements IBaseMessage
         * @constructor
         * @param {pb.IBaseMessage=} [properties] Properties to set
         */
        function BaseMessage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BaseMessage txId.
         * @member {string} txId
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.txId = "";

        /**
         * BaseMessage fromAddress.
         * @member {string} fromAddress
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.fromAddress = "";

        /**
         * BaseMessage executedHeight.
         * @member {number|Long} executedHeight
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.executedHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BaseMessage publicKeys.
         * @member {pb.IPublicKeys|null|undefined} publicKeys
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.publicKeys = null;

        /**
         * BaseMessage signature.
         * @member {Uint8Array} signature
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.signature = $util.newBuffer([]);

        /**
         * BaseMessage fee.
         * @member {string} fee
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.fee = "";

        /**
         * BaseMessage status.
         * @member {pb.Status} status
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.status = 0;

        /**
         * BaseMessage nonce.
         * @member {number|Long} nonce
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.nonce = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BaseMessage statusText.
         * @member {string} statusText
         * @memberof pb.BaseMessage
         * @instance
         */
        BaseMessage.prototype.statusText = "";

        /**
         * Creates a new BaseMessage instance using the specified properties.
         * @function create
         * @memberof pb.BaseMessage
         * @static
         * @param {pb.IBaseMessage=} [properties] Properties to set
         * @returns {pb.BaseMessage} BaseMessage instance
         */
        BaseMessage.create = function create(properties) {
            return new BaseMessage(properties);
        };

        /**
         * Encodes the specified BaseMessage message. Does not implicitly {@link pb.BaseMessage.verify|verify} messages.
         * @function encode
         * @memberof pb.BaseMessage
         * @static
         * @param {pb.IBaseMessage} message BaseMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BaseMessage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.txId != null && Object.hasOwnProperty.call(message, "txId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txId);
            if (message.fromAddress != null && Object.hasOwnProperty.call(message, "fromAddress"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.fromAddress);
            if (message.executedHeight != null && Object.hasOwnProperty.call(message, "executedHeight"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.executedHeight);
            if (message.publicKeys != null && Object.hasOwnProperty.call(message, "publicKeys"))
                $root.pb.PublicKeys.encode(message.publicKeys, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.signature);
            if (message.fee != null && Object.hasOwnProperty.call(message, "fee"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.fee);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.status);
            if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.nonce);
            if (message.statusText != null && Object.hasOwnProperty.call(message, "statusText"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.statusText);
            return writer;
        };

        /**
         * Encodes the specified BaseMessage message, length delimited. Does not implicitly {@link pb.BaseMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.BaseMessage
         * @static
         * @param {pb.IBaseMessage} message BaseMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BaseMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BaseMessage message from the specified reader or buffer.
         * @function decode
         * @memberof pb.BaseMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.BaseMessage} BaseMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BaseMessage.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.BaseMessage();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.txId = reader.string();
                        break;
                    }
                case 2: {
                        message.fromAddress = reader.string();
                        break;
                    }
                case 3: {
                        message.executedHeight = reader.uint64();
                        break;
                    }
                case 4: {
                        message.publicKeys = $root.pb.PublicKeys.decode(reader, reader.uint32());
                        break;
                    }
                case 5: {
                        message.signature = reader.bytes();
                        break;
                    }
                case 6: {
                        message.fee = reader.string();
                        break;
                    }
                case 7: {
                        message.status = reader.int32();
                        break;
                    }
                case 8: {
                        message.nonce = reader.uint64();
                        break;
                    }
                case 9: {
                        message.statusText = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BaseMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.BaseMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.BaseMessage} BaseMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BaseMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BaseMessage message.
         * @function verify
         * @memberof pb.BaseMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BaseMessage.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.txId != null && message.hasOwnProperty("txId"))
                if (!$util.isString(message.txId))
                    return "txId: string expected";
            if (message.fromAddress != null && message.hasOwnProperty("fromAddress"))
                if (!$util.isString(message.fromAddress))
                    return "fromAddress: string expected";
            if (message.executedHeight != null && message.hasOwnProperty("executedHeight"))
                if (!$util.isInteger(message.executedHeight) && !(message.executedHeight && $util.isInteger(message.executedHeight.low) && $util.isInteger(message.executedHeight.high)))
                    return "executedHeight: integer|Long expected";
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys")) {
                let error = $root.pb.PublicKeys.verify(message.publicKeys);
                if (error)
                    return "publicKeys." + error;
            }
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            if (message.fee != null && message.hasOwnProperty("fee"))
                if (!$util.isString(message.fee))
                    return "fee: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.nonce != null && message.hasOwnProperty("nonce"))
                if (!$util.isInteger(message.nonce) && !(message.nonce && $util.isInteger(message.nonce.low) && $util.isInteger(message.nonce.high)))
                    return "nonce: integer|Long expected";
            if (message.statusText != null && message.hasOwnProperty("statusText"))
                if (!$util.isString(message.statusText))
                    return "statusText: string expected";
            return null;
        };

        /**
         * Creates a BaseMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.BaseMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.BaseMessage} BaseMessage
         */
        BaseMessage.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.BaseMessage)
                return object;
            let message = new $root.pb.BaseMessage();
            if (object.txId != null)
                message.txId = String(object.txId);
            if (object.fromAddress != null)
                message.fromAddress = String(object.fromAddress);
            if (object.executedHeight != null)
                if ($util.Long)
                    (message.executedHeight = $util.Long.fromValue(object.executedHeight)).unsigned = true;
                else if (typeof object.executedHeight === "string")
                    message.executedHeight = parseInt(object.executedHeight, 10);
                else if (typeof object.executedHeight === "number")
                    message.executedHeight = object.executedHeight;
                else if (typeof object.executedHeight === "object")
                    message.executedHeight = new $util.LongBits(object.executedHeight.low >>> 0, object.executedHeight.high >>> 0).toNumber(true);
            if (object.publicKeys != null) {
                if (typeof object.publicKeys !== "object")
                    throw TypeError(".pb.BaseMessage.publicKeys: object expected");
                message.publicKeys = $root.pb.PublicKeys.fromObject(object.publicKeys);
            }
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            if (object.fee != null)
                message.fee = String(object.fee);
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "PENDING":
            case 0:
                message.status = 0;
                break;
            case "FAILED":
            case 1:
                message.status = 1;
                break;
            case "SUCCEED":
            case 2:
                message.status = 2;
                break;
            }
            if (object.nonce != null)
                if ($util.Long)
                    (message.nonce = $util.Long.fromValue(object.nonce)).unsigned = true;
                else if (typeof object.nonce === "string")
                    message.nonce = parseInt(object.nonce, 10);
                else if (typeof object.nonce === "number")
                    message.nonce = object.nonce;
                else if (typeof object.nonce === "object")
                    message.nonce = new $util.LongBits(object.nonce.low >>> 0, object.nonce.high >>> 0).toNumber(true);
            if (object.statusText != null)
                message.statusText = String(object.statusText);
            return message;
        };

        /**
         * Creates a plain object from a BaseMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.BaseMessage
         * @static
         * @param {pb.BaseMessage} message BaseMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BaseMessage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.txId = "";
                object.fromAddress = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.executedHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.executedHeight = options.longs === String ? "0" : 0;
                object.publicKeys = null;
                if (options.bytes === String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== Array)
                        object.signature = $util.newBuffer(object.signature);
                }
                object.fee = "";
                object.status = options.enums === String ? "PENDING" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.nonce = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.nonce = options.longs === String ? "0" : 0;
                object.statusText = "";
            }
            if (message.txId != null && message.hasOwnProperty("txId"))
                object.txId = message.txId;
            if (message.fromAddress != null && message.hasOwnProperty("fromAddress"))
                object.fromAddress = message.fromAddress;
            if (message.executedHeight != null && message.hasOwnProperty("executedHeight"))
                if (typeof message.executedHeight === "number")
                    object.executedHeight = options.longs === String ? String(message.executedHeight) : message.executedHeight;
                else
                    object.executedHeight = options.longs === String ? $util.Long.prototype.toString.call(message.executedHeight) : options.longs === Number ? new $util.LongBits(message.executedHeight.low >>> 0, message.executedHeight.high >>> 0).toNumber(true) : message.executedHeight;
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys"))
                object.publicKeys = $root.pb.PublicKeys.toObject(message.publicKeys, options);
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            if (message.fee != null && message.hasOwnProperty("fee"))
                object.fee = message.fee;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.pb.Status[message.status] === undefined ? message.status : $root.pb.Status[message.status] : message.status;
            if (message.nonce != null && message.hasOwnProperty("nonce"))
                if (typeof message.nonce === "number")
                    object.nonce = options.longs === String ? String(message.nonce) : message.nonce;
                else
                    object.nonce = options.longs === String ? $util.Long.prototype.toString.call(message.nonce) : options.longs === Number ? new $util.LongBits(message.nonce.low >>> 0, message.nonce.high >>> 0).toNumber(true) : message.nonce;
            if (message.statusText != null && message.hasOwnProperty("statusText"))
                object.statusText = message.statusText;
            return object;
        };

        /**
         * Converts this BaseMessage to JSON.
         * @function toJSON
         * @memberof pb.BaseMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BaseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BaseMessage
         * @function getTypeUrl
         * @memberof pb.BaseMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BaseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.BaseMessage";
        };

        return BaseMessage;
    })();

    pb.Receipt = (function() {

        /**
         * Properties of a Receipt.
         * @memberof pb
         * @interface IReceipt
         * @property {string|null} [txId] Receipt txId
         * @property {string|null} [status] Receipt status
         * @property {string|null} [error] Receipt error
         * @property {number|Long|null} [blockHeight] Receipt blockHeight
         * @property {number|Long|null} [timestamp] Receipt timestamp
         * @property {number|Long|null} [gasUsed] Receipt gasUsed
         * @property {string|null} [fee] Receipt fee
         * @property {Array.<string>|null} [logs] Receipt logs
         */

        /**
         * Constructs a new Receipt.
         * @memberof pb
         * @classdesc Represents a Receipt.
         * @implements IReceipt
         * @constructor
         * @param {pb.IReceipt=} [properties] Properties to set
         */
        function Receipt(properties) {
            this.logs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Receipt txId.
         * @member {string} txId
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.txId = "";

        /**
         * Receipt status.
         * @member {string} status
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.status = "";

        /**
         * Receipt error.
         * @member {string} error
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.error = "";

        /**
         * Receipt blockHeight.
         * @member {number|Long} blockHeight
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.blockHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Receipt timestamp.
         * @member {number|Long} timestamp
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Receipt gasUsed.
         * @member {number|Long} gasUsed
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.gasUsed = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Receipt fee.
         * @member {string} fee
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.fee = "";

        /**
         * Receipt logs.
         * @member {Array.<string>} logs
         * @memberof pb.Receipt
         * @instance
         */
        Receipt.prototype.logs = $util.emptyArray;

        /**
         * Creates a new Receipt instance using the specified properties.
         * @function create
         * @memberof pb.Receipt
         * @static
         * @param {pb.IReceipt=} [properties] Properties to set
         * @returns {pb.Receipt} Receipt instance
         */
        Receipt.create = function create(properties) {
            return new Receipt(properties);
        };

        /**
         * Encodes the specified Receipt message. Does not implicitly {@link pb.Receipt.verify|verify} messages.
         * @function encode
         * @memberof pb.Receipt
         * @static
         * @param {pb.IReceipt} message Receipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Receipt.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.txId != null && Object.hasOwnProperty.call(message, "txId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txId);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.status);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.error);
            if (message.blockHeight != null && Object.hasOwnProperty.call(message, "blockHeight"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.blockHeight);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.timestamp);
            if (message.gasUsed != null && Object.hasOwnProperty.call(message, "gasUsed"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.gasUsed);
            if (message.fee != null && Object.hasOwnProperty.call(message, "fee"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.fee);
            if (message.logs != null && message.logs.length)
                for (let i = 0; i < message.logs.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.logs[i]);
            return writer;
        };

        /**
         * Encodes the specified Receipt message, length delimited. Does not implicitly {@link pb.Receipt.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.Receipt
         * @static
         * @param {pb.IReceipt} message Receipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Receipt.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Receipt message from the specified reader or buffer.
         * @function decode
         * @memberof pb.Receipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.Receipt} Receipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Receipt.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.Receipt();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.txId = reader.string();
                        break;
                    }
                case 2: {
                        message.status = reader.string();
                        break;
                    }
                case 3: {
                        message.error = reader.string();
                        break;
                    }
                case 4: {
                        message.blockHeight = reader.uint64();
                        break;
                    }
                case 5: {
                        message.timestamp = reader.int64();
                        break;
                    }
                case 6: {
                        message.gasUsed = reader.uint64();
                        break;
                    }
                case 7: {
                        message.fee = reader.string();
                        break;
                    }
                case 8: {
                        if (!(message.logs && message.logs.length))
                            message.logs = [];
                        message.logs.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Receipt message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.Receipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.Receipt} Receipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Receipt.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Receipt message.
         * @function verify
         * @memberof pb.Receipt
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Receipt.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.txId != null && message.hasOwnProperty("txId"))
                if (!$util.isString(message.txId))
                    return "txId: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            if (message.blockHeight != null && message.hasOwnProperty("blockHeight"))
                if (!$util.isInteger(message.blockHeight) && !(message.blockHeight && $util.isInteger(message.blockHeight.low) && $util.isInteger(message.blockHeight.high)))
                    return "blockHeight: integer|Long expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.gasUsed != null && message.hasOwnProperty("gasUsed"))
                if (!$util.isInteger(message.gasUsed) && !(message.gasUsed && $util.isInteger(message.gasUsed.low) && $util.isInteger(message.gasUsed.high)))
                    return "gasUsed: integer|Long expected";
            if (message.fee != null && message.hasOwnProperty("fee"))
                if (!$util.isString(message.fee))
                    return "fee: string expected";
            if (message.logs != null && message.hasOwnProperty("logs")) {
                if (!Array.isArray(message.logs))
                    return "logs: array expected";
                for (let i = 0; i < message.logs.length; ++i)
                    if (!$util.isString(message.logs[i]))
                        return "logs: string[] expected";
            }
            return null;
        };

        /**
         * Creates a Receipt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.Receipt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.Receipt} Receipt
         */
        Receipt.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.Receipt)
                return object;
            let message = new $root.pb.Receipt();
            if (object.txId != null)
                message.txId = String(object.txId);
            if (object.status != null)
                message.status = String(object.status);
            if (object.error != null)
                message.error = String(object.error);
            if (object.blockHeight != null)
                if ($util.Long)
                    (message.blockHeight = $util.Long.fromValue(object.blockHeight)).unsigned = true;
                else if (typeof object.blockHeight === "string")
                    message.blockHeight = parseInt(object.blockHeight, 10);
                else if (typeof object.blockHeight === "number")
                    message.blockHeight = object.blockHeight;
                else if (typeof object.blockHeight === "object")
                    message.blockHeight = new $util.LongBits(object.blockHeight.low >>> 0, object.blockHeight.high >>> 0).toNumber(true);
            if (object.timestamp != null)
                if ($util.Long)
                    (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = false;
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            if (object.gasUsed != null)
                if ($util.Long)
                    (message.gasUsed = $util.Long.fromValue(object.gasUsed)).unsigned = true;
                else if (typeof object.gasUsed === "string")
                    message.gasUsed = parseInt(object.gasUsed, 10);
                else if (typeof object.gasUsed === "number")
                    message.gasUsed = object.gasUsed;
                else if (typeof object.gasUsed === "object")
                    message.gasUsed = new $util.LongBits(object.gasUsed.low >>> 0, object.gasUsed.high >>> 0).toNumber(true);
            if (object.fee != null)
                message.fee = String(object.fee);
            if (object.logs) {
                if (!Array.isArray(object.logs))
                    throw TypeError(".pb.Receipt.logs: array expected");
                message.logs = [];
                for (let i = 0; i < object.logs.length; ++i)
                    message.logs[i] = String(object.logs[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a Receipt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.Receipt
         * @static
         * @param {pb.Receipt} message Receipt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Receipt.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.logs = [];
            if (options.defaults) {
                object.txId = "";
                object.status = "";
                object.error = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.blockHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.blockHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.gasUsed = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.gasUsed = options.longs === String ? "0" : 0;
                object.fee = "";
            }
            if (message.txId != null && message.hasOwnProperty("txId"))
                object.txId = message.txId;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            if (message.blockHeight != null && message.hasOwnProperty("blockHeight"))
                if (typeof message.blockHeight === "number")
                    object.blockHeight = options.longs === String ? String(message.blockHeight) : message.blockHeight;
                else
                    object.blockHeight = options.longs === String ? $util.Long.prototype.toString.call(message.blockHeight) : options.longs === Number ? new $util.LongBits(message.blockHeight.low >>> 0, message.blockHeight.high >>> 0).toNumber(true) : message.blockHeight;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            if (message.gasUsed != null && message.hasOwnProperty("gasUsed"))
                if (typeof message.gasUsed === "number")
                    object.gasUsed = options.longs === String ? String(message.gasUsed) : message.gasUsed;
                else
                    object.gasUsed = options.longs === String ? $util.Long.prototype.toString.call(message.gasUsed) : options.longs === Number ? new $util.LongBits(message.gasUsed.low >>> 0, message.gasUsed.high >>> 0).toNumber(true) : message.gasUsed;
            if (message.fee != null && message.hasOwnProperty("fee"))
                object.fee = message.fee;
            if (message.logs && message.logs.length) {
                object.logs = [];
                for (let j = 0; j < message.logs.length; ++j)
                    object.logs[j] = message.logs[j];
            }
            return object;
        };

        /**
         * Converts this Receipt to JSON.
         * @function toJSON
         * @memberof pb.Receipt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Receipt.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Receipt
         * @function getTypeUrl
         * @memberof pb.Receipt
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Receipt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.Receipt";
        };

        return Receipt;
    })();

    pb.GetReceiptResponse = (function() {

        /**
         * Properties of a GetReceiptResponse.
         * @memberof pb
         * @interface IGetReceiptResponse
         * @property {pb.IReceipt|null} [receipt] GetReceiptResponse receipt
         * @property {string|null} [error] GetReceiptResponse error
         */

        /**
         * Constructs a new GetReceiptResponse.
         * @memberof pb
         * @classdesc Represents a GetReceiptResponse.
         * @implements IGetReceiptResponse
         * @constructor
         * @param {pb.IGetReceiptResponse=} [properties] Properties to set
         */
        function GetReceiptResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetReceiptResponse receipt.
         * @member {pb.IReceipt|null|undefined} receipt
         * @memberof pb.GetReceiptResponse
         * @instance
         */
        GetReceiptResponse.prototype.receipt = null;

        /**
         * GetReceiptResponse error.
         * @member {string} error
         * @memberof pb.GetReceiptResponse
         * @instance
         */
        GetReceiptResponse.prototype.error = "";

        /**
         * Creates a new GetReceiptResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {pb.IGetReceiptResponse=} [properties] Properties to set
         * @returns {pb.GetReceiptResponse} GetReceiptResponse instance
         */
        GetReceiptResponse.create = function create(properties) {
            return new GetReceiptResponse(properties);
        };

        /**
         * Encodes the specified GetReceiptResponse message. Does not implicitly {@link pb.GetReceiptResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {pb.IGetReceiptResponse} message GetReceiptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetReceiptResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.receipt != null && Object.hasOwnProperty.call(message, "receipt"))
                $root.pb.Receipt.encode(message.receipt, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified GetReceiptResponse message, length delimited. Does not implicitly {@link pb.GetReceiptResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {pb.IGetReceiptResponse} message GetReceiptResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetReceiptResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetReceiptResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetReceiptResponse} GetReceiptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetReceiptResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetReceiptResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.receipt = $root.pb.Receipt.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetReceiptResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetReceiptResponse} GetReceiptResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetReceiptResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetReceiptResponse message.
         * @function verify
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetReceiptResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.receipt != null && message.hasOwnProperty("receipt")) {
                let error = $root.pb.Receipt.verify(message.receipt);
                if (error)
                    return "receipt." + error;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a GetReceiptResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetReceiptResponse} GetReceiptResponse
         */
        GetReceiptResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetReceiptResponse)
                return object;
            let message = new $root.pb.GetReceiptResponse();
            if (object.receipt != null) {
                if (typeof object.receipt !== "object")
                    throw TypeError(".pb.GetReceiptResponse.receipt: object expected");
                message.receipt = $root.pb.Receipt.fromObject(object.receipt);
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a GetReceiptResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {pb.GetReceiptResponse} message GetReceiptResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetReceiptResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.receipt = null;
                object.error = "";
            }
            if (message.receipt != null && message.hasOwnProperty("receipt"))
                object.receipt = $root.pb.Receipt.toObject(message.receipt, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this GetReceiptResponse to JSON.
         * @function toJSON
         * @memberof pb.GetReceiptResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetReceiptResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetReceiptResponse
         * @function getTypeUrl
         * @memberof pb.GetReceiptResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetReceiptResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetReceiptResponse";
        };

        return GetReceiptResponse;
    })();

    pb.Account = (function() {

        /**
         * Properties of an Account.
         * @memberof pb
         * @interface IAccount
         * @property {string|null} [address] Account address
         * @property {pb.IPublicKeys|null} [publicKeys] Account publicKeys
         * @property {number|Long|null} [nonce] Account nonce
         * @property {string|null} [unclaimedReward] Account unclaimedReward
         * @property {string|null} [lastAccReward] Account lastAccReward
         * @property {boolean|null} [isMiner] Account isMiner
         * @property {string|null} [ip] Account ip
         * @property {number|Long|null} [index] Account index
         * @property {string|null} [code] Account code
         */

        /**
         * Constructs a new Account.
         * @memberof pb
         * @classdesc Represents an Account.
         * @implements IAccount
         * @constructor
         * @param {pb.IAccount=} [properties] Properties to set
         */
        function Account(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Account address.
         * @member {string} address
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.address = "";

        /**
         * Account publicKeys.
         * @member {pb.IPublicKeys|null|undefined} publicKeys
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.publicKeys = null;

        /**
         * Account nonce.
         * @member {number|Long} nonce
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.nonce = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Account unclaimedReward.
         * @member {string} unclaimedReward
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.unclaimedReward = "";

        /**
         * Account lastAccReward.
         * @member {string} lastAccReward
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.lastAccReward = "";

        /**
         * Account isMiner.
         * @member {boolean} isMiner
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.isMiner = false;

        /**
         * Account ip.
         * @member {string} ip
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.ip = "";

        /**
         * Account index.
         * @member {number|Long} index
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.index = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Account code.
         * @member {string} code
         * @memberof pb.Account
         * @instance
         */
        Account.prototype.code = "";

        /**
         * Creates a new Account instance using the specified properties.
         * @function create
         * @memberof pb.Account
         * @static
         * @param {pb.IAccount=} [properties] Properties to set
         * @returns {pb.Account} Account instance
         */
        Account.create = function create(properties) {
            return new Account(properties);
        };

        /**
         * Encodes the specified Account message. Does not implicitly {@link pb.Account.verify|verify} messages.
         * @function encode
         * @memberof pb.Account
         * @static
         * @param {pb.IAccount} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            if (message.publicKeys != null && Object.hasOwnProperty.call(message, "publicKeys"))
                $root.pb.PublicKeys.encode(message.publicKeys, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.nonce);
            if (message.unclaimedReward != null && Object.hasOwnProperty.call(message, "unclaimedReward"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.unclaimedReward);
            if (message.lastAccReward != null && Object.hasOwnProperty.call(message, "lastAccReward"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.lastAccReward);
            if (message.isMiner != null && Object.hasOwnProperty.call(message, "isMiner"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.isMiner);
            if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.ip);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.index);
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.code);
            return writer;
        };

        /**
         * Encodes the specified Account message, length delimited. Does not implicitly {@link pb.Account.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.Account
         * @static
         * @param {pb.IAccount} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an Account message from the specified reader or buffer.
         * @function decode
         * @memberof pb.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.Account} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.Account();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.string();
                        break;
                    }
                case 2: {
                        message.publicKeys = $root.pb.PublicKeys.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.nonce = reader.uint64();
                        break;
                    }
                case 8: {
                        message.unclaimedReward = reader.string();
                        break;
                    }
                case 9: {
                        message.lastAccReward = reader.string();
                        break;
                    }
                case 10: {
                        message.isMiner = reader.bool();
                        break;
                    }
                case 11: {
                        message.ip = reader.string();
                        break;
                    }
                case 12: {
                        message.index = reader.uint64();
                        break;
                    }
                case 13: {
                        message.code = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Account message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.Account} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Account message.
         * @function verify
         * @memberof pb.Account
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Account.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys")) {
                let error = $root.pb.PublicKeys.verify(message.publicKeys);
                if (error)
                    return "publicKeys." + error;
            }
            if (message.nonce != null && message.hasOwnProperty("nonce"))
                if (!$util.isInteger(message.nonce) && !(message.nonce && $util.isInteger(message.nonce.low) && $util.isInteger(message.nonce.high)))
                    return "nonce: integer|Long expected";
            if (message.unclaimedReward != null && message.hasOwnProperty("unclaimedReward"))
                if (!$util.isString(message.unclaimedReward))
                    return "unclaimedReward: string expected";
            if (message.lastAccReward != null && message.hasOwnProperty("lastAccReward"))
                if (!$util.isString(message.lastAccReward))
                    return "lastAccReward: string expected";
            if (message.isMiner != null && message.hasOwnProperty("isMiner"))
                if (typeof message.isMiner !== "boolean")
                    return "isMiner: boolean expected";
            if (message.ip != null && message.hasOwnProperty("ip"))
                if (!$util.isString(message.ip))
                    return "ip: string expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index) && !(message.index && $util.isInteger(message.index.low) && $util.isInteger(message.index.high)))
                    return "index: integer|Long expected";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            return null;
        };

        /**
         * Creates an Account message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.Account
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.Account} Account
         */
        Account.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.Account)
                return object;
            let message = new $root.pb.Account();
            if (object.address != null)
                message.address = String(object.address);
            if (object.publicKeys != null) {
                if (typeof object.publicKeys !== "object")
                    throw TypeError(".pb.Account.publicKeys: object expected");
                message.publicKeys = $root.pb.PublicKeys.fromObject(object.publicKeys);
            }
            if (object.nonce != null)
                if ($util.Long)
                    (message.nonce = $util.Long.fromValue(object.nonce)).unsigned = true;
                else if (typeof object.nonce === "string")
                    message.nonce = parseInt(object.nonce, 10);
                else if (typeof object.nonce === "number")
                    message.nonce = object.nonce;
                else if (typeof object.nonce === "object")
                    message.nonce = new $util.LongBits(object.nonce.low >>> 0, object.nonce.high >>> 0).toNumber(true);
            if (object.unclaimedReward != null)
                message.unclaimedReward = String(object.unclaimedReward);
            if (object.lastAccReward != null)
                message.lastAccReward = String(object.lastAccReward);
            if (object.isMiner != null)
                message.isMiner = Boolean(object.isMiner);
            if (object.ip != null)
                message.ip = String(object.ip);
            if (object.index != null)
                if ($util.Long)
                    (message.index = $util.Long.fromValue(object.index)).unsigned = true;
                else if (typeof object.index === "string")
                    message.index = parseInt(object.index, 10);
                else if (typeof object.index === "number")
                    message.index = object.index;
                else if (typeof object.index === "object")
                    message.index = new $util.LongBits(object.index.low >>> 0, object.index.high >>> 0).toNumber(true);
            if (object.code != null)
                message.code = String(object.code);
            return message;
        };

        /**
         * Creates a plain object from an Account message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.Account
         * @static
         * @param {pb.Account} message Account
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Account.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.address = "";
                object.publicKeys = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.nonce = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.nonce = options.longs === String ? "0" : 0;
                object.unclaimedReward = "";
                object.lastAccReward = "";
                object.isMiner = false;
                object.ip = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.index = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.index = options.longs === String ? "0" : 0;
                object.code = "";
            }
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys"))
                object.publicKeys = $root.pb.PublicKeys.toObject(message.publicKeys, options);
            if (message.nonce != null && message.hasOwnProperty("nonce"))
                if (typeof message.nonce === "number")
                    object.nonce = options.longs === String ? String(message.nonce) : message.nonce;
                else
                    object.nonce = options.longs === String ? $util.Long.prototype.toString.call(message.nonce) : options.longs === Number ? new $util.LongBits(message.nonce.low >>> 0, message.nonce.high >>> 0).toNumber(true) : message.nonce;
            if (message.unclaimedReward != null && message.hasOwnProperty("unclaimedReward"))
                object.unclaimedReward = message.unclaimedReward;
            if (message.lastAccReward != null && message.hasOwnProperty("lastAccReward"))
                object.lastAccReward = message.lastAccReward;
            if (message.isMiner != null && message.hasOwnProperty("isMiner"))
                object.isMiner = message.isMiner;
            if (message.ip != null && message.hasOwnProperty("ip"))
                object.ip = message.ip;
            if (message.index != null && message.hasOwnProperty("index"))
                if (typeof message.index === "number")
                    object.index = options.longs === String ? String(message.index) : message.index;
                else
                    object.index = options.longs === String ? $util.Long.prototype.toString.call(message.index) : options.longs === Number ? new $util.LongBits(message.index.low >>> 0, message.index.high >>> 0).toNumber(true) : message.index;
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            return object;
        };

        /**
         * Converts this Account to JSON.
         * @function toJSON
         * @memberof pb.Account
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Account.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Account
         * @function getTypeUrl
         * @memberof pb.Account
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Account.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.Account";
        };

        return Account;
    })();

    pb.AccountOrders = (function() {

        /**
         * Properties of an AccountOrders.
         * @memberof pb
         * @interface IAccountOrders
         * @property {Array.<string>|null} [orders] AccountOrders orders
         */

        /**
         * Constructs a new AccountOrders.
         * @memberof pb
         * @classdesc Represents an AccountOrders.
         * @implements IAccountOrders
         * @constructor
         * @param {pb.IAccountOrders=} [properties] Properties to set
         */
        function AccountOrders(properties) {
            this.orders = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AccountOrders orders.
         * @member {Array.<string>} orders
         * @memberof pb.AccountOrders
         * @instance
         */
        AccountOrders.prototype.orders = $util.emptyArray;

        /**
         * Creates a new AccountOrders instance using the specified properties.
         * @function create
         * @memberof pb.AccountOrders
         * @static
         * @param {pb.IAccountOrders=} [properties] Properties to set
         * @returns {pb.AccountOrders} AccountOrders instance
         */
        AccountOrders.create = function create(properties) {
            return new AccountOrders(properties);
        };

        /**
         * Encodes the specified AccountOrders message. Does not implicitly {@link pb.AccountOrders.verify|verify} messages.
         * @function encode
         * @memberof pb.AccountOrders
         * @static
         * @param {pb.IAccountOrders} message AccountOrders message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountOrders.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.orders != null && message.orders.length)
                for (let i = 0; i < message.orders.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.orders[i]);
            return writer;
        };

        /**
         * Encodes the specified AccountOrders message, length delimited. Does not implicitly {@link pb.AccountOrders.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.AccountOrders
         * @static
         * @param {pb.IAccountOrders} message AccountOrders message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountOrders.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AccountOrders message from the specified reader or buffer.
         * @function decode
         * @memberof pb.AccountOrders
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.AccountOrders} AccountOrders
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountOrders.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.AccountOrders();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.orders && message.orders.length))
                            message.orders = [];
                        message.orders.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AccountOrders message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.AccountOrders
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.AccountOrders} AccountOrders
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountOrders.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AccountOrders message.
         * @function verify
         * @memberof pb.AccountOrders
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AccountOrders.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.orders != null && message.hasOwnProperty("orders")) {
                if (!Array.isArray(message.orders))
                    return "orders: array expected";
                for (let i = 0; i < message.orders.length; ++i)
                    if (!$util.isString(message.orders[i]))
                        return "orders: string[] expected";
            }
            return null;
        };

        /**
         * Creates an AccountOrders message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.AccountOrders
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.AccountOrders} AccountOrders
         */
        AccountOrders.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.AccountOrders)
                return object;
            let message = new $root.pb.AccountOrders();
            if (object.orders) {
                if (!Array.isArray(object.orders))
                    throw TypeError(".pb.AccountOrders.orders: array expected");
                message.orders = [];
                for (let i = 0; i < object.orders.length; ++i)
                    message.orders[i] = String(object.orders[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from an AccountOrders message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.AccountOrders
         * @static
         * @param {pb.AccountOrders} message AccountOrders
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountOrders.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.orders = [];
            if (message.orders && message.orders.length) {
                object.orders = [];
                for (let j = 0; j < message.orders.length; ++j)
                    object.orders[j] = message.orders[j];
            }
            return object;
        };

        /**
         * Converts this AccountOrders to JSON.
         * @function toJSON
         * @memberof pb.AccountOrders
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountOrders.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AccountOrders
         * @function getTypeUrl
         * @memberof pb.AccountOrders
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AccountOrders.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.AccountOrders";
        };

        return AccountOrders;
    })();

    pb.TokenBalanceRecord = (function() {

        /**
         * Properties of a TokenBalanceRecord.
         * @memberof pb
         * @interface ITokenBalanceRecord
         * @property {string|null} [address] TokenBalanceRecord address
         * @property {string|null} [token] TokenBalanceRecord token
         * @property {pb.ITokenBalance|null} [balance] TokenBalanceRecord balance
         */

        /**
         * Constructs a new TokenBalanceRecord.
         * @memberof pb
         * @classdesc Represents a TokenBalanceRecord.
         * @implements ITokenBalanceRecord
         * @constructor
         * @param {pb.ITokenBalanceRecord=} [properties] Properties to set
         */
        function TokenBalanceRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TokenBalanceRecord address.
         * @member {string} address
         * @memberof pb.TokenBalanceRecord
         * @instance
         */
        TokenBalanceRecord.prototype.address = "";

        /**
         * TokenBalanceRecord token.
         * @member {string} token
         * @memberof pb.TokenBalanceRecord
         * @instance
         */
        TokenBalanceRecord.prototype.token = "";

        /**
         * TokenBalanceRecord balance.
         * @member {pb.ITokenBalance|null|undefined} balance
         * @memberof pb.TokenBalanceRecord
         * @instance
         */
        TokenBalanceRecord.prototype.balance = null;

        /**
         * Creates a new TokenBalanceRecord instance using the specified properties.
         * @function create
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {pb.ITokenBalanceRecord=} [properties] Properties to set
         * @returns {pb.TokenBalanceRecord} TokenBalanceRecord instance
         */
        TokenBalanceRecord.create = function create(properties) {
            return new TokenBalanceRecord(properties);
        };

        /**
         * Encodes the specified TokenBalanceRecord message. Does not implicitly {@link pb.TokenBalanceRecord.verify|verify} messages.
         * @function encode
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {pb.ITokenBalanceRecord} message TokenBalanceRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalanceRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
            if (message.balance != null && Object.hasOwnProperty.call(message, "balance"))
                $root.pb.TokenBalance.encode(message.balance, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TokenBalanceRecord message, length delimited. Does not implicitly {@link pb.TokenBalanceRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {pb.ITokenBalanceRecord} message TokenBalanceRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalanceRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TokenBalanceRecord message from the specified reader or buffer.
         * @function decode
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.TokenBalanceRecord} TokenBalanceRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalanceRecord.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.TokenBalanceRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.string();
                        break;
                    }
                case 2: {
                        message.token = reader.string();
                        break;
                    }
                case 3: {
                        message.balance = $root.pb.TokenBalance.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TokenBalanceRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.TokenBalanceRecord} TokenBalanceRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalanceRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenBalanceRecord message.
         * @function verify
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenBalanceRecord.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.balance != null && message.hasOwnProperty("balance")) {
                let error = $root.pb.TokenBalance.verify(message.balance);
                if (error)
                    return "balance." + error;
            }
            return null;
        };

        /**
         * Creates a TokenBalanceRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.TokenBalanceRecord} TokenBalanceRecord
         */
        TokenBalanceRecord.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.TokenBalanceRecord)
                return object;
            let message = new $root.pb.TokenBalanceRecord();
            if (object.address != null)
                message.address = String(object.address);
            if (object.token != null)
                message.token = String(object.token);
            if (object.balance != null) {
                if (typeof object.balance !== "object")
                    throw TypeError(".pb.TokenBalanceRecord.balance: object expected");
                message.balance = $root.pb.TokenBalance.fromObject(object.balance);
            }
            return message;
        };

        /**
         * Creates a plain object from a TokenBalanceRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {pb.TokenBalanceRecord} message TokenBalanceRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenBalanceRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.address = "";
                object.token = "";
                object.balance = null;
            }
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.balance != null && message.hasOwnProperty("balance"))
                object.balance = $root.pb.TokenBalance.toObject(message.balance, options);
            return object;
        };

        /**
         * Converts this TokenBalanceRecord to JSON.
         * @function toJSON
         * @memberof pb.TokenBalanceRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenBalanceRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TokenBalanceRecord
         * @function getTypeUrl
         * @memberof pb.TokenBalanceRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TokenBalanceRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.TokenBalanceRecord";
        };

        return TokenBalanceRecord;
    })();

    pb.TokenBalance = (function() {

        /**
         * Properties of a TokenBalance.
         * @memberof pb
         * @interface ITokenBalance
         * @property {string|null} [balance] TokenBalance balance
         * @property {string|null} [minerLockedBalance] TokenBalance minerLockedBalance
         * @property {string|null} [liquidLockedBalance] TokenBalance liquidLockedBalance
         * @property {string|null} [witnessLockedBalance] TokenBalance witnessLockedBalance
         * @property {string|null} [leverageLockedBalance] TokenBalance leverageLockedBalance
         * @property {string|null} [orderFrozenBalance] TokenBalance orderFrozenBalance
         */

        /**
         * Constructs a new TokenBalance.
         * @memberof pb
         * @classdesc Represents a TokenBalance.
         * @implements ITokenBalance
         * @constructor
         * @param {pb.ITokenBalance=} [properties] Properties to set
         */
        function TokenBalance(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TokenBalance balance.
         * @member {string} balance
         * @memberof pb.TokenBalance
         * @instance
         */
        TokenBalance.prototype.balance = "";

        /**
         * TokenBalance minerLockedBalance.
         * @member {string} minerLockedBalance
         * @memberof pb.TokenBalance
         * @instance
         */
        TokenBalance.prototype.minerLockedBalance = "";

        /**
         * TokenBalance liquidLockedBalance.
         * @member {string} liquidLockedBalance
         * @memberof pb.TokenBalance
         * @instance
         */
        TokenBalance.prototype.liquidLockedBalance = "";

        /**
         * TokenBalance witnessLockedBalance.
         * @member {string} witnessLockedBalance
         * @memberof pb.TokenBalance
         * @instance
         */
        TokenBalance.prototype.witnessLockedBalance = "";

        /**
         * TokenBalance leverageLockedBalance.
         * @member {string} leverageLockedBalance
         * @memberof pb.TokenBalance
         * @instance
         */
        TokenBalance.prototype.leverageLockedBalance = "";

        /**
         * TokenBalance orderFrozenBalance.
         * @member {string} orderFrozenBalance
         * @memberof pb.TokenBalance
         * @instance
         */
        TokenBalance.prototype.orderFrozenBalance = "";

        /**
         * Creates a new TokenBalance instance using the specified properties.
         * @function create
         * @memberof pb.TokenBalance
         * @static
         * @param {pb.ITokenBalance=} [properties] Properties to set
         * @returns {pb.TokenBalance} TokenBalance instance
         */
        TokenBalance.create = function create(properties) {
            return new TokenBalance(properties);
        };

        /**
         * Encodes the specified TokenBalance message. Does not implicitly {@link pb.TokenBalance.verify|verify} messages.
         * @function encode
         * @memberof pb.TokenBalance
         * @static
         * @param {pb.ITokenBalance} message TokenBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalance.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.balance != null && Object.hasOwnProperty.call(message, "balance"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.balance);
            if (message.minerLockedBalance != null && Object.hasOwnProperty.call(message, "minerLockedBalance"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.minerLockedBalance);
            if (message.liquidLockedBalance != null && Object.hasOwnProperty.call(message, "liquidLockedBalance"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.liquidLockedBalance);
            if (message.witnessLockedBalance != null && Object.hasOwnProperty.call(message, "witnessLockedBalance"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.witnessLockedBalance);
            if (message.leverageLockedBalance != null && Object.hasOwnProperty.call(message, "leverageLockedBalance"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.leverageLockedBalance);
            if (message.orderFrozenBalance != null && Object.hasOwnProperty.call(message, "orderFrozenBalance"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.orderFrozenBalance);
            return writer;
        };

        /**
         * Encodes the specified TokenBalance message, length delimited. Does not implicitly {@link pb.TokenBalance.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.TokenBalance
         * @static
         * @param {pb.ITokenBalance} message TokenBalance message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TokenBalance.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TokenBalance message from the specified reader or buffer.
         * @function decode
         * @memberof pb.TokenBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.TokenBalance} TokenBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalance.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.TokenBalance();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.balance = reader.string();
                        break;
                    }
                case 3: {
                        message.minerLockedBalance = reader.string();
                        break;
                    }
                case 4: {
                        message.liquidLockedBalance = reader.string();
                        break;
                    }
                case 5: {
                        message.witnessLockedBalance = reader.string();
                        break;
                    }
                case 6: {
                        message.leverageLockedBalance = reader.string();
                        break;
                    }
                case 7: {
                        message.orderFrozenBalance = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TokenBalance message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.TokenBalance
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.TokenBalance} TokenBalance
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TokenBalance.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TokenBalance message.
         * @function verify
         * @memberof pb.TokenBalance
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TokenBalance.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.balance != null && message.hasOwnProperty("balance"))
                if (!$util.isString(message.balance))
                    return "balance: string expected";
            if (message.minerLockedBalance != null && message.hasOwnProperty("minerLockedBalance"))
                if (!$util.isString(message.minerLockedBalance))
                    return "minerLockedBalance: string expected";
            if (message.liquidLockedBalance != null && message.hasOwnProperty("liquidLockedBalance"))
                if (!$util.isString(message.liquidLockedBalance))
                    return "liquidLockedBalance: string expected";
            if (message.witnessLockedBalance != null && message.hasOwnProperty("witnessLockedBalance"))
                if (!$util.isString(message.witnessLockedBalance))
                    return "witnessLockedBalance: string expected";
            if (message.leverageLockedBalance != null && message.hasOwnProperty("leverageLockedBalance"))
                if (!$util.isString(message.leverageLockedBalance))
                    return "leverageLockedBalance: string expected";
            if (message.orderFrozenBalance != null && message.hasOwnProperty("orderFrozenBalance"))
                if (!$util.isString(message.orderFrozenBalance))
                    return "orderFrozenBalance: string expected";
            return null;
        };

        /**
         * Creates a TokenBalance message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.TokenBalance
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.TokenBalance} TokenBalance
         */
        TokenBalance.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.TokenBalance)
                return object;
            let message = new $root.pb.TokenBalance();
            if (object.balance != null)
                message.balance = String(object.balance);
            if (object.minerLockedBalance != null)
                message.minerLockedBalance = String(object.minerLockedBalance);
            if (object.liquidLockedBalance != null)
                message.liquidLockedBalance = String(object.liquidLockedBalance);
            if (object.witnessLockedBalance != null)
                message.witnessLockedBalance = String(object.witnessLockedBalance);
            if (object.leverageLockedBalance != null)
                message.leverageLockedBalance = String(object.leverageLockedBalance);
            if (object.orderFrozenBalance != null)
                message.orderFrozenBalance = String(object.orderFrozenBalance);
            return message;
        };

        /**
         * Creates a plain object from a TokenBalance message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.TokenBalance
         * @static
         * @param {pb.TokenBalance} message TokenBalance
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TokenBalance.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.balance = "";
                object.minerLockedBalance = "";
                object.liquidLockedBalance = "";
                object.witnessLockedBalance = "";
                object.leverageLockedBalance = "";
                object.orderFrozenBalance = "";
            }
            if (message.balance != null && message.hasOwnProperty("balance"))
                object.balance = message.balance;
            if (message.minerLockedBalance != null && message.hasOwnProperty("minerLockedBalance"))
                object.minerLockedBalance = message.minerLockedBalance;
            if (message.liquidLockedBalance != null && message.hasOwnProperty("liquidLockedBalance"))
                object.liquidLockedBalance = message.liquidLockedBalance;
            if (message.witnessLockedBalance != null && message.hasOwnProperty("witnessLockedBalance"))
                object.witnessLockedBalance = message.witnessLockedBalance;
            if (message.leverageLockedBalance != null && message.hasOwnProperty("leverageLockedBalance"))
                object.leverageLockedBalance = message.leverageLockedBalance;
            if (message.orderFrozenBalance != null && message.hasOwnProperty("orderFrozenBalance"))
                object.orderFrozenBalance = message.orderFrozenBalance;
            return object;
        };

        /**
         * Converts this TokenBalance to JSON.
         * @function toJSON
         * @memberof pb.TokenBalance
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TokenBalance.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TokenBalance
         * @function getTypeUrl
         * @memberof pb.TokenBalance
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TokenBalance.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.TokenBalance";
        };

        return TokenBalance;
    })();

    pb.BlockHeader = (function() {

        /**
         * Properties of a BlockHeader.
         * @memberof pb
         * @interface IBlockHeader
         * @property {number|Long|null} [height] BlockHeader height
         * @property {string|null} [prevBlockHash] BlockHeader prevBlockHash
         * @property {number|Long|null} [timestamp] BlockHeader timestamp
         * @property {Uint8Array|null} [stateRoot] BlockHeader stateRoot
         * @property {string|null} [txsHash] BlockHeader txsHash
         * @property {string|null} [miner] BlockHeader miner
         * @property {number|null} [window] BlockHeader window
         * @property {Uint8Array|null} [vrfProof] BlockHeader vrfProof
         * @property {Uint8Array|null} [vrfOutput] BlockHeader vrfOutput
         * @property {Uint8Array|null} [blsPublicKey] BlockHeader blsPublicKey
         */

        /**
         * Constructs a new BlockHeader.
         * @memberof pb
         * @classdesc Represents a BlockHeader.
         * @implements IBlockHeader
         * @constructor
         * @param {pb.IBlockHeader=} [properties] Properties to set
         */
        function BlockHeader(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BlockHeader height.
         * @member {number|Long} height
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BlockHeader prevBlockHash.
         * @member {string} prevBlockHash
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.prevBlockHash = "";

        /**
         * BlockHeader timestamp.
         * @member {number|Long} timestamp
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * BlockHeader stateRoot.
         * @member {Uint8Array} stateRoot
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.stateRoot = $util.newBuffer([]);

        /**
         * BlockHeader txsHash.
         * @member {string} txsHash
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.txsHash = "";

        /**
         * BlockHeader miner.
         * @member {string} miner
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.miner = "";

        /**
         * BlockHeader window.
         * @member {number} window
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.window = 0;

        /**
         * BlockHeader vrfProof.
         * @member {Uint8Array} vrfProof
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.vrfProof = $util.newBuffer([]);

        /**
         * BlockHeader vrfOutput.
         * @member {Uint8Array} vrfOutput
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.vrfOutput = $util.newBuffer([]);

        /**
         * BlockHeader blsPublicKey.
         * @member {Uint8Array} blsPublicKey
         * @memberof pb.BlockHeader
         * @instance
         */
        BlockHeader.prototype.blsPublicKey = $util.newBuffer([]);

        /**
         * Creates a new BlockHeader instance using the specified properties.
         * @function create
         * @memberof pb.BlockHeader
         * @static
         * @param {pb.IBlockHeader=} [properties] Properties to set
         * @returns {pb.BlockHeader} BlockHeader instance
         */
        BlockHeader.create = function create(properties) {
            return new BlockHeader(properties);
        };

        /**
         * Encodes the specified BlockHeader message. Does not implicitly {@link pb.BlockHeader.verify|verify} messages.
         * @function encode
         * @memberof pb.BlockHeader
         * @static
         * @param {pb.IBlockHeader} message BlockHeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockHeader.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.height);
            if (message.prevBlockHash != null && Object.hasOwnProperty.call(message, "prevBlockHash"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.prevBlockHash);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timestamp);
            if (message.stateRoot != null && Object.hasOwnProperty.call(message, "stateRoot"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.stateRoot);
            if (message.txsHash != null && Object.hasOwnProperty.call(message, "txsHash"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.txsHash);
            if (message.miner != null && Object.hasOwnProperty.call(message, "miner"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.miner);
            if (message.window != null && Object.hasOwnProperty.call(message, "window"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.window);
            if (message.vrfProof != null && Object.hasOwnProperty.call(message, "vrfProof"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.vrfProof);
            if (message.vrfOutput != null && Object.hasOwnProperty.call(message, "vrfOutput"))
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.vrfOutput);
            if (message.blsPublicKey != null && Object.hasOwnProperty.call(message, "blsPublicKey"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.blsPublicKey);
            return writer;
        };

        /**
         * Encodes the specified BlockHeader message, length delimited. Does not implicitly {@link pb.BlockHeader.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.BlockHeader
         * @static
         * @param {pb.IBlockHeader} message BlockHeader message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockHeader.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BlockHeader message from the specified reader or buffer.
         * @function decode
         * @memberof pb.BlockHeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.BlockHeader} BlockHeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockHeader.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.BlockHeader();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.height = reader.uint64();
                        break;
                    }
                case 2: {
                        message.prevBlockHash = reader.string();
                        break;
                    }
                case 3: {
                        message.timestamp = reader.int64();
                        break;
                    }
                case 4: {
                        message.stateRoot = reader.bytes();
                        break;
                    }
                case 5: {
                        message.txsHash = reader.string();
                        break;
                    }
                case 6: {
                        message.miner = reader.string();
                        break;
                    }
                case 7: {
                        message.window = reader.int32();
                        break;
                    }
                case 8: {
                        message.vrfProof = reader.bytes();
                        break;
                    }
                case 9: {
                        message.vrfOutput = reader.bytes();
                        break;
                    }
                case 10: {
                        message.blsPublicKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BlockHeader message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.BlockHeader
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.BlockHeader} BlockHeader
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockHeader.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlockHeader message.
         * @function verify
         * @memberof pb.BlockHeader
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlockHeader.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.prevBlockHash != null && message.hasOwnProperty("prevBlockHash"))
                if (!$util.isString(message.prevBlockHash))
                    return "prevBlockHash: string expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.stateRoot != null && message.hasOwnProperty("stateRoot"))
                if (!(message.stateRoot && typeof message.stateRoot.length === "number" || $util.isString(message.stateRoot)))
                    return "stateRoot: buffer expected";
            if (message.txsHash != null && message.hasOwnProperty("txsHash"))
                if (!$util.isString(message.txsHash))
                    return "txsHash: string expected";
            if (message.miner != null && message.hasOwnProperty("miner"))
                if (!$util.isString(message.miner))
                    return "miner: string expected";
            if (message.window != null && message.hasOwnProperty("window"))
                if (!$util.isInteger(message.window))
                    return "window: integer expected";
            if (message.vrfProof != null && message.hasOwnProperty("vrfProof"))
                if (!(message.vrfProof && typeof message.vrfProof.length === "number" || $util.isString(message.vrfProof)))
                    return "vrfProof: buffer expected";
            if (message.vrfOutput != null && message.hasOwnProperty("vrfOutput"))
                if (!(message.vrfOutput && typeof message.vrfOutput.length === "number" || $util.isString(message.vrfOutput)))
                    return "vrfOutput: buffer expected";
            if (message.blsPublicKey != null && message.hasOwnProperty("blsPublicKey"))
                if (!(message.blsPublicKey && typeof message.blsPublicKey.length === "number" || $util.isString(message.blsPublicKey)))
                    return "blsPublicKey: buffer expected";
            return null;
        };

        /**
         * Creates a BlockHeader message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.BlockHeader
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.BlockHeader} BlockHeader
         */
        BlockHeader.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.BlockHeader)
                return object;
            let message = new $root.pb.BlockHeader();
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.prevBlockHash != null)
                message.prevBlockHash = String(object.prevBlockHash);
            if (object.timestamp != null)
                if ($util.Long)
                    (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = false;
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            if (object.stateRoot != null)
                if (typeof object.stateRoot === "string")
                    $util.base64.decode(object.stateRoot, message.stateRoot = $util.newBuffer($util.base64.length(object.stateRoot)), 0);
                else if (object.stateRoot.length >= 0)
                    message.stateRoot = object.stateRoot;
            if (object.txsHash != null)
                message.txsHash = String(object.txsHash);
            if (object.miner != null)
                message.miner = String(object.miner);
            if (object.window != null)
                message.window = object.window | 0;
            if (object.vrfProof != null)
                if (typeof object.vrfProof === "string")
                    $util.base64.decode(object.vrfProof, message.vrfProof = $util.newBuffer($util.base64.length(object.vrfProof)), 0);
                else if (object.vrfProof.length >= 0)
                    message.vrfProof = object.vrfProof;
            if (object.vrfOutput != null)
                if (typeof object.vrfOutput === "string")
                    $util.base64.decode(object.vrfOutput, message.vrfOutput = $util.newBuffer($util.base64.length(object.vrfOutput)), 0);
                else if (object.vrfOutput.length >= 0)
                    message.vrfOutput = object.vrfOutput;
            if (object.blsPublicKey != null)
                if (typeof object.blsPublicKey === "string")
                    $util.base64.decode(object.blsPublicKey, message.blsPublicKey = $util.newBuffer($util.base64.length(object.blsPublicKey)), 0);
                else if (object.blsPublicKey.length >= 0)
                    message.blsPublicKey = object.blsPublicKey;
            return message;
        };

        /**
         * Creates a plain object from a BlockHeader message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.BlockHeader
         * @static
         * @param {pb.BlockHeader} message BlockHeader
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlockHeader.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.prevBlockHash = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.stateRoot = "";
                else {
                    object.stateRoot = [];
                    if (options.bytes !== Array)
                        object.stateRoot = $util.newBuffer(object.stateRoot);
                }
                object.txsHash = "";
                object.miner = "";
                object.window = 0;
                if (options.bytes === String)
                    object.vrfProof = "";
                else {
                    object.vrfProof = [];
                    if (options.bytes !== Array)
                        object.vrfProof = $util.newBuffer(object.vrfProof);
                }
                if (options.bytes === String)
                    object.vrfOutput = "";
                else {
                    object.vrfOutput = [];
                    if (options.bytes !== Array)
                        object.vrfOutput = $util.newBuffer(object.vrfOutput);
                }
                if (options.bytes === String)
                    object.blsPublicKey = "";
                else {
                    object.blsPublicKey = [];
                    if (options.bytes !== Array)
                        object.blsPublicKey = $util.newBuffer(object.blsPublicKey);
                }
            }
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.prevBlockHash != null && message.hasOwnProperty("prevBlockHash"))
                object.prevBlockHash = message.prevBlockHash;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            if (message.stateRoot != null && message.hasOwnProperty("stateRoot"))
                object.stateRoot = options.bytes === String ? $util.base64.encode(message.stateRoot, 0, message.stateRoot.length) : options.bytes === Array ? Array.prototype.slice.call(message.stateRoot) : message.stateRoot;
            if (message.txsHash != null && message.hasOwnProperty("txsHash"))
                object.txsHash = message.txsHash;
            if (message.miner != null && message.hasOwnProperty("miner"))
                object.miner = message.miner;
            if (message.window != null && message.hasOwnProperty("window"))
                object.window = message.window;
            if (message.vrfProof != null && message.hasOwnProperty("vrfProof"))
                object.vrfProof = options.bytes === String ? $util.base64.encode(message.vrfProof, 0, message.vrfProof.length) : options.bytes === Array ? Array.prototype.slice.call(message.vrfProof) : message.vrfProof;
            if (message.vrfOutput != null && message.hasOwnProperty("vrfOutput"))
                object.vrfOutput = options.bytes === String ? $util.base64.encode(message.vrfOutput, 0, message.vrfOutput.length) : options.bytes === Array ? Array.prototype.slice.call(message.vrfOutput) : message.vrfOutput;
            if (message.blsPublicKey != null && message.hasOwnProperty("blsPublicKey"))
                object.blsPublicKey = options.bytes === String ? $util.base64.encode(message.blsPublicKey, 0, message.blsPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.blsPublicKey) : message.blsPublicKey;
            return object;
        };

        /**
         * Converts this BlockHeader to JSON.
         * @function toJSON
         * @memberof pb.BlockHeader
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlockHeader.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BlockHeader
         * @function getTypeUrl
         * @memberof pb.BlockHeader
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BlockHeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.BlockHeader";
        };

        return BlockHeader;
    })();

    pb.Block = (function() {

        /**
         * Properties of a Block.
         * @memberof pb
         * @interface IBlock
         * @property {string|null} [blockHash] Block blockHash
         * @property {pb.IBlockHeader|null} [header] Block header
         * @property {Array.<pb.IAnyTx>|null} [body] Block body
         * @property {string|null} [accumulatedReward] Block accumulatedReward
         * @property {Uint8Array|null} [bitMap] Block bitMap
         * @property {Uint8Array|null} [minerSignature] Block minerSignature
         * @property {Uint8Array|null} [shortTxs] Block shortTxs
         */

        /**
         * Constructs a new Block.
         * @memberof pb
         * @classdesc Represents a Block.
         * @implements IBlock
         * @constructor
         * @param {pb.IBlock=} [properties] Properties to set
         */
        function Block(properties) {
            this.body = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Block blockHash.
         * @member {string} blockHash
         * @memberof pb.Block
         * @instance
         */
        Block.prototype.blockHash = "";

        /**
         * Block header.
         * @member {pb.IBlockHeader|null|undefined} header
         * @memberof pb.Block
         * @instance
         */
        Block.prototype.header = null;

        /**
         * Block body.
         * @member {Array.<pb.IAnyTx>} body
         * @memberof pb.Block
         * @instance
         */
        Block.prototype.body = $util.emptyArray;

        /**
         * Block accumulatedReward.
         * @member {string} accumulatedReward
         * @memberof pb.Block
         * @instance
         */
        Block.prototype.accumulatedReward = "";

        /**
         * Block bitMap.
         * @member {Uint8Array} bitMap
         * @memberof pb.Block
         * @instance
         */
        Block.prototype.bitMap = $util.newBuffer([]);

        /**
         * Block minerSignature.
         * @member {Uint8Array} minerSignature
         * @memberof pb.Block
         * @instance
         */
        Block.prototype.minerSignature = $util.newBuffer([]);

        /**
         * Block shortTxs.
         * @member {Uint8Array} shortTxs
         * @memberof pb.Block
         * @instance
         */
        Block.prototype.shortTxs = $util.newBuffer([]);

        /**
         * Creates a new Block instance using the specified properties.
         * @function create
         * @memberof pb.Block
         * @static
         * @param {pb.IBlock=} [properties] Properties to set
         * @returns {pb.Block} Block instance
         */
        Block.create = function create(properties) {
            return new Block(properties);
        };

        /**
         * Encodes the specified Block message. Does not implicitly {@link pb.Block.verify|verify} messages.
         * @function encode
         * @memberof pb.Block
         * @static
         * @param {pb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.blockHash != null && Object.hasOwnProperty.call(message, "blockHash"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.blockHash);
            if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                $root.pb.BlockHeader.encode(message.header, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.body != null && message.body.length)
                for (let i = 0; i < message.body.length; ++i)
                    $root.pb.AnyTx.encode(message.body[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.accumulatedReward != null && Object.hasOwnProperty.call(message, "accumulatedReward"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.accumulatedReward);
            if (message.bitMap != null && Object.hasOwnProperty.call(message, "bitMap"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.bitMap);
            if (message.minerSignature != null && Object.hasOwnProperty.call(message, "minerSignature"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.minerSignature);
            if (message.shortTxs != null && Object.hasOwnProperty.call(message, "shortTxs"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.shortTxs);
            return writer;
        };

        /**
         * Encodes the specified Block message, length delimited. Does not implicitly {@link pb.Block.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.Block
         * @static
         * @param {pb.IBlock} message Block message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Block.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Block message from the specified reader or buffer.
         * @function decode
         * @memberof pb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.Block();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.blockHash = reader.string();
                        break;
                    }
                case 2: {
                        message.header = $root.pb.BlockHeader.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        if (!(message.body && message.body.length))
                            message.body = [];
                        message.body.push($root.pb.AnyTx.decode(reader, reader.uint32()));
                        break;
                    }
                case 4: {
                        message.accumulatedReward = reader.string();
                        break;
                    }
                case 5: {
                        message.bitMap = reader.bytes();
                        break;
                    }
                case 6: {
                        message.minerSignature = reader.bytes();
                        break;
                    }
                case 7: {
                        message.shortTxs = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Block message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.Block
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.Block} Block
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Block.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Block message.
         * @function verify
         * @memberof pb.Block
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Block.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.blockHash != null && message.hasOwnProperty("blockHash"))
                if (!$util.isString(message.blockHash))
                    return "blockHash: string expected";
            if (message.header != null && message.hasOwnProperty("header")) {
                let error = $root.pb.BlockHeader.verify(message.header);
                if (error)
                    return "header." + error;
            }
            if (message.body != null && message.hasOwnProperty("body")) {
                if (!Array.isArray(message.body))
                    return "body: array expected";
                for (let i = 0; i < message.body.length; ++i) {
                    let error = $root.pb.AnyTx.verify(message.body[i]);
                    if (error)
                        return "body." + error;
                }
            }
            if (message.accumulatedReward != null && message.hasOwnProperty("accumulatedReward"))
                if (!$util.isString(message.accumulatedReward))
                    return "accumulatedReward: string expected";
            if (message.bitMap != null && message.hasOwnProperty("bitMap"))
                if (!(message.bitMap && typeof message.bitMap.length === "number" || $util.isString(message.bitMap)))
                    return "bitMap: buffer expected";
            if (message.minerSignature != null && message.hasOwnProperty("minerSignature"))
                if (!(message.minerSignature && typeof message.minerSignature.length === "number" || $util.isString(message.minerSignature)))
                    return "minerSignature: buffer expected";
            if (message.shortTxs != null && message.hasOwnProperty("shortTxs"))
                if (!(message.shortTxs && typeof message.shortTxs.length === "number" || $util.isString(message.shortTxs)))
                    return "shortTxs: buffer expected";
            return null;
        };

        /**
         * Creates a Block message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.Block
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.Block} Block
         */
        Block.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.Block)
                return object;
            let message = new $root.pb.Block();
            if (object.blockHash != null)
                message.blockHash = String(object.blockHash);
            if (object.header != null) {
                if (typeof object.header !== "object")
                    throw TypeError(".pb.Block.header: object expected");
                message.header = $root.pb.BlockHeader.fromObject(object.header);
            }
            if (object.body) {
                if (!Array.isArray(object.body))
                    throw TypeError(".pb.Block.body: array expected");
                message.body = [];
                for (let i = 0; i < object.body.length; ++i) {
                    if (typeof object.body[i] !== "object")
                        throw TypeError(".pb.Block.body: object expected");
                    message.body[i] = $root.pb.AnyTx.fromObject(object.body[i]);
                }
            }
            if (object.accumulatedReward != null)
                message.accumulatedReward = String(object.accumulatedReward);
            if (object.bitMap != null)
                if (typeof object.bitMap === "string")
                    $util.base64.decode(object.bitMap, message.bitMap = $util.newBuffer($util.base64.length(object.bitMap)), 0);
                else if (object.bitMap.length >= 0)
                    message.bitMap = object.bitMap;
            if (object.minerSignature != null)
                if (typeof object.minerSignature === "string")
                    $util.base64.decode(object.minerSignature, message.minerSignature = $util.newBuffer($util.base64.length(object.minerSignature)), 0);
                else if (object.minerSignature.length >= 0)
                    message.minerSignature = object.minerSignature;
            if (object.shortTxs != null)
                if (typeof object.shortTxs === "string")
                    $util.base64.decode(object.shortTxs, message.shortTxs = $util.newBuffer($util.base64.length(object.shortTxs)), 0);
                else if (object.shortTxs.length >= 0)
                    message.shortTxs = object.shortTxs;
            return message;
        };

        /**
         * Creates a plain object from a Block message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.Block
         * @static
         * @param {pb.Block} message Block
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Block.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.body = [];
            if (options.defaults) {
                object.blockHash = "";
                object.header = null;
                object.accumulatedReward = "";
                if (options.bytes === String)
                    object.bitMap = "";
                else {
                    object.bitMap = [];
                    if (options.bytes !== Array)
                        object.bitMap = $util.newBuffer(object.bitMap);
                }
                if (options.bytes === String)
                    object.minerSignature = "";
                else {
                    object.minerSignature = [];
                    if (options.bytes !== Array)
                        object.minerSignature = $util.newBuffer(object.minerSignature);
                }
                if (options.bytes === String)
                    object.shortTxs = "";
                else {
                    object.shortTxs = [];
                    if (options.bytes !== Array)
                        object.shortTxs = $util.newBuffer(object.shortTxs);
                }
            }
            if (message.blockHash != null && message.hasOwnProperty("blockHash"))
                object.blockHash = message.blockHash;
            if (message.header != null && message.hasOwnProperty("header"))
                object.header = $root.pb.BlockHeader.toObject(message.header, options);
            if (message.body && message.body.length) {
                object.body = [];
                for (let j = 0; j < message.body.length; ++j)
                    object.body[j] = $root.pb.AnyTx.toObject(message.body[j], options);
            }
            if (message.accumulatedReward != null && message.hasOwnProperty("accumulatedReward"))
                object.accumulatedReward = message.accumulatedReward;
            if (message.bitMap != null && message.hasOwnProperty("bitMap"))
                object.bitMap = options.bytes === String ? $util.base64.encode(message.bitMap, 0, message.bitMap.length) : options.bytes === Array ? Array.prototype.slice.call(message.bitMap) : message.bitMap;
            if (message.minerSignature != null && message.hasOwnProperty("minerSignature"))
                object.minerSignature = options.bytes === String ? $util.base64.encode(message.minerSignature, 0, message.minerSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.minerSignature) : message.minerSignature;
            if (message.shortTxs != null && message.hasOwnProperty("shortTxs"))
                object.shortTxs = options.bytes === String ? $util.base64.encode(message.shortTxs, 0, message.shortTxs.length) : options.bytes === Array ? Array.prototype.slice.call(message.shortTxs) : message.shortTxs;
            return object;
        };

        /**
         * Converts this Block to JSON.
         * @function toJSON
         * @memberof pb.Block
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Block.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Block
         * @function getTypeUrl
         * @memberof pb.Block
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Block.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.Block";
        };

        return Block;
    })();

    pb.BlockReward = (function() {

        /**
         * Properties of a BlockReward.
         * @memberof pb
         * @interface IBlockReward
         * @property {number|Long|null} [height] BlockReward height
         * @property {string|null} [miner] BlockReward miner
         * @property {string|null} [minerReward] BlockReward minerReward
         * @property {string|null} [witnessReward] BlockReward witnessReward
         * @property {string|null} [totalFees] BlockReward totalFees
         * @property {string|null} [blockReward] BlockReward blockReward
         * @property {number|Long|null} [timestamp] BlockReward timestamp
         */

        /**
         * Constructs a new BlockReward.
         * @memberof pb
         * @classdesc Represents a BlockReward.
         * @implements IBlockReward
         * @constructor
         * @param {pb.IBlockReward=} [properties] Properties to set
         */
        function BlockReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BlockReward height.
         * @member {number|Long} height
         * @memberof pb.BlockReward
         * @instance
         */
        BlockReward.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BlockReward miner.
         * @member {string} miner
         * @memberof pb.BlockReward
         * @instance
         */
        BlockReward.prototype.miner = "";

        /**
         * BlockReward minerReward.
         * @member {string} minerReward
         * @memberof pb.BlockReward
         * @instance
         */
        BlockReward.prototype.minerReward = "";

        /**
         * BlockReward witnessReward.
         * @member {string} witnessReward
         * @memberof pb.BlockReward
         * @instance
         */
        BlockReward.prototype.witnessReward = "";

        /**
         * BlockReward totalFees.
         * @member {string} totalFees
         * @memberof pb.BlockReward
         * @instance
         */
        BlockReward.prototype.totalFees = "";

        /**
         * BlockReward blockReward.
         * @member {string} blockReward
         * @memberof pb.BlockReward
         * @instance
         */
        BlockReward.prototype.blockReward = "";

        /**
         * BlockReward timestamp.
         * @member {number|Long} timestamp
         * @memberof pb.BlockReward
         * @instance
         */
        BlockReward.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new BlockReward instance using the specified properties.
         * @function create
         * @memberof pb.BlockReward
         * @static
         * @param {pb.IBlockReward=} [properties] Properties to set
         * @returns {pb.BlockReward} BlockReward instance
         */
        BlockReward.create = function create(properties) {
            return new BlockReward(properties);
        };

        /**
         * Encodes the specified BlockReward message. Does not implicitly {@link pb.BlockReward.verify|verify} messages.
         * @function encode
         * @memberof pb.BlockReward
         * @static
         * @param {pb.IBlockReward} message BlockReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.height);
            if (message.miner != null && Object.hasOwnProperty.call(message, "miner"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.miner);
            if (message.minerReward != null && Object.hasOwnProperty.call(message, "minerReward"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.minerReward);
            if (message.witnessReward != null && Object.hasOwnProperty.call(message, "witnessReward"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.witnessReward);
            if (message.totalFees != null && Object.hasOwnProperty.call(message, "totalFees"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.totalFees);
            if (message.blockReward != null && Object.hasOwnProperty.call(message, "blockReward"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.blockReward);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.timestamp);
            return writer;
        };

        /**
         * Encodes the specified BlockReward message, length delimited. Does not implicitly {@link pb.BlockReward.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.BlockReward
         * @static
         * @param {pb.IBlockReward} message BlockReward message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BlockReward message from the specified reader or buffer.
         * @function decode
         * @memberof pb.BlockReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.BlockReward} BlockReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockReward.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.BlockReward();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.height = reader.uint64();
                        break;
                    }
                case 2: {
                        message.miner = reader.string();
                        break;
                    }
                case 3: {
                        message.minerReward = reader.string();
                        break;
                    }
                case 4: {
                        message.witnessReward = reader.string();
                        break;
                    }
                case 5: {
                        message.totalFees = reader.string();
                        break;
                    }
                case 6: {
                        message.blockReward = reader.string();
                        break;
                    }
                case 7: {
                        message.timestamp = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BlockReward message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.BlockReward
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.BlockReward} BlockReward
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlockReward message.
         * @function verify
         * @memberof pb.BlockReward
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlockReward.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.miner != null && message.hasOwnProperty("miner"))
                if (!$util.isString(message.miner))
                    return "miner: string expected";
            if (message.minerReward != null && message.hasOwnProperty("minerReward"))
                if (!$util.isString(message.minerReward))
                    return "minerReward: string expected";
            if (message.witnessReward != null && message.hasOwnProperty("witnessReward"))
                if (!$util.isString(message.witnessReward))
                    return "witnessReward: string expected";
            if (message.totalFees != null && message.hasOwnProperty("totalFees"))
                if (!$util.isString(message.totalFees))
                    return "totalFees: string expected";
            if (message.blockReward != null && message.hasOwnProperty("blockReward"))
                if (!$util.isString(message.blockReward))
                    return "blockReward: string expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            return null;
        };

        /**
         * Creates a BlockReward message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.BlockReward
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.BlockReward} BlockReward
         */
        BlockReward.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.BlockReward)
                return object;
            let message = new $root.pb.BlockReward();
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.miner != null)
                message.miner = String(object.miner);
            if (object.minerReward != null)
                message.minerReward = String(object.minerReward);
            if (object.witnessReward != null)
                message.witnessReward = String(object.witnessReward);
            if (object.totalFees != null)
                message.totalFees = String(object.totalFees);
            if (object.blockReward != null)
                message.blockReward = String(object.blockReward);
            if (object.timestamp != null)
                if ($util.Long)
                    (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = false;
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a BlockReward message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.BlockReward
         * @static
         * @param {pb.BlockReward} message BlockReward
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlockReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.miner = "";
                object.minerReward = "";
                object.witnessReward = "";
                object.totalFees = "";
                object.blockReward = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : 0;
            }
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.miner != null && message.hasOwnProperty("miner"))
                object.miner = message.miner;
            if (message.minerReward != null && message.hasOwnProperty("minerReward"))
                object.minerReward = message.minerReward;
            if (message.witnessReward != null && message.hasOwnProperty("witnessReward"))
                object.witnessReward = message.witnessReward;
            if (message.totalFees != null && message.hasOwnProperty("totalFees"))
                object.totalFees = message.totalFees;
            if (message.blockReward != null && message.hasOwnProperty("blockReward"))
                object.blockReward = message.blockReward;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            return object;
        };

        /**
         * Converts this BlockReward to JSON.
         * @function toJSON
         * @memberof pb.BlockReward
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlockReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BlockReward
         * @function getTypeUrl
         * @memberof pb.BlockReward
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BlockReward.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.BlockReward";
        };

        return BlockReward;
    })();

    pb.OrderPriceIndex = (function() {

        /**
         * Properties of an OrderPriceIndex.
         * @memberof pb
         * @interface IOrderPriceIndex
         * @property {boolean|null} [ok] OrderPriceIndex ok
         */

        /**
         * Constructs a new OrderPriceIndex.
         * @memberof pb
         * @classdesc Represents an OrderPriceIndex.
         * @implements IOrderPriceIndex
         * @constructor
         * @param {pb.IOrderPriceIndex=} [properties] Properties to set
         */
        function OrderPriceIndex(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * OrderPriceIndex ok.
         * @member {boolean} ok
         * @memberof pb.OrderPriceIndex
         * @instance
         */
        OrderPriceIndex.prototype.ok = false;

        /**
         * Creates a new OrderPriceIndex instance using the specified properties.
         * @function create
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {pb.IOrderPriceIndex=} [properties] Properties to set
         * @returns {pb.OrderPriceIndex} OrderPriceIndex instance
         */
        OrderPriceIndex.create = function create(properties) {
            return new OrderPriceIndex(properties);
        };

        /**
         * Encodes the specified OrderPriceIndex message. Does not implicitly {@link pb.OrderPriceIndex.verify|verify} messages.
         * @function encode
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {pb.IOrderPriceIndex} message OrderPriceIndex message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OrderPriceIndex.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ok != null && Object.hasOwnProperty.call(message, "ok"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ok);
            return writer;
        };

        /**
         * Encodes the specified OrderPriceIndex message, length delimited. Does not implicitly {@link pb.OrderPriceIndex.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {pb.IOrderPriceIndex} message OrderPriceIndex message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OrderPriceIndex.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an OrderPriceIndex message from the specified reader or buffer.
         * @function decode
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.OrderPriceIndex} OrderPriceIndex
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OrderPriceIndex.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.OrderPriceIndex();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ok = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an OrderPriceIndex message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.OrderPriceIndex} OrderPriceIndex
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OrderPriceIndex.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OrderPriceIndex message.
         * @function verify
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OrderPriceIndex.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ok != null && message.hasOwnProperty("ok"))
                if (typeof message.ok !== "boolean")
                    return "ok: boolean expected";
            return null;
        };

        /**
         * Creates an OrderPriceIndex message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.OrderPriceIndex} OrderPriceIndex
         */
        OrderPriceIndex.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.OrderPriceIndex)
                return object;
            let message = new $root.pb.OrderPriceIndex();
            if (object.ok != null)
                message.ok = Boolean(object.ok);
            return message;
        };

        /**
         * Creates a plain object from an OrderPriceIndex message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {pb.OrderPriceIndex} message OrderPriceIndex
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OrderPriceIndex.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.ok = false;
            if (message.ok != null && message.hasOwnProperty("ok"))
                object.ok = message.ok;
            return object;
        };

        /**
         * Converts this OrderPriceIndex to JSON.
         * @function toJSON
         * @memberof pb.OrderPriceIndex
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OrderPriceIndex.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for OrderPriceIndex
         * @function getTypeUrl
         * @memberof pb.OrderPriceIndex
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        OrderPriceIndex.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.OrderPriceIndex";
        };

        return OrderPriceIndex;
    })();

    pb.TradeRecord = (function() {

        /**
         * Properties of a TradeRecord.
         * @memberof pb
         * @interface ITradeRecord
         * @property {string|null} [tradeId] TradeRecord tradeId
         * @property {string|null} [pair] TradeRecord pair
         * @property {string|null} [price] TradeRecord price
         * @property {string|null} [amount] TradeRecord amount
         * @property {string|null} [makerOrderId] TradeRecord makerOrderId
         * @property {string|null} [takerOrderId] TradeRecord takerOrderId
         * @property {pb.OrderSide|null} [takerSide] TradeRecord takerSide
         * @property {string|null} [timestamp] TradeRecord timestamp
         * @property {number|Long|null} [height] TradeRecord height
         */

        /**
         * Constructs a new TradeRecord.
         * @memberof pb
         * @classdesc Represents a TradeRecord.
         * @implements ITradeRecord
         * @constructor
         * @param {pb.ITradeRecord=} [properties] Properties to set
         */
        function TradeRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TradeRecord tradeId.
         * @member {string} tradeId
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.tradeId = "";

        /**
         * TradeRecord pair.
         * @member {string} pair
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.pair = "";

        /**
         * TradeRecord price.
         * @member {string} price
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.price = "";

        /**
         * TradeRecord amount.
         * @member {string} amount
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.amount = "";

        /**
         * TradeRecord makerOrderId.
         * @member {string} makerOrderId
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.makerOrderId = "";

        /**
         * TradeRecord takerOrderId.
         * @member {string} takerOrderId
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.takerOrderId = "";

        /**
         * TradeRecord takerSide.
         * @member {pb.OrderSide} takerSide
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.takerSide = 0;

        /**
         * TradeRecord timestamp.
         * @member {string} timestamp
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.timestamp = "";

        /**
         * TradeRecord height.
         * @member {number|Long} height
         * @memberof pb.TradeRecord
         * @instance
         */
        TradeRecord.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new TradeRecord instance using the specified properties.
         * @function create
         * @memberof pb.TradeRecord
         * @static
         * @param {pb.ITradeRecord=} [properties] Properties to set
         * @returns {pb.TradeRecord} TradeRecord instance
         */
        TradeRecord.create = function create(properties) {
            return new TradeRecord(properties);
        };

        /**
         * Encodes the specified TradeRecord message. Does not implicitly {@link pb.TradeRecord.verify|verify} messages.
         * @function encode
         * @memberof pb.TradeRecord
         * @static
         * @param {pb.ITradeRecord} message TradeRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TradeRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tradeId != null && Object.hasOwnProperty.call(message, "tradeId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.tradeId);
            if (message.pair != null && Object.hasOwnProperty.call(message, "pair"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.pair);
            if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.price);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.amount);
            if (message.makerOrderId != null && Object.hasOwnProperty.call(message, "makerOrderId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.makerOrderId);
            if (message.takerOrderId != null && Object.hasOwnProperty.call(message, "takerOrderId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.takerOrderId);
            if (message.takerSide != null && Object.hasOwnProperty.call(message, "takerSide"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.takerSide);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.timestamp);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.height);
            return writer;
        };

        /**
         * Encodes the specified TradeRecord message, length delimited. Does not implicitly {@link pb.TradeRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.TradeRecord
         * @static
         * @param {pb.ITradeRecord} message TradeRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TradeRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a TradeRecord message from the specified reader or buffer.
         * @function decode
         * @memberof pb.TradeRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.TradeRecord} TradeRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TradeRecord.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.TradeRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.tradeId = reader.string();
                        break;
                    }
                case 2: {
                        message.pair = reader.string();
                        break;
                    }
                case 3: {
                        message.price = reader.string();
                        break;
                    }
                case 4: {
                        message.amount = reader.string();
                        break;
                    }
                case 5: {
                        message.makerOrderId = reader.string();
                        break;
                    }
                case 6: {
                        message.takerOrderId = reader.string();
                        break;
                    }
                case 7: {
                        message.takerSide = reader.int32();
                        break;
                    }
                case 8: {
                        message.timestamp = reader.string();
                        break;
                    }
                case 9: {
                        message.height = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TradeRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.TradeRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.TradeRecord} TradeRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TradeRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TradeRecord message.
         * @function verify
         * @memberof pb.TradeRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TradeRecord.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.tradeId != null && message.hasOwnProperty("tradeId"))
                if (!$util.isString(message.tradeId))
                    return "tradeId: string expected";
            if (message.pair != null && message.hasOwnProperty("pair"))
                if (!$util.isString(message.pair))
                    return "pair: string expected";
            if (message.price != null && message.hasOwnProperty("price"))
                if (!$util.isString(message.price))
                    return "price: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.makerOrderId != null && message.hasOwnProperty("makerOrderId"))
                if (!$util.isString(message.makerOrderId))
                    return "makerOrderId: string expected";
            if (message.takerOrderId != null && message.hasOwnProperty("takerOrderId"))
                if (!$util.isString(message.takerOrderId))
                    return "takerOrderId: string expected";
            if (message.takerSide != null && message.hasOwnProperty("takerSide"))
                switch (message.takerSide) {
                default:
                    return "takerSide: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isString(message.timestamp))
                    return "timestamp: string expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            return null;
        };

        /**
         * Creates a TradeRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.TradeRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.TradeRecord} TradeRecord
         */
        TradeRecord.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.TradeRecord)
                return object;
            let message = new $root.pb.TradeRecord();
            if (object.tradeId != null)
                message.tradeId = String(object.tradeId);
            if (object.pair != null)
                message.pair = String(object.pair);
            if (object.price != null)
                message.price = String(object.price);
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.makerOrderId != null)
                message.makerOrderId = String(object.makerOrderId);
            if (object.takerOrderId != null)
                message.takerOrderId = String(object.takerOrderId);
            switch (object.takerSide) {
            default:
                if (typeof object.takerSide === "number") {
                    message.takerSide = object.takerSide;
                    break;
                }
                break;
            case "BUY":
            case 0:
                message.takerSide = 0;
                break;
            case "SELL":
            case 1:
                message.takerSide = 1;
                break;
            }
            if (object.timestamp != null)
                message.timestamp = String(object.timestamp);
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a TradeRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.TradeRecord
         * @static
         * @param {pb.TradeRecord} message TradeRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TradeRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.tradeId = "";
                object.pair = "";
                object.price = "";
                object.amount = "";
                object.makerOrderId = "";
                object.takerOrderId = "";
                object.takerSide = options.enums === String ? "BUY" : 0;
                object.timestamp = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
            }
            if (message.tradeId != null && message.hasOwnProperty("tradeId"))
                object.tradeId = message.tradeId;
            if (message.pair != null && message.hasOwnProperty("pair"))
                object.pair = message.pair;
            if (message.price != null && message.hasOwnProperty("price"))
                object.price = message.price;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.makerOrderId != null && message.hasOwnProperty("makerOrderId"))
                object.makerOrderId = message.makerOrderId;
            if (message.takerOrderId != null && message.hasOwnProperty("takerOrderId"))
                object.takerOrderId = message.takerOrderId;
            if (message.takerSide != null && message.hasOwnProperty("takerSide"))
                object.takerSide = options.enums === String ? $root.pb.OrderSide[message.takerSide] === undefined ? message.takerSide : $root.pb.OrderSide[message.takerSide] : message.takerSide;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                object.timestamp = message.timestamp;
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            return object;
        };

        /**
         * Converts this TradeRecord to JSON.
         * @function toJSON
         * @memberof pb.TradeRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TradeRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TradeRecord
         * @function getTypeUrl
         * @memberof pb.TradeRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TradeRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.TradeRecord";
        };

        return TradeRecord;
    })();

    pb.IssueTokenTx = (function() {

        /**
         * Properties of an IssueTokenTx.
         * @memberof pb
         * @interface IIssueTokenTx
         * @property {pb.IBaseMessage|null} [base] IssueTokenTx base
         * @property {string|null} [tokenName] IssueTokenTx tokenName
         * @property {string|null} [tokenSymbol] IssueTokenTx tokenSymbol
         * @property {string|null} [totalSupply] IssueTokenTx totalSupply
         * @property {boolean|null} [canMint] IssueTokenTx canMint
         */

        /**
         * Constructs a new IssueTokenTx.
         * @memberof pb
         * @classdesc Represents an IssueTokenTx.
         * @implements IIssueTokenTx
         * @constructor
         * @param {pb.IIssueTokenTx=} [properties] Properties to set
         */
        function IssueTokenTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IssueTokenTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.IssueTokenTx
         * @instance
         */
        IssueTokenTx.prototype.base = null;

        /**
         * IssueTokenTx tokenName.
         * @member {string} tokenName
         * @memberof pb.IssueTokenTx
         * @instance
         */
        IssueTokenTx.prototype.tokenName = "";

        /**
         * IssueTokenTx tokenSymbol.
         * @member {string} tokenSymbol
         * @memberof pb.IssueTokenTx
         * @instance
         */
        IssueTokenTx.prototype.tokenSymbol = "";

        /**
         * IssueTokenTx totalSupply.
         * @member {string} totalSupply
         * @memberof pb.IssueTokenTx
         * @instance
         */
        IssueTokenTx.prototype.totalSupply = "";

        /**
         * IssueTokenTx canMint.
         * @member {boolean} canMint
         * @memberof pb.IssueTokenTx
         * @instance
         */
        IssueTokenTx.prototype.canMint = false;

        /**
         * Creates a new IssueTokenTx instance using the specified properties.
         * @function create
         * @memberof pb.IssueTokenTx
         * @static
         * @param {pb.IIssueTokenTx=} [properties] Properties to set
         * @returns {pb.IssueTokenTx} IssueTokenTx instance
         */
        IssueTokenTx.create = function create(properties) {
            return new IssueTokenTx(properties);
        };

        /**
         * Encodes the specified IssueTokenTx message. Does not implicitly {@link pb.IssueTokenTx.verify|verify} messages.
         * @function encode
         * @memberof pb.IssueTokenTx
         * @static
         * @param {pb.IIssueTokenTx} message IssueTokenTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IssueTokenTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.tokenName != null && Object.hasOwnProperty.call(message, "tokenName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.tokenName);
            if (message.tokenSymbol != null && Object.hasOwnProperty.call(message, "tokenSymbol"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.tokenSymbol);
            if (message.totalSupply != null && Object.hasOwnProperty.call(message, "totalSupply"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.totalSupply);
            if (message.canMint != null && Object.hasOwnProperty.call(message, "canMint"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.canMint);
            return writer;
        };

        /**
         * Encodes the specified IssueTokenTx message, length delimited. Does not implicitly {@link pb.IssueTokenTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.IssueTokenTx
         * @static
         * @param {pb.IIssueTokenTx} message IssueTokenTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IssueTokenTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an IssueTokenTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.IssueTokenTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.IssueTokenTx} IssueTokenTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IssueTokenTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.IssueTokenTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.tokenName = reader.string();
                        break;
                    }
                case 3: {
                        message.tokenSymbol = reader.string();
                        break;
                    }
                case 4: {
                        message.totalSupply = reader.string();
                        break;
                    }
                case 5: {
                        message.canMint = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an IssueTokenTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.IssueTokenTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.IssueTokenTx} IssueTokenTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IssueTokenTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IssueTokenTx message.
         * @function verify
         * @memberof pb.IssueTokenTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IssueTokenTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.tokenName != null && message.hasOwnProperty("tokenName"))
                if (!$util.isString(message.tokenName))
                    return "tokenName: string expected";
            if (message.tokenSymbol != null && message.hasOwnProperty("tokenSymbol"))
                if (!$util.isString(message.tokenSymbol))
                    return "tokenSymbol: string expected";
            if (message.totalSupply != null && message.hasOwnProperty("totalSupply"))
                if (!$util.isString(message.totalSupply))
                    return "totalSupply: string expected";
            if (message.canMint != null && message.hasOwnProperty("canMint"))
                if (typeof message.canMint !== "boolean")
                    return "canMint: boolean expected";
            return null;
        };

        /**
         * Creates an IssueTokenTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.IssueTokenTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.IssueTokenTx} IssueTokenTx
         */
        IssueTokenTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.IssueTokenTx)
                return object;
            let message = new $root.pb.IssueTokenTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.IssueTokenTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.tokenName != null)
                message.tokenName = String(object.tokenName);
            if (object.tokenSymbol != null)
                message.tokenSymbol = String(object.tokenSymbol);
            if (object.totalSupply != null)
                message.totalSupply = String(object.totalSupply);
            if (object.canMint != null)
                message.canMint = Boolean(object.canMint);
            return message;
        };

        /**
         * Creates a plain object from an IssueTokenTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.IssueTokenTx
         * @static
         * @param {pb.IssueTokenTx} message IssueTokenTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IssueTokenTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.tokenName = "";
                object.tokenSymbol = "";
                object.totalSupply = "";
                object.canMint = false;
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.tokenName != null && message.hasOwnProperty("tokenName"))
                object.tokenName = message.tokenName;
            if (message.tokenSymbol != null && message.hasOwnProperty("tokenSymbol"))
                object.tokenSymbol = message.tokenSymbol;
            if (message.totalSupply != null && message.hasOwnProperty("totalSupply"))
                object.totalSupply = message.totalSupply;
            if (message.canMint != null && message.hasOwnProperty("canMint"))
                object.canMint = message.canMint;
            return object;
        };

        /**
         * Converts this IssueTokenTx to JSON.
         * @function toJSON
         * @memberof pb.IssueTokenTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IssueTokenTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IssueTokenTx
         * @function getTypeUrl
         * @memberof pb.IssueTokenTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IssueTokenTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.IssueTokenTx";
        };

        return IssueTokenTx;
    })();

    pb.FreezeTx = (function() {

        /**
         * Properties of a FreezeTx.
         * @memberof pb
         * @interface IFreezeTx
         * @property {pb.IBaseMessage|null} [base] FreezeTx base
         * @property {string|null} [tokenAddr] FreezeTx tokenAddr
         * @property {string|null} [targetAddr] FreezeTx targetAddr
         * @property {boolean|null} [freeze] FreezeTx freeze
         */

        /**
         * Constructs a new FreezeTx.
         * @memberof pb
         * @classdesc Represents a FreezeTx.
         * @implements IFreezeTx
         * @constructor
         * @param {pb.IFreezeTx=} [properties] Properties to set
         */
        function FreezeTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FreezeTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FreezeTx
         * @instance
         */
        FreezeTx.prototype.base = null;

        /**
         * FreezeTx tokenAddr.
         * @member {string} tokenAddr
         * @memberof pb.FreezeTx
         * @instance
         */
        FreezeTx.prototype.tokenAddr = "";

        /**
         * FreezeTx targetAddr.
         * @member {string} targetAddr
         * @memberof pb.FreezeTx
         * @instance
         */
        FreezeTx.prototype.targetAddr = "";

        /**
         * FreezeTx freeze.
         * @member {boolean} freeze
         * @memberof pb.FreezeTx
         * @instance
         */
        FreezeTx.prototype.freeze = false;

        /**
         * Creates a new FreezeTx instance using the specified properties.
         * @function create
         * @memberof pb.FreezeTx
         * @static
         * @param {pb.IFreezeTx=} [properties] Properties to set
         * @returns {pb.FreezeTx} FreezeTx instance
         */
        FreezeTx.create = function create(properties) {
            return new FreezeTx(properties);
        };

        /**
         * Encodes the specified FreezeTx message. Does not implicitly {@link pb.FreezeTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FreezeTx
         * @static
         * @param {pb.IFreezeTx} message FreezeTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.tokenAddr != null && Object.hasOwnProperty.call(message, "tokenAddr"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.tokenAddr);
            if (message.targetAddr != null && Object.hasOwnProperty.call(message, "targetAddr"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.targetAddr);
            if (message.freeze != null && Object.hasOwnProperty.call(message, "freeze"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.freeze);
            return writer;
        };

        /**
         * Encodes the specified FreezeTx message, length delimited. Does not implicitly {@link pb.FreezeTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FreezeTx
         * @static
         * @param {pb.IFreezeTx} message FreezeTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FreezeTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FreezeTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FreezeTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FreezeTx} FreezeTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FreezeTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.tokenAddr = reader.string();
                        break;
                    }
                case 3: {
                        message.targetAddr = reader.string();
                        break;
                    }
                case 4: {
                        message.freeze = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FreezeTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FreezeTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FreezeTx} FreezeTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FreezeTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FreezeTx message.
         * @function verify
         * @memberof pb.FreezeTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FreezeTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.tokenAddr != null && message.hasOwnProperty("tokenAddr"))
                if (!$util.isString(message.tokenAddr))
                    return "tokenAddr: string expected";
            if (message.targetAddr != null && message.hasOwnProperty("targetAddr"))
                if (!$util.isString(message.targetAddr))
                    return "targetAddr: string expected";
            if (message.freeze != null && message.hasOwnProperty("freeze"))
                if (typeof message.freeze !== "boolean")
                    return "freeze: boolean expected";
            return null;
        };

        /**
         * Creates a FreezeTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FreezeTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FreezeTx} FreezeTx
         */
        FreezeTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FreezeTx)
                return object;
            let message = new $root.pb.FreezeTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FreezeTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.tokenAddr != null)
                message.tokenAddr = String(object.tokenAddr);
            if (object.targetAddr != null)
                message.targetAddr = String(object.targetAddr);
            if (object.freeze != null)
                message.freeze = Boolean(object.freeze);
            return message;
        };

        /**
         * Creates a plain object from a FreezeTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FreezeTx
         * @static
         * @param {pb.FreezeTx} message FreezeTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FreezeTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.tokenAddr = "";
                object.targetAddr = "";
                object.freeze = false;
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.tokenAddr != null && message.hasOwnProperty("tokenAddr"))
                object.tokenAddr = message.tokenAddr;
            if (message.targetAddr != null && message.hasOwnProperty("targetAddr"))
                object.targetAddr = message.targetAddr;
            if (message.freeze != null && message.hasOwnProperty("freeze"))
                object.freeze = message.freeze;
            return object;
        };

        /**
         * Converts this FreezeTx to JSON.
         * @function toJSON
         * @memberof pb.FreezeTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FreezeTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FreezeTx
         * @function getTypeUrl
         * @memberof pb.FreezeTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FreezeTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FreezeTx";
        };

        return FreezeTx;
    })();

    pb.Transaction = (function() {

        /**
         * Properties of a Transaction.
         * @memberof pb
         * @interface ITransaction
         * @property {pb.IBaseMessage|null} [base] Transaction base
         * @property {string|null} [to] Transaction to
         * @property {string|null} [tokenAddress] Transaction tokenAddress
         * @property {string|null} [amount] Transaction amount
         */

        /**
         * Constructs a new Transaction.
         * @memberof pb
         * @classdesc Represents a Transaction.
         * @implements ITransaction
         * @constructor
         * @param {pb.ITransaction=} [properties] Properties to set
         */
        function Transaction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Transaction base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.Transaction
         * @instance
         */
        Transaction.prototype.base = null;

        /**
         * Transaction to.
         * @member {string} to
         * @memberof pb.Transaction
         * @instance
         */
        Transaction.prototype.to = "";

        /**
         * Transaction tokenAddress.
         * @member {string} tokenAddress
         * @memberof pb.Transaction
         * @instance
         */
        Transaction.prototype.tokenAddress = "";

        /**
         * Transaction amount.
         * @member {string} amount
         * @memberof pb.Transaction
         * @instance
         */
        Transaction.prototype.amount = "";

        /**
         * Creates a new Transaction instance using the specified properties.
         * @function create
         * @memberof pb.Transaction
         * @static
         * @param {pb.ITransaction=} [properties] Properties to set
         * @returns {pb.Transaction} Transaction instance
         */
        Transaction.create = function create(properties) {
            return new Transaction(properties);
        };

        /**
         * Encodes the specified Transaction message. Does not implicitly {@link pb.Transaction.verify|verify} messages.
         * @function encode
         * @memberof pb.Transaction
         * @static
         * @param {pb.ITransaction} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.to);
            if (message.tokenAddress != null && Object.hasOwnProperty.call(message, "tokenAddress"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.tokenAddress);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.amount);
            return writer;
        };

        /**
         * Encodes the specified Transaction message, length delimited. Does not implicitly {@link pb.Transaction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.Transaction
         * @static
         * @param {pb.ITransaction} message Transaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transaction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer.
         * @function decode
         * @memberof pb.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.Transaction} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.Transaction();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.to = reader.string();
                        break;
                    }
                case 3: {
                        message.tokenAddress = reader.string();
                        break;
                    }
                case 4: {
                        message.amount = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Transaction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.Transaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.Transaction} Transaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transaction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Transaction message.
         * @function verify
         * @memberof pb.Transaction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Transaction.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.to != null && message.hasOwnProperty("to"))
                if (!$util.isString(message.to))
                    return "to: string expected";
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                if (!$util.isString(message.tokenAddress))
                    return "tokenAddress: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            return null;
        };

        /**
         * Creates a Transaction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.Transaction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.Transaction} Transaction
         */
        Transaction.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.Transaction)
                return object;
            let message = new $root.pb.Transaction();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.Transaction.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.to != null)
                message.to = String(object.to);
            if (object.tokenAddress != null)
                message.tokenAddress = String(object.tokenAddress);
            if (object.amount != null)
                message.amount = String(object.amount);
            return message;
        };

        /**
         * Creates a plain object from a Transaction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.Transaction
         * @static
         * @param {pb.Transaction} message Transaction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Transaction.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.to = "";
                object.tokenAddress = "";
                object.amount = "";
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.to != null && message.hasOwnProperty("to"))
                object.to = message.to;
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                object.tokenAddress = message.tokenAddress;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            return object;
        };

        /**
         * Converts this Transaction to JSON.
         * @function toJSON
         * @memberof pb.Transaction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Transaction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Transaction
         * @function getTypeUrl
         * @memberof pb.Transaction
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Transaction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.Transaction";
        };

        return Transaction;
    })();

    /**
     * OrderSide enum.
     * @name pb.OrderSide
     * @enum {number}
     * @property {number} BUY=0 BUY value
     * @property {number} SELL=1 SELL value
     */
    pb.OrderSide = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "BUY"] = 0;
        values[valuesById[1] = "SELL"] = 1;
        return values;
    })();

    pb.OrderTx = (function() {

        /**
         * Properties of an OrderTx.
         * @memberof pb
         * @interface IOrderTx
         * @property {pb.IBaseMessage|null} [base] OrderTx base
         * @property {string|null} [baseToken] OrderTx baseToken
         * @property {string|null} [quoteToken] OrderTx quoteToken
         * @property {pb.OrderOp|null} [op] OrderTx op
         * @property {string|null} [opTargetId] OrderTx opTargetId
         * @property {string|null} [amount] OrderTx amount
         * @property {string|null} [price] OrderTx price
         * @property {pb.OrderSide|null} [side] OrderTx side
         */

        /**
         * Constructs a new OrderTx.
         * @memberof pb
         * @classdesc Represents an OrderTx.
         * @implements IOrderTx
         * @constructor
         * @param {pb.IOrderTx=} [properties] Properties to set
         */
        function OrderTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * OrderTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.base = null;

        /**
         * OrderTx baseToken.
         * @member {string} baseToken
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.baseToken = "";

        /**
         * OrderTx quoteToken.
         * @member {string} quoteToken
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.quoteToken = "";

        /**
         * OrderTx op.
         * @member {pb.OrderOp} op
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.op = 0;

        /**
         * OrderTx opTargetId.
         * @member {string} opTargetId
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.opTargetId = "";

        /**
         * OrderTx amount.
         * @member {string} amount
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.amount = "";

        /**
         * OrderTx price.
         * @member {string} price
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.price = "";

        /**
         * OrderTx side.
         * @member {pb.OrderSide} side
         * @memberof pb.OrderTx
         * @instance
         */
        OrderTx.prototype.side = 0;

        /**
         * Creates a new OrderTx instance using the specified properties.
         * @function create
         * @memberof pb.OrderTx
         * @static
         * @param {pb.IOrderTx=} [properties] Properties to set
         * @returns {pb.OrderTx} OrderTx instance
         */
        OrderTx.create = function create(properties) {
            return new OrderTx(properties);
        };

        /**
         * Encodes the specified OrderTx message. Does not implicitly {@link pb.OrderTx.verify|verify} messages.
         * @function encode
         * @memberof pb.OrderTx
         * @static
         * @param {pb.IOrderTx} message OrderTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OrderTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.baseToken != null && Object.hasOwnProperty.call(message, "baseToken"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.baseToken);
            if (message.quoteToken != null && Object.hasOwnProperty.call(message, "quoteToken"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.quoteToken);
            if (message.op != null && Object.hasOwnProperty.call(message, "op"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.op);
            if (message.opTargetId != null && Object.hasOwnProperty.call(message, "opTargetId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.opTargetId);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.amount);
            if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.price);
            if (message.side != null && Object.hasOwnProperty.call(message, "side"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.side);
            return writer;
        };

        /**
         * Encodes the specified OrderTx message, length delimited. Does not implicitly {@link pb.OrderTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.OrderTx
         * @static
         * @param {pb.IOrderTx} message OrderTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OrderTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an OrderTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.OrderTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.OrderTx} OrderTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OrderTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.OrderTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.baseToken = reader.string();
                        break;
                    }
                case 3: {
                        message.quoteToken = reader.string();
                        break;
                    }
                case 4: {
                        message.op = reader.int32();
                        break;
                    }
                case 5: {
                        message.opTargetId = reader.string();
                        break;
                    }
                case 6: {
                        message.amount = reader.string();
                        break;
                    }
                case 7: {
                        message.price = reader.string();
                        break;
                    }
                case 8: {
                        message.side = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an OrderTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.OrderTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.OrderTx} OrderTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OrderTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OrderTx message.
         * @function verify
         * @memberof pb.OrderTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OrderTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.baseToken != null && message.hasOwnProperty("baseToken"))
                if (!$util.isString(message.baseToken))
                    return "baseToken: string expected";
            if (message.quoteToken != null && message.hasOwnProperty("quoteToken"))
                if (!$util.isString(message.quoteToken))
                    return "quoteToken: string expected";
            if (message.op != null && message.hasOwnProperty("op"))
                switch (message.op) {
                default:
                    return "op: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.opTargetId != null && message.hasOwnProperty("opTargetId"))
                if (!$util.isString(message.opTargetId))
                    return "opTargetId: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.price != null && message.hasOwnProperty("price"))
                if (!$util.isString(message.price))
                    return "price: string expected";
            if (message.side != null && message.hasOwnProperty("side"))
                switch (message.side) {
                default:
                    return "side: enum value expected";
                case 0:
                case 1:
                    break;
                }
            return null;
        };

        /**
         * Creates an OrderTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.OrderTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.OrderTx} OrderTx
         */
        OrderTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.OrderTx)
                return object;
            let message = new $root.pb.OrderTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.OrderTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.baseToken != null)
                message.baseToken = String(object.baseToken);
            if (object.quoteToken != null)
                message.quoteToken = String(object.quoteToken);
            switch (object.op) {
            default:
                if (typeof object.op === "number") {
                    message.op = object.op;
                    break;
                }
                break;
            case "ADD":
            case 0:
                message.op = 0;
                break;
            case "REMOVE":
            case 1:
                message.op = 1;
                break;
            }
            if (object.opTargetId != null)
                message.opTargetId = String(object.opTargetId);
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.price != null)
                message.price = String(object.price);
            switch (object.side) {
            default:
                if (typeof object.side === "number") {
                    message.side = object.side;
                    break;
                }
                break;
            case "BUY":
            case 0:
                message.side = 0;
                break;
            case "SELL":
            case 1:
                message.side = 1;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an OrderTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.OrderTx
         * @static
         * @param {pb.OrderTx} message OrderTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OrderTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.baseToken = "";
                object.quoteToken = "";
                object.op = options.enums === String ? "ADD" : 0;
                object.opTargetId = "";
                object.amount = "";
                object.price = "";
                object.side = options.enums === String ? "BUY" : 0;
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.baseToken != null && message.hasOwnProperty("baseToken"))
                object.baseToken = message.baseToken;
            if (message.quoteToken != null && message.hasOwnProperty("quoteToken"))
                object.quoteToken = message.quoteToken;
            if (message.op != null && message.hasOwnProperty("op"))
                object.op = options.enums === String ? $root.pb.OrderOp[message.op] === undefined ? message.op : $root.pb.OrderOp[message.op] : message.op;
            if (message.opTargetId != null && message.hasOwnProperty("opTargetId"))
                object.opTargetId = message.opTargetId;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.price != null && message.hasOwnProperty("price"))
                object.price = message.price;
            if (message.side != null && message.hasOwnProperty("side"))
                object.side = options.enums === String ? $root.pb.OrderSide[message.side] === undefined ? message.side : $root.pb.OrderSide[message.side] : message.side;
            return object;
        };

        /**
         * Converts this OrderTx to JSON.
         * @function toJSON
         * @memberof pb.OrderTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OrderTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for OrderTx
         * @function getTypeUrl
         * @memberof pb.OrderTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        OrderTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.OrderTx";
        };

        return OrderTx;
    })();

    pb.OrderState = (function() {

        /**
         * Properties of an OrderState.
         * @memberof pb
         * @interface IOrderState
         * @property {string|null} [orderId] OrderState orderId
         * @property {string|null} [filledBase] OrderState filledBase
         * @property {string|null} [filledQuote] OrderState filledQuote
         * @property {boolean|null} [isFilled] OrderState isFilled
         * @property {pb.OrderStateStatus|null} [status] OrderState status
         * @property {number|Long|null} [createHeight] OrderState createHeight
         * @property {number|Long|null} [lastUpdateHeight] OrderState lastUpdateHeight
         * @property {string|null} [baseToken] OrderState baseToken
         * @property {string|null} [quoteToken] OrderState quoteToken
         * @property {string|null} [amount] OrderState amount
         * @property {string|null} [price] OrderState price
         * @property {pb.OrderSide|null} [side] OrderState side
         * @property {string|null} [owner] OrderState owner
         */

        /**
         * Constructs a new OrderState.
         * @memberof pb
         * @classdesc Represents an OrderState.
         * @implements IOrderState
         * @constructor
         * @param {pb.IOrderState=} [properties] Properties to set
         */
        function OrderState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * OrderState orderId.
         * @member {string} orderId
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.orderId = "";

        /**
         * OrderState filledBase.
         * @member {string} filledBase
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.filledBase = "";

        /**
         * OrderState filledQuote.
         * @member {string} filledQuote
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.filledQuote = "";

        /**
         * OrderState isFilled.
         * @member {boolean} isFilled
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.isFilled = false;

        /**
         * OrderState status.
         * @member {pb.OrderStateStatus} status
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.status = 0;

        /**
         * OrderState createHeight.
         * @member {number|Long} createHeight
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.createHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * OrderState lastUpdateHeight.
         * @member {number|Long} lastUpdateHeight
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.lastUpdateHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * OrderState baseToken.
         * @member {string} baseToken
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.baseToken = "";

        /**
         * OrderState quoteToken.
         * @member {string} quoteToken
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.quoteToken = "";

        /**
         * OrderState amount.
         * @member {string} amount
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.amount = "";

        /**
         * OrderState price.
         * @member {string} price
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.price = "";

        /**
         * OrderState side.
         * @member {pb.OrderSide} side
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.side = 0;

        /**
         * OrderState owner.
         * @member {string} owner
         * @memberof pb.OrderState
         * @instance
         */
        OrderState.prototype.owner = "";

        /**
         * Creates a new OrderState instance using the specified properties.
         * @function create
         * @memberof pb.OrderState
         * @static
         * @param {pb.IOrderState=} [properties] Properties to set
         * @returns {pb.OrderState} OrderState instance
         */
        OrderState.create = function create(properties) {
            return new OrderState(properties);
        };

        /**
         * Encodes the specified OrderState message. Does not implicitly {@link pb.OrderState.verify|verify} messages.
         * @function encode
         * @memberof pb.OrderState
         * @static
         * @param {pb.IOrderState} message OrderState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OrderState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.orderId != null && Object.hasOwnProperty.call(message, "orderId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.orderId);
            if (message.filledBase != null && Object.hasOwnProperty.call(message, "filledBase"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.filledBase);
            if (message.filledQuote != null && Object.hasOwnProperty.call(message, "filledQuote"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.filledQuote);
            if (message.isFilled != null && Object.hasOwnProperty.call(message, "isFilled"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isFilled);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.status);
            if (message.createHeight != null && Object.hasOwnProperty.call(message, "createHeight"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.createHeight);
            if (message.lastUpdateHeight != null && Object.hasOwnProperty.call(message, "lastUpdateHeight"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.lastUpdateHeight);
            if (message.baseToken != null && Object.hasOwnProperty.call(message, "baseToken"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.baseToken);
            if (message.quoteToken != null && Object.hasOwnProperty.call(message, "quoteToken"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.quoteToken);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.amount);
            if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.price);
            if (message.side != null && Object.hasOwnProperty.call(message, "side"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.side);
            if (message.owner != null && Object.hasOwnProperty.call(message, "owner"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.owner);
            return writer;
        };

        /**
         * Encodes the specified OrderState message, length delimited. Does not implicitly {@link pb.OrderState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.OrderState
         * @static
         * @param {pb.IOrderState} message OrderState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OrderState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an OrderState message from the specified reader or buffer.
         * @function decode
         * @memberof pb.OrderState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.OrderState} OrderState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OrderState.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.OrderState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.orderId = reader.string();
                        break;
                    }
                case 2: {
                        message.filledBase = reader.string();
                        break;
                    }
                case 3: {
                        message.filledQuote = reader.string();
                        break;
                    }
                case 4: {
                        message.isFilled = reader.bool();
                        break;
                    }
                case 5: {
                        message.status = reader.int32();
                        break;
                    }
                case 6: {
                        message.createHeight = reader.uint64();
                        break;
                    }
                case 7: {
                        message.lastUpdateHeight = reader.uint64();
                        break;
                    }
                case 8: {
                        message.baseToken = reader.string();
                        break;
                    }
                case 9: {
                        message.quoteToken = reader.string();
                        break;
                    }
                case 10: {
                        message.amount = reader.string();
                        break;
                    }
                case 11: {
                        message.price = reader.string();
                        break;
                    }
                case 12: {
                        message.side = reader.int32();
                        break;
                    }
                case 13: {
                        message.owner = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an OrderState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.OrderState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.OrderState} OrderState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OrderState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an OrderState message.
         * @function verify
         * @memberof pb.OrderState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        OrderState.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.orderId != null && message.hasOwnProperty("orderId"))
                if (!$util.isString(message.orderId))
                    return "orderId: string expected";
            if (message.filledBase != null && message.hasOwnProperty("filledBase"))
                if (!$util.isString(message.filledBase))
                    return "filledBase: string expected";
            if (message.filledQuote != null && message.hasOwnProperty("filledQuote"))
                if (!$util.isString(message.filledQuote))
                    return "filledQuote: string expected";
            if (message.isFilled != null && message.hasOwnProperty("isFilled"))
                if (typeof message.isFilled !== "boolean")
                    return "isFilled: boolean expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.createHeight != null && message.hasOwnProperty("createHeight"))
                if (!$util.isInteger(message.createHeight) && !(message.createHeight && $util.isInteger(message.createHeight.low) && $util.isInteger(message.createHeight.high)))
                    return "createHeight: integer|Long expected";
            if (message.lastUpdateHeight != null && message.hasOwnProperty("lastUpdateHeight"))
                if (!$util.isInteger(message.lastUpdateHeight) && !(message.lastUpdateHeight && $util.isInteger(message.lastUpdateHeight.low) && $util.isInteger(message.lastUpdateHeight.high)))
                    return "lastUpdateHeight: integer|Long expected";
            if (message.baseToken != null && message.hasOwnProperty("baseToken"))
                if (!$util.isString(message.baseToken))
                    return "baseToken: string expected";
            if (message.quoteToken != null && message.hasOwnProperty("quoteToken"))
                if (!$util.isString(message.quoteToken))
                    return "quoteToken: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.price != null && message.hasOwnProperty("price"))
                if (!$util.isString(message.price))
                    return "price: string expected";
            if (message.side != null && message.hasOwnProperty("side"))
                switch (message.side) {
                default:
                    return "side: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.owner != null && message.hasOwnProperty("owner"))
                if (!$util.isString(message.owner))
                    return "owner: string expected";
            return null;
        };

        /**
         * Creates an OrderState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.OrderState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.OrderState} OrderState
         */
        OrderState.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.OrderState)
                return object;
            let message = new $root.pb.OrderState();
            if (object.orderId != null)
                message.orderId = String(object.orderId);
            if (object.filledBase != null)
                message.filledBase = String(object.filledBase);
            if (object.filledQuote != null)
                message.filledQuote = String(object.filledQuote);
            if (object.isFilled != null)
                message.isFilled = Boolean(object.isFilled);
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "ORDER_OPEN":
            case 0:
                message.status = 0;
                break;
            case "ORDER_FILLED":
            case 1:
                message.status = 1;
                break;
            case "ORDER_CANCELLED":
            case 2:
                message.status = 2;
                break;
            }
            if (object.createHeight != null)
                if ($util.Long)
                    (message.createHeight = $util.Long.fromValue(object.createHeight)).unsigned = true;
                else if (typeof object.createHeight === "string")
                    message.createHeight = parseInt(object.createHeight, 10);
                else if (typeof object.createHeight === "number")
                    message.createHeight = object.createHeight;
                else if (typeof object.createHeight === "object")
                    message.createHeight = new $util.LongBits(object.createHeight.low >>> 0, object.createHeight.high >>> 0).toNumber(true);
            if (object.lastUpdateHeight != null)
                if ($util.Long)
                    (message.lastUpdateHeight = $util.Long.fromValue(object.lastUpdateHeight)).unsigned = true;
                else if (typeof object.lastUpdateHeight === "string")
                    message.lastUpdateHeight = parseInt(object.lastUpdateHeight, 10);
                else if (typeof object.lastUpdateHeight === "number")
                    message.lastUpdateHeight = object.lastUpdateHeight;
                else if (typeof object.lastUpdateHeight === "object")
                    message.lastUpdateHeight = new $util.LongBits(object.lastUpdateHeight.low >>> 0, object.lastUpdateHeight.high >>> 0).toNumber(true);
            if (object.baseToken != null)
                message.baseToken = String(object.baseToken);
            if (object.quoteToken != null)
                message.quoteToken = String(object.quoteToken);
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.price != null)
                message.price = String(object.price);
            switch (object.side) {
            default:
                if (typeof object.side === "number") {
                    message.side = object.side;
                    break;
                }
                break;
            case "BUY":
            case 0:
                message.side = 0;
                break;
            case "SELL":
            case 1:
                message.side = 1;
                break;
            }
            if (object.owner != null)
                message.owner = String(object.owner);
            return message;
        };

        /**
         * Creates a plain object from an OrderState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.OrderState
         * @static
         * @param {pb.OrderState} message OrderState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OrderState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.orderId = "";
                object.filledBase = "";
                object.filledQuote = "";
                object.isFilled = false;
                object.status = options.enums === String ? "ORDER_OPEN" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.createHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.createHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.lastUpdateHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.lastUpdateHeight = options.longs === String ? "0" : 0;
                object.baseToken = "";
                object.quoteToken = "";
                object.amount = "";
                object.price = "";
                object.side = options.enums === String ? "BUY" : 0;
                object.owner = "";
            }
            if (message.orderId != null && message.hasOwnProperty("orderId"))
                object.orderId = message.orderId;
            if (message.filledBase != null && message.hasOwnProperty("filledBase"))
                object.filledBase = message.filledBase;
            if (message.filledQuote != null && message.hasOwnProperty("filledQuote"))
                object.filledQuote = message.filledQuote;
            if (message.isFilled != null && message.hasOwnProperty("isFilled"))
                object.isFilled = message.isFilled;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.pb.OrderStateStatus[message.status] === undefined ? message.status : $root.pb.OrderStateStatus[message.status] : message.status;
            if (message.createHeight != null && message.hasOwnProperty("createHeight"))
                if (typeof message.createHeight === "number")
                    object.createHeight = options.longs === String ? String(message.createHeight) : message.createHeight;
                else
                    object.createHeight = options.longs === String ? $util.Long.prototype.toString.call(message.createHeight) : options.longs === Number ? new $util.LongBits(message.createHeight.low >>> 0, message.createHeight.high >>> 0).toNumber(true) : message.createHeight;
            if (message.lastUpdateHeight != null && message.hasOwnProperty("lastUpdateHeight"))
                if (typeof message.lastUpdateHeight === "number")
                    object.lastUpdateHeight = options.longs === String ? String(message.lastUpdateHeight) : message.lastUpdateHeight;
                else
                    object.lastUpdateHeight = options.longs === String ? $util.Long.prototype.toString.call(message.lastUpdateHeight) : options.longs === Number ? new $util.LongBits(message.lastUpdateHeight.low >>> 0, message.lastUpdateHeight.high >>> 0).toNumber(true) : message.lastUpdateHeight;
            if (message.baseToken != null && message.hasOwnProperty("baseToken"))
                object.baseToken = message.baseToken;
            if (message.quoteToken != null && message.hasOwnProperty("quoteToken"))
                object.quoteToken = message.quoteToken;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.price != null && message.hasOwnProperty("price"))
                object.price = message.price;
            if (message.side != null && message.hasOwnProperty("side"))
                object.side = options.enums === String ? $root.pb.OrderSide[message.side] === undefined ? message.side : $root.pb.OrderSide[message.side] : message.side;
            if (message.owner != null && message.hasOwnProperty("owner"))
                object.owner = message.owner;
            return object;
        };

        /**
         * Converts this OrderState to JSON.
         * @function toJSON
         * @memberof pb.OrderState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OrderState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for OrderState
         * @function getTypeUrl
         * @memberof pb.OrderState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        OrderState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.OrderState";
        };

        return OrderState;
    })();

    /**
     * OrderStateStatus enum.
     * @name pb.OrderStateStatus
     * @enum {number}
     * @property {number} ORDER_OPEN=0 ORDER_OPEN value
     * @property {number} ORDER_FILLED=1 ORDER_FILLED value
     * @property {number} ORDER_CANCELLED=2 ORDER_CANCELLED value
     */
    pb.OrderStateStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ORDER_OPEN"] = 0;
        values[valuesById[1] = "ORDER_FILLED"] = 1;
        values[valuesById[2] = "ORDER_CANCELLED"] = 2;
        return values;
    })();

    pb.MinerTx = (function() {

        /**
         * Properties of a MinerTx.
         * @memberof pb
         * @interface IMinerTx
         * @property {pb.IBaseMessage|null} [base] MinerTx base
         * @property {pb.OrderOp|null} [op] MinerTx op
         * @property {string|null} [amount] MinerTx amount
         */

        /**
         * Constructs a new MinerTx.
         * @memberof pb
         * @classdesc Represents a MinerTx.
         * @implements IMinerTx
         * @constructor
         * @param {pb.IMinerTx=} [properties] Properties to set
         */
        function MinerTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MinerTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.MinerTx
         * @instance
         */
        MinerTx.prototype.base = null;

        /**
         * MinerTx op.
         * @member {pb.OrderOp} op
         * @memberof pb.MinerTx
         * @instance
         */
        MinerTx.prototype.op = 0;

        /**
         * MinerTx amount.
         * @member {string} amount
         * @memberof pb.MinerTx
         * @instance
         */
        MinerTx.prototype.amount = "";

        /**
         * Creates a new MinerTx instance using the specified properties.
         * @function create
         * @memberof pb.MinerTx
         * @static
         * @param {pb.IMinerTx=} [properties] Properties to set
         * @returns {pb.MinerTx} MinerTx instance
         */
        MinerTx.create = function create(properties) {
            return new MinerTx(properties);
        };

        /**
         * Encodes the specified MinerTx message. Does not implicitly {@link pb.MinerTx.verify|verify} messages.
         * @function encode
         * @memberof pb.MinerTx
         * @static
         * @param {pb.IMinerTx} message MinerTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MinerTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.op != null && Object.hasOwnProperty.call(message, "op"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.op);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.amount);
            return writer;
        };

        /**
         * Encodes the specified MinerTx message, length delimited. Does not implicitly {@link pb.MinerTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.MinerTx
         * @static
         * @param {pb.IMinerTx} message MinerTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MinerTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MinerTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.MinerTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.MinerTx} MinerTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MinerTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.MinerTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.op = reader.int32();
                        break;
                    }
                case 3: {
                        message.amount = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MinerTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.MinerTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.MinerTx} MinerTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MinerTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MinerTx message.
         * @function verify
         * @memberof pb.MinerTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MinerTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.op != null && message.hasOwnProperty("op"))
                switch (message.op) {
                default:
                    return "op: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            return null;
        };

        /**
         * Creates a MinerTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.MinerTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.MinerTx} MinerTx
         */
        MinerTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.MinerTx)
                return object;
            let message = new $root.pb.MinerTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.MinerTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            switch (object.op) {
            default:
                if (typeof object.op === "number") {
                    message.op = object.op;
                    break;
                }
                break;
            case "ADD":
            case 0:
                message.op = 0;
                break;
            case "REMOVE":
            case 1:
                message.op = 1;
                break;
            }
            if (object.amount != null)
                message.amount = String(object.amount);
            return message;
        };

        /**
         * Creates a plain object from a MinerTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.MinerTx
         * @static
         * @param {pb.MinerTx} message MinerTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MinerTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.op = options.enums === String ? "ADD" : 0;
                object.amount = "";
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.op != null && message.hasOwnProperty("op"))
                object.op = options.enums === String ? $root.pb.OrderOp[message.op] === undefined ? message.op : $root.pb.OrderOp[message.op] : message.op;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            return object;
        };

        /**
         * Converts this MinerTx to JSON.
         * @function toJSON
         * @memberof pb.MinerTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MinerTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MinerTx
         * @function getTypeUrl
         * @memberof pb.MinerTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MinerTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.MinerTx";
        };

        return MinerTx;
    })();

    pb.AnyTx = (function() {

        /**
         * Properties of an AnyTx.
         * @memberof pb
         * @interface IAnyTx
         * @property {pb.IIssueTokenTx|null} [issueTokenTx] AnyTx issueTokenTx
         * @property {pb.IFreezeTx|null} [freezeTx] AnyTx freezeTx
         * @property {pb.ITransaction|null} [transaction] AnyTx transaction
         * @property {pb.IOrderTx|null} [orderTx] AnyTx orderTx
         * @property {pb.IMinerTx|null} [minerTx] AnyTx minerTx
         * @property {pb.IWitnessStakeTx|null} [witnessStakeTx] AnyTx witnessStakeTx
         * @property {pb.IWitnessRequestTx|null} [witnessRequestTx] AnyTx witnessRequestTx
         * @property {pb.IWitnessVoteTx|null} [witnessVoteTx] AnyTx witnessVoteTx
         * @property {pb.IWitnessChallengeTx|null} [witnessChallengeTx] AnyTx witnessChallengeTx
         * @property {pb.IArbitrationVoteTx|null} [arbitrationVoteTx] AnyTx arbitrationVoteTx
         * @property {pb.IWitnessClaimRewardTx|null} [witnessClaimRewardTx] AnyTx witnessClaimRewardTx
         * @property {pb.IFrostWithdrawRequestTx|null} [frostWithdrawRequestTx] AnyTx frostWithdrawRequestTx
         * @property {pb.IFrostWithdrawSignedTx|null} [frostWithdrawSignedTx] AnyTx frostWithdrawSignedTx
         * @property {pb.IFrostVaultDkgCommitTx|null} [frostVaultDkgCommitTx] AnyTx frostVaultDkgCommitTx
         * @property {pb.IFrostVaultDkgShareTx|null} [frostVaultDkgShareTx] AnyTx frostVaultDkgShareTx
         * @property {pb.IFrostVaultDkgComplaintTx|null} [frostVaultDkgComplaintTx] AnyTx frostVaultDkgComplaintTx
         * @property {pb.IFrostVaultDkgRevealTx|null} [frostVaultDkgRevealTx] AnyTx frostVaultDkgRevealTx
         * @property {pb.IFrostVaultDkgValidationSignedTx|null} [frostVaultDkgValidationSignedTx] AnyTx frostVaultDkgValidationSignedTx
         * @property {pb.IFrostVaultTransitionSignedTx|null} [frostVaultTransitionSignedTx] AnyTx frostVaultTransitionSignedTx
         */

        /**
         * Constructs a new AnyTx.
         * @memberof pb
         * @classdesc Represents an AnyTx.
         * @implements IAnyTx
         * @constructor
         * @param {pb.IAnyTx=} [properties] Properties to set
         */
        function AnyTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AnyTx issueTokenTx.
         * @member {pb.IIssueTokenTx|null|undefined} issueTokenTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.issueTokenTx = null;

        /**
         * AnyTx freezeTx.
         * @member {pb.IFreezeTx|null|undefined} freezeTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.freezeTx = null;

        /**
         * AnyTx transaction.
         * @member {pb.ITransaction|null|undefined} transaction
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.transaction = null;

        /**
         * AnyTx orderTx.
         * @member {pb.IOrderTx|null|undefined} orderTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.orderTx = null;

        /**
         * AnyTx minerTx.
         * @member {pb.IMinerTx|null|undefined} minerTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.minerTx = null;

        /**
         * AnyTx witnessStakeTx.
         * @member {pb.IWitnessStakeTx|null|undefined} witnessStakeTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.witnessStakeTx = null;

        /**
         * AnyTx witnessRequestTx.
         * @member {pb.IWitnessRequestTx|null|undefined} witnessRequestTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.witnessRequestTx = null;

        /**
         * AnyTx witnessVoteTx.
         * @member {pb.IWitnessVoteTx|null|undefined} witnessVoteTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.witnessVoteTx = null;

        /**
         * AnyTx witnessChallengeTx.
         * @member {pb.IWitnessChallengeTx|null|undefined} witnessChallengeTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.witnessChallengeTx = null;

        /**
         * AnyTx arbitrationVoteTx.
         * @member {pb.IArbitrationVoteTx|null|undefined} arbitrationVoteTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.arbitrationVoteTx = null;

        /**
         * AnyTx witnessClaimRewardTx.
         * @member {pb.IWitnessClaimRewardTx|null|undefined} witnessClaimRewardTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.witnessClaimRewardTx = null;

        /**
         * AnyTx frostWithdrawRequestTx.
         * @member {pb.IFrostWithdrawRequestTx|null|undefined} frostWithdrawRequestTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostWithdrawRequestTx = null;

        /**
         * AnyTx frostWithdrawSignedTx.
         * @member {pb.IFrostWithdrawSignedTx|null|undefined} frostWithdrawSignedTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostWithdrawSignedTx = null;

        /**
         * AnyTx frostVaultDkgCommitTx.
         * @member {pb.IFrostVaultDkgCommitTx|null|undefined} frostVaultDkgCommitTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostVaultDkgCommitTx = null;

        /**
         * AnyTx frostVaultDkgShareTx.
         * @member {pb.IFrostVaultDkgShareTx|null|undefined} frostVaultDkgShareTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostVaultDkgShareTx = null;

        /**
         * AnyTx frostVaultDkgComplaintTx.
         * @member {pb.IFrostVaultDkgComplaintTx|null|undefined} frostVaultDkgComplaintTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostVaultDkgComplaintTx = null;

        /**
         * AnyTx frostVaultDkgRevealTx.
         * @member {pb.IFrostVaultDkgRevealTx|null|undefined} frostVaultDkgRevealTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostVaultDkgRevealTx = null;

        /**
         * AnyTx frostVaultDkgValidationSignedTx.
         * @member {pb.IFrostVaultDkgValidationSignedTx|null|undefined} frostVaultDkgValidationSignedTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostVaultDkgValidationSignedTx = null;

        /**
         * AnyTx frostVaultTransitionSignedTx.
         * @member {pb.IFrostVaultTransitionSignedTx|null|undefined} frostVaultTransitionSignedTx
         * @memberof pb.AnyTx
         * @instance
         */
        AnyTx.prototype.frostVaultTransitionSignedTx = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * AnyTx content.
         * @member {"issueTokenTx"|"freezeTx"|"transaction"|"orderTx"|"minerTx"|"witnessStakeTx"|"witnessRequestTx"|"witnessVoteTx"|"witnessChallengeTx"|"arbitrationVoteTx"|"witnessClaimRewardTx"|"frostWithdrawRequestTx"|"frostWithdrawSignedTx"|"frostVaultDkgCommitTx"|"frostVaultDkgShareTx"|"frostVaultDkgComplaintTx"|"frostVaultDkgRevealTx"|"frostVaultDkgValidationSignedTx"|"frostVaultTransitionSignedTx"|undefined} content
         * @memberof pb.AnyTx
         * @instance
         */
        Object.defineProperty(AnyTx.prototype, "content", {
            get: $util.oneOfGetter($oneOfFields = ["issueTokenTx", "freezeTx", "transaction", "orderTx", "minerTx", "witnessStakeTx", "witnessRequestTx", "witnessVoteTx", "witnessChallengeTx", "arbitrationVoteTx", "witnessClaimRewardTx", "frostWithdrawRequestTx", "frostWithdrawSignedTx", "frostVaultDkgCommitTx", "frostVaultDkgShareTx", "frostVaultDkgComplaintTx", "frostVaultDkgRevealTx", "frostVaultDkgValidationSignedTx", "frostVaultTransitionSignedTx"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AnyTx instance using the specified properties.
         * @function create
         * @memberof pb.AnyTx
         * @static
         * @param {pb.IAnyTx=} [properties] Properties to set
         * @returns {pb.AnyTx} AnyTx instance
         */
        AnyTx.create = function create(properties) {
            return new AnyTx(properties);
        };

        /**
         * Encodes the specified AnyTx message. Does not implicitly {@link pb.AnyTx.verify|verify} messages.
         * @function encode
         * @memberof pb.AnyTx
         * @static
         * @param {pb.IAnyTx} message AnyTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnyTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.issueTokenTx != null && Object.hasOwnProperty.call(message, "issueTokenTx"))
                $root.pb.IssueTokenTx.encode(message.issueTokenTx, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.freezeTx != null && Object.hasOwnProperty.call(message, "freezeTx"))
                $root.pb.FreezeTx.encode(message.freezeTx, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.transaction != null && Object.hasOwnProperty.call(message, "transaction"))
                $root.pb.Transaction.encode(message.transaction, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            if (message.orderTx != null && Object.hasOwnProperty.call(message, "orderTx"))
                $root.pb.OrderTx.encode(message.orderTx, writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.minerTx != null && Object.hasOwnProperty.call(message, "minerTx"))
                $root.pb.MinerTx.encode(message.minerTx, writer.uint32(/* id 6, wireType 2 =*/50).fork()).ldelim();
            if (message.witnessStakeTx != null && Object.hasOwnProperty.call(message, "witnessStakeTx"))
                $root.pb.WitnessStakeTx.encode(message.witnessStakeTx, writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            if (message.witnessRequestTx != null && Object.hasOwnProperty.call(message, "witnessRequestTx"))
                $root.pb.WitnessRequestTx.encode(message.witnessRequestTx, writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            if (message.witnessVoteTx != null && Object.hasOwnProperty.call(message, "witnessVoteTx"))
                $root.pb.WitnessVoteTx.encode(message.witnessVoteTx, writer.uint32(/* id 9, wireType 2 =*/74).fork()).ldelim();
            if (message.witnessChallengeTx != null && Object.hasOwnProperty.call(message, "witnessChallengeTx"))
                $root.pb.WitnessChallengeTx.encode(message.witnessChallengeTx, writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.arbitrationVoteTx != null && Object.hasOwnProperty.call(message, "arbitrationVoteTx"))
                $root.pb.ArbitrationVoteTx.encode(message.arbitrationVoteTx, writer.uint32(/* id 11, wireType 2 =*/90).fork()).ldelim();
            if (message.witnessClaimRewardTx != null && Object.hasOwnProperty.call(message, "witnessClaimRewardTx"))
                $root.pb.WitnessClaimRewardTx.encode(message.witnessClaimRewardTx, writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            if (message.frostWithdrawRequestTx != null && Object.hasOwnProperty.call(message, "frostWithdrawRequestTx"))
                $root.pb.FrostWithdrawRequestTx.encode(message.frostWithdrawRequestTx, writer.uint32(/* id 13, wireType 2 =*/106).fork()).ldelim();
            if (message.frostWithdrawSignedTx != null && Object.hasOwnProperty.call(message, "frostWithdrawSignedTx"))
                $root.pb.FrostWithdrawSignedTx.encode(message.frostWithdrawSignedTx, writer.uint32(/* id 14, wireType 2 =*/114).fork()).ldelim();
            if (message.frostVaultDkgCommitTx != null && Object.hasOwnProperty.call(message, "frostVaultDkgCommitTx"))
                $root.pb.FrostVaultDkgCommitTx.encode(message.frostVaultDkgCommitTx, writer.uint32(/* id 15, wireType 2 =*/122).fork()).ldelim();
            if (message.frostVaultDkgShareTx != null && Object.hasOwnProperty.call(message, "frostVaultDkgShareTx"))
                $root.pb.FrostVaultDkgShareTx.encode(message.frostVaultDkgShareTx, writer.uint32(/* id 16, wireType 2 =*/130).fork()).ldelim();
            if (message.frostVaultDkgComplaintTx != null && Object.hasOwnProperty.call(message, "frostVaultDkgComplaintTx"))
                $root.pb.FrostVaultDkgComplaintTx.encode(message.frostVaultDkgComplaintTx, writer.uint32(/* id 17, wireType 2 =*/138).fork()).ldelim();
            if (message.frostVaultDkgRevealTx != null && Object.hasOwnProperty.call(message, "frostVaultDkgRevealTx"))
                $root.pb.FrostVaultDkgRevealTx.encode(message.frostVaultDkgRevealTx, writer.uint32(/* id 18, wireType 2 =*/146).fork()).ldelim();
            if (message.frostVaultDkgValidationSignedTx != null && Object.hasOwnProperty.call(message, "frostVaultDkgValidationSignedTx"))
                $root.pb.FrostVaultDkgValidationSignedTx.encode(message.frostVaultDkgValidationSignedTx, writer.uint32(/* id 19, wireType 2 =*/154).fork()).ldelim();
            if (message.frostVaultTransitionSignedTx != null && Object.hasOwnProperty.call(message, "frostVaultTransitionSignedTx"))
                $root.pb.FrostVaultTransitionSignedTx.encode(message.frostVaultTransitionSignedTx, writer.uint32(/* id 20, wireType 2 =*/162).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified AnyTx message, length delimited. Does not implicitly {@link pb.AnyTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.AnyTx
         * @static
         * @param {pb.IAnyTx} message AnyTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnyTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an AnyTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.AnyTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.AnyTx} AnyTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnyTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.AnyTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.issueTokenTx = $root.pb.IssueTokenTx.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.freezeTx = $root.pb.FreezeTx.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.transaction = $root.pb.Transaction.decode(reader, reader.uint32());
                        break;
                    }
                case 4: {
                        message.orderTx = $root.pb.OrderTx.decode(reader, reader.uint32());
                        break;
                    }
                case 6: {
                        message.minerTx = $root.pb.MinerTx.decode(reader, reader.uint32());
                        break;
                    }
                case 7: {
                        message.witnessStakeTx = $root.pb.WitnessStakeTx.decode(reader, reader.uint32());
                        break;
                    }
                case 8: {
                        message.witnessRequestTx = $root.pb.WitnessRequestTx.decode(reader, reader.uint32());
                        break;
                    }
                case 9: {
                        message.witnessVoteTx = $root.pb.WitnessVoteTx.decode(reader, reader.uint32());
                        break;
                    }
                case 10: {
                        message.witnessChallengeTx = $root.pb.WitnessChallengeTx.decode(reader, reader.uint32());
                        break;
                    }
                case 11: {
                        message.arbitrationVoteTx = $root.pb.ArbitrationVoteTx.decode(reader, reader.uint32());
                        break;
                    }
                case 12: {
                        message.witnessClaimRewardTx = $root.pb.WitnessClaimRewardTx.decode(reader, reader.uint32());
                        break;
                    }
                case 13: {
                        message.frostWithdrawRequestTx = $root.pb.FrostWithdrawRequestTx.decode(reader, reader.uint32());
                        break;
                    }
                case 14: {
                        message.frostWithdrawSignedTx = $root.pb.FrostWithdrawSignedTx.decode(reader, reader.uint32());
                        break;
                    }
                case 15: {
                        message.frostVaultDkgCommitTx = $root.pb.FrostVaultDkgCommitTx.decode(reader, reader.uint32());
                        break;
                    }
                case 16: {
                        message.frostVaultDkgShareTx = $root.pb.FrostVaultDkgShareTx.decode(reader, reader.uint32());
                        break;
                    }
                case 17: {
                        message.frostVaultDkgComplaintTx = $root.pb.FrostVaultDkgComplaintTx.decode(reader, reader.uint32());
                        break;
                    }
                case 18: {
                        message.frostVaultDkgRevealTx = $root.pb.FrostVaultDkgRevealTx.decode(reader, reader.uint32());
                        break;
                    }
                case 19: {
                        message.frostVaultDkgValidationSignedTx = $root.pb.FrostVaultDkgValidationSignedTx.decode(reader, reader.uint32());
                        break;
                    }
                case 20: {
                        message.frostVaultTransitionSignedTx = $root.pb.FrostVaultTransitionSignedTx.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AnyTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.AnyTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.AnyTx} AnyTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnyTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AnyTx message.
         * @function verify
         * @memberof pb.AnyTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AnyTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            let properties = {};
            if (message.issueTokenTx != null && message.hasOwnProperty("issueTokenTx")) {
                properties.content = 1;
                {
                    let error = $root.pb.IssueTokenTx.verify(message.issueTokenTx);
                    if (error)
                        return "issueTokenTx." + error;
                }
            }
            if (message.freezeTx != null && message.hasOwnProperty("freezeTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FreezeTx.verify(message.freezeTx);
                    if (error)
                        return "freezeTx." + error;
                }
            }
            if (message.transaction != null && message.hasOwnProperty("transaction")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.Transaction.verify(message.transaction);
                    if (error)
                        return "transaction." + error;
                }
            }
            if (message.orderTx != null && message.hasOwnProperty("orderTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.OrderTx.verify(message.orderTx);
                    if (error)
                        return "orderTx." + error;
                }
            }
            if (message.minerTx != null && message.hasOwnProperty("minerTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.MinerTx.verify(message.minerTx);
                    if (error)
                        return "minerTx." + error;
                }
            }
            if (message.witnessStakeTx != null && message.hasOwnProperty("witnessStakeTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.WitnessStakeTx.verify(message.witnessStakeTx);
                    if (error)
                        return "witnessStakeTx." + error;
                }
            }
            if (message.witnessRequestTx != null && message.hasOwnProperty("witnessRequestTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.WitnessRequestTx.verify(message.witnessRequestTx);
                    if (error)
                        return "witnessRequestTx." + error;
                }
            }
            if (message.witnessVoteTx != null && message.hasOwnProperty("witnessVoteTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.WitnessVoteTx.verify(message.witnessVoteTx);
                    if (error)
                        return "witnessVoteTx." + error;
                }
            }
            if (message.witnessChallengeTx != null && message.hasOwnProperty("witnessChallengeTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.WitnessChallengeTx.verify(message.witnessChallengeTx);
                    if (error)
                        return "witnessChallengeTx." + error;
                }
            }
            if (message.arbitrationVoteTx != null && message.hasOwnProperty("arbitrationVoteTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.ArbitrationVoteTx.verify(message.arbitrationVoteTx);
                    if (error)
                        return "arbitrationVoteTx." + error;
                }
            }
            if (message.witnessClaimRewardTx != null && message.hasOwnProperty("witnessClaimRewardTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.WitnessClaimRewardTx.verify(message.witnessClaimRewardTx);
                    if (error)
                        return "witnessClaimRewardTx." + error;
                }
            }
            if (message.frostWithdrawRequestTx != null && message.hasOwnProperty("frostWithdrawRequestTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostWithdrawRequestTx.verify(message.frostWithdrawRequestTx);
                    if (error)
                        return "frostWithdrawRequestTx." + error;
                }
            }
            if (message.frostWithdrawSignedTx != null && message.hasOwnProperty("frostWithdrawSignedTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostWithdrawSignedTx.verify(message.frostWithdrawSignedTx);
                    if (error)
                        return "frostWithdrawSignedTx." + error;
                }
            }
            if (message.frostVaultDkgCommitTx != null && message.hasOwnProperty("frostVaultDkgCommitTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostVaultDkgCommitTx.verify(message.frostVaultDkgCommitTx);
                    if (error)
                        return "frostVaultDkgCommitTx." + error;
                }
            }
            if (message.frostVaultDkgShareTx != null && message.hasOwnProperty("frostVaultDkgShareTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostVaultDkgShareTx.verify(message.frostVaultDkgShareTx);
                    if (error)
                        return "frostVaultDkgShareTx." + error;
                }
            }
            if (message.frostVaultDkgComplaintTx != null && message.hasOwnProperty("frostVaultDkgComplaintTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostVaultDkgComplaintTx.verify(message.frostVaultDkgComplaintTx);
                    if (error)
                        return "frostVaultDkgComplaintTx." + error;
                }
            }
            if (message.frostVaultDkgRevealTx != null && message.hasOwnProperty("frostVaultDkgRevealTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostVaultDkgRevealTx.verify(message.frostVaultDkgRevealTx);
                    if (error)
                        return "frostVaultDkgRevealTx." + error;
                }
            }
            if (message.frostVaultDkgValidationSignedTx != null && message.hasOwnProperty("frostVaultDkgValidationSignedTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostVaultDkgValidationSignedTx.verify(message.frostVaultDkgValidationSignedTx);
                    if (error)
                        return "frostVaultDkgValidationSignedTx." + error;
                }
            }
            if (message.frostVaultTransitionSignedTx != null && message.hasOwnProperty("frostVaultTransitionSignedTx")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    let error = $root.pb.FrostVaultTransitionSignedTx.verify(message.frostVaultTransitionSignedTx);
                    if (error)
                        return "frostVaultTransitionSignedTx." + error;
                }
            }
            return null;
        };

        /**
         * Creates an AnyTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.AnyTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.AnyTx} AnyTx
         */
        AnyTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.AnyTx)
                return object;
            let message = new $root.pb.AnyTx();
            if (object.issueTokenTx != null) {
                if (typeof object.issueTokenTx !== "object")
                    throw TypeError(".pb.AnyTx.issueTokenTx: object expected");
                message.issueTokenTx = $root.pb.IssueTokenTx.fromObject(object.issueTokenTx);
            }
            if (object.freezeTx != null) {
                if (typeof object.freezeTx !== "object")
                    throw TypeError(".pb.AnyTx.freezeTx: object expected");
                message.freezeTx = $root.pb.FreezeTx.fromObject(object.freezeTx);
            }
            if (object.transaction != null) {
                if (typeof object.transaction !== "object")
                    throw TypeError(".pb.AnyTx.transaction: object expected");
                message.transaction = $root.pb.Transaction.fromObject(object.transaction);
            }
            if (object.orderTx != null) {
                if (typeof object.orderTx !== "object")
                    throw TypeError(".pb.AnyTx.orderTx: object expected");
                message.orderTx = $root.pb.OrderTx.fromObject(object.orderTx);
            }
            if (object.minerTx != null) {
                if (typeof object.minerTx !== "object")
                    throw TypeError(".pb.AnyTx.minerTx: object expected");
                message.minerTx = $root.pb.MinerTx.fromObject(object.minerTx);
            }
            if (object.witnessStakeTx != null) {
                if (typeof object.witnessStakeTx !== "object")
                    throw TypeError(".pb.AnyTx.witnessStakeTx: object expected");
                message.witnessStakeTx = $root.pb.WitnessStakeTx.fromObject(object.witnessStakeTx);
            }
            if (object.witnessRequestTx != null) {
                if (typeof object.witnessRequestTx !== "object")
                    throw TypeError(".pb.AnyTx.witnessRequestTx: object expected");
                message.witnessRequestTx = $root.pb.WitnessRequestTx.fromObject(object.witnessRequestTx);
            }
            if (object.witnessVoteTx != null) {
                if (typeof object.witnessVoteTx !== "object")
                    throw TypeError(".pb.AnyTx.witnessVoteTx: object expected");
                message.witnessVoteTx = $root.pb.WitnessVoteTx.fromObject(object.witnessVoteTx);
            }
            if (object.witnessChallengeTx != null) {
                if (typeof object.witnessChallengeTx !== "object")
                    throw TypeError(".pb.AnyTx.witnessChallengeTx: object expected");
                message.witnessChallengeTx = $root.pb.WitnessChallengeTx.fromObject(object.witnessChallengeTx);
            }
            if (object.arbitrationVoteTx != null) {
                if (typeof object.arbitrationVoteTx !== "object")
                    throw TypeError(".pb.AnyTx.arbitrationVoteTx: object expected");
                message.arbitrationVoteTx = $root.pb.ArbitrationVoteTx.fromObject(object.arbitrationVoteTx);
            }
            if (object.witnessClaimRewardTx != null) {
                if (typeof object.witnessClaimRewardTx !== "object")
                    throw TypeError(".pb.AnyTx.witnessClaimRewardTx: object expected");
                message.witnessClaimRewardTx = $root.pb.WitnessClaimRewardTx.fromObject(object.witnessClaimRewardTx);
            }
            if (object.frostWithdrawRequestTx != null) {
                if (typeof object.frostWithdrawRequestTx !== "object")
                    throw TypeError(".pb.AnyTx.frostWithdrawRequestTx: object expected");
                message.frostWithdrawRequestTx = $root.pb.FrostWithdrawRequestTx.fromObject(object.frostWithdrawRequestTx);
            }
            if (object.frostWithdrawSignedTx != null) {
                if (typeof object.frostWithdrawSignedTx !== "object")
                    throw TypeError(".pb.AnyTx.frostWithdrawSignedTx: object expected");
                message.frostWithdrawSignedTx = $root.pb.FrostWithdrawSignedTx.fromObject(object.frostWithdrawSignedTx);
            }
            if (object.frostVaultDkgCommitTx != null) {
                if (typeof object.frostVaultDkgCommitTx !== "object")
                    throw TypeError(".pb.AnyTx.frostVaultDkgCommitTx: object expected");
                message.frostVaultDkgCommitTx = $root.pb.FrostVaultDkgCommitTx.fromObject(object.frostVaultDkgCommitTx);
            }
            if (object.frostVaultDkgShareTx != null) {
                if (typeof object.frostVaultDkgShareTx !== "object")
                    throw TypeError(".pb.AnyTx.frostVaultDkgShareTx: object expected");
                message.frostVaultDkgShareTx = $root.pb.FrostVaultDkgShareTx.fromObject(object.frostVaultDkgShareTx);
            }
            if (object.frostVaultDkgComplaintTx != null) {
                if (typeof object.frostVaultDkgComplaintTx !== "object")
                    throw TypeError(".pb.AnyTx.frostVaultDkgComplaintTx: object expected");
                message.frostVaultDkgComplaintTx = $root.pb.FrostVaultDkgComplaintTx.fromObject(object.frostVaultDkgComplaintTx);
            }
            if (object.frostVaultDkgRevealTx != null) {
                if (typeof object.frostVaultDkgRevealTx !== "object")
                    throw TypeError(".pb.AnyTx.frostVaultDkgRevealTx: object expected");
                message.frostVaultDkgRevealTx = $root.pb.FrostVaultDkgRevealTx.fromObject(object.frostVaultDkgRevealTx);
            }
            if (object.frostVaultDkgValidationSignedTx != null) {
                if (typeof object.frostVaultDkgValidationSignedTx !== "object")
                    throw TypeError(".pb.AnyTx.frostVaultDkgValidationSignedTx: object expected");
                message.frostVaultDkgValidationSignedTx = $root.pb.FrostVaultDkgValidationSignedTx.fromObject(object.frostVaultDkgValidationSignedTx);
            }
            if (object.frostVaultTransitionSignedTx != null) {
                if (typeof object.frostVaultTransitionSignedTx !== "object")
                    throw TypeError(".pb.AnyTx.frostVaultTransitionSignedTx: object expected");
                message.frostVaultTransitionSignedTx = $root.pb.FrostVaultTransitionSignedTx.fromObject(object.frostVaultTransitionSignedTx);
            }
            return message;
        };

        /**
         * Creates a plain object from an AnyTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.AnyTx
         * @static
         * @param {pb.AnyTx} message AnyTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AnyTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (message.issueTokenTx != null && message.hasOwnProperty("issueTokenTx")) {
                object.issueTokenTx = $root.pb.IssueTokenTx.toObject(message.issueTokenTx, options);
                if (options.oneofs)
                    object.content = "issueTokenTx";
            }
            if (message.freezeTx != null && message.hasOwnProperty("freezeTx")) {
                object.freezeTx = $root.pb.FreezeTx.toObject(message.freezeTx, options);
                if (options.oneofs)
                    object.content = "freezeTx";
            }
            if (message.transaction != null && message.hasOwnProperty("transaction")) {
                object.transaction = $root.pb.Transaction.toObject(message.transaction, options);
                if (options.oneofs)
                    object.content = "transaction";
            }
            if (message.orderTx != null && message.hasOwnProperty("orderTx")) {
                object.orderTx = $root.pb.OrderTx.toObject(message.orderTx, options);
                if (options.oneofs)
                    object.content = "orderTx";
            }
            if (message.minerTx != null && message.hasOwnProperty("minerTx")) {
                object.minerTx = $root.pb.MinerTx.toObject(message.minerTx, options);
                if (options.oneofs)
                    object.content = "minerTx";
            }
            if (message.witnessStakeTx != null && message.hasOwnProperty("witnessStakeTx")) {
                object.witnessStakeTx = $root.pb.WitnessStakeTx.toObject(message.witnessStakeTx, options);
                if (options.oneofs)
                    object.content = "witnessStakeTx";
            }
            if (message.witnessRequestTx != null && message.hasOwnProperty("witnessRequestTx")) {
                object.witnessRequestTx = $root.pb.WitnessRequestTx.toObject(message.witnessRequestTx, options);
                if (options.oneofs)
                    object.content = "witnessRequestTx";
            }
            if (message.witnessVoteTx != null && message.hasOwnProperty("witnessVoteTx")) {
                object.witnessVoteTx = $root.pb.WitnessVoteTx.toObject(message.witnessVoteTx, options);
                if (options.oneofs)
                    object.content = "witnessVoteTx";
            }
            if (message.witnessChallengeTx != null && message.hasOwnProperty("witnessChallengeTx")) {
                object.witnessChallengeTx = $root.pb.WitnessChallengeTx.toObject(message.witnessChallengeTx, options);
                if (options.oneofs)
                    object.content = "witnessChallengeTx";
            }
            if (message.arbitrationVoteTx != null && message.hasOwnProperty("arbitrationVoteTx")) {
                object.arbitrationVoteTx = $root.pb.ArbitrationVoteTx.toObject(message.arbitrationVoteTx, options);
                if (options.oneofs)
                    object.content = "arbitrationVoteTx";
            }
            if (message.witnessClaimRewardTx != null && message.hasOwnProperty("witnessClaimRewardTx")) {
                object.witnessClaimRewardTx = $root.pb.WitnessClaimRewardTx.toObject(message.witnessClaimRewardTx, options);
                if (options.oneofs)
                    object.content = "witnessClaimRewardTx";
            }
            if (message.frostWithdrawRequestTx != null && message.hasOwnProperty("frostWithdrawRequestTx")) {
                object.frostWithdrawRequestTx = $root.pb.FrostWithdrawRequestTx.toObject(message.frostWithdrawRequestTx, options);
                if (options.oneofs)
                    object.content = "frostWithdrawRequestTx";
            }
            if (message.frostWithdrawSignedTx != null && message.hasOwnProperty("frostWithdrawSignedTx")) {
                object.frostWithdrawSignedTx = $root.pb.FrostWithdrawSignedTx.toObject(message.frostWithdrawSignedTx, options);
                if (options.oneofs)
                    object.content = "frostWithdrawSignedTx";
            }
            if (message.frostVaultDkgCommitTx != null && message.hasOwnProperty("frostVaultDkgCommitTx")) {
                object.frostVaultDkgCommitTx = $root.pb.FrostVaultDkgCommitTx.toObject(message.frostVaultDkgCommitTx, options);
                if (options.oneofs)
                    object.content = "frostVaultDkgCommitTx";
            }
            if (message.frostVaultDkgShareTx != null && message.hasOwnProperty("frostVaultDkgShareTx")) {
                object.frostVaultDkgShareTx = $root.pb.FrostVaultDkgShareTx.toObject(message.frostVaultDkgShareTx, options);
                if (options.oneofs)
                    object.content = "frostVaultDkgShareTx";
            }
            if (message.frostVaultDkgComplaintTx != null && message.hasOwnProperty("frostVaultDkgComplaintTx")) {
                object.frostVaultDkgComplaintTx = $root.pb.FrostVaultDkgComplaintTx.toObject(message.frostVaultDkgComplaintTx, options);
                if (options.oneofs)
                    object.content = "frostVaultDkgComplaintTx";
            }
            if (message.frostVaultDkgRevealTx != null && message.hasOwnProperty("frostVaultDkgRevealTx")) {
                object.frostVaultDkgRevealTx = $root.pb.FrostVaultDkgRevealTx.toObject(message.frostVaultDkgRevealTx, options);
                if (options.oneofs)
                    object.content = "frostVaultDkgRevealTx";
            }
            if (message.frostVaultDkgValidationSignedTx != null && message.hasOwnProperty("frostVaultDkgValidationSignedTx")) {
                object.frostVaultDkgValidationSignedTx = $root.pb.FrostVaultDkgValidationSignedTx.toObject(message.frostVaultDkgValidationSignedTx, options);
                if (options.oneofs)
                    object.content = "frostVaultDkgValidationSignedTx";
            }
            if (message.frostVaultTransitionSignedTx != null && message.hasOwnProperty("frostVaultTransitionSignedTx")) {
                object.frostVaultTransitionSignedTx = $root.pb.FrostVaultTransitionSignedTx.toObject(message.frostVaultTransitionSignedTx, options);
                if (options.oneofs)
                    object.content = "frostVaultTransitionSignedTx";
            }
            return object;
        };

        /**
         * Converts this AnyTx to JSON.
         * @function toJSON
         * @memberof pb.AnyTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AnyTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AnyTx
         * @function getTypeUrl
         * @memberof pb.AnyTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AnyTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.AnyTx";
        };

        return AnyTx;
    })();

    /**
     * OrderOp enum.
     * @name pb.OrderOp
     * @enum {number}
     * @property {number} ADD=0 ADD value
     * @property {number} REMOVE=1 REMOVE value
     */
    pb.OrderOp = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ADD"] = 0;
        values[valuesById[1] = "REMOVE"] = 1;
        return values;
    })();

    /**
     * Status enum.
     * @name pb.Status
     * @enum {number}
     * @property {number} PENDING=0 PENDING value
     * @property {number} FAILED=1 FAILED value
     * @property {number} SUCCEED=2 SUCCEED value
     */
    pb.Status = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "PENDING"] = 0;
        values[valuesById[1] = "FAILED"] = 1;
        values[valuesById[2] = "SUCCEED"] = 2;
        return values;
    })();

    pb.NodeInfo = (function() {

        /**
         * Properties of a NodeInfo.
         * @memberof pb
         * @interface INodeInfo
         * @property {pb.IPublicKeys|null} [publicKeys] NodeInfo publicKeys
         * @property {string|null} [ip] NodeInfo ip
         * @property {boolean|null} [isOnline] NodeInfo isOnline
         */

        /**
         * Constructs a new NodeInfo.
         * @memberof pb
         * @classdesc Represents a NodeInfo.
         * @implements INodeInfo
         * @constructor
         * @param {pb.INodeInfo=} [properties] Properties to set
         */
        function NodeInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NodeInfo publicKeys.
         * @member {pb.IPublicKeys|null|undefined} publicKeys
         * @memberof pb.NodeInfo
         * @instance
         */
        NodeInfo.prototype.publicKeys = null;

        /**
         * NodeInfo ip.
         * @member {string} ip
         * @memberof pb.NodeInfo
         * @instance
         */
        NodeInfo.prototype.ip = "";

        /**
         * NodeInfo isOnline.
         * @member {boolean} isOnline
         * @memberof pb.NodeInfo
         * @instance
         */
        NodeInfo.prototype.isOnline = false;

        /**
         * Creates a new NodeInfo instance using the specified properties.
         * @function create
         * @memberof pb.NodeInfo
         * @static
         * @param {pb.INodeInfo=} [properties] Properties to set
         * @returns {pb.NodeInfo} NodeInfo instance
         */
        NodeInfo.create = function create(properties) {
            return new NodeInfo(properties);
        };

        /**
         * Encodes the specified NodeInfo message. Does not implicitly {@link pb.NodeInfo.verify|verify} messages.
         * @function encode
         * @memberof pb.NodeInfo
         * @static
         * @param {pb.INodeInfo} message NodeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.publicKeys != null && Object.hasOwnProperty.call(message, "publicKeys"))
                $root.pb.PublicKeys.encode(message.publicKeys, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.ip);
            if (message.isOnline != null && Object.hasOwnProperty.call(message, "isOnline"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isOnline);
            return writer;
        };

        /**
         * Encodes the specified NodeInfo message, length delimited. Does not implicitly {@link pb.NodeInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.NodeInfo
         * @static
         * @param {pb.INodeInfo} message NodeInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a NodeInfo message from the specified reader or buffer.
         * @function decode
         * @memberof pb.NodeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.NodeInfo} NodeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.NodeInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.publicKeys = $root.pb.PublicKeys.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.ip = reader.string();
                        break;
                    }
                case 3: {
                        message.isOnline = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a NodeInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.NodeInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.NodeInfo} NodeInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeInfo message.
         * @function verify
         * @memberof pb.NodeInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys")) {
                let error = $root.pb.PublicKeys.verify(message.publicKeys);
                if (error)
                    return "publicKeys." + error;
            }
            if (message.ip != null && message.hasOwnProperty("ip"))
                if (!$util.isString(message.ip))
                    return "ip: string expected";
            if (message.isOnline != null && message.hasOwnProperty("isOnline"))
                if (typeof message.isOnline !== "boolean")
                    return "isOnline: boolean expected";
            return null;
        };

        /**
         * Creates a NodeInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.NodeInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.NodeInfo} NodeInfo
         */
        NodeInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.NodeInfo)
                return object;
            let message = new $root.pb.NodeInfo();
            if (object.publicKeys != null) {
                if (typeof object.publicKeys !== "object")
                    throw TypeError(".pb.NodeInfo.publicKeys: object expected");
                message.publicKeys = $root.pb.PublicKeys.fromObject(object.publicKeys);
            }
            if (object.ip != null)
                message.ip = String(object.ip);
            if (object.isOnline != null)
                message.isOnline = Boolean(object.isOnline);
            return message;
        };

        /**
         * Creates a plain object from a NodeInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.NodeInfo
         * @static
         * @param {pb.NodeInfo} message NodeInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.publicKeys = null;
                object.ip = "";
                object.isOnline = false;
            }
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys"))
                object.publicKeys = $root.pb.PublicKeys.toObject(message.publicKeys, options);
            if (message.ip != null && message.hasOwnProperty("ip"))
                object.ip = message.ip;
            if (message.isOnline != null && message.hasOwnProperty("isOnline"))
                object.isOnline = message.isOnline;
            return object;
        };

        /**
         * Converts this NodeInfo to JSON.
         * @function toJSON
         * @memberof pb.NodeInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NodeInfo
         * @function getTypeUrl
         * @memberof pb.NodeInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NodeInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.NodeInfo";
        };

        return NodeInfo;
    })();

    pb.NodeList = (function() {

        /**
         * Properties of a NodeList.
         * @memberof pb
         * @interface INodeList
         * @property {Array.<pb.INodeInfo>|null} [nodes] NodeList nodes
         */

        /**
         * Constructs a new NodeList.
         * @memberof pb
         * @classdesc Represents a NodeList.
         * @implements INodeList
         * @constructor
         * @param {pb.INodeList=} [properties] Properties to set
         */
        function NodeList(properties) {
            this.nodes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NodeList nodes.
         * @member {Array.<pb.INodeInfo>} nodes
         * @memberof pb.NodeList
         * @instance
         */
        NodeList.prototype.nodes = $util.emptyArray;

        /**
         * Creates a new NodeList instance using the specified properties.
         * @function create
         * @memberof pb.NodeList
         * @static
         * @param {pb.INodeList=} [properties] Properties to set
         * @returns {pb.NodeList} NodeList instance
         */
        NodeList.create = function create(properties) {
            return new NodeList(properties);
        };

        /**
         * Encodes the specified NodeList message. Does not implicitly {@link pb.NodeList.verify|verify} messages.
         * @function encode
         * @memberof pb.NodeList
         * @static
         * @param {pb.INodeList} message NodeList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.nodes != null && message.nodes.length)
                for (let i = 0; i < message.nodes.length; ++i)
                    $root.pb.NodeInfo.encode(message.nodes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NodeList message, length delimited. Does not implicitly {@link pb.NodeList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.NodeList
         * @static
         * @param {pb.INodeList} message NodeList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a NodeList message from the specified reader or buffer.
         * @function decode
         * @memberof pb.NodeList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.NodeList} NodeList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.NodeList();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.nodes && message.nodes.length))
                            message.nodes = [];
                        message.nodes.push($root.pb.NodeInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a NodeList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.NodeList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.NodeList} NodeList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NodeList message.
         * @function verify
         * @memberof pb.NodeList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NodeList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.nodes != null && message.hasOwnProperty("nodes")) {
                if (!Array.isArray(message.nodes))
                    return "nodes: array expected";
                for (let i = 0; i < message.nodes.length; ++i) {
                    let error = $root.pb.NodeInfo.verify(message.nodes[i]);
                    if (error)
                        return "nodes." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NodeList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.NodeList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.NodeList} NodeList
         */
        NodeList.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.NodeList)
                return object;
            let message = new $root.pb.NodeList();
            if (object.nodes) {
                if (!Array.isArray(object.nodes))
                    throw TypeError(".pb.NodeList.nodes: array expected");
                message.nodes = [];
                for (let i = 0; i < object.nodes.length; ++i) {
                    if (typeof object.nodes[i] !== "object")
                        throw TypeError(".pb.NodeList.nodes: object expected");
                    message.nodes[i] = $root.pb.NodeInfo.fromObject(object.nodes[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a NodeList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.NodeList
         * @static
         * @param {pb.NodeList} message NodeList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.nodes = [];
            if (message.nodes && message.nodes.length) {
                object.nodes = [];
                for (let j = 0; j < message.nodes.length; ++j)
                    object.nodes[j] = $root.pb.NodeInfo.toObject(message.nodes[j], options);
            }
            return object;
        };

        /**
         * Converts this NodeList to JSON.
         * @function toJSON
         * @memberof pb.NodeList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NodeList
         * @function getTypeUrl
         * @memberof pb.NodeList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NodeList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.NodeList";
        };

        return NodeList;
    })();

    pb.ClientInfo = (function() {

        /**
         * Properties of a ClientInfo.
         * @memberof pb
         * @interface IClientInfo
         * @property {string|null} [ip] ClientInfo ip
         * @property {boolean|null} [authed] ClientInfo authed
         * @property {pb.IPublicKeys|null} [publicKeys] ClientInfo publicKeys
         */

        /**
         * Constructs a new ClientInfo.
         * @memberof pb
         * @classdesc Represents a ClientInfo.
         * @implements IClientInfo
         * @constructor
         * @param {pb.IClientInfo=} [properties] Properties to set
         */
        function ClientInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ClientInfo ip.
         * @member {string} ip
         * @memberof pb.ClientInfo
         * @instance
         */
        ClientInfo.prototype.ip = "";

        /**
         * ClientInfo authed.
         * @member {boolean} authed
         * @memberof pb.ClientInfo
         * @instance
         */
        ClientInfo.prototype.authed = false;

        /**
         * ClientInfo publicKeys.
         * @member {pb.IPublicKeys|null|undefined} publicKeys
         * @memberof pb.ClientInfo
         * @instance
         */
        ClientInfo.prototype.publicKeys = null;

        /**
         * Creates a new ClientInfo instance using the specified properties.
         * @function create
         * @memberof pb.ClientInfo
         * @static
         * @param {pb.IClientInfo=} [properties] Properties to set
         * @returns {pb.ClientInfo} ClientInfo instance
         */
        ClientInfo.create = function create(properties) {
            return new ClientInfo(properties);
        };

        /**
         * Encodes the specified ClientInfo message. Does not implicitly {@link pb.ClientInfo.verify|verify} messages.
         * @function encode
         * @memberof pb.ClientInfo
         * @static
         * @param {pb.IClientInfo} message ClientInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ip != null && Object.hasOwnProperty.call(message, "ip"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.ip);
            if (message.authed != null && Object.hasOwnProperty.call(message, "authed"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.authed);
            if (message.publicKeys != null && Object.hasOwnProperty.call(message, "publicKeys"))
                $root.pb.PublicKeys.encode(message.publicKeys, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ClientInfo message, length delimited. Does not implicitly {@link pb.ClientInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ClientInfo
         * @static
         * @param {pb.IClientInfo} message ClientInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ClientInfo message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ClientInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ClientInfo} ClientInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ClientInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ip = reader.string();
                        break;
                    }
                case 2: {
                        message.authed = reader.bool();
                        break;
                    }
                case 3: {
                        message.publicKeys = $root.pb.PublicKeys.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ClientInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ClientInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ClientInfo} ClientInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClientInfo message.
         * @function verify
         * @memberof pb.ClientInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClientInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.ip != null && message.hasOwnProperty("ip"))
                if (!$util.isString(message.ip))
                    return "ip: string expected";
            if (message.authed != null && message.hasOwnProperty("authed"))
                if (typeof message.authed !== "boolean")
                    return "authed: boolean expected";
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys")) {
                let error = $root.pb.PublicKeys.verify(message.publicKeys);
                if (error)
                    return "publicKeys." + error;
            }
            return null;
        };

        /**
         * Creates a ClientInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ClientInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ClientInfo} ClientInfo
         */
        ClientInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ClientInfo)
                return object;
            let message = new $root.pb.ClientInfo();
            if (object.ip != null)
                message.ip = String(object.ip);
            if (object.authed != null)
                message.authed = Boolean(object.authed);
            if (object.publicKeys != null) {
                if (typeof object.publicKeys !== "object")
                    throw TypeError(".pb.ClientInfo.publicKeys: object expected");
                message.publicKeys = $root.pb.PublicKeys.fromObject(object.publicKeys);
            }
            return message;
        };

        /**
         * Creates a plain object from a ClientInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ClientInfo
         * @static
         * @param {pb.ClientInfo} message ClientInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClientInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.ip = "";
                object.authed = false;
                object.publicKeys = null;
            }
            if (message.ip != null && message.hasOwnProperty("ip"))
                object.ip = message.ip;
            if (message.authed != null && message.hasOwnProperty("authed"))
                object.authed = message.authed;
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys"))
                object.publicKeys = $root.pb.PublicKeys.toObject(message.publicKeys, options);
            return object;
        };

        /**
         * Converts this ClientInfo to JSON.
         * @function toJSON
         * @memberof pb.ClientInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClientInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ClientInfo
         * @function getTypeUrl
         * @memberof pb.ClientInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ClientInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ClientInfo";
        };

        return ClientInfo;
    })();

    pb.HandshakeRequest = (function() {

        /**
         * Properties of a HandshakeRequest.
         * @memberof pb
         * @interface IHandshakeRequest
         * @property {string|null} [clientId] HandshakeRequest clientId
         * @property {pb.IPublicKeys|null} [publicKeys] HandshakeRequest publicKeys
         * @property {Uint8Array|null} [signature] HandshakeRequest signature
         */

        /**
         * Constructs a new HandshakeRequest.
         * @memberof pb
         * @classdesc Represents a HandshakeRequest.
         * @implements IHandshakeRequest
         * @constructor
         * @param {pb.IHandshakeRequest=} [properties] Properties to set
         */
        function HandshakeRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HandshakeRequest clientId.
         * @member {string} clientId
         * @memberof pb.HandshakeRequest
         * @instance
         */
        HandshakeRequest.prototype.clientId = "";

        /**
         * HandshakeRequest publicKeys.
         * @member {pb.IPublicKeys|null|undefined} publicKeys
         * @memberof pb.HandshakeRequest
         * @instance
         */
        HandshakeRequest.prototype.publicKeys = null;

        /**
         * HandshakeRequest signature.
         * @member {Uint8Array} signature
         * @memberof pb.HandshakeRequest
         * @instance
         */
        HandshakeRequest.prototype.signature = $util.newBuffer([]);

        /**
         * Creates a new HandshakeRequest instance using the specified properties.
         * @function create
         * @memberof pb.HandshakeRequest
         * @static
         * @param {pb.IHandshakeRequest=} [properties] Properties to set
         * @returns {pb.HandshakeRequest} HandshakeRequest instance
         */
        HandshakeRequest.create = function create(properties) {
            return new HandshakeRequest(properties);
        };

        /**
         * Encodes the specified HandshakeRequest message. Does not implicitly {@link pb.HandshakeRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.HandshakeRequest
         * @static
         * @param {pb.IHandshakeRequest} message HandshakeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandshakeRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.clientId);
            if (message.publicKeys != null && Object.hasOwnProperty.call(message, "publicKeys"))
                $root.pb.PublicKeys.encode(message.publicKeys, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.signature);
            return writer;
        };

        /**
         * Encodes the specified HandshakeRequest message, length delimited. Does not implicitly {@link pb.HandshakeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.HandshakeRequest
         * @static
         * @param {pb.IHandshakeRequest} message HandshakeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandshakeRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a HandshakeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.HandshakeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.HandshakeRequest} HandshakeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandshakeRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.HandshakeRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.clientId = reader.string();
                        break;
                    }
                case 2: {
                        message.publicKeys = $root.pb.PublicKeys.decode(reader, reader.uint32());
                        break;
                    }
                case 3: {
                        message.signature = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a HandshakeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.HandshakeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.HandshakeRequest} HandshakeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandshakeRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HandshakeRequest message.
         * @function verify
         * @memberof pb.HandshakeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HandshakeRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.clientId != null && message.hasOwnProperty("clientId"))
                if (!$util.isString(message.clientId))
                    return "clientId: string expected";
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys")) {
                let error = $root.pb.PublicKeys.verify(message.publicKeys);
                if (error)
                    return "publicKeys." + error;
            }
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            return null;
        };

        /**
         * Creates a HandshakeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.HandshakeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.HandshakeRequest} HandshakeRequest
         */
        HandshakeRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.HandshakeRequest)
                return object;
            let message = new $root.pb.HandshakeRequest();
            if (object.clientId != null)
                message.clientId = String(object.clientId);
            if (object.publicKeys != null) {
                if (typeof object.publicKeys !== "object")
                    throw TypeError(".pb.HandshakeRequest.publicKeys: object expected");
                message.publicKeys = $root.pb.PublicKeys.fromObject(object.publicKeys);
            }
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            return message;
        };

        /**
         * Creates a plain object from a HandshakeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.HandshakeRequest
         * @static
         * @param {pb.HandshakeRequest} message HandshakeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HandshakeRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.clientId = "";
                object.publicKeys = null;
                if (options.bytes === String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== Array)
                        object.signature = $util.newBuffer(object.signature);
                }
            }
            if (message.clientId != null && message.hasOwnProperty("clientId"))
                object.clientId = message.clientId;
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys"))
                object.publicKeys = $root.pb.PublicKeys.toObject(message.publicKeys, options);
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            return object;
        };

        /**
         * Converts this HandshakeRequest to JSON.
         * @function toJSON
         * @memberof pb.HandshakeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HandshakeRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HandshakeRequest
         * @function getTypeUrl
         * @memberof pb.HandshakeRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HandshakeRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.HandshakeRequest";
        };

        return HandshakeRequest;
    })();

    pb.HandshakeResponse = (function() {

        /**
         * Properties of a HandshakeResponse.
         * @memberof pb
         * @interface IHandshakeResponse
         * @property {string|null} [status] HandshakeResponse status
         */

        /**
         * Constructs a new HandshakeResponse.
         * @memberof pb
         * @classdesc Represents a HandshakeResponse.
         * @implements IHandshakeResponse
         * @constructor
         * @param {pb.IHandshakeResponse=} [properties] Properties to set
         */
        function HandshakeResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HandshakeResponse status.
         * @member {string} status
         * @memberof pb.HandshakeResponse
         * @instance
         */
        HandshakeResponse.prototype.status = "";

        /**
         * Creates a new HandshakeResponse instance using the specified properties.
         * @function create
         * @memberof pb.HandshakeResponse
         * @static
         * @param {pb.IHandshakeResponse=} [properties] Properties to set
         * @returns {pb.HandshakeResponse} HandshakeResponse instance
         */
        HandshakeResponse.create = function create(properties) {
            return new HandshakeResponse(properties);
        };

        /**
         * Encodes the specified HandshakeResponse message. Does not implicitly {@link pb.HandshakeResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.HandshakeResponse
         * @static
         * @param {pb.IHandshakeResponse} message HandshakeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandshakeResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            return writer;
        };

        /**
         * Encodes the specified HandshakeResponse message, length delimited. Does not implicitly {@link pb.HandshakeResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.HandshakeResponse
         * @static
         * @param {pb.IHandshakeResponse} message HandshakeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandshakeResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a HandshakeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.HandshakeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.HandshakeResponse} HandshakeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandshakeResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.HandshakeResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a HandshakeResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.HandshakeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.HandshakeResponse} HandshakeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandshakeResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HandshakeResponse message.
         * @function verify
         * @memberof pb.HandshakeResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HandshakeResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a HandshakeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.HandshakeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.HandshakeResponse} HandshakeResponse
         */
        HandshakeResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.HandshakeResponse)
                return object;
            let message = new $root.pb.HandshakeResponse();
            if (object.status != null)
                message.status = String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a HandshakeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.HandshakeResponse
         * @static
         * @param {pb.HandshakeResponse} message HandshakeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HandshakeResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.status = "";
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this HandshakeResponse to JSON.
         * @function toJSON
         * @memberof pb.HandshakeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HandshakeResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HandshakeResponse
         * @function getTypeUrl
         * @memberof pb.HandshakeResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HandshakeResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.HandshakeResponse";
        };

        return HandshakeResponse;
    })();

    pb.StatusRequest = (function() {

        /**
         * Properties of a StatusRequest.
         * @memberof pb
         * @interface IStatusRequest
         * @property {string|null} [address] StatusRequest address
         */

        /**
         * Constructs a new StatusRequest.
         * @memberof pb
         * @classdesc Represents a StatusRequest.
         * @implements IStatusRequest
         * @constructor
         * @param {pb.IStatusRequest=} [properties] Properties to set
         */
        function StatusRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StatusRequest address.
         * @member {string} address
         * @memberof pb.StatusRequest
         * @instance
         */
        StatusRequest.prototype.address = "";

        /**
         * Creates a new StatusRequest instance using the specified properties.
         * @function create
         * @memberof pb.StatusRequest
         * @static
         * @param {pb.IStatusRequest=} [properties] Properties to set
         * @returns {pb.StatusRequest} StatusRequest instance
         */
        StatusRequest.create = function create(properties) {
            return new StatusRequest(properties);
        };

        /**
         * Encodes the specified StatusRequest message. Does not implicitly {@link pb.StatusRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.StatusRequest
         * @static
         * @param {pb.IStatusRequest} message StatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            return writer;
        };

        /**
         * Encodes the specified StatusRequest message, length delimited. Does not implicitly {@link pb.StatusRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.StatusRequest
         * @static
         * @param {pb.IStatusRequest} message StatusRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StatusRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.StatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.StatusRequest} StatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.StatusRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StatusRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.StatusRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.StatusRequest} StatusRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusRequest message.
         * @function verify
         * @memberof pb.StatusRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates a StatusRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.StatusRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.StatusRequest} StatusRequest
         */
        StatusRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.StatusRequest)
                return object;
            let message = new $root.pb.StatusRequest();
            if (object.address != null)
                message.address = String(object.address);
            return message;
        };

        /**
         * Creates a plain object from a StatusRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.StatusRequest
         * @static
         * @param {pb.StatusRequest} message StatusRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.address = "";
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this StatusRequest to JSON.
         * @function toJSON
         * @memberof pb.StatusRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StatusRequest
         * @function getTypeUrl
         * @memberof pb.StatusRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StatusRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.StatusRequest";
        };

        return StatusRequest;
    })();

    pb.StatusResponse = (function() {

        /**
         * Properties of a StatusResponse.
         * @memberof pb
         * @interface IStatusResponse
         * @property {string|null} [status] StatusResponse status
         * @property {string|null} [info] StatusResponse info
         */

        /**
         * Constructs a new StatusResponse.
         * @memberof pb
         * @classdesc Represents a StatusResponse.
         * @implements IStatusResponse
         * @constructor
         * @param {pb.IStatusResponse=} [properties] Properties to set
         */
        function StatusResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StatusResponse status.
         * @member {string} status
         * @memberof pb.StatusResponse
         * @instance
         */
        StatusResponse.prototype.status = "";

        /**
         * StatusResponse info.
         * @member {string} info
         * @memberof pb.StatusResponse
         * @instance
         */
        StatusResponse.prototype.info = "";

        /**
         * Creates a new StatusResponse instance using the specified properties.
         * @function create
         * @memberof pb.StatusResponse
         * @static
         * @param {pb.IStatusResponse=} [properties] Properties to set
         * @returns {pb.StatusResponse} StatusResponse instance
         */
        StatusResponse.create = function create(properties) {
            return new StatusResponse(properties);
        };

        /**
         * Encodes the specified StatusResponse message. Does not implicitly {@link pb.StatusResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.StatusResponse
         * @static
         * @param {pb.IStatusResponse} message StatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.info);
            return writer;
        };

        /**
         * Encodes the specified StatusResponse message, length delimited. Does not implicitly {@link pb.StatusResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.StatusResponse
         * @static
         * @param {pb.IStatusResponse} message StatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a StatusResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.StatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.StatusResponse} StatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.StatusResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.string();
                        break;
                    }
                case 2: {
                        message.info = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a StatusResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.StatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.StatusResponse} StatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusResponse message.
         * @function verify
         * @memberof pb.StatusResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.info != null && message.hasOwnProperty("info"))
                if (!$util.isString(message.info))
                    return "info: string expected";
            return null;
        };

        /**
         * Creates a StatusResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.StatusResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.StatusResponse} StatusResponse
         */
        StatusResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.StatusResponse)
                return object;
            let message = new $root.pb.StatusResponse();
            if (object.status != null)
                message.status = String(object.status);
            if (object.info != null)
                message.info = String(object.info);
            return message;
        };

        /**
         * Creates a plain object from a StatusResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.StatusResponse
         * @static
         * @param {pb.StatusResponse} message StatusResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.status = "";
                object.info = "";
            }
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = message.info;
            return object;
        };

        /**
         * Converts this StatusResponse to JSON.
         * @function toJSON
         * @memberof pb.StatusResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StatusResponse
         * @function getTypeUrl
         * @memberof pb.StatusResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StatusResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.StatusResponse";
        };

        return StatusResponse;
    })();

    pb.GetData = (function() {

        /**
         * Properties of a GetData.
         * @memberof pb
         * @interface IGetData
         * @property {string|null} [txId] GetData txId
         * @property {string|null} [address] GetData address
         */

        /**
         * Constructs a new GetData.
         * @memberof pb
         * @classdesc Represents a GetData.
         * @implements IGetData
         * @constructor
         * @param {pb.IGetData=} [properties] Properties to set
         */
        function GetData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetData txId.
         * @member {string} txId
         * @memberof pb.GetData
         * @instance
         */
        GetData.prototype.txId = "";

        /**
         * GetData address.
         * @member {string} address
         * @memberof pb.GetData
         * @instance
         */
        GetData.prototype.address = "";

        /**
         * Creates a new GetData instance using the specified properties.
         * @function create
         * @memberof pb.GetData
         * @static
         * @param {pb.IGetData=} [properties] Properties to set
         * @returns {pb.GetData} GetData instance
         */
        GetData.create = function create(properties) {
            return new GetData(properties);
        };

        /**
         * Encodes the specified GetData message. Does not implicitly {@link pb.GetData.verify|verify} messages.
         * @function encode
         * @memberof pb.GetData
         * @static
         * @param {pb.IGetData} message GetData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.txId != null && Object.hasOwnProperty.call(message, "txId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txId);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            return writer;
        };

        /**
         * Encodes the specified GetData message, length delimited. Does not implicitly {@link pb.GetData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetData
         * @static
         * @param {pb.IGetData} message GetData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetData message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetData} GetData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetData.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.txId = reader.string();
                        break;
                    }
                case 2: {
                        message.address = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetData} GetData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetData message.
         * @function verify
         * @memberof pb.GetData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetData.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.txId != null && message.hasOwnProperty("txId"))
                if (!$util.isString(message.txId))
                    return "txId: string expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates a GetData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetData} GetData
         */
        GetData.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetData)
                return object;
            let message = new $root.pb.GetData();
            if (object.txId != null)
                message.txId = String(object.txId);
            if (object.address != null)
                message.address = String(object.address);
            return message;
        };

        /**
         * Creates a plain object from a GetData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetData
         * @static
         * @param {pb.GetData} message GetData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.txId = "";
                object.address = "";
            }
            if (message.txId != null && message.hasOwnProperty("txId"))
                object.txId = message.txId;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this GetData to JSON.
         * @function toJSON
         * @memberof pb.GetData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetData
         * @function getTypeUrl
         * @memberof pb.GetData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetData";
        };

        return GetData;
    })();

    pb.GetBlockRequest = (function() {

        /**
         * Properties of a GetBlockRequest.
         * @memberof pb
         * @interface IGetBlockRequest
         * @property {number|Long|null} [height] GetBlockRequest height
         */

        /**
         * Constructs a new GetBlockRequest.
         * @memberof pb
         * @classdesc Represents a GetBlockRequest.
         * @implements IGetBlockRequest
         * @constructor
         * @param {pb.IGetBlockRequest=} [properties] Properties to set
         */
        function GetBlockRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetBlockRequest height.
         * @member {number|Long} height
         * @memberof pb.GetBlockRequest
         * @instance
         */
        GetBlockRequest.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new GetBlockRequest instance using the specified properties.
         * @function create
         * @memberof pb.GetBlockRequest
         * @static
         * @param {pb.IGetBlockRequest=} [properties] Properties to set
         * @returns {pb.GetBlockRequest} GetBlockRequest instance
         */
        GetBlockRequest.create = function create(properties) {
            return new GetBlockRequest(properties);
        };

        /**
         * Encodes the specified GetBlockRequest message. Does not implicitly {@link pb.GetBlockRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.GetBlockRequest
         * @static
         * @param {pb.IGetBlockRequest} message GetBlockRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.height);
            return writer;
        };

        /**
         * Encodes the specified GetBlockRequest message, length delimited. Does not implicitly {@link pb.GetBlockRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetBlockRequest
         * @static
         * @param {pb.IGetBlockRequest} message GetBlockRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetBlockRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetBlockRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetBlockRequest} GetBlockRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetBlockRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.height = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetBlockRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetBlockRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetBlockRequest} GetBlockRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockRequest message.
         * @function verify
         * @memberof pb.GetBlockRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            return null;
        };

        /**
         * Creates a GetBlockRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetBlockRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetBlockRequest} GetBlockRequest
         */
        GetBlockRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetBlockRequest)
                return object;
            let message = new $root.pb.GetBlockRequest();
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a GetBlockRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetBlockRequest
         * @static
         * @param {pb.GetBlockRequest} message GetBlockRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            return object;
        };

        /**
         * Converts this GetBlockRequest to JSON.
         * @function toJSON
         * @memberof pb.GetBlockRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetBlockRequest
         * @function getTypeUrl
         * @memberof pb.GetBlockRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetBlockRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetBlockRequest";
        };

        return GetBlockRequest;
    })();

    pb.GetBlockResponse = (function() {

        /**
         * Properties of a GetBlockResponse.
         * @memberof pb
         * @interface IGetBlockResponse
         * @property {pb.IBlock|null} [block] GetBlockResponse block
         * @property {string|null} [error] GetBlockResponse error
         */

        /**
         * Constructs a new GetBlockResponse.
         * @memberof pb
         * @classdesc Represents a GetBlockResponse.
         * @implements IGetBlockResponse
         * @constructor
         * @param {pb.IGetBlockResponse=} [properties] Properties to set
         */
        function GetBlockResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetBlockResponse block.
         * @member {pb.IBlock|null|undefined} block
         * @memberof pb.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.block = null;

        /**
         * GetBlockResponse error.
         * @member {string} error
         * @memberof pb.GetBlockResponse
         * @instance
         */
        GetBlockResponse.prototype.error = "";

        /**
         * Creates a new GetBlockResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetBlockResponse
         * @static
         * @param {pb.IGetBlockResponse=} [properties] Properties to set
         * @returns {pb.GetBlockResponse} GetBlockResponse instance
         */
        GetBlockResponse.create = function create(properties) {
            return new GetBlockResponse(properties);
        };

        /**
         * Encodes the specified GetBlockResponse message. Does not implicitly {@link pb.GetBlockResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetBlockResponse
         * @static
         * @param {pb.IGetBlockResponse} message GetBlockResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.block != null && Object.hasOwnProperty.call(message, "block"))
                $root.pb.Block.encode(message.block, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified GetBlockResponse message, length delimited. Does not implicitly {@link pb.GetBlockResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetBlockResponse
         * @static
         * @param {pb.IGetBlockResponse} message GetBlockResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetBlockResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetBlockResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetBlockResponse} GetBlockResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetBlockResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.block = $root.pb.Block.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetBlockResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetBlockResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetBlockResponse} GetBlockResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockResponse message.
         * @function verify
         * @memberof pb.GetBlockResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.block != null && message.hasOwnProperty("block")) {
                let error = $root.pb.Block.verify(message.block);
                if (error)
                    return "block." + error;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a GetBlockResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetBlockResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetBlockResponse} GetBlockResponse
         */
        GetBlockResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetBlockResponse)
                return object;
            let message = new $root.pb.GetBlockResponse();
            if (object.block != null) {
                if (typeof object.block !== "object")
                    throw TypeError(".pb.GetBlockResponse.block: object expected");
                message.block = $root.pb.Block.fromObject(object.block);
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a GetBlockResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetBlockResponse
         * @static
         * @param {pb.GetBlockResponse} message GetBlockResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.block = null;
                object.error = "";
            }
            if (message.block != null && message.hasOwnProperty("block"))
                object.block = $root.pb.Block.toObject(message.block, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this GetBlockResponse to JSON.
         * @function toJSON
         * @memberof pb.GetBlockResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetBlockResponse
         * @function getTypeUrl
         * @memberof pb.GetBlockResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetBlockResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetBlockResponse";
        };

        return GetBlockResponse;
    })();

    pb.BatchGetShortTxRequest = (function() {

        /**
         * Properties of a BatchGetShortTxRequest.
         * @memberof pb
         * @interface IBatchGetShortTxRequest
         * @property {Array.<Uint8Array>|null} [shortHashes] BatchGetShortTxRequest shortHashes
         * @property {string|null} [address] BatchGetShortTxRequest address
         */

        /**
         * Constructs a new BatchGetShortTxRequest.
         * @memberof pb
         * @classdesc Represents a BatchGetShortTxRequest.
         * @implements IBatchGetShortTxRequest
         * @constructor
         * @param {pb.IBatchGetShortTxRequest=} [properties] Properties to set
         */
        function BatchGetShortTxRequest(properties) {
            this.shortHashes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BatchGetShortTxRequest shortHashes.
         * @member {Array.<Uint8Array>} shortHashes
         * @memberof pb.BatchGetShortTxRequest
         * @instance
         */
        BatchGetShortTxRequest.prototype.shortHashes = $util.emptyArray;

        /**
         * BatchGetShortTxRequest address.
         * @member {string} address
         * @memberof pb.BatchGetShortTxRequest
         * @instance
         */
        BatchGetShortTxRequest.prototype.address = "";

        /**
         * Creates a new BatchGetShortTxRequest instance using the specified properties.
         * @function create
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {pb.IBatchGetShortTxRequest=} [properties] Properties to set
         * @returns {pb.BatchGetShortTxRequest} BatchGetShortTxRequest instance
         */
        BatchGetShortTxRequest.create = function create(properties) {
            return new BatchGetShortTxRequest(properties);
        };

        /**
         * Encodes the specified BatchGetShortTxRequest message. Does not implicitly {@link pb.BatchGetShortTxRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {pb.IBatchGetShortTxRequest} message BatchGetShortTxRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchGetShortTxRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.shortHashes != null && message.shortHashes.length)
                for (let i = 0; i < message.shortHashes.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.shortHashes[i]);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            return writer;
        };

        /**
         * Encodes the specified BatchGetShortTxRequest message, length delimited. Does not implicitly {@link pb.BatchGetShortTxRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {pb.IBatchGetShortTxRequest} message BatchGetShortTxRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchGetShortTxRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BatchGetShortTxRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.BatchGetShortTxRequest} BatchGetShortTxRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchGetShortTxRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.BatchGetShortTxRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.shortHashes && message.shortHashes.length))
                            message.shortHashes = [];
                        message.shortHashes.push(reader.bytes());
                        break;
                    }
                case 2: {
                        message.address = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BatchGetShortTxRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.BatchGetShortTxRequest} BatchGetShortTxRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchGetShortTxRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BatchGetShortTxRequest message.
         * @function verify
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BatchGetShortTxRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.shortHashes != null && message.hasOwnProperty("shortHashes")) {
                if (!Array.isArray(message.shortHashes))
                    return "shortHashes: array expected";
                for (let i = 0; i < message.shortHashes.length; ++i)
                    if (!(message.shortHashes[i] && typeof message.shortHashes[i].length === "number" || $util.isString(message.shortHashes[i])))
                        return "shortHashes: buffer[] expected";
            }
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates a BatchGetShortTxRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.BatchGetShortTxRequest} BatchGetShortTxRequest
         */
        BatchGetShortTxRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.BatchGetShortTxRequest)
                return object;
            let message = new $root.pb.BatchGetShortTxRequest();
            if (object.shortHashes) {
                if (!Array.isArray(object.shortHashes))
                    throw TypeError(".pb.BatchGetShortTxRequest.shortHashes: array expected");
                message.shortHashes = [];
                for (let i = 0; i < object.shortHashes.length; ++i)
                    if (typeof object.shortHashes[i] === "string")
                        $util.base64.decode(object.shortHashes[i], message.shortHashes[i] = $util.newBuffer($util.base64.length(object.shortHashes[i])), 0);
                    else if (object.shortHashes[i].length >= 0)
                        message.shortHashes[i] = object.shortHashes[i];
            }
            if (object.address != null)
                message.address = String(object.address);
            return message;
        };

        /**
         * Creates a plain object from a BatchGetShortTxRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {pb.BatchGetShortTxRequest} message BatchGetShortTxRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BatchGetShortTxRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.shortHashes = [];
            if (options.defaults)
                object.address = "";
            if (message.shortHashes && message.shortHashes.length) {
                object.shortHashes = [];
                for (let j = 0; j < message.shortHashes.length; ++j)
                    object.shortHashes[j] = options.bytes === String ? $util.base64.encode(message.shortHashes[j], 0, message.shortHashes[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.shortHashes[j]) : message.shortHashes[j];
            }
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this BatchGetShortTxRequest to JSON.
         * @function toJSON
         * @memberof pb.BatchGetShortTxRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BatchGetShortTxRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BatchGetShortTxRequest
         * @function getTypeUrl
         * @memberof pb.BatchGetShortTxRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BatchGetShortTxRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.BatchGetShortTxRequest";
        };

        return BatchGetShortTxRequest;
    })();

    pb.BatchGetShortTxResponse = (function() {

        /**
         * Properties of a BatchGetShortTxResponse.
         * @memberof pb
         * @interface IBatchGetShortTxResponse
         * @property {Array.<pb.IAnyTx>|null} [transactions] BatchGetShortTxResponse transactions
         */

        /**
         * Constructs a new BatchGetShortTxResponse.
         * @memberof pb
         * @classdesc Represents a BatchGetShortTxResponse.
         * @implements IBatchGetShortTxResponse
         * @constructor
         * @param {pb.IBatchGetShortTxResponse=} [properties] Properties to set
         */
        function BatchGetShortTxResponse(properties) {
            this.transactions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BatchGetShortTxResponse transactions.
         * @member {Array.<pb.IAnyTx>} transactions
         * @memberof pb.BatchGetShortTxResponse
         * @instance
         */
        BatchGetShortTxResponse.prototype.transactions = $util.emptyArray;

        /**
         * Creates a new BatchGetShortTxResponse instance using the specified properties.
         * @function create
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {pb.IBatchGetShortTxResponse=} [properties] Properties to set
         * @returns {pb.BatchGetShortTxResponse} BatchGetShortTxResponse instance
         */
        BatchGetShortTxResponse.create = function create(properties) {
            return new BatchGetShortTxResponse(properties);
        };

        /**
         * Encodes the specified BatchGetShortTxResponse message. Does not implicitly {@link pb.BatchGetShortTxResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {pb.IBatchGetShortTxResponse} message BatchGetShortTxResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchGetShortTxResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.transactions != null && message.transactions.length)
                for (let i = 0; i < message.transactions.length; ++i)
                    $root.pb.AnyTx.encode(message.transactions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BatchGetShortTxResponse message, length delimited. Does not implicitly {@link pb.BatchGetShortTxResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {pb.IBatchGetShortTxResponse} message BatchGetShortTxResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BatchGetShortTxResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BatchGetShortTxResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.BatchGetShortTxResponse} BatchGetShortTxResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchGetShortTxResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.BatchGetShortTxResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.transactions && message.transactions.length))
                            message.transactions = [];
                        message.transactions.push($root.pb.AnyTx.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BatchGetShortTxResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.BatchGetShortTxResponse} BatchGetShortTxResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BatchGetShortTxResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BatchGetShortTxResponse message.
         * @function verify
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BatchGetShortTxResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.transactions != null && message.hasOwnProperty("transactions")) {
                if (!Array.isArray(message.transactions))
                    return "transactions: array expected";
                for (let i = 0; i < message.transactions.length; ++i) {
                    let error = $root.pb.AnyTx.verify(message.transactions[i]);
                    if (error)
                        return "transactions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BatchGetShortTxResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.BatchGetShortTxResponse} BatchGetShortTxResponse
         */
        BatchGetShortTxResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.BatchGetShortTxResponse)
                return object;
            let message = new $root.pb.BatchGetShortTxResponse();
            if (object.transactions) {
                if (!Array.isArray(object.transactions))
                    throw TypeError(".pb.BatchGetShortTxResponse.transactions: array expected");
                message.transactions = [];
                for (let i = 0; i < object.transactions.length; ++i) {
                    if (typeof object.transactions[i] !== "object")
                        throw TypeError(".pb.BatchGetShortTxResponse.transactions: object expected");
                    message.transactions[i] = $root.pb.AnyTx.fromObject(object.transactions[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BatchGetShortTxResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {pb.BatchGetShortTxResponse} message BatchGetShortTxResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BatchGetShortTxResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.transactions = [];
            if (message.transactions && message.transactions.length) {
                object.transactions = [];
                for (let j = 0; j < message.transactions.length; ++j)
                    object.transactions[j] = $root.pb.AnyTx.toObject(message.transactions[j], options);
            }
            return object;
        };

        /**
         * Converts this BatchGetShortTxResponse to JSON.
         * @function toJSON
         * @memberof pb.BatchGetShortTxResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BatchGetShortTxResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BatchGetShortTxResponse
         * @function getTypeUrl
         * @memberof pb.BatchGetShortTxResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BatchGetShortTxResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.BatchGetShortTxResponse";
        };

        return BatchGetShortTxResponse;
    })();

    pb.CheckPointInfo = (function() {

        /**
         * Properties of a CheckPointInfo.
         * @memberof pb
         * @interface ICheckPointInfo
         * @property {number|Long|null} [height] CheckPointInfo height
         * @property {string|null} [txsHash] CheckPointInfo txsHash
         * @property {Uint8Array|null} [aggregateSignature] CheckPointInfo aggregateSignature
         * @property {Uint8Array|null} [bitMap] CheckPointInfo bitMap
         * @property {Uint8Array|null} [txs] CheckPointInfo txs
         */

        /**
         * Constructs a new CheckPointInfo.
         * @memberof pb
         * @classdesc Represents a CheckPointInfo.
         * @implements ICheckPointInfo
         * @constructor
         * @param {pb.ICheckPointInfo=} [properties] Properties to set
         */
        function CheckPointInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CheckPointInfo height.
         * @member {number|Long} height
         * @memberof pb.CheckPointInfo
         * @instance
         */
        CheckPointInfo.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * CheckPointInfo txsHash.
         * @member {string} txsHash
         * @memberof pb.CheckPointInfo
         * @instance
         */
        CheckPointInfo.prototype.txsHash = "";

        /**
         * CheckPointInfo aggregateSignature.
         * @member {Uint8Array} aggregateSignature
         * @memberof pb.CheckPointInfo
         * @instance
         */
        CheckPointInfo.prototype.aggregateSignature = $util.newBuffer([]);

        /**
         * CheckPointInfo bitMap.
         * @member {Uint8Array} bitMap
         * @memberof pb.CheckPointInfo
         * @instance
         */
        CheckPointInfo.prototype.bitMap = $util.newBuffer([]);

        /**
         * CheckPointInfo txs.
         * @member {Uint8Array} txs
         * @memberof pb.CheckPointInfo
         * @instance
         */
        CheckPointInfo.prototype.txs = $util.newBuffer([]);

        /**
         * Creates a new CheckPointInfo instance using the specified properties.
         * @function create
         * @memberof pb.CheckPointInfo
         * @static
         * @param {pb.ICheckPointInfo=} [properties] Properties to set
         * @returns {pb.CheckPointInfo} CheckPointInfo instance
         */
        CheckPointInfo.create = function create(properties) {
            return new CheckPointInfo(properties);
        };

        /**
         * Encodes the specified CheckPointInfo message. Does not implicitly {@link pb.CheckPointInfo.verify|verify} messages.
         * @function encode
         * @memberof pb.CheckPointInfo
         * @static
         * @param {pb.ICheckPointInfo} message CheckPointInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckPointInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.height);
            if (message.txsHash != null && Object.hasOwnProperty.call(message, "txsHash"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.txsHash);
            if (message.aggregateSignature != null && Object.hasOwnProperty.call(message, "aggregateSignature"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.aggregateSignature);
            if (message.bitMap != null && Object.hasOwnProperty.call(message, "bitMap"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.bitMap);
            if (message.txs != null && Object.hasOwnProperty.call(message, "txs"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.txs);
            return writer;
        };

        /**
         * Encodes the specified CheckPointInfo message, length delimited. Does not implicitly {@link pb.CheckPointInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.CheckPointInfo
         * @static
         * @param {pb.ICheckPointInfo} message CheckPointInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckPointInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a CheckPointInfo message from the specified reader or buffer.
         * @function decode
         * @memberof pb.CheckPointInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.CheckPointInfo} CheckPointInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckPointInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.CheckPointInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.height = reader.uint64();
                        break;
                    }
                case 2: {
                        message.txsHash = reader.string();
                        break;
                    }
                case 3: {
                        message.aggregateSignature = reader.bytes();
                        break;
                    }
                case 4: {
                        message.bitMap = reader.bytes();
                        break;
                    }
                case 5: {
                        message.txs = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CheckPointInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.CheckPointInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.CheckPointInfo} CheckPointInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckPointInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckPointInfo message.
         * @function verify
         * @memberof pb.CheckPointInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckPointInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.txsHash != null && message.hasOwnProperty("txsHash"))
                if (!$util.isString(message.txsHash))
                    return "txsHash: string expected";
            if (message.aggregateSignature != null && message.hasOwnProperty("aggregateSignature"))
                if (!(message.aggregateSignature && typeof message.aggregateSignature.length === "number" || $util.isString(message.aggregateSignature)))
                    return "aggregateSignature: buffer expected";
            if (message.bitMap != null && message.hasOwnProperty("bitMap"))
                if (!(message.bitMap && typeof message.bitMap.length === "number" || $util.isString(message.bitMap)))
                    return "bitMap: buffer expected";
            if (message.txs != null && message.hasOwnProperty("txs"))
                if (!(message.txs && typeof message.txs.length === "number" || $util.isString(message.txs)))
                    return "txs: buffer expected";
            return null;
        };

        /**
         * Creates a CheckPointInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.CheckPointInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.CheckPointInfo} CheckPointInfo
         */
        CheckPointInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.CheckPointInfo)
                return object;
            let message = new $root.pb.CheckPointInfo();
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.txsHash != null)
                message.txsHash = String(object.txsHash);
            if (object.aggregateSignature != null)
                if (typeof object.aggregateSignature === "string")
                    $util.base64.decode(object.aggregateSignature, message.aggregateSignature = $util.newBuffer($util.base64.length(object.aggregateSignature)), 0);
                else if (object.aggregateSignature.length >= 0)
                    message.aggregateSignature = object.aggregateSignature;
            if (object.bitMap != null)
                if (typeof object.bitMap === "string")
                    $util.base64.decode(object.bitMap, message.bitMap = $util.newBuffer($util.base64.length(object.bitMap)), 0);
                else if (object.bitMap.length >= 0)
                    message.bitMap = object.bitMap;
            if (object.txs != null)
                if (typeof object.txs === "string")
                    $util.base64.decode(object.txs, message.txs = $util.newBuffer($util.base64.length(object.txs)), 0);
                else if (object.txs.length >= 0)
                    message.txs = object.txs;
            return message;
        };

        /**
         * Creates a plain object from a CheckPointInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.CheckPointInfo
         * @static
         * @param {pb.CheckPointInfo} message CheckPointInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckPointInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.txsHash = "";
                if (options.bytes === String)
                    object.aggregateSignature = "";
                else {
                    object.aggregateSignature = [];
                    if (options.bytes !== Array)
                        object.aggregateSignature = $util.newBuffer(object.aggregateSignature);
                }
                if (options.bytes === String)
                    object.bitMap = "";
                else {
                    object.bitMap = [];
                    if (options.bytes !== Array)
                        object.bitMap = $util.newBuffer(object.bitMap);
                }
                if (options.bytes === String)
                    object.txs = "";
                else {
                    object.txs = [];
                    if (options.bytes !== Array)
                        object.txs = $util.newBuffer(object.txs);
                }
            }
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.txsHash != null && message.hasOwnProperty("txsHash"))
                object.txsHash = message.txsHash;
            if (message.aggregateSignature != null && message.hasOwnProperty("aggregateSignature"))
                object.aggregateSignature = options.bytes === String ? $util.base64.encode(message.aggregateSignature, 0, message.aggregateSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.aggregateSignature) : message.aggregateSignature;
            if (message.bitMap != null && message.hasOwnProperty("bitMap"))
                object.bitMap = options.bytes === String ? $util.base64.encode(message.bitMap, 0, message.bitMap.length) : options.bytes === Array ? Array.prototype.slice.call(message.bitMap) : message.bitMap;
            if (message.txs != null && message.hasOwnProperty("txs"))
                object.txs = options.bytes === String ? $util.base64.encode(message.txs, 0, message.txs.length) : options.bytes === Array ? Array.prototype.slice.call(message.txs) : message.txs;
            return object;
        };

        /**
         * Converts this CheckPointInfo to JSON.
         * @function toJSON
         * @memberof pb.CheckPointInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckPointInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CheckPointInfo
         * @function getTypeUrl
         * @memberof pb.CheckPointInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CheckPointInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.CheckPointInfo";
        };

        return CheckPointInfo;
    })();

    pb.GetConsensusStateRequest = (function() {

        /**
         * Properties of a GetConsensusStateRequest.
         * @memberof pb
         * @interface IGetConsensusStateRequest
         */

        /**
         * Constructs a new GetConsensusStateRequest.
         * @memberof pb
         * @classdesc Represents a GetConsensusStateRequest.
         * @implements IGetConsensusStateRequest
         * @constructor
         * @param {pb.IGetConsensusStateRequest=} [properties] Properties to set
         */
        function GetConsensusStateRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new GetConsensusStateRequest instance using the specified properties.
         * @function create
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {pb.IGetConsensusStateRequest=} [properties] Properties to set
         * @returns {pb.GetConsensusStateRequest} GetConsensusStateRequest instance
         */
        GetConsensusStateRequest.create = function create(properties) {
            return new GetConsensusStateRequest(properties);
        };

        /**
         * Encodes the specified GetConsensusStateRequest message. Does not implicitly {@link pb.GetConsensusStateRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {pb.IGetConsensusStateRequest} message GetConsensusStateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetConsensusStateRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            return writer;
        };

        /**
         * Encodes the specified GetConsensusStateRequest message, length delimited. Does not implicitly {@link pb.GetConsensusStateRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {pb.IGetConsensusStateRequest} message GetConsensusStateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetConsensusStateRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetConsensusStateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetConsensusStateRequest} GetConsensusStateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetConsensusStateRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetConsensusStateRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetConsensusStateRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetConsensusStateRequest} GetConsensusStateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetConsensusStateRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetConsensusStateRequest message.
         * @function verify
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetConsensusStateRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            return null;
        };

        /**
         * Creates a GetConsensusStateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetConsensusStateRequest} GetConsensusStateRequest
         */
        GetConsensusStateRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetConsensusStateRequest)
                return object;
            return new $root.pb.GetConsensusStateRequest();
        };

        /**
         * Creates a plain object from a GetConsensusStateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {pb.GetConsensusStateRequest} message GetConsensusStateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetConsensusStateRequest.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this GetConsensusStateRequest to JSON.
         * @function toJSON
         * @memberof pb.GetConsensusStateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetConsensusStateRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetConsensusStateRequest
         * @function getTypeUrl
         * @memberof pb.GetConsensusStateRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetConsensusStateRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetConsensusStateRequest";
        };

        return GetConsensusStateRequest;
    })();

    pb.PushQuery = (function() {

        /**
         * Properties of a PushQuery.
         * @memberof pb
         * @interface IPushQuery
         * @property {number|null} [requestId] PushQuery requestId
         * @property {string|null} [address] PushQuery address
         * @property {number|Long|null} [deadline] PushQuery deadline
         * @property {boolean|null} [containerIsBlock] PushQuery containerIsBlock
         * @property {Uint8Array|null} [container] PushQuery container
         * @property {number|Long|null} [requestedHeight] PushQuery requestedHeight
         * @property {string|null} [blockId] PushQuery blockId
         * @property {Uint8Array|null} [signature] PushQuery signature
         * @property {number|Long|null} [nonce] PushQuery nonce
         * @property {Uint8Array|null} [vrfSeed] PushQuery vrfSeed
         * @property {number|null} [seqId] PushQuery seqId
         */

        /**
         * Constructs a new PushQuery.
         * @memberof pb
         * @classdesc Represents a PushQuery.
         * @implements IPushQuery
         * @constructor
         * @param {pb.IPushQuery=} [properties] Properties to set
         */
        function PushQuery(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PushQuery requestId.
         * @member {number} requestId
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.requestId = 0;

        /**
         * PushQuery address.
         * @member {string} address
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.address = "";

        /**
         * PushQuery deadline.
         * @member {number|Long} deadline
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.deadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PushQuery containerIsBlock.
         * @member {boolean} containerIsBlock
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.containerIsBlock = false;

        /**
         * PushQuery container.
         * @member {Uint8Array} container
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.container = $util.newBuffer([]);

        /**
         * PushQuery requestedHeight.
         * @member {number|Long} requestedHeight
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.requestedHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PushQuery blockId.
         * @member {string} blockId
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.blockId = "";

        /**
         * PushQuery signature.
         * @member {Uint8Array} signature
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.signature = $util.newBuffer([]);

        /**
         * PushQuery nonce.
         * @member {number|Long} nonce
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.nonce = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PushQuery vrfSeed.
         * @member {Uint8Array} vrfSeed
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.vrfSeed = $util.newBuffer([]);

        /**
         * PushQuery seqId.
         * @member {number} seqId
         * @memberof pb.PushQuery
         * @instance
         */
        PushQuery.prototype.seqId = 0;

        /**
         * Creates a new PushQuery instance using the specified properties.
         * @function create
         * @memberof pb.PushQuery
         * @static
         * @param {pb.IPushQuery=} [properties] Properties to set
         * @returns {pb.PushQuery} PushQuery instance
         */
        PushQuery.create = function create(properties) {
            return new PushQuery(properties);
        };

        /**
         * Encodes the specified PushQuery message. Does not implicitly {@link pb.PushQuery.verify|verify} messages.
         * @function encode
         * @memberof pb.PushQuery
         * @static
         * @param {pb.IPushQuery} message PushQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PushQuery.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.requestId);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            if (message.deadline != null && Object.hasOwnProperty.call(message, "deadline"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.deadline);
            if (message.containerIsBlock != null && Object.hasOwnProperty.call(message, "containerIsBlock"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.containerIsBlock);
            if (message.container != null && Object.hasOwnProperty.call(message, "container"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.container);
            if (message.requestedHeight != null && Object.hasOwnProperty.call(message, "requestedHeight"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.requestedHeight);
            if (message.blockId != null && Object.hasOwnProperty.call(message, "blockId"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.blockId);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.signature);
            if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.nonce);
            if (message.vrfSeed != null && Object.hasOwnProperty.call(message, "vrfSeed"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.vrfSeed);
            if (message.seqId != null && Object.hasOwnProperty.call(message, "seqId"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.seqId);
            return writer;
        };

        /**
         * Encodes the specified PushQuery message, length delimited. Does not implicitly {@link pb.PushQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.PushQuery
         * @static
         * @param {pb.IPushQuery} message PushQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PushQuery.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PushQuery message from the specified reader or buffer.
         * @function decode
         * @memberof pb.PushQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.PushQuery} PushQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PushQuery.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.PushQuery();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.requestId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.address = reader.string();
                        break;
                    }
                case 3: {
                        message.deadline = reader.uint64();
                        break;
                    }
                case 4: {
                        message.containerIsBlock = reader.bool();
                        break;
                    }
                case 5: {
                        message.container = reader.bytes();
                        break;
                    }
                case 6: {
                        message.requestedHeight = reader.uint64();
                        break;
                    }
                case 7: {
                        message.blockId = reader.string();
                        break;
                    }
                case 8: {
                        message.signature = reader.bytes();
                        break;
                    }
                case 9: {
                        message.nonce = reader.uint64();
                        break;
                    }
                case 10: {
                        message.vrfSeed = reader.bytes();
                        break;
                    }
                case 11: {
                        message.seqId = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PushQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.PushQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.PushQuery} PushQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PushQuery.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PushQuery message.
         * @function verify
         * @memberof pb.PushQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PushQuery.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isInteger(message.requestId))
                    return "requestId: integer expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.deadline != null && message.hasOwnProperty("deadline"))
                if (!$util.isInteger(message.deadline) && !(message.deadline && $util.isInteger(message.deadline.low) && $util.isInteger(message.deadline.high)))
                    return "deadline: integer|Long expected";
            if (message.containerIsBlock != null && message.hasOwnProperty("containerIsBlock"))
                if (typeof message.containerIsBlock !== "boolean")
                    return "containerIsBlock: boolean expected";
            if (message.container != null && message.hasOwnProperty("container"))
                if (!(message.container && typeof message.container.length === "number" || $util.isString(message.container)))
                    return "container: buffer expected";
            if (message.requestedHeight != null && message.hasOwnProperty("requestedHeight"))
                if (!$util.isInteger(message.requestedHeight) && !(message.requestedHeight && $util.isInteger(message.requestedHeight.low) && $util.isInteger(message.requestedHeight.high)))
                    return "requestedHeight: integer|Long expected";
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                if (!$util.isString(message.blockId))
                    return "blockId: string expected";
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            if (message.nonce != null && message.hasOwnProperty("nonce"))
                if (!$util.isInteger(message.nonce) && !(message.nonce && $util.isInteger(message.nonce.low) && $util.isInteger(message.nonce.high)))
                    return "nonce: integer|Long expected";
            if (message.vrfSeed != null && message.hasOwnProperty("vrfSeed"))
                if (!(message.vrfSeed && typeof message.vrfSeed.length === "number" || $util.isString(message.vrfSeed)))
                    return "vrfSeed: buffer expected";
            if (message.seqId != null && message.hasOwnProperty("seqId"))
                if (!$util.isInteger(message.seqId))
                    return "seqId: integer expected";
            return null;
        };

        /**
         * Creates a PushQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.PushQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.PushQuery} PushQuery
         */
        PushQuery.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.PushQuery)
                return object;
            let message = new $root.pb.PushQuery();
            if (object.requestId != null)
                message.requestId = object.requestId >>> 0;
            if (object.address != null)
                message.address = String(object.address);
            if (object.deadline != null)
                if ($util.Long)
                    (message.deadline = $util.Long.fromValue(object.deadline)).unsigned = true;
                else if (typeof object.deadline === "string")
                    message.deadline = parseInt(object.deadline, 10);
                else if (typeof object.deadline === "number")
                    message.deadline = object.deadline;
                else if (typeof object.deadline === "object")
                    message.deadline = new $util.LongBits(object.deadline.low >>> 0, object.deadline.high >>> 0).toNumber(true);
            if (object.containerIsBlock != null)
                message.containerIsBlock = Boolean(object.containerIsBlock);
            if (object.container != null)
                if (typeof object.container === "string")
                    $util.base64.decode(object.container, message.container = $util.newBuffer($util.base64.length(object.container)), 0);
                else if (object.container.length >= 0)
                    message.container = object.container;
            if (object.requestedHeight != null)
                if ($util.Long)
                    (message.requestedHeight = $util.Long.fromValue(object.requestedHeight)).unsigned = true;
                else if (typeof object.requestedHeight === "string")
                    message.requestedHeight = parseInt(object.requestedHeight, 10);
                else if (typeof object.requestedHeight === "number")
                    message.requestedHeight = object.requestedHeight;
                else if (typeof object.requestedHeight === "object")
                    message.requestedHeight = new $util.LongBits(object.requestedHeight.low >>> 0, object.requestedHeight.high >>> 0).toNumber(true);
            if (object.blockId != null)
                message.blockId = String(object.blockId);
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            if (object.nonce != null)
                if ($util.Long)
                    (message.nonce = $util.Long.fromValue(object.nonce)).unsigned = true;
                else if (typeof object.nonce === "string")
                    message.nonce = parseInt(object.nonce, 10);
                else if (typeof object.nonce === "number")
                    message.nonce = object.nonce;
                else if (typeof object.nonce === "object")
                    message.nonce = new $util.LongBits(object.nonce.low >>> 0, object.nonce.high >>> 0).toNumber(true);
            if (object.vrfSeed != null)
                if (typeof object.vrfSeed === "string")
                    $util.base64.decode(object.vrfSeed, message.vrfSeed = $util.newBuffer($util.base64.length(object.vrfSeed)), 0);
                else if (object.vrfSeed.length >= 0)
                    message.vrfSeed = object.vrfSeed;
            if (object.seqId != null)
                message.seqId = object.seqId >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a PushQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.PushQuery
         * @static
         * @param {pb.PushQuery} message PushQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PushQuery.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.requestId = 0;
                object.address = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.deadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.deadline = options.longs === String ? "0" : 0;
                object.containerIsBlock = false;
                if (options.bytes === String)
                    object.container = "";
                else {
                    object.container = [];
                    if (options.bytes !== Array)
                        object.container = $util.newBuffer(object.container);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.requestedHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.requestedHeight = options.longs === String ? "0" : 0;
                object.blockId = "";
                if (options.bytes === String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== Array)
                        object.signature = $util.newBuffer(object.signature);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.nonce = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.nonce = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.vrfSeed = "";
                else {
                    object.vrfSeed = [];
                    if (options.bytes !== Array)
                        object.vrfSeed = $util.newBuffer(object.vrfSeed);
                }
                object.seqId = 0;
            }
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.deadline != null && message.hasOwnProperty("deadline"))
                if (typeof message.deadline === "number")
                    object.deadline = options.longs === String ? String(message.deadline) : message.deadline;
                else
                    object.deadline = options.longs === String ? $util.Long.prototype.toString.call(message.deadline) : options.longs === Number ? new $util.LongBits(message.deadline.low >>> 0, message.deadline.high >>> 0).toNumber(true) : message.deadline;
            if (message.containerIsBlock != null && message.hasOwnProperty("containerIsBlock"))
                object.containerIsBlock = message.containerIsBlock;
            if (message.container != null && message.hasOwnProperty("container"))
                object.container = options.bytes === String ? $util.base64.encode(message.container, 0, message.container.length) : options.bytes === Array ? Array.prototype.slice.call(message.container) : message.container;
            if (message.requestedHeight != null && message.hasOwnProperty("requestedHeight"))
                if (typeof message.requestedHeight === "number")
                    object.requestedHeight = options.longs === String ? String(message.requestedHeight) : message.requestedHeight;
                else
                    object.requestedHeight = options.longs === String ? $util.Long.prototype.toString.call(message.requestedHeight) : options.longs === Number ? new $util.LongBits(message.requestedHeight.low >>> 0, message.requestedHeight.high >>> 0).toNumber(true) : message.requestedHeight;
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                object.blockId = message.blockId;
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            if (message.nonce != null && message.hasOwnProperty("nonce"))
                if (typeof message.nonce === "number")
                    object.nonce = options.longs === String ? String(message.nonce) : message.nonce;
                else
                    object.nonce = options.longs === String ? $util.Long.prototype.toString.call(message.nonce) : options.longs === Number ? new $util.LongBits(message.nonce.low >>> 0, message.nonce.high >>> 0).toNumber(true) : message.nonce;
            if (message.vrfSeed != null && message.hasOwnProperty("vrfSeed"))
                object.vrfSeed = options.bytes === String ? $util.base64.encode(message.vrfSeed, 0, message.vrfSeed.length) : options.bytes === Array ? Array.prototype.slice.call(message.vrfSeed) : message.vrfSeed;
            if (message.seqId != null && message.hasOwnProperty("seqId"))
                object.seqId = message.seqId;
            return object;
        };

        /**
         * Converts this PushQuery to JSON.
         * @function toJSON
         * @memberof pb.PushQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PushQuery.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PushQuery
         * @function getTypeUrl
         * @memberof pb.PushQuery
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PushQuery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.PushQuery";
        };

        return PushQuery;
    })();

    pb.PullQuery = (function() {

        /**
         * Properties of a PullQuery.
         * @memberof pb
         * @interface IPullQuery
         * @property {number|null} [requestId] PullQuery requestId
         * @property {string|null} [address] PullQuery address
         * @property {number|Long|null} [deadline] PullQuery deadline
         * @property {string|null} [blockId] PullQuery blockId
         * @property {number|Long|null} [requestedHeight] PullQuery requestedHeight
         * @property {Uint8Array|null} [vrfSeed] PullQuery vrfSeed
         * @property {number|null} [seqId] PullQuery seqId
         */

        /**
         * Constructs a new PullQuery.
         * @memberof pb
         * @classdesc Represents a PullQuery.
         * @implements IPullQuery
         * @constructor
         * @param {pb.IPullQuery=} [properties] Properties to set
         */
        function PullQuery(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PullQuery requestId.
         * @member {number} requestId
         * @memberof pb.PullQuery
         * @instance
         */
        PullQuery.prototype.requestId = 0;

        /**
         * PullQuery address.
         * @member {string} address
         * @memberof pb.PullQuery
         * @instance
         */
        PullQuery.prototype.address = "";

        /**
         * PullQuery deadline.
         * @member {number|Long} deadline
         * @memberof pb.PullQuery
         * @instance
         */
        PullQuery.prototype.deadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PullQuery blockId.
         * @member {string} blockId
         * @memberof pb.PullQuery
         * @instance
         */
        PullQuery.prototype.blockId = "";

        /**
         * PullQuery requestedHeight.
         * @member {number|Long} requestedHeight
         * @memberof pb.PullQuery
         * @instance
         */
        PullQuery.prototype.requestedHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PullQuery vrfSeed.
         * @member {Uint8Array} vrfSeed
         * @memberof pb.PullQuery
         * @instance
         */
        PullQuery.prototype.vrfSeed = $util.newBuffer([]);

        /**
         * PullQuery seqId.
         * @member {number} seqId
         * @memberof pb.PullQuery
         * @instance
         */
        PullQuery.prototype.seqId = 0;

        /**
         * Creates a new PullQuery instance using the specified properties.
         * @function create
         * @memberof pb.PullQuery
         * @static
         * @param {pb.IPullQuery=} [properties] Properties to set
         * @returns {pb.PullQuery} PullQuery instance
         */
        PullQuery.create = function create(properties) {
            return new PullQuery(properties);
        };

        /**
         * Encodes the specified PullQuery message. Does not implicitly {@link pb.PullQuery.verify|verify} messages.
         * @function encode
         * @memberof pb.PullQuery
         * @static
         * @param {pb.IPullQuery} message PullQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PullQuery.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.requestId);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            if (message.deadline != null && Object.hasOwnProperty.call(message, "deadline"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.deadline);
            if (message.blockId != null && Object.hasOwnProperty.call(message, "blockId"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.blockId);
            if (message.requestedHeight != null && Object.hasOwnProperty.call(message, "requestedHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.requestedHeight);
            if (message.vrfSeed != null && Object.hasOwnProperty.call(message, "vrfSeed"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.vrfSeed);
            if (message.seqId != null && Object.hasOwnProperty.call(message, "seqId"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.seqId);
            return writer;
        };

        /**
         * Encodes the specified PullQuery message, length delimited. Does not implicitly {@link pb.PullQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.PullQuery
         * @static
         * @param {pb.IPullQuery} message PullQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PullQuery.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PullQuery message from the specified reader or buffer.
         * @function decode
         * @memberof pb.PullQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.PullQuery} PullQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PullQuery.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.PullQuery();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.requestId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.address = reader.string();
                        break;
                    }
                case 3: {
                        message.deadline = reader.uint64();
                        break;
                    }
                case 4: {
                        message.blockId = reader.string();
                        break;
                    }
                case 5: {
                        message.requestedHeight = reader.uint64();
                        break;
                    }
                case 6: {
                        message.vrfSeed = reader.bytes();
                        break;
                    }
                case 7: {
                        message.seqId = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PullQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.PullQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.PullQuery} PullQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PullQuery.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PullQuery message.
         * @function verify
         * @memberof pb.PullQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PullQuery.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isInteger(message.requestId))
                    return "requestId: integer expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.deadline != null && message.hasOwnProperty("deadline"))
                if (!$util.isInteger(message.deadline) && !(message.deadline && $util.isInteger(message.deadline.low) && $util.isInteger(message.deadline.high)))
                    return "deadline: integer|Long expected";
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                if (!$util.isString(message.blockId))
                    return "blockId: string expected";
            if (message.requestedHeight != null && message.hasOwnProperty("requestedHeight"))
                if (!$util.isInteger(message.requestedHeight) && !(message.requestedHeight && $util.isInteger(message.requestedHeight.low) && $util.isInteger(message.requestedHeight.high)))
                    return "requestedHeight: integer|Long expected";
            if (message.vrfSeed != null && message.hasOwnProperty("vrfSeed"))
                if (!(message.vrfSeed && typeof message.vrfSeed.length === "number" || $util.isString(message.vrfSeed)))
                    return "vrfSeed: buffer expected";
            if (message.seqId != null && message.hasOwnProperty("seqId"))
                if (!$util.isInteger(message.seqId))
                    return "seqId: integer expected";
            return null;
        };

        /**
         * Creates a PullQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.PullQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.PullQuery} PullQuery
         */
        PullQuery.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.PullQuery)
                return object;
            let message = new $root.pb.PullQuery();
            if (object.requestId != null)
                message.requestId = object.requestId >>> 0;
            if (object.address != null)
                message.address = String(object.address);
            if (object.deadline != null)
                if ($util.Long)
                    (message.deadline = $util.Long.fromValue(object.deadline)).unsigned = true;
                else if (typeof object.deadline === "string")
                    message.deadline = parseInt(object.deadline, 10);
                else if (typeof object.deadline === "number")
                    message.deadline = object.deadline;
                else if (typeof object.deadline === "object")
                    message.deadline = new $util.LongBits(object.deadline.low >>> 0, object.deadline.high >>> 0).toNumber(true);
            if (object.blockId != null)
                message.blockId = String(object.blockId);
            if (object.requestedHeight != null)
                if ($util.Long)
                    (message.requestedHeight = $util.Long.fromValue(object.requestedHeight)).unsigned = true;
                else if (typeof object.requestedHeight === "string")
                    message.requestedHeight = parseInt(object.requestedHeight, 10);
                else if (typeof object.requestedHeight === "number")
                    message.requestedHeight = object.requestedHeight;
                else if (typeof object.requestedHeight === "object")
                    message.requestedHeight = new $util.LongBits(object.requestedHeight.low >>> 0, object.requestedHeight.high >>> 0).toNumber(true);
            if (object.vrfSeed != null)
                if (typeof object.vrfSeed === "string")
                    $util.base64.decode(object.vrfSeed, message.vrfSeed = $util.newBuffer($util.base64.length(object.vrfSeed)), 0);
                else if (object.vrfSeed.length >= 0)
                    message.vrfSeed = object.vrfSeed;
            if (object.seqId != null)
                message.seqId = object.seqId >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a PullQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.PullQuery
         * @static
         * @param {pb.PullQuery} message PullQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PullQuery.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.requestId = 0;
                object.address = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.deadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.deadline = options.longs === String ? "0" : 0;
                object.blockId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.requestedHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.requestedHeight = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.vrfSeed = "";
                else {
                    object.vrfSeed = [];
                    if (options.bytes !== Array)
                        object.vrfSeed = $util.newBuffer(object.vrfSeed);
                }
                object.seqId = 0;
            }
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.deadline != null && message.hasOwnProperty("deadline"))
                if (typeof message.deadline === "number")
                    object.deadline = options.longs === String ? String(message.deadline) : message.deadline;
                else
                    object.deadline = options.longs === String ? $util.Long.prototype.toString.call(message.deadline) : options.longs === Number ? new $util.LongBits(message.deadline.low >>> 0, message.deadline.high >>> 0).toNumber(true) : message.deadline;
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                object.blockId = message.blockId;
            if (message.requestedHeight != null && message.hasOwnProperty("requestedHeight"))
                if (typeof message.requestedHeight === "number")
                    object.requestedHeight = options.longs === String ? String(message.requestedHeight) : message.requestedHeight;
                else
                    object.requestedHeight = options.longs === String ? $util.Long.prototype.toString.call(message.requestedHeight) : options.longs === Number ? new $util.LongBits(message.requestedHeight.low >>> 0, message.requestedHeight.high >>> 0).toNumber(true) : message.requestedHeight;
            if (message.vrfSeed != null && message.hasOwnProperty("vrfSeed"))
                object.vrfSeed = options.bytes === String ? $util.base64.encode(message.vrfSeed, 0, message.vrfSeed.length) : options.bytes === Array ? Array.prototype.slice.call(message.vrfSeed) : message.vrfSeed;
            if (message.seqId != null && message.hasOwnProperty("seqId"))
                object.seqId = message.seqId;
            return object;
        };

        /**
         * Converts this PullQuery to JSON.
         * @function toJSON
         * @memberof pb.PullQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PullQuery.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PullQuery
         * @function getTypeUrl
         * @memberof pb.PullQuery
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PullQuery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.PullQuery";
        };

        return PullQuery;
    })();

    pb.Chits = (function() {

        /**
         * Properties of a Chits.
         * @memberof pb
         * @interface IChits
         * @property {number|null} [requestId] Chits requestId
         * @property {string|null} [address] Chits address
         * @property {string|null} [preferredBlock] Chits preferredBlock
         * @property {string|null} [acceptedBlock] Chits acceptedBlock
         * @property {number|Long|null} [preferredBlockAtHeight] Chits preferredBlockAtHeight
         * @property {number|Long|null} [acceptedHeight] Chits acceptedHeight
         * @property {Uint8Array|null} [bitmap] Chits bitmap
         * @property {Uint8Array|null} [chitSignature] Chits chitSignature
         */

        /**
         * Constructs a new Chits.
         * @memberof pb
         * @classdesc Represents a Chits.
         * @implements IChits
         * @constructor
         * @param {pb.IChits=} [properties] Properties to set
         */
        function Chits(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Chits requestId.
         * @member {number} requestId
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.requestId = 0;

        /**
         * Chits address.
         * @member {string} address
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.address = "";

        /**
         * Chits preferredBlock.
         * @member {string} preferredBlock
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.preferredBlock = "";

        /**
         * Chits acceptedBlock.
         * @member {string} acceptedBlock
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.acceptedBlock = "";

        /**
         * Chits preferredBlockAtHeight.
         * @member {number|Long} preferredBlockAtHeight
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.preferredBlockAtHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Chits acceptedHeight.
         * @member {number|Long} acceptedHeight
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.acceptedHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Chits bitmap.
         * @member {Uint8Array} bitmap
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.bitmap = $util.newBuffer([]);

        /**
         * Chits chitSignature.
         * @member {Uint8Array} chitSignature
         * @memberof pb.Chits
         * @instance
         */
        Chits.prototype.chitSignature = $util.newBuffer([]);

        /**
         * Creates a new Chits instance using the specified properties.
         * @function create
         * @memberof pb.Chits
         * @static
         * @param {pb.IChits=} [properties] Properties to set
         * @returns {pb.Chits} Chits instance
         */
        Chits.create = function create(properties) {
            return new Chits(properties);
        };

        /**
         * Encodes the specified Chits message. Does not implicitly {@link pb.Chits.verify|verify} messages.
         * @function encode
         * @memberof pb.Chits
         * @static
         * @param {pb.IChits} message Chits message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Chits.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.requestId);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.address);
            if (message.preferredBlock != null && Object.hasOwnProperty.call(message, "preferredBlock"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.preferredBlock);
            if (message.acceptedBlock != null && Object.hasOwnProperty.call(message, "acceptedBlock"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.acceptedBlock);
            if (message.preferredBlockAtHeight != null && Object.hasOwnProperty.call(message, "preferredBlockAtHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.preferredBlockAtHeight);
            if (message.acceptedHeight != null && Object.hasOwnProperty.call(message, "acceptedHeight"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.acceptedHeight);
            if (message.bitmap != null && Object.hasOwnProperty.call(message, "bitmap"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.bitmap);
            if (message.chitSignature != null && Object.hasOwnProperty.call(message, "chitSignature"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.chitSignature);
            return writer;
        };

        /**
         * Encodes the specified Chits message, length delimited. Does not implicitly {@link pb.Chits.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.Chits
         * @static
         * @param {pb.IChits} message Chits message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Chits.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a Chits message from the specified reader or buffer.
         * @function decode
         * @memberof pb.Chits
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.Chits} Chits
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Chits.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.Chits();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.requestId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.address = reader.string();
                        break;
                    }
                case 3: {
                        message.preferredBlock = reader.string();
                        break;
                    }
                case 4: {
                        message.acceptedBlock = reader.string();
                        break;
                    }
                case 5: {
                        message.preferredBlockAtHeight = reader.uint64();
                        break;
                    }
                case 6: {
                        message.acceptedHeight = reader.uint64();
                        break;
                    }
                case 7: {
                        message.bitmap = reader.bytes();
                        break;
                    }
                case 8: {
                        message.chitSignature = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Chits message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.Chits
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.Chits} Chits
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Chits.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Chits message.
         * @function verify
         * @memberof pb.Chits
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Chits.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isInteger(message.requestId))
                    return "requestId: integer expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.preferredBlock != null && message.hasOwnProperty("preferredBlock"))
                if (!$util.isString(message.preferredBlock))
                    return "preferredBlock: string expected";
            if (message.acceptedBlock != null && message.hasOwnProperty("acceptedBlock"))
                if (!$util.isString(message.acceptedBlock))
                    return "acceptedBlock: string expected";
            if (message.preferredBlockAtHeight != null && message.hasOwnProperty("preferredBlockAtHeight"))
                if (!$util.isInteger(message.preferredBlockAtHeight) && !(message.preferredBlockAtHeight && $util.isInteger(message.preferredBlockAtHeight.low) && $util.isInteger(message.preferredBlockAtHeight.high)))
                    return "preferredBlockAtHeight: integer|Long expected";
            if (message.acceptedHeight != null && message.hasOwnProperty("acceptedHeight"))
                if (!$util.isInteger(message.acceptedHeight) && !(message.acceptedHeight && $util.isInteger(message.acceptedHeight.low) && $util.isInteger(message.acceptedHeight.high)))
                    return "acceptedHeight: integer|Long expected";
            if (message.bitmap != null && message.hasOwnProperty("bitmap"))
                if (!(message.bitmap && typeof message.bitmap.length === "number" || $util.isString(message.bitmap)))
                    return "bitmap: buffer expected";
            if (message.chitSignature != null && message.hasOwnProperty("chitSignature"))
                if (!(message.chitSignature && typeof message.chitSignature.length === "number" || $util.isString(message.chitSignature)))
                    return "chitSignature: buffer expected";
            return null;
        };

        /**
         * Creates a Chits message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.Chits
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.Chits} Chits
         */
        Chits.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.Chits)
                return object;
            let message = new $root.pb.Chits();
            if (object.requestId != null)
                message.requestId = object.requestId >>> 0;
            if (object.address != null)
                message.address = String(object.address);
            if (object.preferredBlock != null)
                message.preferredBlock = String(object.preferredBlock);
            if (object.acceptedBlock != null)
                message.acceptedBlock = String(object.acceptedBlock);
            if (object.preferredBlockAtHeight != null)
                if ($util.Long)
                    (message.preferredBlockAtHeight = $util.Long.fromValue(object.preferredBlockAtHeight)).unsigned = true;
                else if (typeof object.preferredBlockAtHeight === "string")
                    message.preferredBlockAtHeight = parseInt(object.preferredBlockAtHeight, 10);
                else if (typeof object.preferredBlockAtHeight === "number")
                    message.preferredBlockAtHeight = object.preferredBlockAtHeight;
                else if (typeof object.preferredBlockAtHeight === "object")
                    message.preferredBlockAtHeight = new $util.LongBits(object.preferredBlockAtHeight.low >>> 0, object.preferredBlockAtHeight.high >>> 0).toNumber(true);
            if (object.acceptedHeight != null)
                if ($util.Long)
                    (message.acceptedHeight = $util.Long.fromValue(object.acceptedHeight)).unsigned = true;
                else if (typeof object.acceptedHeight === "string")
                    message.acceptedHeight = parseInt(object.acceptedHeight, 10);
                else if (typeof object.acceptedHeight === "number")
                    message.acceptedHeight = object.acceptedHeight;
                else if (typeof object.acceptedHeight === "object")
                    message.acceptedHeight = new $util.LongBits(object.acceptedHeight.low >>> 0, object.acceptedHeight.high >>> 0).toNumber(true);
            if (object.bitmap != null)
                if (typeof object.bitmap === "string")
                    $util.base64.decode(object.bitmap, message.bitmap = $util.newBuffer($util.base64.length(object.bitmap)), 0);
                else if (object.bitmap.length >= 0)
                    message.bitmap = object.bitmap;
            if (object.chitSignature != null)
                if (typeof object.chitSignature === "string")
                    $util.base64.decode(object.chitSignature, message.chitSignature = $util.newBuffer($util.base64.length(object.chitSignature)), 0);
                else if (object.chitSignature.length >= 0)
                    message.chitSignature = object.chitSignature;
            return message;
        };

        /**
         * Creates a plain object from a Chits message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.Chits
         * @static
         * @param {pb.Chits} message Chits
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Chits.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.requestId = 0;
                object.address = "";
                object.preferredBlock = "";
                object.acceptedBlock = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.preferredBlockAtHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.preferredBlockAtHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.acceptedHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.acceptedHeight = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.bitmap = "";
                else {
                    object.bitmap = [];
                    if (options.bytes !== Array)
                        object.bitmap = $util.newBuffer(object.bitmap);
                }
                if (options.bytes === String)
                    object.chitSignature = "";
                else {
                    object.chitSignature = [];
                    if (options.bytes !== Array)
                        object.chitSignature = $util.newBuffer(object.chitSignature);
                }
            }
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.preferredBlock != null && message.hasOwnProperty("preferredBlock"))
                object.preferredBlock = message.preferredBlock;
            if (message.acceptedBlock != null && message.hasOwnProperty("acceptedBlock"))
                object.acceptedBlock = message.acceptedBlock;
            if (message.preferredBlockAtHeight != null && message.hasOwnProperty("preferredBlockAtHeight"))
                if (typeof message.preferredBlockAtHeight === "number")
                    object.preferredBlockAtHeight = options.longs === String ? String(message.preferredBlockAtHeight) : message.preferredBlockAtHeight;
                else
                    object.preferredBlockAtHeight = options.longs === String ? $util.Long.prototype.toString.call(message.preferredBlockAtHeight) : options.longs === Number ? new $util.LongBits(message.preferredBlockAtHeight.low >>> 0, message.preferredBlockAtHeight.high >>> 0).toNumber(true) : message.preferredBlockAtHeight;
            if (message.acceptedHeight != null && message.hasOwnProperty("acceptedHeight"))
                if (typeof message.acceptedHeight === "number")
                    object.acceptedHeight = options.longs === String ? String(message.acceptedHeight) : message.acceptedHeight;
                else
                    object.acceptedHeight = options.longs === String ? $util.Long.prototype.toString.call(message.acceptedHeight) : options.longs === Number ? new $util.LongBits(message.acceptedHeight.low >>> 0, message.acceptedHeight.high >>> 0).toNumber(true) : message.acceptedHeight;
            if (message.bitmap != null && message.hasOwnProperty("bitmap"))
                object.bitmap = options.bytes === String ? $util.base64.encode(message.bitmap, 0, message.bitmap.length) : options.bytes === Array ? Array.prototype.slice.call(message.bitmap) : message.bitmap;
            if (message.chitSignature != null && message.hasOwnProperty("chitSignature"))
                object.chitSignature = options.bytes === String ? $util.base64.encode(message.chitSignature, 0, message.chitSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.chitSignature) : message.chitSignature;
            return object;
        };

        /**
         * Converts this Chits to JSON.
         * @function toJSON
         * @memberof pb.Chits
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Chits.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Chits
         * @function getTypeUrl
         * @memberof pb.Chits
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Chits.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.Chits";
        };

        return Chits;
    })();

    pb.ChitSignature = (function() {

        /**
         * Properties of a ChitSignature.
         * @memberof pb
         * @interface IChitSignature
         * @property {string|null} [nodeId] ChitSignature nodeId
         * @property {string|null} [preferredId] ChitSignature preferredId
         * @property {Uint8Array|null} [signature] ChitSignature signature
         */

        /**
         * Constructs a new ChitSignature.
         * @memberof pb
         * @classdesc Represents a ChitSignature.
         * @implements IChitSignature
         * @constructor
         * @param {pb.IChitSignature=} [properties] Properties to set
         */
        function ChitSignature(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChitSignature nodeId.
         * @member {string} nodeId
         * @memberof pb.ChitSignature
         * @instance
         */
        ChitSignature.prototype.nodeId = "";

        /**
         * ChitSignature preferredId.
         * @member {string} preferredId
         * @memberof pb.ChitSignature
         * @instance
         */
        ChitSignature.prototype.preferredId = "";

        /**
         * ChitSignature signature.
         * @member {Uint8Array} signature
         * @memberof pb.ChitSignature
         * @instance
         */
        ChitSignature.prototype.signature = $util.newBuffer([]);

        /**
         * Creates a new ChitSignature instance using the specified properties.
         * @function create
         * @memberof pb.ChitSignature
         * @static
         * @param {pb.IChitSignature=} [properties] Properties to set
         * @returns {pb.ChitSignature} ChitSignature instance
         */
        ChitSignature.create = function create(properties) {
            return new ChitSignature(properties);
        };

        /**
         * Encodes the specified ChitSignature message. Does not implicitly {@link pb.ChitSignature.verify|verify} messages.
         * @function encode
         * @memberof pb.ChitSignature
         * @static
         * @param {pb.IChitSignature} message ChitSignature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChitSignature.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.nodeId);
            if (message.preferredId != null && Object.hasOwnProperty.call(message, "preferredId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.preferredId);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.signature);
            return writer;
        };

        /**
         * Encodes the specified ChitSignature message, length delimited. Does not implicitly {@link pb.ChitSignature.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ChitSignature
         * @static
         * @param {pb.IChitSignature} message ChitSignature message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChitSignature.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChitSignature message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ChitSignature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ChitSignature} ChitSignature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChitSignature.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ChitSignature();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.nodeId = reader.string();
                        break;
                    }
                case 2: {
                        message.preferredId = reader.string();
                        break;
                    }
                case 3: {
                        message.signature = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChitSignature message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ChitSignature
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ChitSignature} ChitSignature
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChitSignature.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChitSignature message.
         * @function verify
         * @memberof pb.ChitSignature
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChitSignature.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.nodeId != null && message.hasOwnProperty("nodeId"))
                if (!$util.isString(message.nodeId))
                    return "nodeId: string expected";
            if (message.preferredId != null && message.hasOwnProperty("preferredId"))
                if (!$util.isString(message.preferredId))
                    return "preferredId: string expected";
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            return null;
        };

        /**
         * Creates a ChitSignature message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ChitSignature
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ChitSignature} ChitSignature
         */
        ChitSignature.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ChitSignature)
                return object;
            let message = new $root.pb.ChitSignature();
            if (object.nodeId != null)
                message.nodeId = String(object.nodeId);
            if (object.preferredId != null)
                message.preferredId = String(object.preferredId);
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            return message;
        };

        /**
         * Creates a plain object from a ChitSignature message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ChitSignature
         * @static
         * @param {pb.ChitSignature} message ChitSignature
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChitSignature.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.nodeId = "";
                object.preferredId = "";
                if (options.bytes === String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== Array)
                        object.signature = $util.newBuffer(object.signature);
                }
            }
            if (message.nodeId != null && message.hasOwnProperty("nodeId"))
                object.nodeId = message.nodeId;
            if (message.preferredId != null && message.hasOwnProperty("preferredId"))
                object.preferredId = message.preferredId;
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            return object;
        };

        /**
         * Converts this ChitSignature to JSON.
         * @function toJSON
         * @memberof pb.ChitSignature
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChitSignature.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChitSignature
         * @function getTypeUrl
         * @memberof pb.ChitSignature
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChitSignature.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ChitSignature";
        };

        return ChitSignature;
    })();

    pb.RoundSignatures = (function() {

        /**
         * Properties of a RoundSignatures.
         * @memberof pb
         * @interface IRoundSignatures
         * @property {number|null} [seqId] RoundSignatures seqId
         * @property {Array.<pb.IChitSignature>|null} [signatures] RoundSignatures signatures
         */

        /**
         * Constructs a new RoundSignatures.
         * @memberof pb
         * @classdesc Represents a RoundSignatures.
         * @implements IRoundSignatures
         * @constructor
         * @param {pb.IRoundSignatures=} [properties] Properties to set
         */
        function RoundSignatures(properties) {
            this.signatures = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RoundSignatures seqId.
         * @member {number} seqId
         * @memberof pb.RoundSignatures
         * @instance
         */
        RoundSignatures.prototype.seqId = 0;

        /**
         * RoundSignatures signatures.
         * @member {Array.<pb.IChitSignature>} signatures
         * @memberof pb.RoundSignatures
         * @instance
         */
        RoundSignatures.prototype.signatures = $util.emptyArray;

        /**
         * Creates a new RoundSignatures instance using the specified properties.
         * @function create
         * @memberof pb.RoundSignatures
         * @static
         * @param {pb.IRoundSignatures=} [properties] Properties to set
         * @returns {pb.RoundSignatures} RoundSignatures instance
         */
        RoundSignatures.create = function create(properties) {
            return new RoundSignatures(properties);
        };

        /**
         * Encodes the specified RoundSignatures message. Does not implicitly {@link pb.RoundSignatures.verify|verify} messages.
         * @function encode
         * @memberof pb.RoundSignatures
         * @static
         * @param {pb.IRoundSignatures} message RoundSignatures message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundSignatures.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seqId != null && Object.hasOwnProperty.call(message, "seqId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.seqId);
            if (message.signatures != null && message.signatures.length)
                for (let i = 0; i < message.signatures.length; ++i)
                    $root.pb.ChitSignature.encode(message.signatures[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RoundSignatures message, length delimited. Does not implicitly {@link pb.RoundSignatures.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.RoundSignatures
         * @static
         * @param {pb.IRoundSignatures} message RoundSignatures message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RoundSignatures.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RoundSignatures message from the specified reader or buffer.
         * @function decode
         * @memberof pb.RoundSignatures
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.RoundSignatures} RoundSignatures
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundSignatures.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.RoundSignatures();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.seqId = reader.uint32();
                        break;
                    }
                case 2: {
                        if (!(message.signatures && message.signatures.length))
                            message.signatures = [];
                        message.signatures.push($root.pb.ChitSignature.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RoundSignatures message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.RoundSignatures
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.RoundSignatures} RoundSignatures
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RoundSignatures.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RoundSignatures message.
         * @function verify
         * @memberof pb.RoundSignatures
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RoundSignatures.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.seqId != null && message.hasOwnProperty("seqId"))
                if (!$util.isInteger(message.seqId))
                    return "seqId: integer expected";
            if (message.signatures != null && message.hasOwnProperty("signatures")) {
                if (!Array.isArray(message.signatures))
                    return "signatures: array expected";
                for (let i = 0; i < message.signatures.length; ++i) {
                    let error = $root.pb.ChitSignature.verify(message.signatures[i]);
                    if (error)
                        return "signatures." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RoundSignatures message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.RoundSignatures
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.RoundSignatures} RoundSignatures
         */
        RoundSignatures.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.RoundSignatures)
                return object;
            let message = new $root.pb.RoundSignatures();
            if (object.seqId != null)
                message.seqId = object.seqId >>> 0;
            if (object.signatures) {
                if (!Array.isArray(object.signatures))
                    throw TypeError(".pb.RoundSignatures.signatures: array expected");
                message.signatures = [];
                for (let i = 0; i < object.signatures.length; ++i) {
                    if (typeof object.signatures[i] !== "object")
                        throw TypeError(".pb.RoundSignatures.signatures: object expected");
                    message.signatures[i] = $root.pb.ChitSignature.fromObject(object.signatures[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RoundSignatures message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.RoundSignatures
         * @static
         * @param {pb.RoundSignatures} message RoundSignatures
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RoundSignatures.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.signatures = [];
            if (options.defaults)
                object.seqId = 0;
            if (message.seqId != null && message.hasOwnProperty("seqId"))
                object.seqId = message.seqId;
            if (message.signatures && message.signatures.length) {
                object.signatures = [];
                for (let j = 0; j < message.signatures.length; ++j)
                    object.signatures[j] = $root.pb.ChitSignature.toObject(message.signatures[j], options);
            }
            return object;
        };

        /**
         * Converts this RoundSignatures to JSON.
         * @function toJSON
         * @memberof pb.RoundSignatures
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RoundSignatures.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RoundSignatures
         * @function getTypeUrl
         * @memberof pb.RoundSignatures
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RoundSignatures.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.RoundSignatures";
        };

        return RoundSignatures;
    })();

    pb.ConsensusSignatureSet = (function() {

        /**
         * Properties of a ConsensusSignatureSet.
         * @memberof pb
         * @interface IConsensusSignatureSet
         * @property {string|null} [blockId] ConsensusSignatureSet blockId
         * @property {number|Long|null} [height] ConsensusSignatureSet height
         * @property {string|null} [parentId] ConsensusSignatureSet parentId
         * @property {Uint8Array|null} [vrfSeed] ConsensusSignatureSet vrfSeed
         * @property {Array.<pb.IRoundSignatures>|null} [rounds] ConsensusSignatureSet rounds
         */

        /**
         * Constructs a new ConsensusSignatureSet.
         * @memberof pb
         * @classdesc Represents a ConsensusSignatureSet.
         * @implements IConsensusSignatureSet
         * @constructor
         * @param {pb.IConsensusSignatureSet=} [properties] Properties to set
         */
        function ConsensusSignatureSet(properties) {
            this.rounds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ConsensusSignatureSet blockId.
         * @member {string} blockId
         * @memberof pb.ConsensusSignatureSet
         * @instance
         */
        ConsensusSignatureSet.prototype.blockId = "";

        /**
         * ConsensusSignatureSet height.
         * @member {number|Long} height
         * @memberof pb.ConsensusSignatureSet
         * @instance
         */
        ConsensusSignatureSet.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ConsensusSignatureSet parentId.
         * @member {string} parentId
         * @memberof pb.ConsensusSignatureSet
         * @instance
         */
        ConsensusSignatureSet.prototype.parentId = "";

        /**
         * ConsensusSignatureSet vrfSeed.
         * @member {Uint8Array} vrfSeed
         * @memberof pb.ConsensusSignatureSet
         * @instance
         */
        ConsensusSignatureSet.prototype.vrfSeed = $util.newBuffer([]);

        /**
         * ConsensusSignatureSet rounds.
         * @member {Array.<pb.IRoundSignatures>} rounds
         * @memberof pb.ConsensusSignatureSet
         * @instance
         */
        ConsensusSignatureSet.prototype.rounds = $util.emptyArray;

        /**
         * Creates a new ConsensusSignatureSet instance using the specified properties.
         * @function create
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {pb.IConsensusSignatureSet=} [properties] Properties to set
         * @returns {pb.ConsensusSignatureSet} ConsensusSignatureSet instance
         */
        ConsensusSignatureSet.create = function create(properties) {
            return new ConsensusSignatureSet(properties);
        };

        /**
         * Encodes the specified ConsensusSignatureSet message. Does not implicitly {@link pb.ConsensusSignatureSet.verify|verify} messages.
         * @function encode
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {pb.IConsensusSignatureSet} message ConsensusSignatureSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusSignatureSet.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.blockId != null && Object.hasOwnProperty.call(message, "blockId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.blockId);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.height);
            if (message.parentId != null && Object.hasOwnProperty.call(message, "parentId"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.parentId);
            if (message.vrfSeed != null && Object.hasOwnProperty.call(message, "vrfSeed"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.vrfSeed);
            if (message.rounds != null && message.rounds.length)
                for (let i = 0; i < message.rounds.length; ++i)
                    $root.pb.RoundSignatures.encode(message.rounds[i], writer.uint32(/* id 5, wireType 2 =*/42).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ConsensusSignatureSet message, length delimited. Does not implicitly {@link pb.ConsensusSignatureSet.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {pb.IConsensusSignatureSet} message ConsensusSignatureSet message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConsensusSignatureSet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ConsensusSignatureSet message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ConsensusSignatureSet} ConsensusSignatureSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusSignatureSet.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ConsensusSignatureSet();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.blockId = reader.string();
                        break;
                    }
                case 2: {
                        message.height = reader.uint64();
                        break;
                    }
                case 3: {
                        message.parentId = reader.string();
                        break;
                    }
                case 4: {
                        message.vrfSeed = reader.bytes();
                        break;
                    }
                case 5: {
                        if (!(message.rounds && message.rounds.length))
                            message.rounds = [];
                        message.rounds.push($root.pb.RoundSignatures.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ConsensusSignatureSet message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ConsensusSignatureSet} ConsensusSignatureSet
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConsensusSignatureSet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ConsensusSignatureSet message.
         * @function verify
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ConsensusSignatureSet.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                if (!$util.isString(message.blockId))
                    return "blockId: string expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.parentId != null && message.hasOwnProperty("parentId"))
                if (!$util.isString(message.parentId))
                    return "parentId: string expected";
            if (message.vrfSeed != null && message.hasOwnProperty("vrfSeed"))
                if (!(message.vrfSeed && typeof message.vrfSeed.length === "number" || $util.isString(message.vrfSeed)))
                    return "vrfSeed: buffer expected";
            if (message.rounds != null && message.hasOwnProperty("rounds")) {
                if (!Array.isArray(message.rounds))
                    return "rounds: array expected";
                for (let i = 0; i < message.rounds.length; ++i) {
                    let error = $root.pb.RoundSignatures.verify(message.rounds[i]);
                    if (error)
                        return "rounds." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ConsensusSignatureSet message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ConsensusSignatureSet} ConsensusSignatureSet
         */
        ConsensusSignatureSet.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ConsensusSignatureSet)
                return object;
            let message = new $root.pb.ConsensusSignatureSet();
            if (object.blockId != null)
                message.blockId = String(object.blockId);
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.parentId != null)
                message.parentId = String(object.parentId);
            if (object.vrfSeed != null)
                if (typeof object.vrfSeed === "string")
                    $util.base64.decode(object.vrfSeed, message.vrfSeed = $util.newBuffer($util.base64.length(object.vrfSeed)), 0);
                else if (object.vrfSeed.length >= 0)
                    message.vrfSeed = object.vrfSeed;
            if (object.rounds) {
                if (!Array.isArray(object.rounds))
                    throw TypeError(".pb.ConsensusSignatureSet.rounds: array expected");
                message.rounds = [];
                for (let i = 0; i < object.rounds.length; ++i) {
                    if (typeof object.rounds[i] !== "object")
                        throw TypeError(".pb.ConsensusSignatureSet.rounds: object expected");
                    message.rounds[i] = $root.pb.RoundSignatures.fromObject(object.rounds[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ConsensusSignatureSet message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {pb.ConsensusSignatureSet} message ConsensusSignatureSet
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConsensusSignatureSet.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rounds = [];
            if (options.defaults) {
                object.blockId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.parentId = "";
                if (options.bytes === String)
                    object.vrfSeed = "";
                else {
                    object.vrfSeed = [];
                    if (options.bytes !== Array)
                        object.vrfSeed = $util.newBuffer(object.vrfSeed);
                }
            }
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                object.blockId = message.blockId;
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.parentId != null && message.hasOwnProperty("parentId"))
                object.parentId = message.parentId;
            if (message.vrfSeed != null && message.hasOwnProperty("vrfSeed"))
                object.vrfSeed = options.bytes === String ? $util.base64.encode(message.vrfSeed, 0, message.vrfSeed.length) : options.bytes === Array ? Array.prototype.slice.call(message.vrfSeed) : message.vrfSeed;
            if (message.rounds && message.rounds.length) {
                object.rounds = [];
                for (let j = 0; j < message.rounds.length; ++j)
                    object.rounds[j] = $root.pb.RoundSignatures.toObject(message.rounds[j], options);
            }
            return object;
        };

        /**
         * Converts this ConsensusSignatureSet to JSON.
         * @function toJSON
         * @memberof pb.ConsensusSignatureSet
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConsensusSignatureSet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ConsensusSignatureSet
         * @function getTypeUrl
         * @memberof pb.ConsensusSignatureSet
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ConsensusSignatureSet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ConsensusSignatureSet";
        };

        return ConsensusSignatureSet;
    })();

    pb.HeightResponse = (function() {

        /**
         * Properties of an HeightResponse.
         * @memberof pb
         * @interface IHeightResponse
         * @property {number|Long|null} [LastAcceptedHeight] HeightResponse LastAcceptedHeight
         * @property {number|Long|null} [CurrentHeight] HeightResponse CurrentHeight
         * @property {string|null} [address] HeightResponse address
         * @property {number|null} [pendingBlocksCount] HeightResponse pendingBlocksCount
         * @property {number|null} [pendingTxCount] HeightResponse pendingTxCount
         * @property {boolean|null} [isSyncing] HeightResponse isSyncing
         * @property {number|Long|null} [syncTargetHeight] HeightResponse syncTargetHeight
         * @property {boolean|null} [isSnapshotSync] HeightResponse isSnapshotSync
         */

        /**
         * Constructs a new HeightResponse.
         * @memberof pb
         * @classdesc Represents an HeightResponse.
         * @implements IHeightResponse
         * @constructor
         * @param {pb.IHeightResponse=} [properties] Properties to set
         */
        function HeightResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HeightResponse LastAcceptedHeight.
         * @member {number|Long} LastAcceptedHeight
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.LastAcceptedHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HeightResponse CurrentHeight.
         * @member {number|Long} CurrentHeight
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.CurrentHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HeightResponse address.
         * @member {string} address
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.address = "";

        /**
         * HeightResponse pendingBlocksCount.
         * @member {number} pendingBlocksCount
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.pendingBlocksCount = 0;

        /**
         * HeightResponse pendingTxCount.
         * @member {number} pendingTxCount
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.pendingTxCount = 0;

        /**
         * HeightResponse isSyncing.
         * @member {boolean} isSyncing
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.isSyncing = false;

        /**
         * HeightResponse syncTargetHeight.
         * @member {number|Long} syncTargetHeight
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.syncTargetHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HeightResponse isSnapshotSync.
         * @member {boolean} isSnapshotSync
         * @memberof pb.HeightResponse
         * @instance
         */
        HeightResponse.prototype.isSnapshotSync = false;

        /**
         * Creates a new HeightResponse instance using the specified properties.
         * @function create
         * @memberof pb.HeightResponse
         * @static
         * @param {pb.IHeightResponse=} [properties] Properties to set
         * @returns {pb.HeightResponse} HeightResponse instance
         */
        HeightResponse.create = function create(properties) {
            return new HeightResponse(properties);
        };

        /**
         * Encodes the specified HeightResponse message. Does not implicitly {@link pb.HeightResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.HeightResponse
         * @static
         * @param {pb.IHeightResponse} message HeightResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeightResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.LastAcceptedHeight != null && Object.hasOwnProperty.call(message, "LastAcceptedHeight"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.LastAcceptedHeight);
            if (message.CurrentHeight != null && Object.hasOwnProperty.call(message, "CurrentHeight"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.CurrentHeight);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.address);
            if (message.pendingBlocksCount != null && Object.hasOwnProperty.call(message, "pendingBlocksCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.pendingBlocksCount);
            if (message.pendingTxCount != null && Object.hasOwnProperty.call(message, "pendingTxCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.pendingTxCount);
            if (message.isSyncing != null && Object.hasOwnProperty.call(message, "isSyncing"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isSyncing);
            if (message.syncTargetHeight != null && Object.hasOwnProperty.call(message, "syncTargetHeight"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.syncTargetHeight);
            if (message.isSnapshotSync != null && Object.hasOwnProperty.call(message, "isSnapshotSync"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.isSnapshotSync);
            return writer;
        };

        /**
         * Encodes the specified HeightResponse message, length delimited. Does not implicitly {@link pb.HeightResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.HeightResponse
         * @static
         * @param {pb.IHeightResponse} message HeightResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeightResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an HeightResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.HeightResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.HeightResponse} HeightResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeightResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.HeightResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.LastAcceptedHeight = reader.uint64();
                        break;
                    }
                case 2: {
                        message.CurrentHeight = reader.uint64();
                        break;
                    }
                case 3: {
                        message.address = reader.string();
                        break;
                    }
                case 4: {
                        message.pendingBlocksCount = reader.uint32();
                        break;
                    }
                case 5: {
                        message.pendingTxCount = reader.uint32();
                        break;
                    }
                case 6: {
                        message.isSyncing = reader.bool();
                        break;
                    }
                case 7: {
                        message.syncTargetHeight = reader.uint64();
                        break;
                    }
                case 8: {
                        message.isSnapshotSync = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an HeightResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.HeightResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.HeightResponse} HeightResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeightResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an HeightResponse message.
         * @function verify
         * @memberof pb.HeightResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HeightResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.LastAcceptedHeight != null && message.hasOwnProperty("LastAcceptedHeight"))
                if (!$util.isInteger(message.LastAcceptedHeight) && !(message.LastAcceptedHeight && $util.isInteger(message.LastAcceptedHeight.low) && $util.isInteger(message.LastAcceptedHeight.high)))
                    return "LastAcceptedHeight: integer|Long expected";
            if (message.CurrentHeight != null && message.hasOwnProperty("CurrentHeight"))
                if (!$util.isInteger(message.CurrentHeight) && !(message.CurrentHeight && $util.isInteger(message.CurrentHeight.low) && $util.isInteger(message.CurrentHeight.high)))
                    return "CurrentHeight: integer|Long expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.pendingBlocksCount != null && message.hasOwnProperty("pendingBlocksCount"))
                if (!$util.isInteger(message.pendingBlocksCount))
                    return "pendingBlocksCount: integer expected";
            if (message.pendingTxCount != null && message.hasOwnProperty("pendingTxCount"))
                if (!$util.isInteger(message.pendingTxCount))
                    return "pendingTxCount: integer expected";
            if (message.isSyncing != null && message.hasOwnProperty("isSyncing"))
                if (typeof message.isSyncing !== "boolean")
                    return "isSyncing: boolean expected";
            if (message.syncTargetHeight != null && message.hasOwnProperty("syncTargetHeight"))
                if (!$util.isInteger(message.syncTargetHeight) && !(message.syncTargetHeight && $util.isInteger(message.syncTargetHeight.low) && $util.isInteger(message.syncTargetHeight.high)))
                    return "syncTargetHeight: integer|Long expected";
            if (message.isSnapshotSync != null && message.hasOwnProperty("isSnapshotSync"))
                if (typeof message.isSnapshotSync !== "boolean")
                    return "isSnapshotSync: boolean expected";
            return null;
        };

        /**
         * Creates an HeightResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.HeightResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.HeightResponse} HeightResponse
         */
        HeightResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.HeightResponse)
                return object;
            let message = new $root.pb.HeightResponse();
            if (object.LastAcceptedHeight != null)
                if ($util.Long)
                    (message.LastAcceptedHeight = $util.Long.fromValue(object.LastAcceptedHeight)).unsigned = true;
                else if (typeof object.LastAcceptedHeight === "string")
                    message.LastAcceptedHeight = parseInt(object.LastAcceptedHeight, 10);
                else if (typeof object.LastAcceptedHeight === "number")
                    message.LastAcceptedHeight = object.LastAcceptedHeight;
                else if (typeof object.LastAcceptedHeight === "object")
                    message.LastAcceptedHeight = new $util.LongBits(object.LastAcceptedHeight.low >>> 0, object.LastAcceptedHeight.high >>> 0).toNumber(true);
            if (object.CurrentHeight != null)
                if ($util.Long)
                    (message.CurrentHeight = $util.Long.fromValue(object.CurrentHeight)).unsigned = true;
                else if (typeof object.CurrentHeight === "string")
                    message.CurrentHeight = parseInt(object.CurrentHeight, 10);
                else if (typeof object.CurrentHeight === "number")
                    message.CurrentHeight = object.CurrentHeight;
                else if (typeof object.CurrentHeight === "object")
                    message.CurrentHeight = new $util.LongBits(object.CurrentHeight.low >>> 0, object.CurrentHeight.high >>> 0).toNumber(true);
            if (object.address != null)
                message.address = String(object.address);
            if (object.pendingBlocksCount != null)
                message.pendingBlocksCount = object.pendingBlocksCount >>> 0;
            if (object.pendingTxCount != null)
                message.pendingTxCount = object.pendingTxCount >>> 0;
            if (object.isSyncing != null)
                message.isSyncing = Boolean(object.isSyncing);
            if (object.syncTargetHeight != null)
                if ($util.Long)
                    (message.syncTargetHeight = $util.Long.fromValue(object.syncTargetHeight)).unsigned = true;
                else if (typeof object.syncTargetHeight === "string")
                    message.syncTargetHeight = parseInt(object.syncTargetHeight, 10);
                else if (typeof object.syncTargetHeight === "number")
                    message.syncTargetHeight = object.syncTargetHeight;
                else if (typeof object.syncTargetHeight === "object")
                    message.syncTargetHeight = new $util.LongBits(object.syncTargetHeight.low >>> 0, object.syncTargetHeight.high >>> 0).toNumber(true);
            if (object.isSnapshotSync != null)
                message.isSnapshotSync = Boolean(object.isSnapshotSync);
            return message;
        };

        /**
         * Creates a plain object from an HeightResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.HeightResponse
         * @static
         * @param {pb.HeightResponse} message HeightResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HeightResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.LastAcceptedHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.LastAcceptedHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.CurrentHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.CurrentHeight = options.longs === String ? "0" : 0;
                object.address = "";
                object.pendingBlocksCount = 0;
                object.pendingTxCount = 0;
                object.isSyncing = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.syncTargetHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.syncTargetHeight = options.longs === String ? "0" : 0;
                object.isSnapshotSync = false;
            }
            if (message.LastAcceptedHeight != null && message.hasOwnProperty("LastAcceptedHeight"))
                if (typeof message.LastAcceptedHeight === "number")
                    object.LastAcceptedHeight = options.longs === String ? String(message.LastAcceptedHeight) : message.LastAcceptedHeight;
                else
                    object.LastAcceptedHeight = options.longs === String ? $util.Long.prototype.toString.call(message.LastAcceptedHeight) : options.longs === Number ? new $util.LongBits(message.LastAcceptedHeight.low >>> 0, message.LastAcceptedHeight.high >>> 0).toNumber(true) : message.LastAcceptedHeight;
            if (message.CurrentHeight != null && message.hasOwnProperty("CurrentHeight"))
                if (typeof message.CurrentHeight === "number")
                    object.CurrentHeight = options.longs === String ? String(message.CurrentHeight) : message.CurrentHeight;
                else
                    object.CurrentHeight = options.longs === String ? $util.Long.prototype.toString.call(message.CurrentHeight) : options.longs === Number ? new $util.LongBits(message.CurrentHeight.low >>> 0, message.CurrentHeight.high >>> 0).toNumber(true) : message.CurrentHeight;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.pendingBlocksCount != null && message.hasOwnProperty("pendingBlocksCount"))
                object.pendingBlocksCount = message.pendingBlocksCount;
            if (message.pendingTxCount != null && message.hasOwnProperty("pendingTxCount"))
                object.pendingTxCount = message.pendingTxCount;
            if (message.isSyncing != null && message.hasOwnProperty("isSyncing"))
                object.isSyncing = message.isSyncing;
            if (message.syncTargetHeight != null && message.hasOwnProperty("syncTargetHeight"))
                if (typeof message.syncTargetHeight === "number")
                    object.syncTargetHeight = options.longs === String ? String(message.syncTargetHeight) : message.syncTargetHeight;
                else
                    object.syncTargetHeight = options.longs === String ? $util.Long.prototype.toString.call(message.syncTargetHeight) : options.longs === Number ? new $util.LongBits(message.syncTargetHeight.low >>> 0, message.syncTargetHeight.high >>> 0).toNumber(true) : message.syncTargetHeight;
            if (message.isSnapshotSync != null && message.hasOwnProperty("isSnapshotSync"))
                object.isSnapshotSync = message.isSnapshotSync;
            return object;
        };

        /**
         * Converts this HeightResponse to JSON.
         * @function toJSON
         * @memberof pb.HeightResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HeightResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HeightResponse
         * @function getTypeUrl
         * @memberof pb.HeightResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HeightResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.HeightResponse";
        };

        return HeightResponse;
    })();

    pb.GetBlockByIDRequest = (function() {

        /**
         * Properties of a GetBlockByIDRequest.
         * @memberof pb
         * @interface IGetBlockByIDRequest
         * @property {string|null} [blockId] GetBlockByIDRequest blockId
         */

        /**
         * Constructs a new GetBlockByIDRequest.
         * @memberof pb
         * @classdesc Represents a GetBlockByIDRequest.
         * @implements IGetBlockByIDRequest
         * @constructor
         * @param {pb.IGetBlockByIDRequest=} [properties] Properties to set
         */
        function GetBlockByIDRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetBlockByIDRequest blockId.
         * @member {string} blockId
         * @memberof pb.GetBlockByIDRequest
         * @instance
         */
        GetBlockByIDRequest.prototype.blockId = "";

        /**
         * Creates a new GetBlockByIDRequest instance using the specified properties.
         * @function create
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {pb.IGetBlockByIDRequest=} [properties] Properties to set
         * @returns {pb.GetBlockByIDRequest} GetBlockByIDRequest instance
         */
        GetBlockByIDRequest.create = function create(properties) {
            return new GetBlockByIDRequest(properties);
        };

        /**
         * Encodes the specified GetBlockByIDRequest message. Does not implicitly {@link pb.GetBlockByIDRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {pb.IGetBlockByIDRequest} message GetBlockByIDRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockByIDRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.blockId != null && Object.hasOwnProperty.call(message, "blockId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.blockId);
            return writer;
        };

        /**
         * Encodes the specified GetBlockByIDRequest message, length delimited. Does not implicitly {@link pb.GetBlockByIDRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {pb.IGetBlockByIDRequest} message GetBlockByIDRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetBlockByIDRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetBlockByIDRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetBlockByIDRequest} GetBlockByIDRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockByIDRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetBlockByIDRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.blockId = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetBlockByIDRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetBlockByIDRequest} GetBlockByIDRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetBlockByIDRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetBlockByIDRequest message.
         * @function verify
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetBlockByIDRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                if (!$util.isString(message.blockId))
                    return "blockId: string expected";
            return null;
        };

        /**
         * Creates a GetBlockByIDRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetBlockByIDRequest} GetBlockByIDRequest
         */
        GetBlockByIDRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetBlockByIDRequest)
                return object;
            let message = new $root.pb.GetBlockByIDRequest();
            if (object.blockId != null)
                message.blockId = String(object.blockId);
            return message;
        };

        /**
         * Creates a plain object from a GetBlockByIDRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {pb.GetBlockByIDRequest} message GetBlockByIDRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetBlockByIDRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.blockId = "";
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                object.blockId = message.blockId;
            return object;
        };

        /**
         * Converts this GetBlockByIDRequest to JSON.
         * @function toJSON
         * @memberof pb.GetBlockByIDRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetBlockByIDRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetBlockByIDRequest
         * @function getTypeUrl
         * @memberof pb.GetBlockByIDRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetBlockByIDRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetBlockByIDRequest";
        };

        return GetBlockByIDRequest;
    })();

    pb.PendingBlockInfo = (function() {

        /**
         * Properties of a PendingBlockInfo.
         * @memberof pb
         * @interface IPendingBlockInfo
         * @property {string|null} [blockId] PendingBlockInfo blockId
         * @property {number|Long|null} [height] PendingBlockInfo height
         * @property {string|null} [parentId] PendingBlockInfo parentId
         * @property {string|null} [proposer] PendingBlockInfo proposer
         * @property {number|null} [window] PendingBlockInfo window
         * @property {number|null} [votes] PendingBlockInfo votes
         * @property {boolean|null} [isPreferred] PendingBlockInfo isPreferred
         */

        /**
         * Constructs a new PendingBlockInfo.
         * @memberof pb
         * @classdesc Represents a PendingBlockInfo.
         * @implements IPendingBlockInfo
         * @constructor
         * @param {pb.IPendingBlockInfo=} [properties] Properties to set
         */
        function PendingBlockInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PendingBlockInfo blockId.
         * @member {string} blockId
         * @memberof pb.PendingBlockInfo
         * @instance
         */
        PendingBlockInfo.prototype.blockId = "";

        /**
         * PendingBlockInfo height.
         * @member {number|Long} height
         * @memberof pb.PendingBlockInfo
         * @instance
         */
        PendingBlockInfo.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * PendingBlockInfo parentId.
         * @member {string} parentId
         * @memberof pb.PendingBlockInfo
         * @instance
         */
        PendingBlockInfo.prototype.parentId = "";

        /**
         * PendingBlockInfo proposer.
         * @member {string} proposer
         * @memberof pb.PendingBlockInfo
         * @instance
         */
        PendingBlockInfo.prototype.proposer = "";

        /**
         * PendingBlockInfo window.
         * @member {number} window
         * @memberof pb.PendingBlockInfo
         * @instance
         */
        PendingBlockInfo.prototype.window = 0;

        /**
         * PendingBlockInfo votes.
         * @member {number} votes
         * @memberof pb.PendingBlockInfo
         * @instance
         */
        PendingBlockInfo.prototype.votes = 0;

        /**
         * PendingBlockInfo isPreferred.
         * @member {boolean} isPreferred
         * @memberof pb.PendingBlockInfo
         * @instance
         */
        PendingBlockInfo.prototype.isPreferred = false;

        /**
         * Creates a new PendingBlockInfo instance using the specified properties.
         * @function create
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {pb.IPendingBlockInfo=} [properties] Properties to set
         * @returns {pb.PendingBlockInfo} PendingBlockInfo instance
         */
        PendingBlockInfo.create = function create(properties) {
            return new PendingBlockInfo(properties);
        };

        /**
         * Encodes the specified PendingBlockInfo message. Does not implicitly {@link pb.PendingBlockInfo.verify|verify} messages.
         * @function encode
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {pb.IPendingBlockInfo} message PendingBlockInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingBlockInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.blockId != null && Object.hasOwnProperty.call(message, "blockId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.blockId);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.height);
            if (message.parentId != null && Object.hasOwnProperty.call(message, "parentId"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.parentId);
            if (message.proposer != null && Object.hasOwnProperty.call(message, "proposer"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.proposer);
            if (message.window != null && Object.hasOwnProperty.call(message, "window"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.window);
            if (message.votes != null && Object.hasOwnProperty.call(message, "votes"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.votes);
            if (message.isPreferred != null && Object.hasOwnProperty.call(message, "isPreferred"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.isPreferred);
            return writer;
        };

        /**
         * Encodes the specified PendingBlockInfo message, length delimited. Does not implicitly {@link pb.PendingBlockInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {pb.IPendingBlockInfo} message PendingBlockInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingBlockInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PendingBlockInfo message from the specified reader or buffer.
         * @function decode
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.PendingBlockInfo} PendingBlockInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingBlockInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.PendingBlockInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.blockId = reader.string();
                        break;
                    }
                case 2: {
                        message.height = reader.uint64();
                        break;
                    }
                case 3: {
                        message.parentId = reader.string();
                        break;
                    }
                case 4: {
                        message.proposer = reader.string();
                        break;
                    }
                case 5: {
                        message.window = reader.int32();
                        break;
                    }
                case 6: {
                        message.votes = reader.int32();
                        break;
                    }
                case 7: {
                        message.isPreferred = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PendingBlockInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.PendingBlockInfo} PendingBlockInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingBlockInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingBlockInfo message.
         * @function verify
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingBlockInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                if (!$util.isString(message.blockId))
                    return "blockId: string expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.parentId != null && message.hasOwnProperty("parentId"))
                if (!$util.isString(message.parentId))
                    return "parentId: string expected";
            if (message.proposer != null && message.hasOwnProperty("proposer"))
                if (!$util.isString(message.proposer))
                    return "proposer: string expected";
            if (message.window != null && message.hasOwnProperty("window"))
                if (!$util.isInteger(message.window))
                    return "window: integer expected";
            if (message.votes != null && message.hasOwnProperty("votes"))
                if (!$util.isInteger(message.votes))
                    return "votes: integer expected";
            if (message.isPreferred != null && message.hasOwnProperty("isPreferred"))
                if (typeof message.isPreferred !== "boolean")
                    return "isPreferred: boolean expected";
            return null;
        };

        /**
         * Creates a PendingBlockInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.PendingBlockInfo} PendingBlockInfo
         */
        PendingBlockInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.PendingBlockInfo)
                return object;
            let message = new $root.pb.PendingBlockInfo();
            if (object.blockId != null)
                message.blockId = String(object.blockId);
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.parentId != null)
                message.parentId = String(object.parentId);
            if (object.proposer != null)
                message.proposer = String(object.proposer);
            if (object.window != null)
                message.window = object.window | 0;
            if (object.votes != null)
                message.votes = object.votes | 0;
            if (object.isPreferred != null)
                message.isPreferred = Boolean(object.isPreferred);
            return message;
        };

        /**
         * Creates a plain object from a PendingBlockInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {pb.PendingBlockInfo} message PendingBlockInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingBlockInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.blockId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.parentId = "";
                object.proposer = "";
                object.window = 0;
                object.votes = 0;
                object.isPreferred = false;
            }
            if (message.blockId != null && message.hasOwnProperty("blockId"))
                object.blockId = message.blockId;
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.parentId != null && message.hasOwnProperty("parentId"))
                object.parentId = message.parentId;
            if (message.proposer != null && message.hasOwnProperty("proposer"))
                object.proposer = message.proposer;
            if (message.window != null && message.hasOwnProperty("window"))
                object.window = message.window;
            if (message.votes != null && message.hasOwnProperty("votes"))
                object.votes = message.votes;
            if (message.isPreferred != null && message.hasOwnProperty("isPreferred"))
                object.isPreferred = message.isPreferred;
            return object;
        };

        /**
         * Converts this PendingBlockInfo to JSON.
         * @function toJSON
         * @memberof pb.PendingBlockInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingBlockInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PendingBlockInfo
         * @function getTypeUrl
         * @memberof pb.PendingBlockInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PendingBlockInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.PendingBlockInfo";
        };

        return PendingBlockInfo;
    })();

    pb.HeightConsensusState = (function() {

        /**
         * Properties of an HeightConsensusState.
         * @memberof pb
         * @interface IHeightConsensusState
         * @property {number|Long|null} [height] HeightConsensusState height
         * @property {string|null} [preference] HeightConsensusState preference
         * @property {number|null} [confidence] HeightConsensusState confidence
         * @property {boolean|null} [finalized] HeightConsensusState finalized
         */

        /**
         * Constructs a new HeightConsensusState.
         * @memberof pb
         * @classdesc Represents an HeightConsensusState.
         * @implements IHeightConsensusState
         * @constructor
         * @param {pb.IHeightConsensusState=} [properties] Properties to set
         */
        function HeightConsensusState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HeightConsensusState height.
         * @member {number|Long} height
         * @memberof pb.HeightConsensusState
         * @instance
         */
        HeightConsensusState.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * HeightConsensusState preference.
         * @member {string} preference
         * @memberof pb.HeightConsensusState
         * @instance
         */
        HeightConsensusState.prototype.preference = "";

        /**
         * HeightConsensusState confidence.
         * @member {number} confidence
         * @memberof pb.HeightConsensusState
         * @instance
         */
        HeightConsensusState.prototype.confidence = 0;

        /**
         * HeightConsensusState finalized.
         * @member {boolean} finalized
         * @memberof pb.HeightConsensusState
         * @instance
         */
        HeightConsensusState.prototype.finalized = false;

        /**
         * Creates a new HeightConsensusState instance using the specified properties.
         * @function create
         * @memberof pb.HeightConsensusState
         * @static
         * @param {pb.IHeightConsensusState=} [properties] Properties to set
         * @returns {pb.HeightConsensusState} HeightConsensusState instance
         */
        HeightConsensusState.create = function create(properties) {
            return new HeightConsensusState(properties);
        };

        /**
         * Encodes the specified HeightConsensusState message. Does not implicitly {@link pb.HeightConsensusState.verify|verify} messages.
         * @function encode
         * @memberof pb.HeightConsensusState
         * @static
         * @param {pb.IHeightConsensusState} message HeightConsensusState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeightConsensusState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.height);
            if (message.preference != null && Object.hasOwnProperty.call(message, "preference"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.preference);
            if (message.confidence != null && Object.hasOwnProperty.call(message, "confidence"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.confidence);
            if (message.finalized != null && Object.hasOwnProperty.call(message, "finalized"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.finalized);
            return writer;
        };

        /**
         * Encodes the specified HeightConsensusState message, length delimited. Does not implicitly {@link pb.HeightConsensusState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.HeightConsensusState
         * @static
         * @param {pb.IHeightConsensusState} message HeightConsensusState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HeightConsensusState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an HeightConsensusState message from the specified reader or buffer.
         * @function decode
         * @memberof pb.HeightConsensusState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.HeightConsensusState} HeightConsensusState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeightConsensusState.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.HeightConsensusState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.height = reader.uint64();
                        break;
                    }
                case 2: {
                        message.preference = reader.string();
                        break;
                    }
                case 3: {
                        message.confidence = reader.int32();
                        break;
                    }
                case 4: {
                        message.finalized = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an HeightConsensusState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.HeightConsensusState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.HeightConsensusState} HeightConsensusState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HeightConsensusState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an HeightConsensusState message.
         * @function verify
         * @memberof pb.HeightConsensusState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HeightConsensusState.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.preference != null && message.hasOwnProperty("preference"))
                if (!$util.isString(message.preference))
                    return "preference: string expected";
            if (message.confidence != null && message.hasOwnProperty("confidence"))
                if (!$util.isInteger(message.confidence))
                    return "confidence: integer expected";
            if (message.finalized != null && message.hasOwnProperty("finalized"))
                if (typeof message.finalized !== "boolean")
                    return "finalized: boolean expected";
            return null;
        };

        /**
         * Creates an HeightConsensusState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.HeightConsensusState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.HeightConsensusState} HeightConsensusState
         */
        HeightConsensusState.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.HeightConsensusState)
                return object;
            let message = new $root.pb.HeightConsensusState();
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.preference != null)
                message.preference = String(object.preference);
            if (object.confidence != null)
                message.confidence = object.confidence | 0;
            if (object.finalized != null)
                message.finalized = Boolean(object.finalized);
            return message;
        };

        /**
         * Creates a plain object from an HeightConsensusState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.HeightConsensusState
         * @static
         * @param {pb.HeightConsensusState} message HeightConsensusState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HeightConsensusState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.preference = "";
                object.confidence = 0;
                object.finalized = false;
            }
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.preference != null && message.hasOwnProperty("preference"))
                object.preference = message.preference;
            if (message.confidence != null && message.hasOwnProperty("confidence"))
                object.confidence = message.confidence;
            if (message.finalized != null && message.hasOwnProperty("finalized"))
                object.finalized = message.finalized;
            return object;
        };

        /**
         * Converts this HeightConsensusState to JSON.
         * @function toJSON
         * @memberof pb.HeightConsensusState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HeightConsensusState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HeightConsensusState
         * @function getTypeUrl
         * @memberof pb.HeightConsensusState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HeightConsensusState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.HeightConsensusState";
        };

        return HeightConsensusState;
    })();

    pb.PendingBlocksResponse = (function() {

        /**
         * Properties of a PendingBlocksResponse.
         * @memberof pb
         * @interface IPendingBlocksResponse
         * @property {Array.<pb.IPendingBlockInfo>|null} [blocks] PendingBlocksResponse blocks
         * @property {Array.<pb.IHeightConsensusState>|null} [heightStates] PendingBlocksResponse heightStates
         */

        /**
         * Constructs a new PendingBlocksResponse.
         * @memberof pb
         * @classdesc Represents a PendingBlocksResponse.
         * @implements IPendingBlocksResponse
         * @constructor
         * @param {pb.IPendingBlocksResponse=} [properties] Properties to set
         */
        function PendingBlocksResponse(properties) {
            this.blocks = [];
            this.heightStates = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PendingBlocksResponse blocks.
         * @member {Array.<pb.IPendingBlockInfo>} blocks
         * @memberof pb.PendingBlocksResponse
         * @instance
         */
        PendingBlocksResponse.prototype.blocks = $util.emptyArray;

        /**
         * PendingBlocksResponse heightStates.
         * @member {Array.<pb.IHeightConsensusState>} heightStates
         * @memberof pb.PendingBlocksResponse
         * @instance
         */
        PendingBlocksResponse.prototype.heightStates = $util.emptyArray;

        /**
         * Creates a new PendingBlocksResponse instance using the specified properties.
         * @function create
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {pb.IPendingBlocksResponse=} [properties] Properties to set
         * @returns {pb.PendingBlocksResponse} PendingBlocksResponse instance
         */
        PendingBlocksResponse.create = function create(properties) {
            return new PendingBlocksResponse(properties);
        };

        /**
         * Encodes the specified PendingBlocksResponse message. Does not implicitly {@link pb.PendingBlocksResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {pb.IPendingBlocksResponse} message PendingBlocksResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingBlocksResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.blocks != null && message.blocks.length)
                for (let i = 0; i < message.blocks.length; ++i)
                    $root.pb.PendingBlockInfo.encode(message.blocks[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.heightStates != null && message.heightStates.length)
                for (let i = 0; i < message.heightStates.length; ++i)
                    $root.pb.HeightConsensusState.encode(message.heightStates[i], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified PendingBlocksResponse message, length delimited. Does not implicitly {@link pb.PendingBlocksResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {pb.IPendingBlocksResponse} message PendingBlocksResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PendingBlocksResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a PendingBlocksResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.PendingBlocksResponse} PendingBlocksResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingBlocksResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.PendingBlocksResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.blocks && message.blocks.length))
                            message.blocks = [];
                        message.blocks.push($root.pb.PendingBlockInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        if (!(message.heightStates && message.heightStates.length))
                            message.heightStates = [];
                        message.heightStates.push($root.pb.HeightConsensusState.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PendingBlocksResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.PendingBlocksResponse} PendingBlocksResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PendingBlocksResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PendingBlocksResponse message.
         * @function verify
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PendingBlocksResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.blocks != null && message.hasOwnProperty("blocks")) {
                if (!Array.isArray(message.blocks))
                    return "blocks: array expected";
                for (let i = 0; i < message.blocks.length; ++i) {
                    let error = $root.pb.PendingBlockInfo.verify(message.blocks[i]);
                    if (error)
                        return "blocks." + error;
                }
            }
            if (message.heightStates != null && message.hasOwnProperty("heightStates")) {
                if (!Array.isArray(message.heightStates))
                    return "heightStates: array expected";
                for (let i = 0; i < message.heightStates.length; ++i) {
                    let error = $root.pb.HeightConsensusState.verify(message.heightStates[i]);
                    if (error)
                        return "heightStates." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PendingBlocksResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.PendingBlocksResponse} PendingBlocksResponse
         */
        PendingBlocksResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.PendingBlocksResponse)
                return object;
            let message = new $root.pb.PendingBlocksResponse();
            if (object.blocks) {
                if (!Array.isArray(object.blocks))
                    throw TypeError(".pb.PendingBlocksResponse.blocks: array expected");
                message.blocks = [];
                for (let i = 0; i < object.blocks.length; ++i) {
                    if (typeof object.blocks[i] !== "object")
                        throw TypeError(".pb.PendingBlocksResponse.blocks: object expected");
                    message.blocks[i] = $root.pb.PendingBlockInfo.fromObject(object.blocks[i]);
                }
            }
            if (object.heightStates) {
                if (!Array.isArray(object.heightStates))
                    throw TypeError(".pb.PendingBlocksResponse.heightStates: array expected");
                message.heightStates = [];
                for (let i = 0; i < object.heightStates.length; ++i) {
                    if (typeof object.heightStates[i] !== "object")
                        throw TypeError(".pb.PendingBlocksResponse.heightStates: object expected");
                    message.heightStates[i] = $root.pb.HeightConsensusState.fromObject(object.heightStates[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PendingBlocksResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {pb.PendingBlocksResponse} message PendingBlocksResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PendingBlocksResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.blocks = [];
                object.heightStates = [];
            }
            if (message.blocks && message.blocks.length) {
                object.blocks = [];
                for (let j = 0; j < message.blocks.length; ++j)
                    object.blocks[j] = $root.pb.PendingBlockInfo.toObject(message.blocks[j], options);
            }
            if (message.heightStates && message.heightStates.length) {
                object.heightStates = [];
                for (let j = 0; j < message.heightStates.length; ++j)
                    object.heightStates[j] = $root.pb.HeightConsensusState.toObject(message.heightStates[j], options);
            }
            return object;
        };

        /**
         * Converts this PendingBlocksResponse to JSON.
         * @function toJSON
         * @memberof pb.PendingBlocksResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PendingBlocksResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PendingBlocksResponse
         * @function getTypeUrl
         * @memberof pb.PendingBlocksResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PendingBlocksResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.PendingBlocksResponse";
        };

        return PendingBlocksResponse;
    })();

    /**
     * WitnessStatus enum.
     * @name pb.WitnessStatus
     * @enum {number}
     * @property {number} WITNESS_CANDIDATE=0 WITNESS_CANDIDATE value
     * @property {number} WITNESS_ACTIVE=1 WITNESS_ACTIVE value
     * @property {number} WITNESS_UNSTAKING=2 WITNESS_UNSTAKING value
     * @property {number} WITNESS_EXITED=3 WITNESS_EXITED value
     * @property {number} WITNESS_SLASHED=4 WITNESS_SLASHED value
     */
    pb.WitnessStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "WITNESS_CANDIDATE"] = 0;
        values[valuesById[1] = "WITNESS_ACTIVE"] = 1;
        values[valuesById[2] = "WITNESS_UNSTAKING"] = 2;
        values[valuesById[3] = "WITNESS_EXITED"] = 3;
        values[valuesById[4] = "WITNESS_SLASHED"] = 4;
        return values;
    })();

    /**
     * WitnessVoteType enum.
     * @name pb.WitnessVoteType
     * @enum {number}
     * @property {number} VOTE_PASS=0 VOTE_PASS value
     * @property {number} VOTE_FAIL=1 VOTE_FAIL value
     * @property {number} VOTE_ABSTAIN=2 VOTE_ABSTAIN value
     */
    pb.WitnessVoteType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "VOTE_PASS"] = 0;
        values[valuesById[1] = "VOTE_FAIL"] = 1;
        values[valuesById[2] = "VOTE_ABSTAIN"] = 2;
        return values;
    })();

    /**
     * RechargeRequestStatus enum.
     * @name pb.RechargeRequestStatus
     * @enum {number}
     * @property {number} RECHARGE_PENDING=0 RECHARGE_PENDING value
     * @property {number} RECHARGE_VOTING=1 RECHARGE_VOTING value
     * @property {number} RECHARGE_CONSENSUS_PASS=2 RECHARGE_CONSENSUS_PASS value
     * @property {number} RECHARGE_CONSENSUS_FAIL=3 RECHARGE_CONSENSUS_FAIL value
     * @property {number} RECHARGE_CHALLENGE_PERIOD=4 RECHARGE_CHALLENGE_PERIOD value
     * @property {number} RECHARGE_CHALLENGED=5 RECHARGE_CHALLENGED value
     * @property {number} RECHARGE_ARBITRATION=6 RECHARGE_ARBITRATION value
     * @property {number} RECHARGE_FINALIZED=7 RECHARGE_FINALIZED value
     * @property {number} RECHARGE_REJECTED=8 RECHARGE_REJECTED value
     * @property {number} RECHARGE_SHELVED=9 RECHARGE_SHELVED value
     */
    pb.RechargeRequestStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "RECHARGE_PENDING"] = 0;
        values[valuesById[1] = "RECHARGE_VOTING"] = 1;
        values[valuesById[2] = "RECHARGE_CONSENSUS_PASS"] = 2;
        values[valuesById[3] = "RECHARGE_CONSENSUS_FAIL"] = 3;
        values[valuesById[4] = "RECHARGE_CHALLENGE_PERIOD"] = 4;
        values[valuesById[5] = "RECHARGE_CHALLENGED"] = 5;
        values[valuesById[6] = "RECHARGE_ARBITRATION"] = 6;
        values[valuesById[7] = "RECHARGE_FINALIZED"] = 7;
        values[valuesById[8] = "RECHARGE_REJECTED"] = 8;
        values[valuesById[9] = "RECHARGE_SHELVED"] = 9;
        return values;
    })();

    pb.WitnessInfo = (function() {

        /**
         * Properties of a WitnessInfo.
         * @memberof pb
         * @interface IWitnessInfo
         * @property {string|null} [address] WitnessInfo address
         * @property {string|null} [stakeAmount] WitnessInfo stakeAmount
         * @property {string|null} [pendingReward] WitnessInfo pendingReward
         * @property {pb.WitnessStatus|null} [status] WitnessInfo status
         * @property {number|Long|null} [unstakeHeight] WitnessInfo unstakeHeight
         * @property {number|Long|null} [totalWitnessCount] WitnessInfo totalWitnessCount
         * @property {number|Long|null} [passCount] WitnessInfo passCount
         * @property {number|Long|null} [failCount] WitnessInfo failCount
         * @property {number|Long|null} [abstainCount] WitnessInfo abstainCount
         * @property {number|Long|null} [slashedCount] WitnessInfo slashedCount
         * @property {Array.<string>|null} [pendingTasks] WitnessInfo pendingTasks
         * @property {Array.<string>|null} [shelvedArbitrations] WitnessInfo shelvedArbitrations
         * @property {string|null} [totalReward] WitnessInfo totalReward
         */

        /**
         * Constructs a new WitnessInfo.
         * @memberof pb
         * @classdesc Represents a WitnessInfo.
         * @implements IWitnessInfo
         * @constructor
         * @param {pb.IWitnessInfo=} [properties] Properties to set
         */
        function WitnessInfo(properties) {
            this.pendingTasks = [];
            this.shelvedArbitrations = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessInfo address.
         * @member {string} address
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.address = "";

        /**
         * WitnessInfo stakeAmount.
         * @member {string} stakeAmount
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.stakeAmount = "";

        /**
         * WitnessInfo pendingReward.
         * @member {string} pendingReward
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.pendingReward = "";

        /**
         * WitnessInfo status.
         * @member {pb.WitnessStatus} status
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.status = 0;

        /**
         * WitnessInfo unstakeHeight.
         * @member {number|Long} unstakeHeight
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.unstakeHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessInfo totalWitnessCount.
         * @member {number|Long} totalWitnessCount
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.totalWitnessCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessInfo passCount.
         * @member {number|Long} passCount
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.passCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessInfo failCount.
         * @member {number|Long} failCount
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.failCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessInfo abstainCount.
         * @member {number|Long} abstainCount
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.abstainCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessInfo slashedCount.
         * @member {number|Long} slashedCount
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.slashedCount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessInfo pendingTasks.
         * @member {Array.<string>} pendingTasks
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.pendingTasks = $util.emptyArray;

        /**
         * WitnessInfo shelvedArbitrations.
         * @member {Array.<string>} shelvedArbitrations
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.shelvedArbitrations = $util.emptyArray;

        /**
         * WitnessInfo totalReward.
         * @member {string} totalReward
         * @memberof pb.WitnessInfo
         * @instance
         */
        WitnessInfo.prototype.totalReward = "";

        /**
         * Creates a new WitnessInfo instance using the specified properties.
         * @function create
         * @memberof pb.WitnessInfo
         * @static
         * @param {pb.IWitnessInfo=} [properties] Properties to set
         * @returns {pb.WitnessInfo} WitnessInfo instance
         */
        WitnessInfo.create = function create(properties) {
            return new WitnessInfo(properties);
        };

        /**
         * Encodes the specified WitnessInfo message. Does not implicitly {@link pb.WitnessInfo.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessInfo
         * @static
         * @param {pb.IWitnessInfo} message WitnessInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            if (message.stakeAmount != null && Object.hasOwnProperty.call(message, "stakeAmount"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.stakeAmount);
            if (message.pendingReward != null && Object.hasOwnProperty.call(message, "pendingReward"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.pendingReward);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.status);
            if (message.unstakeHeight != null && Object.hasOwnProperty.call(message, "unstakeHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.unstakeHeight);
            if (message.totalWitnessCount != null && Object.hasOwnProperty.call(message, "totalWitnessCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.totalWitnessCount);
            if (message.passCount != null && Object.hasOwnProperty.call(message, "passCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.passCount);
            if (message.failCount != null && Object.hasOwnProperty.call(message, "failCount"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.failCount);
            if (message.abstainCount != null && Object.hasOwnProperty.call(message, "abstainCount"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.abstainCount);
            if (message.slashedCount != null && Object.hasOwnProperty.call(message, "slashedCount"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.slashedCount);
            if (message.pendingTasks != null && message.pendingTasks.length)
                for (let i = 0; i < message.pendingTasks.length; ++i)
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.pendingTasks[i]);
            if (message.shelvedArbitrations != null && message.shelvedArbitrations.length)
                for (let i = 0; i < message.shelvedArbitrations.length; ++i)
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.shelvedArbitrations[i]);
            if (message.totalReward != null && Object.hasOwnProperty.call(message, "totalReward"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.totalReward);
            return writer;
        };

        /**
         * Encodes the specified WitnessInfo message, length delimited. Does not implicitly {@link pb.WitnessInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessInfo
         * @static
         * @param {pb.IWitnessInfo} message WitnessInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessInfo message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessInfo} WitnessInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.string();
                        break;
                    }
                case 2: {
                        message.stakeAmount = reader.string();
                        break;
                    }
                case 3: {
                        message.pendingReward = reader.string();
                        break;
                    }
                case 4: {
                        message.status = reader.int32();
                        break;
                    }
                case 5: {
                        message.unstakeHeight = reader.uint64();
                        break;
                    }
                case 6: {
                        message.totalWitnessCount = reader.uint64();
                        break;
                    }
                case 7: {
                        message.passCount = reader.uint64();
                        break;
                    }
                case 8: {
                        message.failCount = reader.uint64();
                        break;
                    }
                case 9: {
                        message.abstainCount = reader.uint64();
                        break;
                    }
                case 10: {
                        message.slashedCount = reader.uint64();
                        break;
                    }
                case 11: {
                        if (!(message.pendingTasks && message.pendingTasks.length))
                            message.pendingTasks = [];
                        message.pendingTasks.push(reader.string());
                        break;
                    }
                case 12: {
                        if (!(message.shelvedArbitrations && message.shelvedArbitrations.length))
                            message.shelvedArbitrations = [];
                        message.shelvedArbitrations.push(reader.string());
                        break;
                    }
                case 13: {
                        message.totalReward = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessInfo} WitnessInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessInfo message.
         * @function verify
         * @memberof pb.WitnessInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.stakeAmount != null && message.hasOwnProperty("stakeAmount"))
                if (!$util.isString(message.stakeAmount))
                    return "stakeAmount: string expected";
            if (message.pendingReward != null && message.hasOwnProperty("pendingReward"))
                if (!$util.isString(message.pendingReward))
                    return "pendingReward: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.unstakeHeight != null && message.hasOwnProperty("unstakeHeight"))
                if (!$util.isInteger(message.unstakeHeight) && !(message.unstakeHeight && $util.isInteger(message.unstakeHeight.low) && $util.isInteger(message.unstakeHeight.high)))
                    return "unstakeHeight: integer|Long expected";
            if (message.totalWitnessCount != null && message.hasOwnProperty("totalWitnessCount"))
                if (!$util.isInteger(message.totalWitnessCount) && !(message.totalWitnessCount && $util.isInteger(message.totalWitnessCount.low) && $util.isInteger(message.totalWitnessCount.high)))
                    return "totalWitnessCount: integer|Long expected";
            if (message.passCount != null && message.hasOwnProperty("passCount"))
                if (!$util.isInteger(message.passCount) && !(message.passCount && $util.isInteger(message.passCount.low) && $util.isInteger(message.passCount.high)))
                    return "passCount: integer|Long expected";
            if (message.failCount != null && message.hasOwnProperty("failCount"))
                if (!$util.isInteger(message.failCount) && !(message.failCount && $util.isInteger(message.failCount.low) && $util.isInteger(message.failCount.high)))
                    return "failCount: integer|Long expected";
            if (message.abstainCount != null && message.hasOwnProperty("abstainCount"))
                if (!$util.isInteger(message.abstainCount) && !(message.abstainCount && $util.isInteger(message.abstainCount.low) && $util.isInteger(message.abstainCount.high)))
                    return "abstainCount: integer|Long expected";
            if (message.slashedCount != null && message.hasOwnProperty("slashedCount"))
                if (!$util.isInteger(message.slashedCount) && !(message.slashedCount && $util.isInteger(message.slashedCount.low) && $util.isInteger(message.slashedCount.high)))
                    return "slashedCount: integer|Long expected";
            if (message.pendingTasks != null && message.hasOwnProperty("pendingTasks")) {
                if (!Array.isArray(message.pendingTasks))
                    return "pendingTasks: array expected";
                for (let i = 0; i < message.pendingTasks.length; ++i)
                    if (!$util.isString(message.pendingTasks[i]))
                        return "pendingTasks: string[] expected";
            }
            if (message.shelvedArbitrations != null && message.hasOwnProperty("shelvedArbitrations")) {
                if (!Array.isArray(message.shelvedArbitrations))
                    return "shelvedArbitrations: array expected";
                for (let i = 0; i < message.shelvedArbitrations.length; ++i)
                    if (!$util.isString(message.shelvedArbitrations[i]))
                        return "shelvedArbitrations: string[] expected";
            }
            if (message.totalReward != null && message.hasOwnProperty("totalReward"))
                if (!$util.isString(message.totalReward))
                    return "totalReward: string expected";
            return null;
        };

        /**
         * Creates a WitnessInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessInfo} WitnessInfo
         */
        WitnessInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessInfo)
                return object;
            let message = new $root.pb.WitnessInfo();
            if (object.address != null)
                message.address = String(object.address);
            if (object.stakeAmount != null)
                message.stakeAmount = String(object.stakeAmount);
            if (object.pendingReward != null)
                message.pendingReward = String(object.pendingReward);
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "WITNESS_CANDIDATE":
            case 0:
                message.status = 0;
                break;
            case "WITNESS_ACTIVE":
            case 1:
                message.status = 1;
                break;
            case "WITNESS_UNSTAKING":
            case 2:
                message.status = 2;
                break;
            case "WITNESS_EXITED":
            case 3:
                message.status = 3;
                break;
            case "WITNESS_SLASHED":
            case 4:
                message.status = 4;
                break;
            }
            if (object.unstakeHeight != null)
                if ($util.Long)
                    (message.unstakeHeight = $util.Long.fromValue(object.unstakeHeight)).unsigned = true;
                else if (typeof object.unstakeHeight === "string")
                    message.unstakeHeight = parseInt(object.unstakeHeight, 10);
                else if (typeof object.unstakeHeight === "number")
                    message.unstakeHeight = object.unstakeHeight;
                else if (typeof object.unstakeHeight === "object")
                    message.unstakeHeight = new $util.LongBits(object.unstakeHeight.low >>> 0, object.unstakeHeight.high >>> 0).toNumber(true);
            if (object.totalWitnessCount != null)
                if ($util.Long)
                    (message.totalWitnessCount = $util.Long.fromValue(object.totalWitnessCount)).unsigned = true;
                else if (typeof object.totalWitnessCount === "string")
                    message.totalWitnessCount = parseInt(object.totalWitnessCount, 10);
                else if (typeof object.totalWitnessCount === "number")
                    message.totalWitnessCount = object.totalWitnessCount;
                else if (typeof object.totalWitnessCount === "object")
                    message.totalWitnessCount = new $util.LongBits(object.totalWitnessCount.low >>> 0, object.totalWitnessCount.high >>> 0).toNumber(true);
            if (object.passCount != null)
                if ($util.Long)
                    (message.passCount = $util.Long.fromValue(object.passCount)).unsigned = true;
                else if (typeof object.passCount === "string")
                    message.passCount = parseInt(object.passCount, 10);
                else if (typeof object.passCount === "number")
                    message.passCount = object.passCount;
                else if (typeof object.passCount === "object")
                    message.passCount = new $util.LongBits(object.passCount.low >>> 0, object.passCount.high >>> 0).toNumber(true);
            if (object.failCount != null)
                if ($util.Long)
                    (message.failCount = $util.Long.fromValue(object.failCount)).unsigned = true;
                else if (typeof object.failCount === "string")
                    message.failCount = parseInt(object.failCount, 10);
                else if (typeof object.failCount === "number")
                    message.failCount = object.failCount;
                else if (typeof object.failCount === "object")
                    message.failCount = new $util.LongBits(object.failCount.low >>> 0, object.failCount.high >>> 0).toNumber(true);
            if (object.abstainCount != null)
                if ($util.Long)
                    (message.abstainCount = $util.Long.fromValue(object.abstainCount)).unsigned = true;
                else if (typeof object.abstainCount === "string")
                    message.abstainCount = parseInt(object.abstainCount, 10);
                else if (typeof object.abstainCount === "number")
                    message.abstainCount = object.abstainCount;
                else if (typeof object.abstainCount === "object")
                    message.abstainCount = new $util.LongBits(object.abstainCount.low >>> 0, object.abstainCount.high >>> 0).toNumber(true);
            if (object.slashedCount != null)
                if ($util.Long)
                    (message.slashedCount = $util.Long.fromValue(object.slashedCount)).unsigned = true;
                else if (typeof object.slashedCount === "string")
                    message.slashedCount = parseInt(object.slashedCount, 10);
                else if (typeof object.slashedCount === "number")
                    message.slashedCount = object.slashedCount;
                else if (typeof object.slashedCount === "object")
                    message.slashedCount = new $util.LongBits(object.slashedCount.low >>> 0, object.slashedCount.high >>> 0).toNumber(true);
            if (object.pendingTasks) {
                if (!Array.isArray(object.pendingTasks))
                    throw TypeError(".pb.WitnessInfo.pendingTasks: array expected");
                message.pendingTasks = [];
                for (let i = 0; i < object.pendingTasks.length; ++i)
                    message.pendingTasks[i] = String(object.pendingTasks[i]);
            }
            if (object.shelvedArbitrations) {
                if (!Array.isArray(object.shelvedArbitrations))
                    throw TypeError(".pb.WitnessInfo.shelvedArbitrations: array expected");
                message.shelvedArbitrations = [];
                for (let i = 0; i < object.shelvedArbitrations.length; ++i)
                    message.shelvedArbitrations[i] = String(object.shelvedArbitrations[i]);
            }
            if (object.totalReward != null)
                message.totalReward = String(object.totalReward);
            return message;
        };

        /**
         * Creates a plain object from a WitnessInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessInfo
         * @static
         * @param {pb.WitnessInfo} message WitnessInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.pendingTasks = [];
                object.shelvedArbitrations = [];
            }
            if (options.defaults) {
                object.address = "";
                object.stakeAmount = "";
                object.pendingReward = "";
                object.status = options.enums === String ? "WITNESS_CANDIDATE" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.unstakeHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.unstakeHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalWitnessCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.totalWitnessCount = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.passCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.passCount = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.failCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.failCount = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.abstainCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.abstainCount = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.slashedCount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.slashedCount = options.longs === String ? "0" : 0;
                object.totalReward = "";
            }
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.stakeAmount != null && message.hasOwnProperty("stakeAmount"))
                object.stakeAmount = message.stakeAmount;
            if (message.pendingReward != null && message.hasOwnProperty("pendingReward"))
                object.pendingReward = message.pendingReward;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.pb.WitnessStatus[message.status] === undefined ? message.status : $root.pb.WitnessStatus[message.status] : message.status;
            if (message.unstakeHeight != null && message.hasOwnProperty("unstakeHeight"))
                if (typeof message.unstakeHeight === "number")
                    object.unstakeHeight = options.longs === String ? String(message.unstakeHeight) : message.unstakeHeight;
                else
                    object.unstakeHeight = options.longs === String ? $util.Long.prototype.toString.call(message.unstakeHeight) : options.longs === Number ? new $util.LongBits(message.unstakeHeight.low >>> 0, message.unstakeHeight.high >>> 0).toNumber(true) : message.unstakeHeight;
            if (message.totalWitnessCount != null && message.hasOwnProperty("totalWitnessCount"))
                if (typeof message.totalWitnessCount === "number")
                    object.totalWitnessCount = options.longs === String ? String(message.totalWitnessCount) : message.totalWitnessCount;
                else
                    object.totalWitnessCount = options.longs === String ? $util.Long.prototype.toString.call(message.totalWitnessCount) : options.longs === Number ? new $util.LongBits(message.totalWitnessCount.low >>> 0, message.totalWitnessCount.high >>> 0).toNumber(true) : message.totalWitnessCount;
            if (message.passCount != null && message.hasOwnProperty("passCount"))
                if (typeof message.passCount === "number")
                    object.passCount = options.longs === String ? String(message.passCount) : message.passCount;
                else
                    object.passCount = options.longs === String ? $util.Long.prototype.toString.call(message.passCount) : options.longs === Number ? new $util.LongBits(message.passCount.low >>> 0, message.passCount.high >>> 0).toNumber(true) : message.passCount;
            if (message.failCount != null && message.hasOwnProperty("failCount"))
                if (typeof message.failCount === "number")
                    object.failCount = options.longs === String ? String(message.failCount) : message.failCount;
                else
                    object.failCount = options.longs === String ? $util.Long.prototype.toString.call(message.failCount) : options.longs === Number ? new $util.LongBits(message.failCount.low >>> 0, message.failCount.high >>> 0).toNumber(true) : message.failCount;
            if (message.abstainCount != null && message.hasOwnProperty("abstainCount"))
                if (typeof message.abstainCount === "number")
                    object.abstainCount = options.longs === String ? String(message.abstainCount) : message.abstainCount;
                else
                    object.abstainCount = options.longs === String ? $util.Long.prototype.toString.call(message.abstainCount) : options.longs === Number ? new $util.LongBits(message.abstainCount.low >>> 0, message.abstainCount.high >>> 0).toNumber(true) : message.abstainCount;
            if (message.slashedCount != null && message.hasOwnProperty("slashedCount"))
                if (typeof message.slashedCount === "number")
                    object.slashedCount = options.longs === String ? String(message.slashedCount) : message.slashedCount;
                else
                    object.slashedCount = options.longs === String ? $util.Long.prototype.toString.call(message.slashedCount) : options.longs === Number ? new $util.LongBits(message.slashedCount.low >>> 0, message.slashedCount.high >>> 0).toNumber(true) : message.slashedCount;
            if (message.pendingTasks && message.pendingTasks.length) {
                object.pendingTasks = [];
                for (let j = 0; j < message.pendingTasks.length; ++j)
                    object.pendingTasks[j] = message.pendingTasks[j];
            }
            if (message.shelvedArbitrations && message.shelvedArbitrations.length) {
                object.shelvedArbitrations = [];
                for (let j = 0; j < message.shelvedArbitrations.length; ++j)
                    object.shelvedArbitrations[j] = message.shelvedArbitrations[j];
            }
            if (message.totalReward != null && message.hasOwnProperty("totalReward"))
                object.totalReward = message.totalReward;
            return object;
        };

        /**
         * Converts this WitnessInfo to JSON.
         * @function toJSON
         * @memberof pb.WitnessInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessInfo
         * @function getTypeUrl
         * @memberof pb.WitnessInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessInfo";
        };

        return WitnessInfo;
    })();

    pb.WitnessStakeTx = (function() {

        /**
         * Properties of a WitnessStakeTx.
         * @memberof pb
         * @interface IWitnessStakeTx
         * @property {pb.IBaseMessage|null} [base] WitnessStakeTx base
         * @property {pb.OrderOp|null} [op] WitnessStakeTx op
         * @property {string|null} [amount] WitnessStakeTx amount
         */

        /**
         * Constructs a new WitnessStakeTx.
         * @memberof pb
         * @classdesc Represents a WitnessStakeTx.
         * @implements IWitnessStakeTx
         * @constructor
         * @param {pb.IWitnessStakeTx=} [properties] Properties to set
         */
        function WitnessStakeTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessStakeTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.WitnessStakeTx
         * @instance
         */
        WitnessStakeTx.prototype.base = null;

        /**
         * WitnessStakeTx op.
         * @member {pb.OrderOp} op
         * @memberof pb.WitnessStakeTx
         * @instance
         */
        WitnessStakeTx.prototype.op = 0;

        /**
         * WitnessStakeTx amount.
         * @member {string} amount
         * @memberof pb.WitnessStakeTx
         * @instance
         */
        WitnessStakeTx.prototype.amount = "";

        /**
         * Creates a new WitnessStakeTx instance using the specified properties.
         * @function create
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {pb.IWitnessStakeTx=} [properties] Properties to set
         * @returns {pb.WitnessStakeTx} WitnessStakeTx instance
         */
        WitnessStakeTx.create = function create(properties) {
            return new WitnessStakeTx(properties);
        };

        /**
         * Encodes the specified WitnessStakeTx message. Does not implicitly {@link pb.WitnessStakeTx.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {pb.IWitnessStakeTx} message WitnessStakeTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessStakeTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.op != null && Object.hasOwnProperty.call(message, "op"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.op);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.amount);
            return writer;
        };

        /**
         * Encodes the specified WitnessStakeTx message, length delimited. Does not implicitly {@link pb.WitnessStakeTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {pb.IWitnessStakeTx} message WitnessStakeTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessStakeTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessStakeTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessStakeTx} WitnessStakeTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessStakeTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessStakeTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.op = reader.int32();
                        break;
                    }
                case 3: {
                        message.amount = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessStakeTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessStakeTx} WitnessStakeTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessStakeTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessStakeTx message.
         * @function verify
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessStakeTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.op != null && message.hasOwnProperty("op"))
                switch (message.op) {
                default:
                    return "op: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            return null;
        };

        /**
         * Creates a WitnessStakeTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessStakeTx} WitnessStakeTx
         */
        WitnessStakeTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessStakeTx)
                return object;
            let message = new $root.pb.WitnessStakeTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.WitnessStakeTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            switch (object.op) {
            default:
                if (typeof object.op === "number") {
                    message.op = object.op;
                    break;
                }
                break;
            case "ADD":
            case 0:
                message.op = 0;
                break;
            case "REMOVE":
            case 1:
                message.op = 1;
                break;
            }
            if (object.amount != null)
                message.amount = String(object.amount);
            return message;
        };

        /**
         * Creates a plain object from a WitnessStakeTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {pb.WitnessStakeTx} message WitnessStakeTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessStakeTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.op = options.enums === String ? "ADD" : 0;
                object.amount = "";
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.op != null && message.hasOwnProperty("op"))
                object.op = options.enums === String ? $root.pb.OrderOp[message.op] === undefined ? message.op : $root.pb.OrderOp[message.op] : message.op;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            return object;
        };

        /**
         * Converts this WitnessStakeTx to JSON.
         * @function toJSON
         * @memberof pb.WitnessStakeTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessStakeTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessStakeTx
         * @function getTypeUrl
         * @memberof pb.WitnessStakeTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessStakeTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessStakeTx";
        };

        return WitnessStakeTx;
    })();

    pb.WitnessRequestTx = (function() {

        /**
         * Properties of a WitnessRequestTx.
         * @memberof pb
         * @interface IWitnessRequestTx
         * @property {pb.IBaseMessage|null} [base] WitnessRequestTx base
         * @property {string|null} [nativeChain] WitnessRequestTx nativeChain
         * @property {string|null} [nativeTxHash] WitnessRequestTx nativeTxHash
         * @property {string|null} [tokenAddress] WitnessRequestTx tokenAddress
         * @property {string|null} [amount] WitnessRequestTx amount
         * @property {string|null} [receiverAddress] WitnessRequestTx receiverAddress
         * @property {string|null} [memo] WitnessRequestTx memo
         * @property {string|null} [rechargeFee] WitnessRequestTx rechargeFee
         * @property {number|null} [nativeVout] WitnessRequestTx nativeVout
         * @property {Uint8Array|null} [nativeScript] WitnessRequestTx nativeScript
         */

        /**
         * Constructs a new WitnessRequestTx.
         * @memberof pb
         * @classdesc Represents a WitnessRequestTx.
         * @implements IWitnessRequestTx
         * @constructor
         * @param {pb.IWitnessRequestTx=} [properties] Properties to set
         */
        function WitnessRequestTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessRequestTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.base = null;

        /**
         * WitnessRequestTx nativeChain.
         * @member {string} nativeChain
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.nativeChain = "";

        /**
         * WitnessRequestTx nativeTxHash.
         * @member {string} nativeTxHash
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.nativeTxHash = "";

        /**
         * WitnessRequestTx tokenAddress.
         * @member {string} tokenAddress
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.tokenAddress = "";

        /**
         * WitnessRequestTx amount.
         * @member {string} amount
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.amount = "";

        /**
         * WitnessRequestTx receiverAddress.
         * @member {string} receiverAddress
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.receiverAddress = "";

        /**
         * WitnessRequestTx memo.
         * @member {string} memo
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.memo = "";

        /**
         * WitnessRequestTx rechargeFee.
         * @member {string} rechargeFee
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.rechargeFee = "";

        /**
         * WitnessRequestTx nativeVout.
         * @member {number} nativeVout
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.nativeVout = 0;

        /**
         * WitnessRequestTx nativeScript.
         * @member {Uint8Array} nativeScript
         * @memberof pb.WitnessRequestTx
         * @instance
         */
        WitnessRequestTx.prototype.nativeScript = $util.newBuffer([]);

        /**
         * Creates a new WitnessRequestTx instance using the specified properties.
         * @function create
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {pb.IWitnessRequestTx=} [properties] Properties to set
         * @returns {pb.WitnessRequestTx} WitnessRequestTx instance
         */
        WitnessRequestTx.create = function create(properties) {
            return new WitnessRequestTx(properties);
        };

        /**
         * Encodes the specified WitnessRequestTx message. Does not implicitly {@link pb.WitnessRequestTx.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {pb.IWitnessRequestTx} message WitnessRequestTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessRequestTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.nativeChain != null && Object.hasOwnProperty.call(message, "nativeChain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.nativeChain);
            if (message.nativeTxHash != null && Object.hasOwnProperty.call(message, "nativeTxHash"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.nativeTxHash);
            if (message.tokenAddress != null && Object.hasOwnProperty.call(message, "tokenAddress"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.tokenAddress);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.amount);
            if (message.receiverAddress != null && Object.hasOwnProperty.call(message, "receiverAddress"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.receiverAddress);
            if (message.memo != null && Object.hasOwnProperty.call(message, "memo"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.memo);
            if (message.rechargeFee != null && Object.hasOwnProperty.call(message, "rechargeFee"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.rechargeFee);
            if (message.nativeVout != null && Object.hasOwnProperty.call(message, "nativeVout"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.nativeVout);
            if (message.nativeScript != null && Object.hasOwnProperty.call(message, "nativeScript"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.nativeScript);
            return writer;
        };

        /**
         * Encodes the specified WitnessRequestTx message, length delimited. Does not implicitly {@link pb.WitnessRequestTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {pb.IWitnessRequestTx} message WitnessRequestTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessRequestTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessRequestTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessRequestTx} WitnessRequestTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessRequestTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessRequestTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.nativeChain = reader.string();
                        break;
                    }
                case 3: {
                        message.nativeTxHash = reader.string();
                        break;
                    }
                case 4: {
                        message.tokenAddress = reader.string();
                        break;
                    }
                case 5: {
                        message.amount = reader.string();
                        break;
                    }
                case 6: {
                        message.receiverAddress = reader.string();
                        break;
                    }
                case 7: {
                        message.memo = reader.string();
                        break;
                    }
                case 8: {
                        message.rechargeFee = reader.string();
                        break;
                    }
                case 9: {
                        message.nativeVout = reader.uint32();
                        break;
                    }
                case 10: {
                        message.nativeScript = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessRequestTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessRequestTx} WitnessRequestTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessRequestTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessRequestTx message.
         * @function verify
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessRequestTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.nativeChain != null && message.hasOwnProperty("nativeChain"))
                if (!$util.isString(message.nativeChain))
                    return "nativeChain: string expected";
            if (message.nativeTxHash != null && message.hasOwnProperty("nativeTxHash"))
                if (!$util.isString(message.nativeTxHash))
                    return "nativeTxHash: string expected";
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                if (!$util.isString(message.tokenAddress))
                    return "tokenAddress: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.receiverAddress != null && message.hasOwnProperty("receiverAddress"))
                if (!$util.isString(message.receiverAddress))
                    return "receiverAddress: string expected";
            if (message.memo != null && message.hasOwnProperty("memo"))
                if (!$util.isString(message.memo))
                    return "memo: string expected";
            if (message.rechargeFee != null && message.hasOwnProperty("rechargeFee"))
                if (!$util.isString(message.rechargeFee))
                    return "rechargeFee: string expected";
            if (message.nativeVout != null && message.hasOwnProperty("nativeVout"))
                if (!$util.isInteger(message.nativeVout))
                    return "nativeVout: integer expected";
            if (message.nativeScript != null && message.hasOwnProperty("nativeScript"))
                if (!(message.nativeScript && typeof message.nativeScript.length === "number" || $util.isString(message.nativeScript)))
                    return "nativeScript: buffer expected";
            return null;
        };

        /**
         * Creates a WitnessRequestTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessRequestTx} WitnessRequestTx
         */
        WitnessRequestTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessRequestTx)
                return object;
            let message = new $root.pb.WitnessRequestTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.WitnessRequestTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.nativeChain != null)
                message.nativeChain = String(object.nativeChain);
            if (object.nativeTxHash != null)
                message.nativeTxHash = String(object.nativeTxHash);
            if (object.tokenAddress != null)
                message.tokenAddress = String(object.tokenAddress);
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.receiverAddress != null)
                message.receiverAddress = String(object.receiverAddress);
            if (object.memo != null)
                message.memo = String(object.memo);
            if (object.rechargeFee != null)
                message.rechargeFee = String(object.rechargeFee);
            if (object.nativeVout != null)
                message.nativeVout = object.nativeVout >>> 0;
            if (object.nativeScript != null)
                if (typeof object.nativeScript === "string")
                    $util.base64.decode(object.nativeScript, message.nativeScript = $util.newBuffer($util.base64.length(object.nativeScript)), 0);
                else if (object.nativeScript.length >= 0)
                    message.nativeScript = object.nativeScript;
            return message;
        };

        /**
         * Creates a plain object from a WitnessRequestTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {pb.WitnessRequestTx} message WitnessRequestTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessRequestTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.nativeChain = "";
                object.nativeTxHash = "";
                object.tokenAddress = "";
                object.amount = "";
                object.receiverAddress = "";
                object.memo = "";
                object.rechargeFee = "";
                object.nativeVout = 0;
                if (options.bytes === String)
                    object.nativeScript = "";
                else {
                    object.nativeScript = [];
                    if (options.bytes !== Array)
                        object.nativeScript = $util.newBuffer(object.nativeScript);
                }
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.nativeChain != null && message.hasOwnProperty("nativeChain"))
                object.nativeChain = message.nativeChain;
            if (message.nativeTxHash != null && message.hasOwnProperty("nativeTxHash"))
                object.nativeTxHash = message.nativeTxHash;
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                object.tokenAddress = message.tokenAddress;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.receiverAddress != null && message.hasOwnProperty("receiverAddress"))
                object.receiverAddress = message.receiverAddress;
            if (message.memo != null && message.hasOwnProperty("memo"))
                object.memo = message.memo;
            if (message.rechargeFee != null && message.hasOwnProperty("rechargeFee"))
                object.rechargeFee = message.rechargeFee;
            if (message.nativeVout != null && message.hasOwnProperty("nativeVout"))
                object.nativeVout = message.nativeVout;
            if (message.nativeScript != null && message.hasOwnProperty("nativeScript"))
                object.nativeScript = options.bytes === String ? $util.base64.encode(message.nativeScript, 0, message.nativeScript.length) : options.bytes === Array ? Array.prototype.slice.call(message.nativeScript) : message.nativeScript;
            return object;
        };

        /**
         * Converts this WitnessRequestTx to JSON.
         * @function toJSON
         * @memberof pb.WitnessRequestTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessRequestTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessRequestTx
         * @function getTypeUrl
         * @memberof pb.WitnessRequestTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessRequestTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessRequestTx";
        };

        return WitnessRequestTx;
    })();

    pb.WitnessVote = (function() {

        /**
         * Properties of a WitnessVote.
         * @memberof pb
         * @interface IWitnessVote
         * @property {string|null} [requestId] WitnessVote requestId
         * @property {string|null} [witnessAddress] WitnessVote witnessAddress
         * @property {pb.WitnessVoteType|null} [voteType] WitnessVote voteType
         * @property {string|null} [reason] WitnessVote reason
         * @property {number|Long|null} [timestamp] WitnessVote timestamp
         * @property {string|null} [txId] WitnessVote txId
         */

        /**
         * Constructs a new WitnessVote.
         * @memberof pb
         * @classdesc Represents a WitnessVote.
         * @implements IWitnessVote
         * @constructor
         * @param {pb.IWitnessVote=} [properties] Properties to set
         */
        function WitnessVote(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessVote requestId.
         * @member {string} requestId
         * @memberof pb.WitnessVote
         * @instance
         */
        WitnessVote.prototype.requestId = "";

        /**
         * WitnessVote witnessAddress.
         * @member {string} witnessAddress
         * @memberof pb.WitnessVote
         * @instance
         */
        WitnessVote.prototype.witnessAddress = "";

        /**
         * WitnessVote voteType.
         * @member {pb.WitnessVoteType} voteType
         * @memberof pb.WitnessVote
         * @instance
         */
        WitnessVote.prototype.voteType = 0;

        /**
         * WitnessVote reason.
         * @member {string} reason
         * @memberof pb.WitnessVote
         * @instance
         */
        WitnessVote.prototype.reason = "";

        /**
         * WitnessVote timestamp.
         * @member {number|Long} timestamp
         * @memberof pb.WitnessVote
         * @instance
         */
        WitnessVote.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessVote txId.
         * @member {string} txId
         * @memberof pb.WitnessVote
         * @instance
         */
        WitnessVote.prototype.txId = "";

        /**
         * Creates a new WitnessVote instance using the specified properties.
         * @function create
         * @memberof pb.WitnessVote
         * @static
         * @param {pb.IWitnessVote=} [properties] Properties to set
         * @returns {pb.WitnessVote} WitnessVote instance
         */
        WitnessVote.create = function create(properties) {
            return new WitnessVote(properties);
        };

        /**
         * Encodes the specified WitnessVote message. Does not implicitly {@link pb.WitnessVote.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessVote
         * @static
         * @param {pb.IWitnessVote} message WitnessVote message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessVote.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.requestId);
            if (message.witnessAddress != null && Object.hasOwnProperty.call(message, "witnessAddress"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.witnessAddress);
            if (message.voteType != null && Object.hasOwnProperty.call(message, "voteType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.voteType);
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.reason);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.timestamp);
            if (message.txId != null && Object.hasOwnProperty.call(message, "txId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.txId);
            return writer;
        };

        /**
         * Encodes the specified WitnessVote message, length delimited. Does not implicitly {@link pb.WitnessVote.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessVote
         * @static
         * @param {pb.IWitnessVote} message WitnessVote message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessVote.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessVote message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessVote
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessVote} WitnessVote
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessVote.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessVote();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.requestId = reader.string();
                        break;
                    }
                case 2: {
                        message.witnessAddress = reader.string();
                        break;
                    }
                case 3: {
                        message.voteType = reader.int32();
                        break;
                    }
                case 4: {
                        message.reason = reader.string();
                        break;
                    }
                case 5: {
                        message.timestamp = reader.uint64();
                        break;
                    }
                case 6: {
                        message.txId = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessVote message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessVote
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessVote} WitnessVote
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessVote.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessVote message.
         * @function verify
         * @memberof pb.WitnessVote
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessVote.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isString(message.requestId))
                    return "requestId: string expected";
            if (message.witnessAddress != null && message.hasOwnProperty("witnessAddress"))
                if (!$util.isString(message.witnessAddress))
                    return "witnessAddress: string expected";
            if (message.voteType != null && message.hasOwnProperty("voteType"))
                switch (message.voteType) {
                default:
                    return "voteType: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.reason != null && message.hasOwnProperty("reason"))
                if (!$util.isString(message.reason))
                    return "reason: string expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.txId != null && message.hasOwnProperty("txId"))
                if (!$util.isString(message.txId))
                    return "txId: string expected";
            return null;
        };

        /**
         * Creates a WitnessVote message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessVote
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessVote} WitnessVote
         */
        WitnessVote.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessVote)
                return object;
            let message = new $root.pb.WitnessVote();
            if (object.requestId != null)
                message.requestId = String(object.requestId);
            if (object.witnessAddress != null)
                message.witnessAddress = String(object.witnessAddress);
            switch (object.voteType) {
            default:
                if (typeof object.voteType === "number") {
                    message.voteType = object.voteType;
                    break;
                }
                break;
            case "VOTE_PASS":
            case 0:
                message.voteType = 0;
                break;
            case "VOTE_FAIL":
            case 1:
                message.voteType = 1;
                break;
            case "VOTE_ABSTAIN":
            case 2:
                message.voteType = 2;
                break;
            }
            if (object.reason != null)
                message.reason = String(object.reason);
            if (object.timestamp != null)
                if ($util.Long)
                    (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = true;
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            if (object.txId != null)
                message.txId = String(object.txId);
            return message;
        };

        /**
         * Creates a plain object from a WitnessVote message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessVote
         * @static
         * @param {pb.WitnessVote} message WitnessVote
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessVote.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.requestId = "";
                object.witnessAddress = "";
                object.voteType = options.enums === String ? "VOTE_PASS" : 0;
                object.reason = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : 0;
                object.txId = "";
            }
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.witnessAddress != null && message.hasOwnProperty("witnessAddress"))
                object.witnessAddress = message.witnessAddress;
            if (message.voteType != null && message.hasOwnProperty("voteType"))
                object.voteType = options.enums === String ? $root.pb.WitnessVoteType[message.voteType] === undefined ? message.voteType : $root.pb.WitnessVoteType[message.voteType] : message.voteType;
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = message.reason;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            if (message.txId != null && message.hasOwnProperty("txId"))
                object.txId = message.txId;
            return object;
        };

        /**
         * Converts this WitnessVote to JSON.
         * @function toJSON
         * @memberof pb.WitnessVote
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessVote.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessVote
         * @function getTypeUrl
         * @memberof pb.WitnessVote
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessVote.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessVote";
        };

        return WitnessVote;
    })();

    pb.WitnessVoteTx = (function() {

        /**
         * Properties of a WitnessVoteTx.
         * @memberof pb
         * @interface IWitnessVoteTx
         * @property {pb.IBaseMessage|null} [base] WitnessVoteTx base
         * @property {pb.IWitnessVote|null} [vote] WitnessVoteTx vote
         */

        /**
         * Constructs a new WitnessVoteTx.
         * @memberof pb
         * @classdesc Represents a WitnessVoteTx.
         * @implements IWitnessVoteTx
         * @constructor
         * @param {pb.IWitnessVoteTx=} [properties] Properties to set
         */
        function WitnessVoteTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessVoteTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.WitnessVoteTx
         * @instance
         */
        WitnessVoteTx.prototype.base = null;

        /**
         * WitnessVoteTx vote.
         * @member {pb.IWitnessVote|null|undefined} vote
         * @memberof pb.WitnessVoteTx
         * @instance
         */
        WitnessVoteTx.prototype.vote = null;

        /**
         * Creates a new WitnessVoteTx instance using the specified properties.
         * @function create
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {pb.IWitnessVoteTx=} [properties] Properties to set
         * @returns {pb.WitnessVoteTx} WitnessVoteTx instance
         */
        WitnessVoteTx.create = function create(properties) {
            return new WitnessVoteTx(properties);
        };

        /**
         * Encodes the specified WitnessVoteTx message. Does not implicitly {@link pb.WitnessVoteTx.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {pb.IWitnessVoteTx} message WitnessVoteTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessVoteTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                $root.pb.WitnessVote.encode(message.vote, writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified WitnessVoteTx message, length delimited. Does not implicitly {@link pb.WitnessVoteTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {pb.IWitnessVoteTx} message WitnessVoteTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessVoteTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessVoteTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessVoteTx} WitnessVoteTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessVoteTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessVoteTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.vote = $root.pb.WitnessVote.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessVoteTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessVoteTx} WitnessVoteTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessVoteTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessVoteTx message.
         * @function verify
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessVoteTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.vote != null && message.hasOwnProperty("vote")) {
                let error = $root.pb.WitnessVote.verify(message.vote);
                if (error)
                    return "vote." + error;
            }
            return null;
        };

        /**
         * Creates a WitnessVoteTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessVoteTx} WitnessVoteTx
         */
        WitnessVoteTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessVoteTx)
                return object;
            let message = new $root.pb.WitnessVoteTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.WitnessVoteTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.vote != null) {
                if (typeof object.vote !== "object")
                    throw TypeError(".pb.WitnessVoteTx.vote: object expected");
                message.vote = $root.pb.WitnessVote.fromObject(object.vote);
            }
            return message;
        };

        /**
         * Creates a plain object from a WitnessVoteTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {pb.WitnessVoteTx} message WitnessVoteTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessVoteTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.vote = null;
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.vote != null && message.hasOwnProperty("vote"))
                object.vote = $root.pb.WitnessVote.toObject(message.vote, options);
            return object;
        };

        /**
         * Converts this WitnessVoteTx to JSON.
         * @function toJSON
         * @memberof pb.WitnessVoteTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessVoteTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessVoteTx
         * @function getTypeUrl
         * @memberof pb.WitnessVoteTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessVoteTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessVoteTx";
        };

        return WitnessVoteTx;
    })();

    pb.RechargeRequest = (function() {

        /**
         * Properties of a RechargeRequest.
         * @memberof pb
         * @interface IRechargeRequest
         * @property {string|null} [requestId] RechargeRequest requestId
         * @property {string|null} [nativeChain] RechargeRequest nativeChain
         * @property {string|null} [nativeTxHash] RechargeRequest nativeTxHash
         * @property {string|null} [tokenAddress] RechargeRequest tokenAddress
         * @property {string|null} [amount] RechargeRequest amount
         * @property {string|null} [receiverAddress] RechargeRequest receiverAddress
         * @property {string|null} [requesterAddress] RechargeRequest requesterAddress
         * @property {pb.RechargeRequestStatus|null} [status] RechargeRequest status
         * @property {number|Long|null} [createHeight] RechargeRequest createHeight
         * @property {number|Long|null} [deadlineHeight] RechargeRequest deadlineHeight
         * @property {number|Long|null} [finalizeHeight] RechargeRequest finalizeHeight
         * @property {number|null} [round] RechargeRequest round
         * @property {Array.<string>|null} [selectedWitnesses] RechargeRequest selectedWitnesses
         * @property {Array.<pb.IWitnessVote>|null} [votes] RechargeRequest votes
         * @property {number|null} [passCount] RechargeRequest passCount
         * @property {number|null} [failCount] RechargeRequest failCount
         * @property {number|null} [abstainCount] RechargeRequest abstainCount
         * @property {string|null} [challengeId] RechargeRequest challengeId
         * @property {string|null} [rechargeFee] RechargeRequest rechargeFee
         * @property {number|null} [vaultId] RechargeRequest vaultId
         * @property {number|null} [nativeVout] RechargeRequest nativeVout
         * @property {Uint8Array|null} [nativeScript] RechargeRequest nativeScript
         */

        /**
         * Constructs a new RechargeRequest.
         * @memberof pb
         * @classdesc Represents a RechargeRequest.
         * @implements IRechargeRequest
         * @constructor
         * @param {pb.IRechargeRequest=} [properties] Properties to set
         */
        function RechargeRequest(properties) {
            this.selectedWitnesses = [];
            this.votes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RechargeRequest requestId.
         * @member {string} requestId
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.requestId = "";

        /**
         * RechargeRequest nativeChain.
         * @member {string} nativeChain
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.nativeChain = "";

        /**
         * RechargeRequest nativeTxHash.
         * @member {string} nativeTxHash
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.nativeTxHash = "";

        /**
         * RechargeRequest tokenAddress.
         * @member {string} tokenAddress
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.tokenAddress = "";

        /**
         * RechargeRequest amount.
         * @member {string} amount
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.amount = "";

        /**
         * RechargeRequest receiverAddress.
         * @member {string} receiverAddress
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.receiverAddress = "";

        /**
         * RechargeRequest requesterAddress.
         * @member {string} requesterAddress
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.requesterAddress = "";

        /**
         * RechargeRequest status.
         * @member {pb.RechargeRequestStatus} status
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.status = 0;

        /**
         * RechargeRequest createHeight.
         * @member {number|Long} createHeight
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.createHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RechargeRequest deadlineHeight.
         * @member {number|Long} deadlineHeight
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.deadlineHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RechargeRequest finalizeHeight.
         * @member {number|Long} finalizeHeight
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.finalizeHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * RechargeRequest round.
         * @member {number} round
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.round = 0;

        /**
         * RechargeRequest selectedWitnesses.
         * @member {Array.<string>} selectedWitnesses
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.selectedWitnesses = $util.emptyArray;

        /**
         * RechargeRequest votes.
         * @member {Array.<pb.IWitnessVote>} votes
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.votes = $util.emptyArray;

        /**
         * RechargeRequest passCount.
         * @member {number} passCount
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.passCount = 0;

        /**
         * RechargeRequest failCount.
         * @member {number} failCount
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.failCount = 0;

        /**
         * RechargeRequest abstainCount.
         * @member {number} abstainCount
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.abstainCount = 0;

        /**
         * RechargeRequest challengeId.
         * @member {string} challengeId
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.challengeId = "";

        /**
         * RechargeRequest rechargeFee.
         * @member {string} rechargeFee
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.rechargeFee = "";

        /**
         * RechargeRequest vaultId.
         * @member {number} vaultId
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.vaultId = 0;

        /**
         * RechargeRequest nativeVout.
         * @member {number} nativeVout
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.nativeVout = 0;

        /**
         * RechargeRequest nativeScript.
         * @member {Uint8Array} nativeScript
         * @memberof pb.RechargeRequest
         * @instance
         */
        RechargeRequest.prototype.nativeScript = $util.newBuffer([]);

        /**
         * Creates a new RechargeRequest instance using the specified properties.
         * @function create
         * @memberof pb.RechargeRequest
         * @static
         * @param {pb.IRechargeRequest=} [properties] Properties to set
         * @returns {pb.RechargeRequest} RechargeRequest instance
         */
        RechargeRequest.create = function create(properties) {
            return new RechargeRequest(properties);
        };

        /**
         * Encodes the specified RechargeRequest message. Does not implicitly {@link pb.RechargeRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.RechargeRequest
         * @static
         * @param {pb.IRechargeRequest} message RechargeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RechargeRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.requestId);
            if (message.nativeChain != null && Object.hasOwnProperty.call(message, "nativeChain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.nativeChain);
            if (message.nativeTxHash != null && Object.hasOwnProperty.call(message, "nativeTxHash"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.nativeTxHash);
            if (message.tokenAddress != null && Object.hasOwnProperty.call(message, "tokenAddress"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.tokenAddress);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.amount);
            if (message.receiverAddress != null && Object.hasOwnProperty.call(message, "receiverAddress"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.receiverAddress);
            if (message.requesterAddress != null && Object.hasOwnProperty.call(message, "requesterAddress"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.requesterAddress);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.status);
            if (message.createHeight != null && Object.hasOwnProperty.call(message, "createHeight"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.createHeight);
            if (message.deadlineHeight != null && Object.hasOwnProperty.call(message, "deadlineHeight"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.deadlineHeight);
            if (message.finalizeHeight != null && Object.hasOwnProperty.call(message, "finalizeHeight"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint64(message.finalizeHeight);
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.round);
            if (message.selectedWitnesses != null && message.selectedWitnesses.length)
                for (let i = 0; i < message.selectedWitnesses.length; ++i)
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.selectedWitnesses[i]);
            if (message.votes != null && message.votes.length)
                for (let i = 0; i < message.votes.length; ++i)
                    $root.pb.WitnessVote.encode(message.votes[i], writer.uint32(/* id 14, wireType 2 =*/114).fork()).ldelim();
            if (message.passCount != null && Object.hasOwnProperty.call(message, "passCount"))
                writer.uint32(/* id 15, wireType 0 =*/120).uint32(message.passCount);
            if (message.failCount != null && Object.hasOwnProperty.call(message, "failCount"))
                writer.uint32(/* id 16, wireType 0 =*/128).uint32(message.failCount);
            if (message.abstainCount != null && Object.hasOwnProperty.call(message, "abstainCount"))
                writer.uint32(/* id 17, wireType 0 =*/136).uint32(message.abstainCount);
            if (message.challengeId != null && Object.hasOwnProperty.call(message, "challengeId"))
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.challengeId);
            if (message.rechargeFee != null && Object.hasOwnProperty.call(message, "rechargeFee"))
                writer.uint32(/* id 19, wireType 2 =*/154).string(message.rechargeFee);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.vaultId);
            if (message.nativeVout != null && Object.hasOwnProperty.call(message, "nativeVout"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint32(message.nativeVout);
            if (message.nativeScript != null && Object.hasOwnProperty.call(message, "nativeScript"))
                writer.uint32(/* id 22, wireType 2 =*/178).bytes(message.nativeScript);
            return writer;
        };

        /**
         * Encodes the specified RechargeRequest message, length delimited. Does not implicitly {@link pb.RechargeRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.RechargeRequest
         * @static
         * @param {pb.IRechargeRequest} message RechargeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RechargeRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RechargeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.RechargeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.RechargeRequest} RechargeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RechargeRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.RechargeRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.requestId = reader.string();
                        break;
                    }
                case 2: {
                        message.nativeChain = reader.string();
                        break;
                    }
                case 3: {
                        message.nativeTxHash = reader.string();
                        break;
                    }
                case 4: {
                        message.tokenAddress = reader.string();
                        break;
                    }
                case 5: {
                        message.amount = reader.string();
                        break;
                    }
                case 6: {
                        message.receiverAddress = reader.string();
                        break;
                    }
                case 7: {
                        message.requesterAddress = reader.string();
                        break;
                    }
                case 8: {
                        message.status = reader.int32();
                        break;
                    }
                case 9: {
                        message.createHeight = reader.uint64();
                        break;
                    }
                case 10: {
                        message.deadlineHeight = reader.uint64();
                        break;
                    }
                case 11: {
                        message.finalizeHeight = reader.uint64();
                        break;
                    }
                case 12: {
                        message.round = reader.uint32();
                        break;
                    }
                case 13: {
                        if (!(message.selectedWitnesses && message.selectedWitnesses.length))
                            message.selectedWitnesses = [];
                        message.selectedWitnesses.push(reader.string());
                        break;
                    }
                case 14: {
                        if (!(message.votes && message.votes.length))
                            message.votes = [];
                        message.votes.push($root.pb.WitnessVote.decode(reader, reader.uint32()));
                        break;
                    }
                case 15: {
                        message.passCount = reader.uint32();
                        break;
                    }
                case 16: {
                        message.failCount = reader.uint32();
                        break;
                    }
                case 17: {
                        message.abstainCount = reader.uint32();
                        break;
                    }
                case 18: {
                        message.challengeId = reader.string();
                        break;
                    }
                case 19: {
                        message.rechargeFee = reader.string();
                        break;
                    }
                case 20: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 21: {
                        message.nativeVout = reader.uint32();
                        break;
                    }
                case 22: {
                        message.nativeScript = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RechargeRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.RechargeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.RechargeRequest} RechargeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RechargeRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RechargeRequest message.
         * @function verify
         * @memberof pb.RechargeRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RechargeRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isString(message.requestId))
                    return "requestId: string expected";
            if (message.nativeChain != null && message.hasOwnProperty("nativeChain"))
                if (!$util.isString(message.nativeChain))
                    return "nativeChain: string expected";
            if (message.nativeTxHash != null && message.hasOwnProperty("nativeTxHash"))
                if (!$util.isString(message.nativeTxHash))
                    return "nativeTxHash: string expected";
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                if (!$util.isString(message.tokenAddress))
                    return "tokenAddress: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.receiverAddress != null && message.hasOwnProperty("receiverAddress"))
                if (!$util.isString(message.receiverAddress))
                    return "receiverAddress: string expected";
            if (message.requesterAddress != null && message.hasOwnProperty("requesterAddress"))
                if (!$util.isString(message.requesterAddress))
                    return "requesterAddress: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                    break;
                }
            if (message.createHeight != null && message.hasOwnProperty("createHeight"))
                if (!$util.isInteger(message.createHeight) && !(message.createHeight && $util.isInteger(message.createHeight.low) && $util.isInteger(message.createHeight.high)))
                    return "createHeight: integer|Long expected";
            if (message.deadlineHeight != null && message.hasOwnProperty("deadlineHeight"))
                if (!$util.isInteger(message.deadlineHeight) && !(message.deadlineHeight && $util.isInteger(message.deadlineHeight.low) && $util.isInteger(message.deadlineHeight.high)))
                    return "deadlineHeight: integer|Long expected";
            if (message.finalizeHeight != null && message.hasOwnProperty("finalizeHeight"))
                if (!$util.isInteger(message.finalizeHeight) && !(message.finalizeHeight && $util.isInteger(message.finalizeHeight.low) && $util.isInteger(message.finalizeHeight.high)))
                    return "finalizeHeight: integer|Long expected";
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.selectedWitnesses != null && message.hasOwnProperty("selectedWitnesses")) {
                if (!Array.isArray(message.selectedWitnesses))
                    return "selectedWitnesses: array expected";
                for (let i = 0; i < message.selectedWitnesses.length; ++i)
                    if (!$util.isString(message.selectedWitnesses[i]))
                        return "selectedWitnesses: string[] expected";
            }
            if (message.votes != null && message.hasOwnProperty("votes")) {
                if (!Array.isArray(message.votes))
                    return "votes: array expected";
                for (let i = 0; i < message.votes.length; ++i) {
                    let error = $root.pb.WitnessVote.verify(message.votes[i]);
                    if (error)
                        return "votes." + error;
                }
            }
            if (message.passCount != null && message.hasOwnProperty("passCount"))
                if (!$util.isInteger(message.passCount))
                    return "passCount: integer expected";
            if (message.failCount != null && message.hasOwnProperty("failCount"))
                if (!$util.isInteger(message.failCount))
                    return "failCount: integer expected";
            if (message.abstainCount != null && message.hasOwnProperty("abstainCount"))
                if (!$util.isInteger(message.abstainCount))
                    return "abstainCount: integer expected";
            if (message.challengeId != null && message.hasOwnProperty("challengeId"))
                if (!$util.isString(message.challengeId))
                    return "challengeId: string expected";
            if (message.rechargeFee != null && message.hasOwnProperty("rechargeFee"))
                if (!$util.isString(message.rechargeFee))
                    return "rechargeFee: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.nativeVout != null && message.hasOwnProperty("nativeVout"))
                if (!$util.isInteger(message.nativeVout))
                    return "nativeVout: integer expected";
            if (message.nativeScript != null && message.hasOwnProperty("nativeScript"))
                if (!(message.nativeScript && typeof message.nativeScript.length === "number" || $util.isString(message.nativeScript)))
                    return "nativeScript: buffer expected";
            return null;
        };

        /**
         * Creates a RechargeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.RechargeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.RechargeRequest} RechargeRequest
         */
        RechargeRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.RechargeRequest)
                return object;
            let message = new $root.pb.RechargeRequest();
            if (object.requestId != null)
                message.requestId = String(object.requestId);
            if (object.nativeChain != null)
                message.nativeChain = String(object.nativeChain);
            if (object.nativeTxHash != null)
                message.nativeTxHash = String(object.nativeTxHash);
            if (object.tokenAddress != null)
                message.tokenAddress = String(object.tokenAddress);
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.receiverAddress != null)
                message.receiverAddress = String(object.receiverAddress);
            if (object.requesterAddress != null)
                message.requesterAddress = String(object.requesterAddress);
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "RECHARGE_PENDING":
            case 0:
                message.status = 0;
                break;
            case "RECHARGE_VOTING":
            case 1:
                message.status = 1;
                break;
            case "RECHARGE_CONSENSUS_PASS":
            case 2:
                message.status = 2;
                break;
            case "RECHARGE_CONSENSUS_FAIL":
            case 3:
                message.status = 3;
                break;
            case "RECHARGE_CHALLENGE_PERIOD":
            case 4:
                message.status = 4;
                break;
            case "RECHARGE_CHALLENGED":
            case 5:
                message.status = 5;
                break;
            case "RECHARGE_ARBITRATION":
            case 6:
                message.status = 6;
                break;
            case "RECHARGE_FINALIZED":
            case 7:
                message.status = 7;
                break;
            case "RECHARGE_REJECTED":
            case 8:
                message.status = 8;
                break;
            case "RECHARGE_SHELVED":
            case 9:
                message.status = 9;
                break;
            }
            if (object.createHeight != null)
                if ($util.Long)
                    (message.createHeight = $util.Long.fromValue(object.createHeight)).unsigned = true;
                else if (typeof object.createHeight === "string")
                    message.createHeight = parseInt(object.createHeight, 10);
                else if (typeof object.createHeight === "number")
                    message.createHeight = object.createHeight;
                else if (typeof object.createHeight === "object")
                    message.createHeight = new $util.LongBits(object.createHeight.low >>> 0, object.createHeight.high >>> 0).toNumber(true);
            if (object.deadlineHeight != null)
                if ($util.Long)
                    (message.deadlineHeight = $util.Long.fromValue(object.deadlineHeight)).unsigned = true;
                else if (typeof object.deadlineHeight === "string")
                    message.deadlineHeight = parseInt(object.deadlineHeight, 10);
                else if (typeof object.deadlineHeight === "number")
                    message.deadlineHeight = object.deadlineHeight;
                else if (typeof object.deadlineHeight === "object")
                    message.deadlineHeight = new $util.LongBits(object.deadlineHeight.low >>> 0, object.deadlineHeight.high >>> 0).toNumber(true);
            if (object.finalizeHeight != null)
                if ($util.Long)
                    (message.finalizeHeight = $util.Long.fromValue(object.finalizeHeight)).unsigned = true;
                else if (typeof object.finalizeHeight === "string")
                    message.finalizeHeight = parseInt(object.finalizeHeight, 10);
                else if (typeof object.finalizeHeight === "number")
                    message.finalizeHeight = object.finalizeHeight;
                else if (typeof object.finalizeHeight === "object")
                    message.finalizeHeight = new $util.LongBits(object.finalizeHeight.low >>> 0, object.finalizeHeight.high >>> 0).toNumber(true);
            if (object.round != null)
                message.round = object.round >>> 0;
            if (object.selectedWitnesses) {
                if (!Array.isArray(object.selectedWitnesses))
                    throw TypeError(".pb.RechargeRequest.selectedWitnesses: array expected");
                message.selectedWitnesses = [];
                for (let i = 0; i < object.selectedWitnesses.length; ++i)
                    message.selectedWitnesses[i] = String(object.selectedWitnesses[i]);
            }
            if (object.votes) {
                if (!Array.isArray(object.votes))
                    throw TypeError(".pb.RechargeRequest.votes: array expected");
                message.votes = [];
                for (let i = 0; i < object.votes.length; ++i) {
                    if (typeof object.votes[i] !== "object")
                        throw TypeError(".pb.RechargeRequest.votes: object expected");
                    message.votes[i] = $root.pb.WitnessVote.fromObject(object.votes[i]);
                }
            }
            if (object.passCount != null)
                message.passCount = object.passCount >>> 0;
            if (object.failCount != null)
                message.failCount = object.failCount >>> 0;
            if (object.abstainCount != null)
                message.abstainCount = object.abstainCount >>> 0;
            if (object.challengeId != null)
                message.challengeId = String(object.challengeId);
            if (object.rechargeFee != null)
                message.rechargeFee = String(object.rechargeFee);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.nativeVout != null)
                message.nativeVout = object.nativeVout >>> 0;
            if (object.nativeScript != null)
                if (typeof object.nativeScript === "string")
                    $util.base64.decode(object.nativeScript, message.nativeScript = $util.newBuffer($util.base64.length(object.nativeScript)), 0);
                else if (object.nativeScript.length >= 0)
                    message.nativeScript = object.nativeScript;
            return message;
        };

        /**
         * Creates a plain object from a RechargeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.RechargeRequest
         * @static
         * @param {pb.RechargeRequest} message RechargeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RechargeRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.selectedWitnesses = [];
                object.votes = [];
            }
            if (options.defaults) {
                object.requestId = "";
                object.nativeChain = "";
                object.nativeTxHash = "";
                object.tokenAddress = "";
                object.amount = "";
                object.receiverAddress = "";
                object.requesterAddress = "";
                object.status = options.enums === String ? "RECHARGE_PENDING" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.createHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.createHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.deadlineHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.deadlineHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.finalizeHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.finalizeHeight = options.longs === String ? "0" : 0;
                object.round = 0;
                object.passCount = 0;
                object.failCount = 0;
                object.abstainCount = 0;
                object.challengeId = "";
                object.rechargeFee = "";
                object.vaultId = 0;
                object.nativeVout = 0;
                if (options.bytes === String)
                    object.nativeScript = "";
                else {
                    object.nativeScript = [];
                    if (options.bytes !== Array)
                        object.nativeScript = $util.newBuffer(object.nativeScript);
                }
            }
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.nativeChain != null && message.hasOwnProperty("nativeChain"))
                object.nativeChain = message.nativeChain;
            if (message.nativeTxHash != null && message.hasOwnProperty("nativeTxHash"))
                object.nativeTxHash = message.nativeTxHash;
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                object.tokenAddress = message.tokenAddress;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.receiverAddress != null && message.hasOwnProperty("receiverAddress"))
                object.receiverAddress = message.receiverAddress;
            if (message.requesterAddress != null && message.hasOwnProperty("requesterAddress"))
                object.requesterAddress = message.requesterAddress;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.pb.RechargeRequestStatus[message.status] === undefined ? message.status : $root.pb.RechargeRequestStatus[message.status] : message.status;
            if (message.createHeight != null && message.hasOwnProperty("createHeight"))
                if (typeof message.createHeight === "number")
                    object.createHeight = options.longs === String ? String(message.createHeight) : message.createHeight;
                else
                    object.createHeight = options.longs === String ? $util.Long.prototype.toString.call(message.createHeight) : options.longs === Number ? new $util.LongBits(message.createHeight.low >>> 0, message.createHeight.high >>> 0).toNumber(true) : message.createHeight;
            if (message.deadlineHeight != null && message.hasOwnProperty("deadlineHeight"))
                if (typeof message.deadlineHeight === "number")
                    object.deadlineHeight = options.longs === String ? String(message.deadlineHeight) : message.deadlineHeight;
                else
                    object.deadlineHeight = options.longs === String ? $util.Long.prototype.toString.call(message.deadlineHeight) : options.longs === Number ? new $util.LongBits(message.deadlineHeight.low >>> 0, message.deadlineHeight.high >>> 0).toNumber(true) : message.deadlineHeight;
            if (message.finalizeHeight != null && message.hasOwnProperty("finalizeHeight"))
                if (typeof message.finalizeHeight === "number")
                    object.finalizeHeight = options.longs === String ? String(message.finalizeHeight) : message.finalizeHeight;
                else
                    object.finalizeHeight = options.longs === String ? $util.Long.prototype.toString.call(message.finalizeHeight) : options.longs === Number ? new $util.LongBits(message.finalizeHeight.low >>> 0, message.finalizeHeight.high >>> 0).toNumber(true) : message.finalizeHeight;
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.selectedWitnesses && message.selectedWitnesses.length) {
                object.selectedWitnesses = [];
                for (let j = 0; j < message.selectedWitnesses.length; ++j)
                    object.selectedWitnesses[j] = message.selectedWitnesses[j];
            }
            if (message.votes && message.votes.length) {
                object.votes = [];
                for (let j = 0; j < message.votes.length; ++j)
                    object.votes[j] = $root.pb.WitnessVote.toObject(message.votes[j], options);
            }
            if (message.passCount != null && message.hasOwnProperty("passCount"))
                object.passCount = message.passCount;
            if (message.failCount != null && message.hasOwnProperty("failCount"))
                object.failCount = message.failCount;
            if (message.abstainCount != null && message.hasOwnProperty("abstainCount"))
                object.abstainCount = message.abstainCount;
            if (message.challengeId != null && message.hasOwnProperty("challengeId"))
                object.challengeId = message.challengeId;
            if (message.rechargeFee != null && message.hasOwnProperty("rechargeFee"))
                object.rechargeFee = message.rechargeFee;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.nativeVout != null && message.hasOwnProperty("nativeVout"))
                object.nativeVout = message.nativeVout;
            if (message.nativeScript != null && message.hasOwnProperty("nativeScript"))
                object.nativeScript = options.bytes === String ? $util.base64.encode(message.nativeScript, 0, message.nativeScript.length) : options.bytes === Array ? Array.prototype.slice.call(message.nativeScript) : message.nativeScript;
            return object;
        };

        /**
         * Converts this RechargeRequest to JSON.
         * @function toJSON
         * @memberof pb.RechargeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RechargeRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RechargeRequest
         * @function getTypeUrl
         * @memberof pb.RechargeRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RechargeRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.RechargeRequest";
        };

        return RechargeRequest;
    })();

    pb.WitnessChallengeTx = (function() {

        /**
         * Properties of a WitnessChallengeTx.
         * @memberof pb
         * @interface IWitnessChallengeTx
         * @property {pb.IBaseMessage|null} [base] WitnessChallengeTx base
         * @property {string|null} [requestId] WitnessChallengeTx requestId
         * @property {string|null} [stakeAmount] WitnessChallengeTx stakeAmount
         * @property {string|null} [reason] WitnessChallengeTx reason
         * @property {string|null} [evidence] WitnessChallengeTx evidence
         */

        /**
         * Constructs a new WitnessChallengeTx.
         * @memberof pb
         * @classdesc Represents a WitnessChallengeTx.
         * @implements IWitnessChallengeTx
         * @constructor
         * @param {pb.IWitnessChallengeTx=} [properties] Properties to set
         */
        function WitnessChallengeTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessChallengeTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.WitnessChallengeTx
         * @instance
         */
        WitnessChallengeTx.prototype.base = null;

        /**
         * WitnessChallengeTx requestId.
         * @member {string} requestId
         * @memberof pb.WitnessChallengeTx
         * @instance
         */
        WitnessChallengeTx.prototype.requestId = "";

        /**
         * WitnessChallengeTx stakeAmount.
         * @member {string} stakeAmount
         * @memberof pb.WitnessChallengeTx
         * @instance
         */
        WitnessChallengeTx.prototype.stakeAmount = "";

        /**
         * WitnessChallengeTx reason.
         * @member {string} reason
         * @memberof pb.WitnessChallengeTx
         * @instance
         */
        WitnessChallengeTx.prototype.reason = "";

        /**
         * WitnessChallengeTx evidence.
         * @member {string} evidence
         * @memberof pb.WitnessChallengeTx
         * @instance
         */
        WitnessChallengeTx.prototype.evidence = "";

        /**
         * Creates a new WitnessChallengeTx instance using the specified properties.
         * @function create
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {pb.IWitnessChallengeTx=} [properties] Properties to set
         * @returns {pb.WitnessChallengeTx} WitnessChallengeTx instance
         */
        WitnessChallengeTx.create = function create(properties) {
            return new WitnessChallengeTx(properties);
        };

        /**
         * Encodes the specified WitnessChallengeTx message. Does not implicitly {@link pb.WitnessChallengeTx.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {pb.IWitnessChallengeTx} message WitnessChallengeTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessChallengeTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.requestId);
            if (message.stakeAmount != null && Object.hasOwnProperty.call(message, "stakeAmount"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.stakeAmount);
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.reason);
            if (message.evidence != null && Object.hasOwnProperty.call(message, "evidence"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.evidence);
            return writer;
        };

        /**
         * Encodes the specified WitnessChallengeTx message, length delimited. Does not implicitly {@link pb.WitnessChallengeTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {pb.IWitnessChallengeTx} message WitnessChallengeTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessChallengeTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessChallengeTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessChallengeTx} WitnessChallengeTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessChallengeTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessChallengeTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.requestId = reader.string();
                        break;
                    }
                case 3: {
                        message.stakeAmount = reader.string();
                        break;
                    }
                case 4: {
                        message.reason = reader.string();
                        break;
                    }
                case 5: {
                        message.evidence = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessChallengeTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessChallengeTx} WitnessChallengeTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessChallengeTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessChallengeTx message.
         * @function verify
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessChallengeTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isString(message.requestId))
                    return "requestId: string expected";
            if (message.stakeAmount != null && message.hasOwnProperty("stakeAmount"))
                if (!$util.isString(message.stakeAmount))
                    return "stakeAmount: string expected";
            if (message.reason != null && message.hasOwnProperty("reason"))
                if (!$util.isString(message.reason))
                    return "reason: string expected";
            if (message.evidence != null && message.hasOwnProperty("evidence"))
                if (!$util.isString(message.evidence))
                    return "evidence: string expected";
            return null;
        };

        /**
         * Creates a WitnessChallengeTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessChallengeTx} WitnessChallengeTx
         */
        WitnessChallengeTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessChallengeTx)
                return object;
            let message = new $root.pb.WitnessChallengeTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.WitnessChallengeTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.requestId != null)
                message.requestId = String(object.requestId);
            if (object.stakeAmount != null)
                message.stakeAmount = String(object.stakeAmount);
            if (object.reason != null)
                message.reason = String(object.reason);
            if (object.evidence != null)
                message.evidence = String(object.evidence);
            return message;
        };

        /**
         * Creates a plain object from a WitnessChallengeTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {pb.WitnessChallengeTx} message WitnessChallengeTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessChallengeTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.requestId = "";
                object.stakeAmount = "";
                object.reason = "";
                object.evidence = "";
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.stakeAmount != null && message.hasOwnProperty("stakeAmount"))
                object.stakeAmount = message.stakeAmount;
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = message.reason;
            if (message.evidence != null && message.hasOwnProperty("evidence"))
                object.evidence = message.evidence;
            return object;
        };

        /**
         * Converts this WitnessChallengeTx to JSON.
         * @function toJSON
         * @memberof pb.WitnessChallengeTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessChallengeTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessChallengeTx
         * @function getTypeUrl
         * @memberof pb.WitnessChallengeTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessChallengeTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessChallengeTx";
        };

        return WitnessChallengeTx;
    })();

    /**
     * ChallengeStatus enum.
     * @name pb.ChallengeStatus
     * @enum {number}
     * @property {number} CHALLENGE_ACTIVE=0 CHALLENGE_ACTIVE value
     * @property {number} CHALLENGE_SHELVED=1 CHALLENGE_SHELVED value
     * @property {number} CHALLENGE_FINALIZED=2 CHALLENGE_FINALIZED value
     */
    pb.ChallengeStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CHALLENGE_ACTIVE"] = 0;
        values[valuesById[1] = "CHALLENGE_SHELVED"] = 1;
        values[valuesById[2] = "CHALLENGE_FINALIZED"] = 2;
        return values;
    })();

    pb.ChallengeRecord = (function() {

        /**
         * Properties of a ChallengeRecord.
         * @memberof pb
         * @interface IChallengeRecord
         * @property {string|null} [challengeId] ChallengeRecord challengeId
         * @property {string|null} [requestId] ChallengeRecord requestId
         * @property {string|null} [challengerAddress] ChallengeRecord challengerAddress
         * @property {string|null} [stakeAmount] ChallengeRecord stakeAmount
         * @property {string|null} [reason] ChallengeRecord reason
         * @property {number|Long|null} [createHeight] ChallengeRecord createHeight
         * @property {number|Long|null} [deadlineHeight] ChallengeRecord deadlineHeight
         * @property {number|null} [arbitrationRound] ChallengeRecord arbitrationRound
         * @property {Array.<string>|null} [arbitrators] ChallengeRecord arbitrators
         * @property {Array.<pb.IWitnessVote>|null} [arbitrationVotes] ChallengeRecord arbitrationVotes
         * @property {number|null} [passCount] ChallengeRecord passCount
         * @property {number|null} [failCount] ChallengeRecord failCount
         * @property {boolean|null} [challengeSuccess] ChallengeRecord challengeSuccess
         * @property {boolean|null} [finalized] ChallengeRecord finalized
         * @property {number|null} [consensusThreshold] ChallengeRecord consensusThreshold
         * @property {pb.ChallengeStatus|null} [status] ChallengeRecord status
         * @property {string|null} [evidence] ChallengeRecord evidence
         */

        /**
         * Constructs a new ChallengeRecord.
         * @memberof pb
         * @classdesc Represents a ChallengeRecord.
         * @implements IChallengeRecord
         * @constructor
         * @param {pb.IChallengeRecord=} [properties] Properties to set
         */
        function ChallengeRecord(properties) {
            this.arbitrators = [];
            this.arbitrationVotes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChallengeRecord challengeId.
         * @member {string} challengeId
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.challengeId = "";

        /**
         * ChallengeRecord requestId.
         * @member {string} requestId
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.requestId = "";

        /**
         * ChallengeRecord challengerAddress.
         * @member {string} challengerAddress
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.challengerAddress = "";

        /**
         * ChallengeRecord stakeAmount.
         * @member {string} stakeAmount
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.stakeAmount = "";

        /**
         * ChallengeRecord reason.
         * @member {string} reason
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.reason = "";

        /**
         * ChallengeRecord createHeight.
         * @member {number|Long} createHeight
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.createHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChallengeRecord deadlineHeight.
         * @member {number|Long} deadlineHeight
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.deadlineHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * ChallengeRecord arbitrationRound.
         * @member {number} arbitrationRound
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.arbitrationRound = 0;

        /**
         * ChallengeRecord arbitrators.
         * @member {Array.<string>} arbitrators
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.arbitrators = $util.emptyArray;

        /**
         * ChallengeRecord arbitrationVotes.
         * @member {Array.<pb.IWitnessVote>} arbitrationVotes
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.arbitrationVotes = $util.emptyArray;

        /**
         * ChallengeRecord passCount.
         * @member {number} passCount
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.passCount = 0;

        /**
         * ChallengeRecord failCount.
         * @member {number} failCount
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.failCount = 0;

        /**
         * ChallengeRecord challengeSuccess.
         * @member {boolean} challengeSuccess
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.challengeSuccess = false;

        /**
         * ChallengeRecord finalized.
         * @member {boolean} finalized
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.finalized = false;

        /**
         * ChallengeRecord consensusThreshold.
         * @member {number} consensusThreshold
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.consensusThreshold = 0;

        /**
         * ChallengeRecord status.
         * @member {pb.ChallengeStatus} status
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.status = 0;

        /**
         * ChallengeRecord evidence.
         * @member {string} evidence
         * @memberof pb.ChallengeRecord
         * @instance
         */
        ChallengeRecord.prototype.evidence = "";

        /**
         * Creates a new ChallengeRecord instance using the specified properties.
         * @function create
         * @memberof pb.ChallengeRecord
         * @static
         * @param {pb.IChallengeRecord=} [properties] Properties to set
         * @returns {pb.ChallengeRecord} ChallengeRecord instance
         */
        ChallengeRecord.create = function create(properties) {
            return new ChallengeRecord(properties);
        };

        /**
         * Encodes the specified ChallengeRecord message. Does not implicitly {@link pb.ChallengeRecord.verify|verify} messages.
         * @function encode
         * @memberof pb.ChallengeRecord
         * @static
         * @param {pb.IChallengeRecord} message ChallengeRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChallengeRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.challengeId != null && Object.hasOwnProperty.call(message, "challengeId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.challengeId);
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.requestId);
            if (message.challengerAddress != null && Object.hasOwnProperty.call(message, "challengerAddress"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.challengerAddress);
            if (message.stakeAmount != null && Object.hasOwnProperty.call(message, "stakeAmount"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.stakeAmount);
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.reason);
            if (message.createHeight != null && Object.hasOwnProperty.call(message, "createHeight"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.createHeight);
            if (message.deadlineHeight != null && Object.hasOwnProperty.call(message, "deadlineHeight"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.deadlineHeight);
            if (message.arbitrationRound != null && Object.hasOwnProperty.call(message, "arbitrationRound"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.arbitrationRound);
            if (message.arbitrators != null && message.arbitrators.length)
                for (let i = 0; i < message.arbitrators.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.arbitrators[i]);
            if (message.arbitrationVotes != null && message.arbitrationVotes.length)
                for (let i = 0; i < message.arbitrationVotes.length; ++i)
                    $root.pb.WitnessVote.encode(message.arbitrationVotes[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.passCount != null && Object.hasOwnProperty.call(message, "passCount"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.passCount);
            if (message.failCount != null && Object.hasOwnProperty.call(message, "failCount"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint32(message.failCount);
            if (message.challengeSuccess != null && Object.hasOwnProperty.call(message, "challengeSuccess"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.challengeSuccess);
            if (message.finalized != null && Object.hasOwnProperty.call(message, "finalized"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.finalized);
            if (message.consensusThreshold != null && Object.hasOwnProperty.call(message, "consensusThreshold"))
                writer.uint32(/* id 15, wireType 0 =*/120).uint32(message.consensusThreshold);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.status);
            if (message.evidence != null && Object.hasOwnProperty.call(message, "evidence"))
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.evidence);
            return writer;
        };

        /**
         * Encodes the specified ChallengeRecord message, length delimited. Does not implicitly {@link pb.ChallengeRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ChallengeRecord
         * @static
         * @param {pb.IChallengeRecord} message ChallengeRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChallengeRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChallengeRecord message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ChallengeRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ChallengeRecord} ChallengeRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChallengeRecord.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ChallengeRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.challengeId = reader.string();
                        break;
                    }
                case 2: {
                        message.requestId = reader.string();
                        break;
                    }
                case 3: {
                        message.challengerAddress = reader.string();
                        break;
                    }
                case 4: {
                        message.stakeAmount = reader.string();
                        break;
                    }
                case 5: {
                        message.reason = reader.string();
                        break;
                    }
                case 6: {
                        message.createHeight = reader.uint64();
                        break;
                    }
                case 7: {
                        message.deadlineHeight = reader.uint64();
                        break;
                    }
                case 8: {
                        message.arbitrationRound = reader.uint32();
                        break;
                    }
                case 9: {
                        if (!(message.arbitrators && message.arbitrators.length))
                            message.arbitrators = [];
                        message.arbitrators.push(reader.string());
                        break;
                    }
                case 10: {
                        if (!(message.arbitrationVotes && message.arbitrationVotes.length))
                            message.arbitrationVotes = [];
                        message.arbitrationVotes.push($root.pb.WitnessVote.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.passCount = reader.uint32();
                        break;
                    }
                case 12: {
                        message.failCount = reader.uint32();
                        break;
                    }
                case 13: {
                        message.challengeSuccess = reader.bool();
                        break;
                    }
                case 14: {
                        message.finalized = reader.bool();
                        break;
                    }
                case 15: {
                        message.consensusThreshold = reader.uint32();
                        break;
                    }
                case 16: {
                        message.status = reader.int32();
                        break;
                    }
                case 17: {
                        message.evidence = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChallengeRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ChallengeRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ChallengeRecord} ChallengeRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChallengeRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChallengeRecord message.
         * @function verify
         * @memberof pb.ChallengeRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChallengeRecord.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.challengeId != null && message.hasOwnProperty("challengeId"))
                if (!$util.isString(message.challengeId))
                    return "challengeId: string expected";
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isString(message.requestId))
                    return "requestId: string expected";
            if (message.challengerAddress != null && message.hasOwnProperty("challengerAddress"))
                if (!$util.isString(message.challengerAddress))
                    return "challengerAddress: string expected";
            if (message.stakeAmount != null && message.hasOwnProperty("stakeAmount"))
                if (!$util.isString(message.stakeAmount))
                    return "stakeAmount: string expected";
            if (message.reason != null && message.hasOwnProperty("reason"))
                if (!$util.isString(message.reason))
                    return "reason: string expected";
            if (message.createHeight != null && message.hasOwnProperty("createHeight"))
                if (!$util.isInteger(message.createHeight) && !(message.createHeight && $util.isInteger(message.createHeight.low) && $util.isInteger(message.createHeight.high)))
                    return "createHeight: integer|Long expected";
            if (message.deadlineHeight != null && message.hasOwnProperty("deadlineHeight"))
                if (!$util.isInteger(message.deadlineHeight) && !(message.deadlineHeight && $util.isInteger(message.deadlineHeight.low) && $util.isInteger(message.deadlineHeight.high)))
                    return "deadlineHeight: integer|Long expected";
            if (message.arbitrationRound != null && message.hasOwnProperty("arbitrationRound"))
                if (!$util.isInteger(message.arbitrationRound))
                    return "arbitrationRound: integer expected";
            if (message.arbitrators != null && message.hasOwnProperty("arbitrators")) {
                if (!Array.isArray(message.arbitrators))
                    return "arbitrators: array expected";
                for (let i = 0; i < message.arbitrators.length; ++i)
                    if (!$util.isString(message.arbitrators[i]))
                        return "arbitrators: string[] expected";
            }
            if (message.arbitrationVotes != null && message.hasOwnProperty("arbitrationVotes")) {
                if (!Array.isArray(message.arbitrationVotes))
                    return "arbitrationVotes: array expected";
                for (let i = 0; i < message.arbitrationVotes.length; ++i) {
                    let error = $root.pb.WitnessVote.verify(message.arbitrationVotes[i]);
                    if (error)
                        return "arbitrationVotes." + error;
                }
            }
            if (message.passCount != null && message.hasOwnProperty("passCount"))
                if (!$util.isInteger(message.passCount))
                    return "passCount: integer expected";
            if (message.failCount != null && message.hasOwnProperty("failCount"))
                if (!$util.isInteger(message.failCount))
                    return "failCount: integer expected";
            if (message.challengeSuccess != null && message.hasOwnProperty("challengeSuccess"))
                if (typeof message.challengeSuccess !== "boolean")
                    return "challengeSuccess: boolean expected";
            if (message.finalized != null && message.hasOwnProperty("finalized"))
                if (typeof message.finalized !== "boolean")
                    return "finalized: boolean expected";
            if (message.consensusThreshold != null && message.hasOwnProperty("consensusThreshold"))
                if (!$util.isInteger(message.consensusThreshold))
                    return "consensusThreshold: integer expected";
            if (message.status != null && message.hasOwnProperty("status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.evidence != null && message.hasOwnProperty("evidence"))
                if (!$util.isString(message.evidence))
                    return "evidence: string expected";
            return null;
        };

        /**
         * Creates a ChallengeRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ChallengeRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ChallengeRecord} ChallengeRecord
         */
        ChallengeRecord.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ChallengeRecord)
                return object;
            let message = new $root.pb.ChallengeRecord();
            if (object.challengeId != null)
                message.challengeId = String(object.challengeId);
            if (object.requestId != null)
                message.requestId = String(object.requestId);
            if (object.challengerAddress != null)
                message.challengerAddress = String(object.challengerAddress);
            if (object.stakeAmount != null)
                message.stakeAmount = String(object.stakeAmount);
            if (object.reason != null)
                message.reason = String(object.reason);
            if (object.createHeight != null)
                if ($util.Long)
                    (message.createHeight = $util.Long.fromValue(object.createHeight)).unsigned = true;
                else if (typeof object.createHeight === "string")
                    message.createHeight = parseInt(object.createHeight, 10);
                else if (typeof object.createHeight === "number")
                    message.createHeight = object.createHeight;
                else if (typeof object.createHeight === "object")
                    message.createHeight = new $util.LongBits(object.createHeight.low >>> 0, object.createHeight.high >>> 0).toNumber(true);
            if (object.deadlineHeight != null)
                if ($util.Long)
                    (message.deadlineHeight = $util.Long.fromValue(object.deadlineHeight)).unsigned = true;
                else if (typeof object.deadlineHeight === "string")
                    message.deadlineHeight = parseInt(object.deadlineHeight, 10);
                else if (typeof object.deadlineHeight === "number")
                    message.deadlineHeight = object.deadlineHeight;
                else if (typeof object.deadlineHeight === "object")
                    message.deadlineHeight = new $util.LongBits(object.deadlineHeight.low >>> 0, object.deadlineHeight.high >>> 0).toNumber(true);
            if (object.arbitrationRound != null)
                message.arbitrationRound = object.arbitrationRound >>> 0;
            if (object.arbitrators) {
                if (!Array.isArray(object.arbitrators))
                    throw TypeError(".pb.ChallengeRecord.arbitrators: array expected");
                message.arbitrators = [];
                for (let i = 0; i < object.arbitrators.length; ++i)
                    message.arbitrators[i] = String(object.arbitrators[i]);
            }
            if (object.arbitrationVotes) {
                if (!Array.isArray(object.arbitrationVotes))
                    throw TypeError(".pb.ChallengeRecord.arbitrationVotes: array expected");
                message.arbitrationVotes = [];
                for (let i = 0; i < object.arbitrationVotes.length; ++i) {
                    if (typeof object.arbitrationVotes[i] !== "object")
                        throw TypeError(".pb.ChallengeRecord.arbitrationVotes: object expected");
                    message.arbitrationVotes[i] = $root.pb.WitnessVote.fromObject(object.arbitrationVotes[i]);
                }
            }
            if (object.passCount != null)
                message.passCount = object.passCount >>> 0;
            if (object.failCount != null)
                message.failCount = object.failCount >>> 0;
            if (object.challengeSuccess != null)
                message.challengeSuccess = Boolean(object.challengeSuccess);
            if (object.finalized != null)
                message.finalized = Boolean(object.finalized);
            if (object.consensusThreshold != null)
                message.consensusThreshold = object.consensusThreshold >>> 0;
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "CHALLENGE_ACTIVE":
            case 0:
                message.status = 0;
                break;
            case "CHALLENGE_SHELVED":
            case 1:
                message.status = 1;
                break;
            case "CHALLENGE_FINALIZED":
            case 2:
                message.status = 2;
                break;
            }
            if (object.evidence != null)
                message.evidence = String(object.evidence);
            return message;
        };

        /**
         * Creates a plain object from a ChallengeRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ChallengeRecord
         * @static
         * @param {pb.ChallengeRecord} message ChallengeRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChallengeRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.arbitrators = [];
                object.arbitrationVotes = [];
            }
            if (options.defaults) {
                object.challengeId = "";
                object.requestId = "";
                object.challengerAddress = "";
                object.stakeAmount = "";
                object.reason = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.createHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.createHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.deadlineHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.deadlineHeight = options.longs === String ? "0" : 0;
                object.arbitrationRound = 0;
                object.passCount = 0;
                object.failCount = 0;
                object.challengeSuccess = false;
                object.finalized = false;
                object.consensusThreshold = 0;
                object.status = options.enums === String ? "CHALLENGE_ACTIVE" : 0;
                object.evidence = "";
            }
            if (message.challengeId != null && message.hasOwnProperty("challengeId"))
                object.challengeId = message.challengeId;
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.challengerAddress != null && message.hasOwnProperty("challengerAddress"))
                object.challengerAddress = message.challengerAddress;
            if (message.stakeAmount != null && message.hasOwnProperty("stakeAmount"))
                object.stakeAmount = message.stakeAmount;
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = message.reason;
            if (message.createHeight != null && message.hasOwnProperty("createHeight"))
                if (typeof message.createHeight === "number")
                    object.createHeight = options.longs === String ? String(message.createHeight) : message.createHeight;
                else
                    object.createHeight = options.longs === String ? $util.Long.prototype.toString.call(message.createHeight) : options.longs === Number ? new $util.LongBits(message.createHeight.low >>> 0, message.createHeight.high >>> 0).toNumber(true) : message.createHeight;
            if (message.deadlineHeight != null && message.hasOwnProperty("deadlineHeight"))
                if (typeof message.deadlineHeight === "number")
                    object.deadlineHeight = options.longs === String ? String(message.deadlineHeight) : message.deadlineHeight;
                else
                    object.deadlineHeight = options.longs === String ? $util.Long.prototype.toString.call(message.deadlineHeight) : options.longs === Number ? new $util.LongBits(message.deadlineHeight.low >>> 0, message.deadlineHeight.high >>> 0).toNumber(true) : message.deadlineHeight;
            if (message.arbitrationRound != null && message.hasOwnProperty("arbitrationRound"))
                object.arbitrationRound = message.arbitrationRound;
            if (message.arbitrators && message.arbitrators.length) {
                object.arbitrators = [];
                for (let j = 0; j < message.arbitrators.length; ++j)
                    object.arbitrators[j] = message.arbitrators[j];
            }
            if (message.arbitrationVotes && message.arbitrationVotes.length) {
                object.arbitrationVotes = [];
                for (let j = 0; j < message.arbitrationVotes.length; ++j)
                    object.arbitrationVotes[j] = $root.pb.WitnessVote.toObject(message.arbitrationVotes[j], options);
            }
            if (message.passCount != null && message.hasOwnProperty("passCount"))
                object.passCount = message.passCount;
            if (message.failCount != null && message.hasOwnProperty("failCount"))
                object.failCount = message.failCount;
            if (message.challengeSuccess != null && message.hasOwnProperty("challengeSuccess"))
                object.challengeSuccess = message.challengeSuccess;
            if (message.finalized != null && message.hasOwnProperty("finalized"))
                object.finalized = message.finalized;
            if (message.consensusThreshold != null && message.hasOwnProperty("consensusThreshold"))
                object.consensusThreshold = message.consensusThreshold;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = options.enums === String ? $root.pb.ChallengeStatus[message.status] === undefined ? message.status : $root.pb.ChallengeStatus[message.status] : message.status;
            if (message.evidence != null && message.hasOwnProperty("evidence"))
                object.evidence = message.evidence;
            return object;
        };

        /**
         * Converts this ChallengeRecord to JSON.
         * @function toJSON
         * @memberof pb.ChallengeRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChallengeRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChallengeRecord
         * @function getTypeUrl
         * @memberof pb.ChallengeRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChallengeRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ChallengeRecord";
        };

        return ChallengeRecord;
    })();

    pb.RechargeRequestListRequest = (function() {

        /**
         * Properties of a RechargeRequestListRequest.
         * @memberof pb
         * @interface IRechargeRequestListRequest
         * @property {number|null} [limit] RechargeRequestListRequest limit
         */

        /**
         * Constructs a new RechargeRequestListRequest.
         * @memberof pb
         * @classdesc Represents a RechargeRequestListRequest.
         * @implements IRechargeRequestListRequest
         * @constructor
         * @param {pb.IRechargeRequestListRequest=} [properties] Properties to set
         */
        function RechargeRequestListRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RechargeRequestListRequest limit.
         * @member {number} limit
         * @memberof pb.RechargeRequestListRequest
         * @instance
         */
        RechargeRequestListRequest.prototype.limit = 0;

        /**
         * Creates a new RechargeRequestListRequest instance using the specified properties.
         * @function create
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {pb.IRechargeRequestListRequest=} [properties] Properties to set
         * @returns {pb.RechargeRequestListRequest} RechargeRequestListRequest instance
         */
        RechargeRequestListRequest.create = function create(properties) {
            return new RechargeRequestListRequest(properties);
        };

        /**
         * Encodes the specified RechargeRequestListRequest message. Does not implicitly {@link pb.RechargeRequestListRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {pb.IRechargeRequestListRequest} message RechargeRequestListRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RechargeRequestListRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.limit);
            return writer;
        };

        /**
         * Encodes the specified RechargeRequestListRequest message, length delimited. Does not implicitly {@link pb.RechargeRequestListRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {pb.IRechargeRequestListRequest} message RechargeRequestListRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RechargeRequestListRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RechargeRequestListRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.RechargeRequestListRequest} RechargeRequestListRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RechargeRequestListRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.RechargeRequestListRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.limit = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RechargeRequestListRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.RechargeRequestListRequest} RechargeRequestListRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RechargeRequestListRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RechargeRequestListRequest message.
         * @function verify
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RechargeRequestListRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            return null;
        };

        /**
         * Creates a RechargeRequestListRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.RechargeRequestListRequest} RechargeRequestListRequest
         */
        RechargeRequestListRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.RechargeRequestListRequest)
                return object;
            let message = new $root.pb.RechargeRequestListRequest();
            if (object.limit != null)
                message.limit = object.limit | 0;
            return message;
        };

        /**
         * Creates a plain object from a RechargeRequestListRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {pb.RechargeRequestListRequest} message RechargeRequestListRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RechargeRequestListRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.limit = 0;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            return object;
        };

        /**
         * Converts this RechargeRequestListRequest to JSON.
         * @function toJSON
         * @memberof pb.RechargeRequestListRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RechargeRequestListRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RechargeRequestListRequest
         * @function getTypeUrl
         * @memberof pb.RechargeRequestListRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RechargeRequestListRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.RechargeRequestListRequest";
        };

        return RechargeRequestListRequest;
    })();

    pb.RechargeRequestList = (function() {

        /**
         * Properties of a RechargeRequestList.
         * @memberof pb
         * @interface IRechargeRequestList
         * @property {Array.<pb.IRechargeRequest>|null} [requests] RechargeRequestList requests
         */

        /**
         * Constructs a new RechargeRequestList.
         * @memberof pb
         * @classdesc Represents a RechargeRequestList.
         * @implements IRechargeRequestList
         * @constructor
         * @param {pb.IRechargeRequestList=} [properties] Properties to set
         */
        function RechargeRequestList(properties) {
            this.requests = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RechargeRequestList requests.
         * @member {Array.<pb.IRechargeRequest>} requests
         * @memberof pb.RechargeRequestList
         * @instance
         */
        RechargeRequestList.prototype.requests = $util.emptyArray;

        /**
         * Creates a new RechargeRequestList instance using the specified properties.
         * @function create
         * @memberof pb.RechargeRequestList
         * @static
         * @param {pb.IRechargeRequestList=} [properties] Properties to set
         * @returns {pb.RechargeRequestList} RechargeRequestList instance
         */
        RechargeRequestList.create = function create(properties) {
            return new RechargeRequestList(properties);
        };

        /**
         * Encodes the specified RechargeRequestList message. Does not implicitly {@link pb.RechargeRequestList.verify|verify} messages.
         * @function encode
         * @memberof pb.RechargeRequestList
         * @static
         * @param {pb.IRechargeRequestList} message RechargeRequestList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RechargeRequestList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.requests != null && message.requests.length)
                for (let i = 0; i < message.requests.length; ++i)
                    $root.pb.RechargeRequest.encode(message.requests[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RechargeRequestList message, length delimited. Does not implicitly {@link pb.RechargeRequestList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.RechargeRequestList
         * @static
         * @param {pb.IRechargeRequestList} message RechargeRequestList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RechargeRequestList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a RechargeRequestList message from the specified reader or buffer.
         * @function decode
         * @memberof pb.RechargeRequestList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.RechargeRequestList} RechargeRequestList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RechargeRequestList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.RechargeRequestList();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.requests && message.requests.length))
                            message.requests = [];
                        message.requests.push($root.pb.RechargeRequest.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RechargeRequestList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.RechargeRequestList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.RechargeRequestList} RechargeRequestList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RechargeRequestList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RechargeRequestList message.
         * @function verify
         * @memberof pb.RechargeRequestList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RechargeRequestList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.requests != null && message.hasOwnProperty("requests")) {
                if (!Array.isArray(message.requests))
                    return "requests: array expected";
                for (let i = 0; i < message.requests.length; ++i) {
                    let error = $root.pb.RechargeRequest.verify(message.requests[i]);
                    if (error)
                        return "requests." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RechargeRequestList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.RechargeRequestList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.RechargeRequestList} RechargeRequestList
         */
        RechargeRequestList.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.RechargeRequestList)
                return object;
            let message = new $root.pb.RechargeRequestList();
            if (object.requests) {
                if (!Array.isArray(object.requests))
                    throw TypeError(".pb.RechargeRequestList.requests: array expected");
                message.requests = [];
                for (let i = 0; i < object.requests.length; ++i) {
                    if (typeof object.requests[i] !== "object")
                        throw TypeError(".pb.RechargeRequestList.requests: object expected");
                    message.requests[i] = $root.pb.RechargeRequest.fromObject(object.requests[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RechargeRequestList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.RechargeRequestList
         * @static
         * @param {pb.RechargeRequestList} message RechargeRequestList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RechargeRequestList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.requests = [];
            if (message.requests && message.requests.length) {
                object.requests = [];
                for (let j = 0; j < message.requests.length; ++j)
                    object.requests[j] = $root.pb.RechargeRequest.toObject(message.requests[j], options);
            }
            return object;
        };

        /**
         * Converts this RechargeRequestList to JSON.
         * @function toJSON
         * @memberof pb.RechargeRequestList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RechargeRequestList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RechargeRequestList
         * @function getTypeUrl
         * @memberof pb.RechargeRequestList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RechargeRequestList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.RechargeRequestList";
        };

        return RechargeRequestList;
    })();

    pb.FrostWithdrawListRequest = (function() {

        /**
         * Properties of a FrostWithdrawListRequest.
         * @memberof pb
         * @interface IFrostWithdrawListRequest
         * @property {string|null} [chain] FrostWithdrawListRequest chain
         * @property {string|null} [asset] FrostWithdrawListRequest asset
         */

        /**
         * Constructs a new FrostWithdrawListRequest.
         * @memberof pb
         * @classdesc Represents a FrostWithdrawListRequest.
         * @implements IFrostWithdrawListRequest
         * @constructor
         * @param {pb.IFrostWithdrawListRequest=} [properties] Properties to set
         */
        function FrostWithdrawListRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostWithdrawListRequest chain.
         * @member {string} chain
         * @memberof pb.FrostWithdrawListRequest
         * @instance
         */
        FrostWithdrawListRequest.prototype.chain = "";

        /**
         * FrostWithdrawListRequest asset.
         * @member {string} asset
         * @memberof pb.FrostWithdrawListRequest
         * @instance
         */
        FrostWithdrawListRequest.prototype.asset = "";

        /**
         * Creates a new FrostWithdrawListRequest instance using the specified properties.
         * @function create
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {pb.IFrostWithdrawListRequest=} [properties] Properties to set
         * @returns {pb.FrostWithdrawListRequest} FrostWithdrawListRequest instance
         */
        FrostWithdrawListRequest.create = function create(properties) {
            return new FrostWithdrawListRequest(properties);
        };

        /**
         * Encodes the specified FrostWithdrawListRequest message. Does not implicitly {@link pb.FrostWithdrawListRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {pb.IFrostWithdrawListRequest} message FrostWithdrawListRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawListRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.asset != null && Object.hasOwnProperty.call(message, "asset"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.asset);
            return writer;
        };

        /**
         * Encodes the specified FrostWithdrawListRequest message, length delimited. Does not implicitly {@link pb.FrostWithdrawListRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {pb.IFrostWithdrawListRequest} message FrostWithdrawListRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawListRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostWithdrawListRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostWithdrawListRequest} FrostWithdrawListRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawListRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostWithdrawListRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.asset = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostWithdrawListRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostWithdrawListRequest} FrostWithdrawListRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawListRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostWithdrawListRequest message.
         * @function verify
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostWithdrawListRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.asset != null && message.hasOwnProperty("asset"))
                if (!$util.isString(message.asset))
                    return "asset: string expected";
            return null;
        };

        /**
         * Creates a FrostWithdrawListRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostWithdrawListRequest} FrostWithdrawListRequest
         */
        FrostWithdrawListRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostWithdrawListRequest)
                return object;
            let message = new $root.pb.FrostWithdrawListRequest();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.asset != null)
                message.asset = String(object.asset);
            return message;
        };

        /**
         * Creates a plain object from a FrostWithdrawListRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {pb.FrostWithdrawListRequest} message FrostWithdrawListRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostWithdrawListRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chain = "";
                object.asset = "";
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.asset != null && message.hasOwnProperty("asset"))
                object.asset = message.asset;
            return object;
        };

        /**
         * Converts this FrostWithdrawListRequest to JSON.
         * @function toJSON
         * @memberof pb.FrostWithdrawListRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostWithdrawListRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostWithdrawListRequest
         * @function getTypeUrl
         * @memberof pb.FrostWithdrawListRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostWithdrawListRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostWithdrawListRequest";
        };

        return FrostWithdrawListRequest;
    })();

    pb.FrostWithdrawStateList = (function() {

        /**
         * Properties of a FrostWithdrawStateList.
         * @memberof pb
         * @interface IFrostWithdrawStateList
         * @property {Array.<pb.IFrostWithdrawState>|null} [states] FrostWithdrawStateList states
         */

        /**
         * Constructs a new FrostWithdrawStateList.
         * @memberof pb
         * @classdesc Represents a FrostWithdrawStateList.
         * @implements IFrostWithdrawStateList
         * @constructor
         * @param {pb.IFrostWithdrawStateList=} [properties] Properties to set
         */
        function FrostWithdrawStateList(properties) {
            this.states = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostWithdrawStateList states.
         * @member {Array.<pb.IFrostWithdrawState>} states
         * @memberof pb.FrostWithdrawStateList
         * @instance
         */
        FrostWithdrawStateList.prototype.states = $util.emptyArray;

        /**
         * Creates a new FrostWithdrawStateList instance using the specified properties.
         * @function create
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {pb.IFrostWithdrawStateList=} [properties] Properties to set
         * @returns {pb.FrostWithdrawStateList} FrostWithdrawStateList instance
         */
        FrostWithdrawStateList.create = function create(properties) {
            return new FrostWithdrawStateList(properties);
        };

        /**
         * Encodes the specified FrostWithdrawStateList message. Does not implicitly {@link pb.FrostWithdrawStateList.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {pb.IFrostWithdrawStateList} message FrostWithdrawStateList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawStateList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.states != null && message.states.length)
                for (let i = 0; i < message.states.length; ++i)
                    $root.pb.FrostWithdrawState.encode(message.states[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FrostWithdrawStateList message, length delimited. Does not implicitly {@link pb.FrostWithdrawStateList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {pb.IFrostWithdrawStateList} message FrostWithdrawStateList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawStateList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostWithdrawStateList message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostWithdrawStateList} FrostWithdrawStateList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawStateList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostWithdrawStateList();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.states && message.states.length))
                            message.states = [];
                        message.states.push($root.pb.FrostWithdrawState.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostWithdrawStateList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostWithdrawStateList} FrostWithdrawStateList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawStateList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostWithdrawStateList message.
         * @function verify
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostWithdrawStateList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.states != null && message.hasOwnProperty("states")) {
                if (!Array.isArray(message.states))
                    return "states: array expected";
                for (let i = 0; i < message.states.length; ++i) {
                    let error = $root.pb.FrostWithdrawState.verify(message.states[i]);
                    if (error)
                        return "states." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FrostWithdrawStateList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostWithdrawStateList} FrostWithdrawStateList
         */
        FrostWithdrawStateList.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostWithdrawStateList)
                return object;
            let message = new $root.pb.FrostWithdrawStateList();
            if (object.states) {
                if (!Array.isArray(object.states))
                    throw TypeError(".pb.FrostWithdrawStateList.states: array expected");
                message.states = [];
                for (let i = 0; i < object.states.length; ++i) {
                    if (typeof object.states[i] !== "object")
                        throw TypeError(".pb.FrostWithdrawStateList.states: object expected");
                    message.states[i] = $root.pb.FrostWithdrawState.fromObject(object.states[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FrostWithdrawStateList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {pb.FrostWithdrawStateList} message FrostWithdrawStateList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostWithdrawStateList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.states = [];
            if (message.states && message.states.length) {
                object.states = [];
                for (let j = 0; j < message.states.length; ++j)
                    object.states[j] = $root.pb.FrostWithdrawState.toObject(message.states[j], options);
            }
            return object;
        };

        /**
         * Converts this FrostWithdrawStateList to JSON.
         * @function toJSON
         * @memberof pb.FrostWithdrawStateList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostWithdrawStateList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostWithdrawStateList
         * @function getTypeUrl
         * @memberof pb.FrostWithdrawStateList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostWithdrawStateList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostWithdrawStateList";
        };

        return FrostWithdrawStateList;
    })();

    pb.VaultTransitionStateList = (function() {

        /**
         * Properties of a VaultTransitionStateList.
         * @memberof pb
         * @interface IVaultTransitionStateList
         * @property {Array.<pb.IVaultTransitionState>|null} [states] VaultTransitionStateList states
         */

        /**
         * Constructs a new VaultTransitionStateList.
         * @memberof pb
         * @classdesc Represents a VaultTransitionStateList.
         * @implements IVaultTransitionStateList
         * @constructor
         * @param {pb.IVaultTransitionStateList=} [properties] Properties to set
         */
        function VaultTransitionStateList(properties) {
            this.states = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VaultTransitionStateList states.
         * @member {Array.<pb.IVaultTransitionState>} states
         * @memberof pb.VaultTransitionStateList
         * @instance
         */
        VaultTransitionStateList.prototype.states = $util.emptyArray;

        /**
         * Creates a new VaultTransitionStateList instance using the specified properties.
         * @function create
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {pb.IVaultTransitionStateList=} [properties] Properties to set
         * @returns {pb.VaultTransitionStateList} VaultTransitionStateList instance
         */
        VaultTransitionStateList.create = function create(properties) {
            return new VaultTransitionStateList(properties);
        };

        /**
         * Encodes the specified VaultTransitionStateList message. Does not implicitly {@link pb.VaultTransitionStateList.verify|verify} messages.
         * @function encode
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {pb.IVaultTransitionStateList} message VaultTransitionStateList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultTransitionStateList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.states != null && message.states.length)
                for (let i = 0; i < message.states.length; ++i)
                    $root.pb.VaultTransitionState.encode(message.states[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified VaultTransitionStateList message, length delimited. Does not implicitly {@link pb.VaultTransitionStateList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {pb.IVaultTransitionStateList} message VaultTransitionStateList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultTransitionStateList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VaultTransitionStateList message from the specified reader or buffer.
         * @function decode
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.VaultTransitionStateList} VaultTransitionStateList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultTransitionStateList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.VaultTransitionStateList();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.states && message.states.length))
                            message.states = [];
                        message.states.push($root.pb.VaultTransitionState.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VaultTransitionStateList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.VaultTransitionStateList} VaultTransitionStateList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultTransitionStateList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VaultTransitionStateList message.
         * @function verify
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VaultTransitionStateList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.states != null && message.hasOwnProperty("states")) {
                if (!Array.isArray(message.states))
                    return "states: array expected";
                for (let i = 0; i < message.states.length; ++i) {
                    let error = $root.pb.VaultTransitionState.verify(message.states[i]);
                    if (error)
                        return "states." + error;
                }
            }
            return null;
        };

        /**
         * Creates a VaultTransitionStateList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.VaultTransitionStateList} VaultTransitionStateList
         */
        VaultTransitionStateList.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.VaultTransitionStateList)
                return object;
            let message = new $root.pb.VaultTransitionStateList();
            if (object.states) {
                if (!Array.isArray(object.states))
                    throw TypeError(".pb.VaultTransitionStateList.states: array expected");
                message.states = [];
                for (let i = 0; i < object.states.length; ++i) {
                    if (typeof object.states[i] !== "object")
                        throw TypeError(".pb.VaultTransitionStateList.states: object expected");
                    message.states[i] = $root.pb.VaultTransitionState.fromObject(object.states[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a VaultTransitionStateList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {pb.VaultTransitionStateList} message VaultTransitionStateList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VaultTransitionStateList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.states = [];
            if (message.states && message.states.length) {
                object.states = [];
                for (let j = 0; j < message.states.length; ++j)
                    object.states[j] = $root.pb.VaultTransitionState.toObject(message.states[j], options);
            }
            return object;
        };

        /**
         * Converts this VaultTransitionStateList to JSON.
         * @function toJSON
         * @memberof pb.VaultTransitionStateList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VaultTransitionStateList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VaultTransitionStateList
         * @function getTypeUrl
         * @memberof pb.VaultTransitionStateList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VaultTransitionStateList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.VaultTransitionStateList";
        };

        return VaultTransitionStateList;
    })();

    pb.WitnessInfoList = (function() {

        /**
         * Properties of a WitnessInfoList.
         * @memberof pb
         * @interface IWitnessInfoList
         * @property {Array.<pb.IWitnessInfo>|null} [witnesses] WitnessInfoList witnesses
         */

        /**
         * Constructs a new WitnessInfoList.
         * @memberof pb
         * @classdesc Represents a WitnessInfoList.
         * @implements IWitnessInfoList
         * @constructor
         * @param {pb.IWitnessInfoList=} [properties] Properties to set
         */
        function WitnessInfoList(properties) {
            this.witnesses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessInfoList witnesses.
         * @member {Array.<pb.IWitnessInfo>} witnesses
         * @memberof pb.WitnessInfoList
         * @instance
         */
        WitnessInfoList.prototype.witnesses = $util.emptyArray;

        /**
         * Creates a new WitnessInfoList instance using the specified properties.
         * @function create
         * @memberof pb.WitnessInfoList
         * @static
         * @param {pb.IWitnessInfoList=} [properties] Properties to set
         * @returns {pb.WitnessInfoList} WitnessInfoList instance
         */
        WitnessInfoList.create = function create(properties) {
            return new WitnessInfoList(properties);
        };

        /**
         * Encodes the specified WitnessInfoList message. Does not implicitly {@link pb.WitnessInfoList.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessInfoList
         * @static
         * @param {pb.IWitnessInfoList} message WitnessInfoList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessInfoList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.witnesses != null && message.witnesses.length)
                for (let i = 0; i < message.witnesses.length; ++i)
                    $root.pb.WitnessInfo.encode(message.witnesses[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified WitnessInfoList message, length delimited. Does not implicitly {@link pb.WitnessInfoList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessInfoList
         * @static
         * @param {pb.IWitnessInfoList} message WitnessInfoList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessInfoList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessInfoList message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessInfoList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessInfoList} WitnessInfoList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessInfoList.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessInfoList();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.witnesses && message.witnesses.length))
                            message.witnesses = [];
                        message.witnesses.push($root.pb.WitnessInfo.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessInfoList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessInfoList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessInfoList} WitnessInfoList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessInfoList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessInfoList message.
         * @function verify
         * @memberof pb.WitnessInfoList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessInfoList.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.witnesses != null && message.hasOwnProperty("witnesses")) {
                if (!Array.isArray(message.witnesses))
                    return "witnesses: array expected";
                for (let i = 0; i < message.witnesses.length; ++i) {
                    let error = $root.pb.WitnessInfo.verify(message.witnesses[i]);
                    if (error)
                        return "witnesses." + error;
                }
            }
            return null;
        };

        /**
         * Creates a WitnessInfoList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessInfoList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessInfoList} WitnessInfoList
         */
        WitnessInfoList.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessInfoList)
                return object;
            let message = new $root.pb.WitnessInfoList();
            if (object.witnesses) {
                if (!Array.isArray(object.witnesses))
                    throw TypeError(".pb.WitnessInfoList.witnesses: array expected");
                message.witnesses = [];
                for (let i = 0; i < object.witnesses.length; ++i) {
                    if (typeof object.witnesses[i] !== "object")
                        throw TypeError(".pb.WitnessInfoList.witnesses: object expected");
                    message.witnesses[i] = $root.pb.WitnessInfo.fromObject(object.witnesses[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a WitnessInfoList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessInfoList
         * @static
         * @param {pb.WitnessInfoList} message WitnessInfoList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessInfoList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.witnesses = [];
            if (message.witnesses && message.witnesses.length) {
                object.witnesses = [];
                for (let j = 0; j < message.witnesses.length; ++j)
                    object.witnesses[j] = $root.pb.WitnessInfo.toObject(message.witnesses[j], options);
            }
            return object;
        };

        /**
         * Converts this WitnessInfoList to JSON.
         * @function toJSON
         * @memberof pb.WitnessInfoList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessInfoList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessInfoList
         * @function getTypeUrl
         * @memberof pb.WitnessInfoList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessInfoList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessInfoList";
        };

        return WitnessInfoList;
    })();

    pb.ArbitrationVoteTx = (function() {

        /**
         * Properties of an ArbitrationVoteTx.
         * @memberof pb
         * @interface IArbitrationVoteTx
         * @property {pb.IBaseMessage|null} [base] ArbitrationVoteTx base
         * @property {string|null} [challengeId] ArbitrationVoteTx challengeId
         * @property {pb.IWitnessVote|null} [vote] ArbitrationVoteTx vote
         */

        /**
         * Constructs a new ArbitrationVoteTx.
         * @memberof pb
         * @classdesc Represents an ArbitrationVoteTx.
         * @implements IArbitrationVoteTx
         * @constructor
         * @param {pb.IArbitrationVoteTx=} [properties] Properties to set
         */
        function ArbitrationVoteTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ArbitrationVoteTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.ArbitrationVoteTx
         * @instance
         */
        ArbitrationVoteTx.prototype.base = null;

        /**
         * ArbitrationVoteTx challengeId.
         * @member {string} challengeId
         * @memberof pb.ArbitrationVoteTx
         * @instance
         */
        ArbitrationVoteTx.prototype.challengeId = "";

        /**
         * ArbitrationVoteTx vote.
         * @member {pb.IWitnessVote|null|undefined} vote
         * @memberof pb.ArbitrationVoteTx
         * @instance
         */
        ArbitrationVoteTx.prototype.vote = null;

        /**
         * Creates a new ArbitrationVoteTx instance using the specified properties.
         * @function create
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {pb.IArbitrationVoteTx=} [properties] Properties to set
         * @returns {pb.ArbitrationVoteTx} ArbitrationVoteTx instance
         */
        ArbitrationVoteTx.create = function create(properties) {
            return new ArbitrationVoteTx(properties);
        };

        /**
         * Encodes the specified ArbitrationVoteTx message. Does not implicitly {@link pb.ArbitrationVoteTx.verify|verify} messages.
         * @function encode
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {pb.IArbitrationVoteTx} message ArbitrationVoteTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArbitrationVoteTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.challengeId != null && Object.hasOwnProperty.call(message, "challengeId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.challengeId);
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                $root.pb.WitnessVote.encode(message.vote, writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ArbitrationVoteTx message, length delimited. Does not implicitly {@link pb.ArbitrationVoteTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {pb.IArbitrationVoteTx} message ArbitrationVoteTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ArbitrationVoteTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an ArbitrationVoteTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ArbitrationVoteTx} ArbitrationVoteTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArbitrationVoteTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ArbitrationVoteTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.challengeId = reader.string();
                        break;
                    }
                case 3: {
                        message.vote = $root.pb.WitnessVote.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ArbitrationVoteTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ArbitrationVoteTx} ArbitrationVoteTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ArbitrationVoteTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ArbitrationVoteTx message.
         * @function verify
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ArbitrationVoteTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.challengeId != null && message.hasOwnProperty("challengeId"))
                if (!$util.isString(message.challengeId))
                    return "challengeId: string expected";
            if (message.vote != null && message.hasOwnProperty("vote")) {
                let error = $root.pb.WitnessVote.verify(message.vote);
                if (error)
                    return "vote." + error;
            }
            return null;
        };

        /**
         * Creates an ArbitrationVoteTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ArbitrationVoteTx} ArbitrationVoteTx
         */
        ArbitrationVoteTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ArbitrationVoteTx)
                return object;
            let message = new $root.pb.ArbitrationVoteTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.ArbitrationVoteTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.challengeId != null)
                message.challengeId = String(object.challengeId);
            if (object.vote != null) {
                if (typeof object.vote !== "object")
                    throw TypeError(".pb.ArbitrationVoteTx.vote: object expected");
                message.vote = $root.pb.WitnessVote.fromObject(object.vote);
            }
            return message;
        };

        /**
         * Creates a plain object from an ArbitrationVoteTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {pb.ArbitrationVoteTx} message ArbitrationVoteTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ArbitrationVoteTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.challengeId = "";
                object.vote = null;
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.challengeId != null && message.hasOwnProperty("challengeId"))
                object.challengeId = message.challengeId;
            if (message.vote != null && message.hasOwnProperty("vote"))
                object.vote = $root.pb.WitnessVote.toObject(message.vote, options);
            return object;
        };

        /**
         * Converts this ArbitrationVoteTx to JSON.
         * @function toJSON
         * @memberof pb.ArbitrationVoteTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ArbitrationVoteTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ArbitrationVoteTx
         * @function getTypeUrl
         * @memberof pb.ArbitrationVoteTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ArbitrationVoteTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ArbitrationVoteTx";
        };

        return ArbitrationVoteTx;
    })();

    pb.WitnessClaimRewardTx = (function() {

        /**
         * Properties of a WitnessClaimRewardTx.
         * @memberof pb
         * @interface IWitnessClaimRewardTx
         * @property {pb.IBaseMessage|null} [base] WitnessClaimRewardTx base
         */

        /**
         * Constructs a new WitnessClaimRewardTx.
         * @memberof pb
         * @classdesc Represents a WitnessClaimRewardTx.
         * @implements IWitnessClaimRewardTx
         * @constructor
         * @param {pb.IWitnessClaimRewardTx=} [properties] Properties to set
         */
        function WitnessClaimRewardTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessClaimRewardTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.WitnessClaimRewardTx
         * @instance
         */
        WitnessClaimRewardTx.prototype.base = null;

        /**
         * Creates a new WitnessClaimRewardTx instance using the specified properties.
         * @function create
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {pb.IWitnessClaimRewardTx=} [properties] Properties to set
         * @returns {pb.WitnessClaimRewardTx} WitnessClaimRewardTx instance
         */
        WitnessClaimRewardTx.create = function create(properties) {
            return new WitnessClaimRewardTx(properties);
        };

        /**
         * Encodes the specified WitnessClaimRewardTx message. Does not implicitly {@link pb.WitnessClaimRewardTx.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {pb.IWitnessClaimRewardTx} message WitnessClaimRewardTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessClaimRewardTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified WitnessClaimRewardTx message, length delimited. Does not implicitly {@link pb.WitnessClaimRewardTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {pb.IWitnessClaimRewardTx} message WitnessClaimRewardTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessClaimRewardTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessClaimRewardTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessClaimRewardTx} WitnessClaimRewardTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessClaimRewardTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessClaimRewardTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessClaimRewardTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessClaimRewardTx} WitnessClaimRewardTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessClaimRewardTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessClaimRewardTx message.
         * @function verify
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessClaimRewardTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            return null;
        };

        /**
         * Creates a WitnessClaimRewardTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessClaimRewardTx} WitnessClaimRewardTx
         */
        WitnessClaimRewardTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessClaimRewardTx)
                return object;
            let message = new $root.pb.WitnessClaimRewardTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.WitnessClaimRewardTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            return message;
        };

        /**
         * Creates a plain object from a WitnessClaimRewardTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {pb.WitnessClaimRewardTx} message WitnessClaimRewardTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessClaimRewardTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.base = null;
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            return object;
        };

        /**
         * Converts this WitnessClaimRewardTx to JSON.
         * @function toJSON
         * @memberof pb.WitnessClaimRewardTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessClaimRewardTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessClaimRewardTx
         * @function getTypeUrl
         * @memberof pb.WitnessClaimRewardTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessClaimRewardTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessClaimRewardTx";
        };

        return WitnessClaimRewardTx;
    })();

    pb.WitnessConfig = (function() {

        /**
         * Properties of a WitnessConfig.
         * @memberof pb
         * @interface IWitnessConfig
         * @property {number|null} [consensusThreshold] WitnessConfig consensusThreshold
         * @property {number|null} [abstainThreshold] WitnessConfig abstainThreshold
         * @property {number|Long|null} [votingPeriodBlocks] WitnessConfig votingPeriodBlocks
         * @property {number|Long|null} [challengePeriodBlocks] WitnessConfig challengePeriodBlocks
         * @property {number|Long|null} [arbitrationPeriodBlocks] WitnessConfig arbitrationPeriodBlocks
         * @property {number|Long|null} [unstakeLockBlocks] WitnessConfig unstakeLockBlocks
         * @property {string|null} [minStakeAmount] WitnessConfig minStakeAmount
         * @property {string|null} [challengeStakeAmount] WitnessConfig challengeStakeAmount
         * @property {number|null} [initialWitnessCount] WitnessConfig initialWitnessCount
         * @property {number|null} [expandMultiplier] WitnessConfig expandMultiplier
         * @property {number|null} [maxRounds] WitnessConfig maxRounds
         */

        /**
         * Constructs a new WitnessConfig.
         * @memberof pb
         * @classdesc Represents a WitnessConfig.
         * @implements IWitnessConfig
         * @constructor
         * @param {pb.IWitnessConfig=} [properties] Properties to set
         */
        function WitnessConfig(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WitnessConfig consensusThreshold.
         * @member {number} consensusThreshold
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.consensusThreshold = 0;

        /**
         * WitnessConfig abstainThreshold.
         * @member {number} abstainThreshold
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.abstainThreshold = 0;

        /**
         * WitnessConfig votingPeriodBlocks.
         * @member {number|Long} votingPeriodBlocks
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.votingPeriodBlocks = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessConfig challengePeriodBlocks.
         * @member {number|Long} challengePeriodBlocks
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.challengePeriodBlocks = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessConfig arbitrationPeriodBlocks.
         * @member {number|Long} arbitrationPeriodBlocks
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.arbitrationPeriodBlocks = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessConfig unstakeLockBlocks.
         * @member {number|Long} unstakeLockBlocks
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.unstakeLockBlocks = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * WitnessConfig minStakeAmount.
         * @member {string} minStakeAmount
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.minStakeAmount = "";

        /**
         * WitnessConfig challengeStakeAmount.
         * @member {string} challengeStakeAmount
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.challengeStakeAmount = "";

        /**
         * WitnessConfig initialWitnessCount.
         * @member {number} initialWitnessCount
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.initialWitnessCount = 0;

        /**
         * WitnessConfig expandMultiplier.
         * @member {number} expandMultiplier
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.expandMultiplier = 0;

        /**
         * WitnessConfig maxRounds.
         * @member {number} maxRounds
         * @memberof pb.WitnessConfig
         * @instance
         */
        WitnessConfig.prototype.maxRounds = 0;

        /**
         * Creates a new WitnessConfig instance using the specified properties.
         * @function create
         * @memberof pb.WitnessConfig
         * @static
         * @param {pb.IWitnessConfig=} [properties] Properties to set
         * @returns {pb.WitnessConfig} WitnessConfig instance
         */
        WitnessConfig.create = function create(properties) {
            return new WitnessConfig(properties);
        };

        /**
         * Encodes the specified WitnessConfig message. Does not implicitly {@link pb.WitnessConfig.verify|verify} messages.
         * @function encode
         * @memberof pb.WitnessConfig
         * @static
         * @param {pb.IWitnessConfig} message WitnessConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessConfig.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.consensusThreshold != null && Object.hasOwnProperty.call(message, "consensusThreshold"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.consensusThreshold);
            if (message.abstainThreshold != null && Object.hasOwnProperty.call(message, "abstainThreshold"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.abstainThreshold);
            if (message.votingPeriodBlocks != null && Object.hasOwnProperty.call(message, "votingPeriodBlocks"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.votingPeriodBlocks);
            if (message.challengePeriodBlocks != null && Object.hasOwnProperty.call(message, "challengePeriodBlocks"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.challengePeriodBlocks);
            if (message.arbitrationPeriodBlocks != null && Object.hasOwnProperty.call(message, "arbitrationPeriodBlocks"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.arbitrationPeriodBlocks);
            if (message.unstakeLockBlocks != null && Object.hasOwnProperty.call(message, "unstakeLockBlocks"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.unstakeLockBlocks);
            if (message.minStakeAmount != null && Object.hasOwnProperty.call(message, "minStakeAmount"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.minStakeAmount);
            if (message.challengeStakeAmount != null && Object.hasOwnProperty.call(message, "challengeStakeAmount"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.challengeStakeAmount);
            if (message.initialWitnessCount != null && Object.hasOwnProperty.call(message, "initialWitnessCount"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.initialWitnessCount);
            if (message.expandMultiplier != null && Object.hasOwnProperty.call(message, "expandMultiplier"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.expandMultiplier);
            if (message.maxRounds != null && Object.hasOwnProperty.call(message, "maxRounds"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.maxRounds);
            return writer;
        };

        /**
         * Encodes the specified WitnessConfig message, length delimited. Does not implicitly {@link pb.WitnessConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.WitnessConfig
         * @static
         * @param {pb.IWitnessConfig} message WitnessConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WitnessConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a WitnessConfig message from the specified reader or buffer.
         * @function decode
         * @memberof pb.WitnessConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.WitnessConfig} WitnessConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessConfig.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.WitnessConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.consensusThreshold = reader.uint32();
                        break;
                    }
                case 2: {
                        message.abstainThreshold = reader.uint32();
                        break;
                    }
                case 3: {
                        message.votingPeriodBlocks = reader.uint64();
                        break;
                    }
                case 4: {
                        message.challengePeriodBlocks = reader.uint64();
                        break;
                    }
                case 5: {
                        message.arbitrationPeriodBlocks = reader.uint64();
                        break;
                    }
                case 6: {
                        message.unstakeLockBlocks = reader.uint64();
                        break;
                    }
                case 7: {
                        message.minStakeAmount = reader.string();
                        break;
                    }
                case 8: {
                        message.challengeStakeAmount = reader.string();
                        break;
                    }
                case 9: {
                        message.initialWitnessCount = reader.uint32();
                        break;
                    }
                case 10: {
                        message.expandMultiplier = reader.uint32();
                        break;
                    }
                case 11: {
                        message.maxRounds = reader.uint32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WitnessConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.WitnessConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.WitnessConfig} WitnessConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WitnessConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WitnessConfig message.
         * @function verify
         * @memberof pb.WitnessConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WitnessConfig.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.consensusThreshold != null && message.hasOwnProperty("consensusThreshold"))
                if (!$util.isInteger(message.consensusThreshold))
                    return "consensusThreshold: integer expected";
            if (message.abstainThreshold != null && message.hasOwnProperty("abstainThreshold"))
                if (!$util.isInteger(message.abstainThreshold))
                    return "abstainThreshold: integer expected";
            if (message.votingPeriodBlocks != null && message.hasOwnProperty("votingPeriodBlocks"))
                if (!$util.isInteger(message.votingPeriodBlocks) && !(message.votingPeriodBlocks && $util.isInteger(message.votingPeriodBlocks.low) && $util.isInteger(message.votingPeriodBlocks.high)))
                    return "votingPeriodBlocks: integer|Long expected";
            if (message.challengePeriodBlocks != null && message.hasOwnProperty("challengePeriodBlocks"))
                if (!$util.isInteger(message.challengePeriodBlocks) && !(message.challengePeriodBlocks && $util.isInteger(message.challengePeriodBlocks.low) && $util.isInteger(message.challengePeriodBlocks.high)))
                    return "challengePeriodBlocks: integer|Long expected";
            if (message.arbitrationPeriodBlocks != null && message.hasOwnProperty("arbitrationPeriodBlocks"))
                if (!$util.isInteger(message.arbitrationPeriodBlocks) && !(message.arbitrationPeriodBlocks && $util.isInteger(message.arbitrationPeriodBlocks.low) && $util.isInteger(message.arbitrationPeriodBlocks.high)))
                    return "arbitrationPeriodBlocks: integer|Long expected";
            if (message.unstakeLockBlocks != null && message.hasOwnProperty("unstakeLockBlocks"))
                if (!$util.isInteger(message.unstakeLockBlocks) && !(message.unstakeLockBlocks && $util.isInteger(message.unstakeLockBlocks.low) && $util.isInteger(message.unstakeLockBlocks.high)))
                    return "unstakeLockBlocks: integer|Long expected";
            if (message.minStakeAmount != null && message.hasOwnProperty("minStakeAmount"))
                if (!$util.isString(message.minStakeAmount))
                    return "minStakeAmount: string expected";
            if (message.challengeStakeAmount != null && message.hasOwnProperty("challengeStakeAmount"))
                if (!$util.isString(message.challengeStakeAmount))
                    return "challengeStakeAmount: string expected";
            if (message.initialWitnessCount != null && message.hasOwnProperty("initialWitnessCount"))
                if (!$util.isInteger(message.initialWitnessCount))
                    return "initialWitnessCount: integer expected";
            if (message.expandMultiplier != null && message.hasOwnProperty("expandMultiplier"))
                if (!$util.isInteger(message.expandMultiplier))
                    return "expandMultiplier: integer expected";
            if (message.maxRounds != null && message.hasOwnProperty("maxRounds"))
                if (!$util.isInteger(message.maxRounds))
                    return "maxRounds: integer expected";
            return null;
        };

        /**
         * Creates a WitnessConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.WitnessConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.WitnessConfig} WitnessConfig
         */
        WitnessConfig.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.WitnessConfig)
                return object;
            let message = new $root.pb.WitnessConfig();
            if (object.consensusThreshold != null)
                message.consensusThreshold = object.consensusThreshold >>> 0;
            if (object.abstainThreshold != null)
                message.abstainThreshold = object.abstainThreshold >>> 0;
            if (object.votingPeriodBlocks != null)
                if ($util.Long)
                    (message.votingPeriodBlocks = $util.Long.fromValue(object.votingPeriodBlocks)).unsigned = true;
                else if (typeof object.votingPeriodBlocks === "string")
                    message.votingPeriodBlocks = parseInt(object.votingPeriodBlocks, 10);
                else if (typeof object.votingPeriodBlocks === "number")
                    message.votingPeriodBlocks = object.votingPeriodBlocks;
                else if (typeof object.votingPeriodBlocks === "object")
                    message.votingPeriodBlocks = new $util.LongBits(object.votingPeriodBlocks.low >>> 0, object.votingPeriodBlocks.high >>> 0).toNumber(true);
            if (object.challengePeriodBlocks != null)
                if ($util.Long)
                    (message.challengePeriodBlocks = $util.Long.fromValue(object.challengePeriodBlocks)).unsigned = true;
                else if (typeof object.challengePeriodBlocks === "string")
                    message.challengePeriodBlocks = parseInt(object.challengePeriodBlocks, 10);
                else if (typeof object.challengePeriodBlocks === "number")
                    message.challengePeriodBlocks = object.challengePeriodBlocks;
                else if (typeof object.challengePeriodBlocks === "object")
                    message.challengePeriodBlocks = new $util.LongBits(object.challengePeriodBlocks.low >>> 0, object.challengePeriodBlocks.high >>> 0).toNumber(true);
            if (object.arbitrationPeriodBlocks != null)
                if ($util.Long)
                    (message.arbitrationPeriodBlocks = $util.Long.fromValue(object.arbitrationPeriodBlocks)).unsigned = true;
                else if (typeof object.arbitrationPeriodBlocks === "string")
                    message.arbitrationPeriodBlocks = parseInt(object.arbitrationPeriodBlocks, 10);
                else if (typeof object.arbitrationPeriodBlocks === "number")
                    message.arbitrationPeriodBlocks = object.arbitrationPeriodBlocks;
                else if (typeof object.arbitrationPeriodBlocks === "object")
                    message.arbitrationPeriodBlocks = new $util.LongBits(object.arbitrationPeriodBlocks.low >>> 0, object.arbitrationPeriodBlocks.high >>> 0).toNumber(true);
            if (object.unstakeLockBlocks != null)
                if ($util.Long)
                    (message.unstakeLockBlocks = $util.Long.fromValue(object.unstakeLockBlocks)).unsigned = true;
                else if (typeof object.unstakeLockBlocks === "string")
                    message.unstakeLockBlocks = parseInt(object.unstakeLockBlocks, 10);
                else if (typeof object.unstakeLockBlocks === "number")
                    message.unstakeLockBlocks = object.unstakeLockBlocks;
                else if (typeof object.unstakeLockBlocks === "object")
                    message.unstakeLockBlocks = new $util.LongBits(object.unstakeLockBlocks.low >>> 0, object.unstakeLockBlocks.high >>> 0).toNumber(true);
            if (object.minStakeAmount != null)
                message.minStakeAmount = String(object.minStakeAmount);
            if (object.challengeStakeAmount != null)
                message.challengeStakeAmount = String(object.challengeStakeAmount);
            if (object.initialWitnessCount != null)
                message.initialWitnessCount = object.initialWitnessCount >>> 0;
            if (object.expandMultiplier != null)
                message.expandMultiplier = object.expandMultiplier >>> 0;
            if (object.maxRounds != null)
                message.maxRounds = object.maxRounds >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a WitnessConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.WitnessConfig
         * @static
         * @param {pb.WitnessConfig} message WitnessConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WitnessConfig.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.consensusThreshold = 0;
                object.abstainThreshold = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.votingPeriodBlocks = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.votingPeriodBlocks = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.challengePeriodBlocks = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.challengePeriodBlocks = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.arbitrationPeriodBlocks = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.arbitrationPeriodBlocks = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.unstakeLockBlocks = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.unstakeLockBlocks = options.longs === String ? "0" : 0;
                object.minStakeAmount = "";
                object.challengeStakeAmount = "";
                object.initialWitnessCount = 0;
                object.expandMultiplier = 0;
                object.maxRounds = 0;
            }
            if (message.consensusThreshold != null && message.hasOwnProperty("consensusThreshold"))
                object.consensusThreshold = message.consensusThreshold;
            if (message.abstainThreshold != null && message.hasOwnProperty("abstainThreshold"))
                object.abstainThreshold = message.abstainThreshold;
            if (message.votingPeriodBlocks != null && message.hasOwnProperty("votingPeriodBlocks"))
                if (typeof message.votingPeriodBlocks === "number")
                    object.votingPeriodBlocks = options.longs === String ? String(message.votingPeriodBlocks) : message.votingPeriodBlocks;
                else
                    object.votingPeriodBlocks = options.longs === String ? $util.Long.prototype.toString.call(message.votingPeriodBlocks) : options.longs === Number ? new $util.LongBits(message.votingPeriodBlocks.low >>> 0, message.votingPeriodBlocks.high >>> 0).toNumber(true) : message.votingPeriodBlocks;
            if (message.challengePeriodBlocks != null && message.hasOwnProperty("challengePeriodBlocks"))
                if (typeof message.challengePeriodBlocks === "number")
                    object.challengePeriodBlocks = options.longs === String ? String(message.challengePeriodBlocks) : message.challengePeriodBlocks;
                else
                    object.challengePeriodBlocks = options.longs === String ? $util.Long.prototype.toString.call(message.challengePeriodBlocks) : options.longs === Number ? new $util.LongBits(message.challengePeriodBlocks.low >>> 0, message.challengePeriodBlocks.high >>> 0).toNumber(true) : message.challengePeriodBlocks;
            if (message.arbitrationPeriodBlocks != null && message.hasOwnProperty("arbitrationPeriodBlocks"))
                if (typeof message.arbitrationPeriodBlocks === "number")
                    object.arbitrationPeriodBlocks = options.longs === String ? String(message.arbitrationPeriodBlocks) : message.arbitrationPeriodBlocks;
                else
                    object.arbitrationPeriodBlocks = options.longs === String ? $util.Long.prototype.toString.call(message.arbitrationPeriodBlocks) : options.longs === Number ? new $util.LongBits(message.arbitrationPeriodBlocks.low >>> 0, message.arbitrationPeriodBlocks.high >>> 0).toNumber(true) : message.arbitrationPeriodBlocks;
            if (message.unstakeLockBlocks != null && message.hasOwnProperty("unstakeLockBlocks"))
                if (typeof message.unstakeLockBlocks === "number")
                    object.unstakeLockBlocks = options.longs === String ? String(message.unstakeLockBlocks) : message.unstakeLockBlocks;
                else
                    object.unstakeLockBlocks = options.longs === String ? $util.Long.prototype.toString.call(message.unstakeLockBlocks) : options.longs === Number ? new $util.LongBits(message.unstakeLockBlocks.low >>> 0, message.unstakeLockBlocks.high >>> 0).toNumber(true) : message.unstakeLockBlocks;
            if (message.minStakeAmount != null && message.hasOwnProperty("minStakeAmount"))
                object.minStakeAmount = message.minStakeAmount;
            if (message.challengeStakeAmount != null && message.hasOwnProperty("challengeStakeAmount"))
                object.challengeStakeAmount = message.challengeStakeAmount;
            if (message.initialWitnessCount != null && message.hasOwnProperty("initialWitnessCount"))
                object.initialWitnessCount = message.initialWitnessCount;
            if (message.expandMultiplier != null && message.hasOwnProperty("expandMultiplier"))
                object.expandMultiplier = message.expandMultiplier;
            if (message.maxRounds != null && message.hasOwnProperty("maxRounds"))
                object.maxRounds = message.maxRounds;
            return object;
        };

        /**
         * Converts this WitnessConfig to JSON.
         * @function toJSON
         * @memberof pb.WitnessConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WitnessConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WitnessConfig
         * @function getTypeUrl
         * @memberof pb.WitnessConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WitnessConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.WitnessConfig";
        };

        return WitnessConfig;
    })();

    pb.FrostWithdrawRequestTx = (function() {

        /**
         * Properties of a FrostWithdrawRequestTx.
         * @memberof pb
         * @interface IFrostWithdrawRequestTx
         * @property {pb.IBaseMessage|null} [base] FrostWithdrawRequestTx base
         * @property {string|null} [chain] FrostWithdrawRequestTx chain
         * @property {string|null} [asset] FrostWithdrawRequestTx asset
         * @property {string|null} [to] FrostWithdrawRequestTx to
         * @property {string|null} [amount] FrostWithdrawRequestTx amount
         */

        /**
         * Constructs a new FrostWithdrawRequestTx.
         * @memberof pb
         * @classdesc Represents a FrostWithdrawRequestTx.
         * @implements IFrostWithdrawRequestTx
         * @constructor
         * @param {pb.IFrostWithdrawRequestTx=} [properties] Properties to set
         */
        function FrostWithdrawRequestTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostWithdrawRequestTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostWithdrawRequestTx
         * @instance
         */
        FrostWithdrawRequestTx.prototype.base = null;

        /**
         * FrostWithdrawRequestTx chain.
         * @member {string} chain
         * @memberof pb.FrostWithdrawRequestTx
         * @instance
         */
        FrostWithdrawRequestTx.prototype.chain = "";

        /**
         * FrostWithdrawRequestTx asset.
         * @member {string} asset
         * @memberof pb.FrostWithdrawRequestTx
         * @instance
         */
        FrostWithdrawRequestTx.prototype.asset = "";

        /**
         * FrostWithdrawRequestTx to.
         * @member {string} to
         * @memberof pb.FrostWithdrawRequestTx
         * @instance
         */
        FrostWithdrawRequestTx.prototype.to = "";

        /**
         * FrostWithdrawRequestTx amount.
         * @member {string} amount
         * @memberof pb.FrostWithdrawRequestTx
         * @instance
         */
        FrostWithdrawRequestTx.prototype.amount = "";

        /**
         * Creates a new FrostWithdrawRequestTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {pb.IFrostWithdrawRequestTx=} [properties] Properties to set
         * @returns {pb.FrostWithdrawRequestTx} FrostWithdrawRequestTx instance
         */
        FrostWithdrawRequestTx.create = function create(properties) {
            return new FrostWithdrawRequestTx(properties);
        };

        /**
         * Encodes the specified FrostWithdrawRequestTx message. Does not implicitly {@link pb.FrostWithdrawRequestTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {pb.IFrostWithdrawRequestTx} message FrostWithdrawRequestTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawRequestTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.asset != null && Object.hasOwnProperty.call(message, "asset"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.asset);
            if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.to);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.amount);
            return writer;
        };

        /**
         * Encodes the specified FrostWithdrawRequestTx message, length delimited. Does not implicitly {@link pb.FrostWithdrawRequestTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {pb.IFrostWithdrawRequestTx} message FrostWithdrawRequestTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawRequestTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostWithdrawRequestTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostWithdrawRequestTx} FrostWithdrawRequestTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawRequestTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostWithdrawRequestTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.asset = reader.string();
                        break;
                    }
                case 4: {
                        message.to = reader.string();
                        break;
                    }
                case 5: {
                        message.amount = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostWithdrawRequestTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostWithdrawRequestTx} FrostWithdrawRequestTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawRequestTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostWithdrawRequestTx message.
         * @function verify
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostWithdrawRequestTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.asset != null && message.hasOwnProperty("asset"))
                if (!$util.isString(message.asset))
                    return "asset: string expected";
            if (message.to != null && message.hasOwnProperty("to"))
                if (!$util.isString(message.to))
                    return "to: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            return null;
        };

        /**
         * Creates a FrostWithdrawRequestTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostWithdrawRequestTx} FrostWithdrawRequestTx
         */
        FrostWithdrawRequestTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostWithdrawRequestTx)
                return object;
            let message = new $root.pb.FrostWithdrawRequestTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostWithdrawRequestTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.asset != null)
                message.asset = String(object.asset);
            if (object.to != null)
                message.to = String(object.to);
            if (object.amount != null)
                message.amount = String(object.amount);
            return message;
        };

        /**
         * Creates a plain object from a FrostWithdrawRequestTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {pb.FrostWithdrawRequestTx} message FrostWithdrawRequestTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostWithdrawRequestTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.chain = "";
                object.asset = "";
                object.to = "";
                object.amount = "";
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.asset != null && message.hasOwnProperty("asset"))
                object.asset = message.asset;
            if (message.to != null && message.hasOwnProperty("to"))
                object.to = message.to;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            return object;
        };

        /**
         * Converts this FrostWithdrawRequestTx to JSON.
         * @function toJSON
         * @memberof pb.FrostWithdrawRequestTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostWithdrawRequestTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostWithdrawRequestTx
         * @function getTypeUrl
         * @memberof pb.FrostWithdrawRequestTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostWithdrawRequestTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostWithdrawRequestTx";
        };

        return FrostWithdrawRequestTx;
    })();

    pb.FrostWithdrawSignedTx = (function() {

        /**
         * Properties of a FrostWithdrawSignedTx.
         * @memberof pb
         * @interface IFrostWithdrawSignedTx
         * @property {pb.IBaseMessage|null} [base] FrostWithdrawSignedTx base
         * @property {string|null} [jobId] FrostWithdrawSignedTx jobId
         * @property {Uint8Array|null} [signedPackageBytes] FrostWithdrawSignedTx signedPackageBytes
         * @property {Array.<string>|null} [withdrawIds] FrostWithdrawSignedTx withdrawIds
         * @property {number|null} [vaultId] FrostWithdrawSignedTx vaultId
         * @property {number|Long|null} [keyEpoch] FrostWithdrawSignedTx keyEpoch
         * @property {Uint8Array|null} [templateHash] FrostWithdrawSignedTx templateHash
         * @property {string|null} [chain] FrostWithdrawSignedTx chain
         * @property {string|null} [asset] FrostWithdrawSignedTx asset
         * @property {Array.<pb.IUtxoInput>|null} [utxoInputs] FrostWithdrawSignedTx utxoInputs
         * @property {Uint8Array|null} [templateData] FrostWithdrawSignedTx templateData
         * @property {Array.<Uint8Array>|null} [inputSigs] FrostWithdrawSignedTx inputSigs
         * @property {Array.<Uint8Array>|null} [scriptPubkeys] FrostWithdrawSignedTx scriptPubkeys
         */

        /**
         * Constructs a new FrostWithdrawSignedTx.
         * @memberof pb
         * @classdesc Represents a FrostWithdrawSignedTx.
         * @implements IFrostWithdrawSignedTx
         * @constructor
         * @param {pb.IFrostWithdrawSignedTx=} [properties] Properties to set
         */
        function FrostWithdrawSignedTx(properties) {
            this.withdrawIds = [];
            this.utxoInputs = [];
            this.inputSigs = [];
            this.scriptPubkeys = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostWithdrawSignedTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.base = null;

        /**
         * FrostWithdrawSignedTx jobId.
         * @member {string} jobId
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.jobId = "";

        /**
         * FrostWithdrawSignedTx signedPackageBytes.
         * @member {Uint8Array} signedPackageBytes
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.signedPackageBytes = $util.newBuffer([]);

        /**
         * FrostWithdrawSignedTx withdrawIds.
         * @member {Array.<string>} withdrawIds
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.withdrawIds = $util.emptyArray;

        /**
         * FrostWithdrawSignedTx vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.vaultId = 0;

        /**
         * FrostWithdrawSignedTx keyEpoch.
         * @member {number|Long} keyEpoch
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.keyEpoch = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostWithdrawSignedTx templateHash.
         * @member {Uint8Array} templateHash
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.templateHash = $util.newBuffer([]);

        /**
         * FrostWithdrawSignedTx chain.
         * @member {string} chain
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.chain = "";

        /**
         * FrostWithdrawSignedTx asset.
         * @member {string} asset
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.asset = "";

        /**
         * FrostWithdrawSignedTx utxoInputs.
         * @member {Array.<pb.IUtxoInput>} utxoInputs
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.utxoInputs = $util.emptyArray;

        /**
         * FrostWithdrawSignedTx templateData.
         * @member {Uint8Array} templateData
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.templateData = $util.newBuffer([]);

        /**
         * FrostWithdrawSignedTx inputSigs.
         * @member {Array.<Uint8Array>} inputSigs
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.inputSigs = $util.emptyArray;

        /**
         * FrostWithdrawSignedTx scriptPubkeys.
         * @member {Array.<Uint8Array>} scriptPubkeys
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         */
        FrostWithdrawSignedTx.prototype.scriptPubkeys = $util.emptyArray;

        /**
         * Creates a new FrostWithdrawSignedTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {pb.IFrostWithdrawSignedTx=} [properties] Properties to set
         * @returns {pb.FrostWithdrawSignedTx} FrostWithdrawSignedTx instance
         */
        FrostWithdrawSignedTx.create = function create(properties) {
            return new FrostWithdrawSignedTx(properties);
        };

        /**
         * Encodes the specified FrostWithdrawSignedTx message. Does not implicitly {@link pb.FrostWithdrawSignedTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {pb.IFrostWithdrawSignedTx} message FrostWithdrawSignedTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawSignedTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.jobId);
            if (message.signedPackageBytes != null && Object.hasOwnProperty.call(message, "signedPackageBytes"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.signedPackageBytes);
            if (message.withdrawIds != null && message.withdrawIds.length)
                for (let i = 0; i < message.withdrawIds.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.withdrawIds[i]);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.vaultId);
            if (message.keyEpoch != null && Object.hasOwnProperty.call(message, "keyEpoch"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.keyEpoch);
            if (message.templateHash != null && Object.hasOwnProperty.call(message, "templateHash"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.templateHash);
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.chain);
            if (message.asset != null && Object.hasOwnProperty.call(message, "asset"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.asset);
            if (message.utxoInputs != null && message.utxoInputs.length)
                for (let i = 0; i < message.utxoInputs.length; ++i)
                    $root.pb.UtxoInput.encode(message.utxoInputs[i], writer.uint32(/* id 10, wireType 2 =*/82).fork()).ldelim();
            if (message.templateData != null && Object.hasOwnProperty.call(message, "templateData"))
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.templateData);
            if (message.inputSigs != null && message.inputSigs.length)
                for (let i = 0; i < message.inputSigs.length; ++i)
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.inputSigs[i]);
            if (message.scriptPubkeys != null && message.scriptPubkeys.length)
                for (let i = 0; i < message.scriptPubkeys.length; ++i)
                    writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.scriptPubkeys[i]);
            return writer;
        };

        /**
         * Encodes the specified FrostWithdrawSignedTx message, length delimited. Does not implicitly {@link pb.FrostWithdrawSignedTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {pb.IFrostWithdrawSignedTx} message FrostWithdrawSignedTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawSignedTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostWithdrawSignedTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostWithdrawSignedTx} FrostWithdrawSignedTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawSignedTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostWithdrawSignedTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.jobId = reader.string();
                        break;
                    }
                case 3: {
                        message.signedPackageBytes = reader.bytes();
                        break;
                    }
                case 4: {
                        if (!(message.withdrawIds && message.withdrawIds.length))
                            message.withdrawIds = [];
                        message.withdrawIds.push(reader.string());
                        break;
                    }
                case 5: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 6: {
                        message.keyEpoch = reader.uint64();
                        break;
                    }
                case 7: {
                        message.templateHash = reader.bytes();
                        break;
                    }
                case 8: {
                        message.chain = reader.string();
                        break;
                    }
                case 9: {
                        message.asset = reader.string();
                        break;
                    }
                case 10: {
                        if (!(message.utxoInputs && message.utxoInputs.length))
                            message.utxoInputs = [];
                        message.utxoInputs.push($root.pb.UtxoInput.decode(reader, reader.uint32()));
                        break;
                    }
                case 11: {
                        message.templateData = reader.bytes();
                        break;
                    }
                case 12: {
                        if (!(message.inputSigs && message.inputSigs.length))
                            message.inputSigs = [];
                        message.inputSigs.push(reader.bytes());
                        break;
                    }
                case 13: {
                        if (!(message.scriptPubkeys && message.scriptPubkeys.length))
                            message.scriptPubkeys = [];
                        message.scriptPubkeys.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostWithdrawSignedTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostWithdrawSignedTx} FrostWithdrawSignedTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawSignedTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostWithdrawSignedTx message.
         * @function verify
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostWithdrawSignedTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                if (!$util.isString(message.jobId))
                    return "jobId: string expected";
            if (message.signedPackageBytes != null && message.hasOwnProperty("signedPackageBytes"))
                if (!(message.signedPackageBytes && typeof message.signedPackageBytes.length === "number" || $util.isString(message.signedPackageBytes)))
                    return "signedPackageBytes: buffer expected";
            if (message.withdrawIds != null && message.hasOwnProperty("withdrawIds")) {
                if (!Array.isArray(message.withdrawIds))
                    return "withdrawIds: array expected";
                for (let i = 0; i < message.withdrawIds.length; ++i)
                    if (!$util.isString(message.withdrawIds[i]))
                        return "withdrawIds: string[] expected";
            }
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.keyEpoch != null && message.hasOwnProperty("keyEpoch"))
                if (!$util.isInteger(message.keyEpoch) && !(message.keyEpoch && $util.isInteger(message.keyEpoch.low) && $util.isInteger(message.keyEpoch.high)))
                    return "keyEpoch: integer|Long expected";
            if (message.templateHash != null && message.hasOwnProperty("templateHash"))
                if (!(message.templateHash && typeof message.templateHash.length === "number" || $util.isString(message.templateHash)))
                    return "templateHash: buffer expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.asset != null && message.hasOwnProperty("asset"))
                if (!$util.isString(message.asset))
                    return "asset: string expected";
            if (message.utxoInputs != null && message.hasOwnProperty("utxoInputs")) {
                if (!Array.isArray(message.utxoInputs))
                    return "utxoInputs: array expected";
                for (let i = 0; i < message.utxoInputs.length; ++i) {
                    let error = $root.pb.UtxoInput.verify(message.utxoInputs[i]);
                    if (error)
                        return "utxoInputs." + error;
                }
            }
            if (message.templateData != null && message.hasOwnProperty("templateData"))
                if (!(message.templateData && typeof message.templateData.length === "number" || $util.isString(message.templateData)))
                    return "templateData: buffer expected";
            if (message.inputSigs != null && message.hasOwnProperty("inputSigs")) {
                if (!Array.isArray(message.inputSigs))
                    return "inputSigs: array expected";
                for (let i = 0; i < message.inputSigs.length; ++i)
                    if (!(message.inputSigs[i] && typeof message.inputSigs[i].length === "number" || $util.isString(message.inputSigs[i])))
                        return "inputSigs: buffer[] expected";
            }
            if (message.scriptPubkeys != null && message.hasOwnProperty("scriptPubkeys")) {
                if (!Array.isArray(message.scriptPubkeys))
                    return "scriptPubkeys: array expected";
                for (let i = 0; i < message.scriptPubkeys.length; ++i)
                    if (!(message.scriptPubkeys[i] && typeof message.scriptPubkeys[i].length === "number" || $util.isString(message.scriptPubkeys[i])))
                        return "scriptPubkeys: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a FrostWithdrawSignedTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostWithdrawSignedTx} FrostWithdrawSignedTx
         */
        FrostWithdrawSignedTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostWithdrawSignedTx)
                return object;
            let message = new $root.pb.FrostWithdrawSignedTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostWithdrawSignedTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.jobId != null)
                message.jobId = String(object.jobId);
            if (object.signedPackageBytes != null)
                if (typeof object.signedPackageBytes === "string")
                    $util.base64.decode(object.signedPackageBytes, message.signedPackageBytes = $util.newBuffer($util.base64.length(object.signedPackageBytes)), 0);
                else if (object.signedPackageBytes.length >= 0)
                    message.signedPackageBytes = object.signedPackageBytes;
            if (object.withdrawIds) {
                if (!Array.isArray(object.withdrawIds))
                    throw TypeError(".pb.FrostWithdrawSignedTx.withdrawIds: array expected");
                message.withdrawIds = [];
                for (let i = 0; i < object.withdrawIds.length; ++i)
                    message.withdrawIds[i] = String(object.withdrawIds[i]);
            }
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.keyEpoch != null)
                if ($util.Long)
                    (message.keyEpoch = $util.Long.fromValue(object.keyEpoch)).unsigned = true;
                else if (typeof object.keyEpoch === "string")
                    message.keyEpoch = parseInt(object.keyEpoch, 10);
                else if (typeof object.keyEpoch === "number")
                    message.keyEpoch = object.keyEpoch;
                else if (typeof object.keyEpoch === "object")
                    message.keyEpoch = new $util.LongBits(object.keyEpoch.low >>> 0, object.keyEpoch.high >>> 0).toNumber(true);
            if (object.templateHash != null)
                if (typeof object.templateHash === "string")
                    $util.base64.decode(object.templateHash, message.templateHash = $util.newBuffer($util.base64.length(object.templateHash)), 0);
                else if (object.templateHash.length >= 0)
                    message.templateHash = object.templateHash;
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.asset != null)
                message.asset = String(object.asset);
            if (object.utxoInputs) {
                if (!Array.isArray(object.utxoInputs))
                    throw TypeError(".pb.FrostWithdrawSignedTx.utxoInputs: array expected");
                message.utxoInputs = [];
                for (let i = 0; i < object.utxoInputs.length; ++i) {
                    if (typeof object.utxoInputs[i] !== "object")
                        throw TypeError(".pb.FrostWithdrawSignedTx.utxoInputs: object expected");
                    message.utxoInputs[i] = $root.pb.UtxoInput.fromObject(object.utxoInputs[i]);
                }
            }
            if (object.templateData != null)
                if (typeof object.templateData === "string")
                    $util.base64.decode(object.templateData, message.templateData = $util.newBuffer($util.base64.length(object.templateData)), 0);
                else if (object.templateData.length >= 0)
                    message.templateData = object.templateData;
            if (object.inputSigs) {
                if (!Array.isArray(object.inputSigs))
                    throw TypeError(".pb.FrostWithdrawSignedTx.inputSigs: array expected");
                message.inputSigs = [];
                for (let i = 0; i < object.inputSigs.length; ++i)
                    if (typeof object.inputSigs[i] === "string")
                        $util.base64.decode(object.inputSigs[i], message.inputSigs[i] = $util.newBuffer($util.base64.length(object.inputSigs[i])), 0);
                    else if (object.inputSigs[i].length >= 0)
                        message.inputSigs[i] = object.inputSigs[i];
            }
            if (object.scriptPubkeys) {
                if (!Array.isArray(object.scriptPubkeys))
                    throw TypeError(".pb.FrostWithdrawSignedTx.scriptPubkeys: array expected");
                message.scriptPubkeys = [];
                for (let i = 0; i < object.scriptPubkeys.length; ++i)
                    if (typeof object.scriptPubkeys[i] === "string")
                        $util.base64.decode(object.scriptPubkeys[i], message.scriptPubkeys[i] = $util.newBuffer($util.base64.length(object.scriptPubkeys[i])), 0);
                    else if (object.scriptPubkeys[i].length >= 0)
                        message.scriptPubkeys[i] = object.scriptPubkeys[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a FrostWithdrawSignedTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {pb.FrostWithdrawSignedTx} message FrostWithdrawSignedTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostWithdrawSignedTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.withdrawIds = [];
                object.utxoInputs = [];
                object.inputSigs = [];
                object.scriptPubkeys = [];
            }
            if (options.defaults) {
                object.base = null;
                object.jobId = "";
                if (options.bytes === String)
                    object.signedPackageBytes = "";
                else {
                    object.signedPackageBytes = [];
                    if (options.bytes !== Array)
                        object.signedPackageBytes = $util.newBuffer(object.signedPackageBytes);
                }
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.keyEpoch = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.keyEpoch = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.templateHash = "";
                else {
                    object.templateHash = [];
                    if (options.bytes !== Array)
                        object.templateHash = $util.newBuffer(object.templateHash);
                }
                object.chain = "";
                object.asset = "";
                if (options.bytes === String)
                    object.templateData = "";
                else {
                    object.templateData = [];
                    if (options.bytes !== Array)
                        object.templateData = $util.newBuffer(object.templateData);
                }
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                object.jobId = message.jobId;
            if (message.signedPackageBytes != null && message.hasOwnProperty("signedPackageBytes"))
                object.signedPackageBytes = options.bytes === String ? $util.base64.encode(message.signedPackageBytes, 0, message.signedPackageBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.signedPackageBytes) : message.signedPackageBytes;
            if (message.withdrawIds && message.withdrawIds.length) {
                object.withdrawIds = [];
                for (let j = 0; j < message.withdrawIds.length; ++j)
                    object.withdrawIds[j] = message.withdrawIds[j];
            }
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.keyEpoch != null && message.hasOwnProperty("keyEpoch"))
                if (typeof message.keyEpoch === "number")
                    object.keyEpoch = options.longs === String ? String(message.keyEpoch) : message.keyEpoch;
                else
                    object.keyEpoch = options.longs === String ? $util.Long.prototype.toString.call(message.keyEpoch) : options.longs === Number ? new $util.LongBits(message.keyEpoch.low >>> 0, message.keyEpoch.high >>> 0).toNumber(true) : message.keyEpoch;
            if (message.templateHash != null && message.hasOwnProperty("templateHash"))
                object.templateHash = options.bytes === String ? $util.base64.encode(message.templateHash, 0, message.templateHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.templateHash) : message.templateHash;
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.asset != null && message.hasOwnProperty("asset"))
                object.asset = message.asset;
            if (message.utxoInputs && message.utxoInputs.length) {
                object.utxoInputs = [];
                for (let j = 0; j < message.utxoInputs.length; ++j)
                    object.utxoInputs[j] = $root.pb.UtxoInput.toObject(message.utxoInputs[j], options);
            }
            if (message.templateData != null && message.hasOwnProperty("templateData"))
                object.templateData = options.bytes === String ? $util.base64.encode(message.templateData, 0, message.templateData.length) : options.bytes === Array ? Array.prototype.slice.call(message.templateData) : message.templateData;
            if (message.inputSigs && message.inputSigs.length) {
                object.inputSigs = [];
                for (let j = 0; j < message.inputSigs.length; ++j)
                    object.inputSigs[j] = options.bytes === String ? $util.base64.encode(message.inputSigs[j], 0, message.inputSigs[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.inputSigs[j]) : message.inputSigs[j];
            }
            if (message.scriptPubkeys && message.scriptPubkeys.length) {
                object.scriptPubkeys = [];
                for (let j = 0; j < message.scriptPubkeys.length; ++j)
                    object.scriptPubkeys[j] = options.bytes === String ? $util.base64.encode(message.scriptPubkeys[j], 0, message.scriptPubkeys[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.scriptPubkeys[j]) : message.scriptPubkeys[j];
            }
            return object;
        };

        /**
         * Converts this FrostWithdrawSignedTx to JSON.
         * @function toJSON
         * @memberof pb.FrostWithdrawSignedTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostWithdrawSignedTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostWithdrawSignedTx
         * @function getTypeUrl
         * @memberof pb.FrostWithdrawSignedTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostWithdrawSignedTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostWithdrawSignedTx";
        };

        return FrostWithdrawSignedTx;
    })();

    pb.UtxoInput = (function() {

        /**
         * Properties of an UtxoInput.
         * @memberof pb
         * @interface IUtxoInput
         * @property {string|null} [txid] UtxoInput txid
         * @property {number|null} [vout] UtxoInput vout
         * @property {number|Long|null} [amount] UtxoInput amount
         * @property {Uint8Array|null} [scriptPubkey] UtxoInput scriptPubkey
         */

        /**
         * Constructs a new UtxoInput.
         * @memberof pb
         * @classdesc Represents an UtxoInput.
         * @implements IUtxoInput
         * @constructor
         * @param {pb.IUtxoInput=} [properties] Properties to set
         */
        function UtxoInput(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UtxoInput txid.
         * @member {string} txid
         * @memberof pb.UtxoInput
         * @instance
         */
        UtxoInput.prototype.txid = "";

        /**
         * UtxoInput vout.
         * @member {number} vout
         * @memberof pb.UtxoInput
         * @instance
         */
        UtxoInput.prototype.vout = 0;

        /**
         * UtxoInput amount.
         * @member {number|Long} amount
         * @memberof pb.UtxoInput
         * @instance
         */
        UtxoInput.prototype.amount = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * UtxoInput scriptPubkey.
         * @member {Uint8Array} scriptPubkey
         * @memberof pb.UtxoInput
         * @instance
         */
        UtxoInput.prototype.scriptPubkey = $util.newBuffer([]);

        /**
         * Creates a new UtxoInput instance using the specified properties.
         * @function create
         * @memberof pb.UtxoInput
         * @static
         * @param {pb.IUtxoInput=} [properties] Properties to set
         * @returns {pb.UtxoInput} UtxoInput instance
         */
        UtxoInput.create = function create(properties) {
            return new UtxoInput(properties);
        };

        /**
         * Encodes the specified UtxoInput message. Does not implicitly {@link pb.UtxoInput.verify|verify} messages.
         * @function encode
         * @memberof pb.UtxoInput
         * @static
         * @param {pb.IUtxoInput} message UtxoInput message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UtxoInput.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.txid != null && Object.hasOwnProperty.call(message, "txid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txid);
            if (message.vout != null && Object.hasOwnProperty.call(message, "vout"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vout);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.amount);
            if (message.scriptPubkey != null && Object.hasOwnProperty.call(message, "scriptPubkey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.scriptPubkey);
            return writer;
        };

        /**
         * Encodes the specified UtxoInput message, length delimited. Does not implicitly {@link pb.UtxoInput.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.UtxoInput
         * @static
         * @param {pb.IUtxoInput} message UtxoInput message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UtxoInput.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes an UtxoInput message from the specified reader or buffer.
         * @function decode
         * @memberof pb.UtxoInput
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.UtxoInput} UtxoInput
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UtxoInput.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.UtxoInput();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.txid = reader.string();
                        break;
                    }
                case 2: {
                        message.vout = reader.uint32();
                        break;
                    }
                case 3: {
                        message.amount = reader.uint64();
                        break;
                    }
                case 4: {
                        message.scriptPubkey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an UtxoInput message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.UtxoInput
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.UtxoInput} UtxoInput
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UtxoInput.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UtxoInput message.
         * @function verify
         * @memberof pb.UtxoInput
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UtxoInput.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.txid != null && message.hasOwnProperty("txid"))
                if (!$util.isString(message.txid))
                    return "txid: string expected";
            if (message.vout != null && message.hasOwnProperty("vout"))
                if (!$util.isInteger(message.vout))
                    return "vout: integer expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isInteger(message.amount) && !(message.amount && $util.isInteger(message.amount.low) && $util.isInteger(message.amount.high)))
                    return "amount: integer|Long expected";
            if (message.scriptPubkey != null && message.hasOwnProperty("scriptPubkey"))
                if (!(message.scriptPubkey && typeof message.scriptPubkey.length === "number" || $util.isString(message.scriptPubkey)))
                    return "scriptPubkey: buffer expected";
            return null;
        };

        /**
         * Creates an UtxoInput message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.UtxoInput
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.UtxoInput} UtxoInput
         */
        UtxoInput.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.UtxoInput)
                return object;
            let message = new $root.pb.UtxoInput();
            if (object.txid != null)
                message.txid = String(object.txid);
            if (object.vout != null)
                message.vout = object.vout >>> 0;
            if (object.amount != null)
                if ($util.Long)
                    (message.amount = $util.Long.fromValue(object.amount)).unsigned = true;
                else if (typeof object.amount === "string")
                    message.amount = parseInt(object.amount, 10);
                else if (typeof object.amount === "number")
                    message.amount = object.amount;
                else if (typeof object.amount === "object")
                    message.amount = new $util.LongBits(object.amount.low >>> 0, object.amount.high >>> 0).toNumber(true);
            if (object.scriptPubkey != null)
                if (typeof object.scriptPubkey === "string")
                    $util.base64.decode(object.scriptPubkey, message.scriptPubkey = $util.newBuffer($util.base64.length(object.scriptPubkey)), 0);
                else if (object.scriptPubkey.length >= 0)
                    message.scriptPubkey = object.scriptPubkey;
            return message;
        };

        /**
         * Creates a plain object from an UtxoInput message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.UtxoInput
         * @static
         * @param {pb.UtxoInput} message UtxoInput
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UtxoInput.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.txid = "";
                object.vout = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.amount = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.amount = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.scriptPubkey = "";
                else {
                    object.scriptPubkey = [];
                    if (options.bytes !== Array)
                        object.scriptPubkey = $util.newBuffer(object.scriptPubkey);
                }
            }
            if (message.txid != null && message.hasOwnProperty("txid"))
                object.txid = message.txid;
            if (message.vout != null && message.hasOwnProperty("vout"))
                object.vout = message.vout;
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (typeof message.amount === "number")
                    object.amount = options.longs === String ? String(message.amount) : message.amount;
                else
                    object.amount = options.longs === String ? $util.Long.prototype.toString.call(message.amount) : options.longs === Number ? new $util.LongBits(message.amount.low >>> 0, message.amount.high >>> 0).toNumber(true) : message.amount;
            if (message.scriptPubkey != null && message.hasOwnProperty("scriptPubkey"))
                object.scriptPubkey = options.bytes === String ? $util.base64.encode(message.scriptPubkey, 0, message.scriptPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.scriptPubkey) : message.scriptPubkey;
            return object;
        };

        /**
         * Converts this UtxoInput to JSON.
         * @function toJSON
         * @memberof pb.UtxoInput
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UtxoInput.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UtxoInput
         * @function getTypeUrl
         * @memberof pb.UtxoInput
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UtxoInput.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.UtxoInput";
        };

        return UtxoInput;
    })();

    pb.FrostUtxo = (function() {

        /**
         * Properties of a FrostUtxo.
         * @memberof pb
         * @interface IFrostUtxo
         * @property {string|null} [txid] FrostUtxo txid
         * @property {number|null} [vout] FrostUtxo vout
         * @property {string|null} [amount] FrostUtxo amount
         * @property {number|null} [vaultId] FrostUtxo vaultId
         * @property {number|Long|null} [finalizeHeight] FrostUtxo finalizeHeight
         * @property {string|null} [requestId] FrostUtxo requestId
         * @property {Uint8Array|null} [scriptPubkey] FrostUtxo scriptPubkey
         */

        /**
         * Constructs a new FrostUtxo.
         * @memberof pb
         * @classdesc Represents a FrostUtxo.
         * @implements IFrostUtxo
         * @constructor
         * @param {pb.IFrostUtxo=} [properties] Properties to set
         */
        function FrostUtxo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostUtxo txid.
         * @member {string} txid
         * @memberof pb.FrostUtxo
         * @instance
         */
        FrostUtxo.prototype.txid = "";

        /**
         * FrostUtxo vout.
         * @member {number} vout
         * @memberof pb.FrostUtxo
         * @instance
         */
        FrostUtxo.prototype.vout = 0;

        /**
         * FrostUtxo amount.
         * @member {string} amount
         * @memberof pb.FrostUtxo
         * @instance
         */
        FrostUtxo.prototype.amount = "";

        /**
         * FrostUtxo vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostUtxo
         * @instance
         */
        FrostUtxo.prototype.vaultId = 0;

        /**
         * FrostUtxo finalizeHeight.
         * @member {number|Long} finalizeHeight
         * @memberof pb.FrostUtxo
         * @instance
         */
        FrostUtxo.prototype.finalizeHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostUtxo requestId.
         * @member {string} requestId
         * @memberof pb.FrostUtxo
         * @instance
         */
        FrostUtxo.prototype.requestId = "";

        /**
         * FrostUtxo scriptPubkey.
         * @member {Uint8Array} scriptPubkey
         * @memberof pb.FrostUtxo
         * @instance
         */
        FrostUtxo.prototype.scriptPubkey = $util.newBuffer([]);

        /**
         * Creates a new FrostUtxo instance using the specified properties.
         * @function create
         * @memberof pb.FrostUtxo
         * @static
         * @param {pb.IFrostUtxo=} [properties] Properties to set
         * @returns {pb.FrostUtxo} FrostUtxo instance
         */
        FrostUtxo.create = function create(properties) {
            return new FrostUtxo(properties);
        };

        /**
         * Encodes the specified FrostUtxo message. Does not implicitly {@link pb.FrostUtxo.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostUtxo
         * @static
         * @param {pb.IFrostUtxo} message FrostUtxo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostUtxo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.txid != null && Object.hasOwnProperty.call(message, "txid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.txid);
            if (message.vout != null && Object.hasOwnProperty.call(message, "vout"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vout);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.amount);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.vaultId);
            if (message.finalizeHeight != null && Object.hasOwnProperty.call(message, "finalizeHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.finalizeHeight);
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.requestId);
            if (message.scriptPubkey != null && Object.hasOwnProperty.call(message, "scriptPubkey"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.scriptPubkey);
            return writer;
        };

        /**
         * Encodes the specified FrostUtxo message, length delimited. Does not implicitly {@link pb.FrostUtxo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostUtxo
         * @static
         * @param {pb.IFrostUtxo} message FrostUtxo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostUtxo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostUtxo message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostUtxo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostUtxo} FrostUtxo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostUtxo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostUtxo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.txid = reader.string();
                        break;
                    }
                case 2: {
                        message.vout = reader.uint32();
                        break;
                    }
                case 3: {
                        message.amount = reader.string();
                        break;
                    }
                case 4: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 5: {
                        message.finalizeHeight = reader.uint64();
                        break;
                    }
                case 6: {
                        message.requestId = reader.string();
                        break;
                    }
                case 7: {
                        message.scriptPubkey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostUtxo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostUtxo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostUtxo} FrostUtxo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostUtxo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostUtxo message.
         * @function verify
         * @memberof pb.FrostUtxo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostUtxo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.txid != null && message.hasOwnProperty("txid"))
                if (!$util.isString(message.txid))
                    return "txid: string expected";
            if (message.vout != null && message.hasOwnProperty("vout"))
                if (!$util.isInteger(message.vout))
                    return "vout: integer expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.finalizeHeight != null && message.hasOwnProperty("finalizeHeight"))
                if (!$util.isInteger(message.finalizeHeight) && !(message.finalizeHeight && $util.isInteger(message.finalizeHeight.low) && $util.isInteger(message.finalizeHeight.high)))
                    return "finalizeHeight: integer|Long expected";
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                if (!$util.isString(message.requestId))
                    return "requestId: string expected";
            if (message.scriptPubkey != null && message.hasOwnProperty("scriptPubkey"))
                if (!(message.scriptPubkey && typeof message.scriptPubkey.length === "number" || $util.isString(message.scriptPubkey)))
                    return "scriptPubkey: buffer expected";
            return null;
        };

        /**
         * Creates a FrostUtxo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostUtxo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostUtxo} FrostUtxo
         */
        FrostUtxo.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostUtxo)
                return object;
            let message = new $root.pb.FrostUtxo();
            if (object.txid != null)
                message.txid = String(object.txid);
            if (object.vout != null)
                message.vout = object.vout >>> 0;
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.finalizeHeight != null)
                if ($util.Long)
                    (message.finalizeHeight = $util.Long.fromValue(object.finalizeHeight)).unsigned = true;
                else if (typeof object.finalizeHeight === "string")
                    message.finalizeHeight = parseInt(object.finalizeHeight, 10);
                else if (typeof object.finalizeHeight === "number")
                    message.finalizeHeight = object.finalizeHeight;
                else if (typeof object.finalizeHeight === "object")
                    message.finalizeHeight = new $util.LongBits(object.finalizeHeight.low >>> 0, object.finalizeHeight.high >>> 0).toNumber(true);
            if (object.requestId != null)
                message.requestId = String(object.requestId);
            if (object.scriptPubkey != null)
                if (typeof object.scriptPubkey === "string")
                    $util.base64.decode(object.scriptPubkey, message.scriptPubkey = $util.newBuffer($util.base64.length(object.scriptPubkey)), 0);
                else if (object.scriptPubkey.length >= 0)
                    message.scriptPubkey = object.scriptPubkey;
            return message;
        };

        /**
         * Creates a plain object from a FrostUtxo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostUtxo
         * @static
         * @param {pb.FrostUtxo} message FrostUtxo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostUtxo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.txid = "";
                object.vout = 0;
                object.amount = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.finalizeHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.finalizeHeight = options.longs === String ? "0" : 0;
                object.requestId = "";
                if (options.bytes === String)
                    object.scriptPubkey = "";
                else {
                    object.scriptPubkey = [];
                    if (options.bytes !== Array)
                        object.scriptPubkey = $util.newBuffer(object.scriptPubkey);
                }
            }
            if (message.txid != null && message.hasOwnProperty("txid"))
                object.txid = message.txid;
            if (message.vout != null && message.hasOwnProperty("vout"))
                object.vout = message.vout;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.finalizeHeight != null && message.hasOwnProperty("finalizeHeight"))
                if (typeof message.finalizeHeight === "number")
                    object.finalizeHeight = options.longs === String ? String(message.finalizeHeight) : message.finalizeHeight;
                else
                    object.finalizeHeight = options.longs === String ? $util.Long.prototype.toString.call(message.finalizeHeight) : options.longs === Number ? new $util.LongBits(message.finalizeHeight.low >>> 0, message.finalizeHeight.high >>> 0).toNumber(true) : message.finalizeHeight;
            if (message.requestId != null && message.hasOwnProperty("requestId"))
                object.requestId = message.requestId;
            if (message.scriptPubkey != null && message.hasOwnProperty("scriptPubkey"))
                object.scriptPubkey = options.bytes === String ? $util.base64.encode(message.scriptPubkey, 0, message.scriptPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.scriptPubkey) : message.scriptPubkey;
            return object;
        };

        /**
         * Converts this FrostUtxo to JSON.
         * @function toJSON
         * @memberof pb.FrostUtxo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostUtxo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostUtxo
         * @function getTypeUrl
         * @memberof pb.FrostUtxo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostUtxo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostUtxo";
        };

        return FrostUtxo;
    })();

    pb.FrostPlanningLog = (function() {

        /**
         * Properties of a FrostPlanningLog.
         * @memberof pb
         * @interface IFrostPlanningLog
         * @property {string|null} [step] FrostPlanningLog step
         * @property {string|null} [status] FrostPlanningLog status
         * @property {string|null} [message] FrostPlanningLog message
         * @property {number|Long|null} [timestamp] FrostPlanningLog timestamp
         */

        /**
         * Constructs a new FrostPlanningLog.
         * @memberof pb
         * @classdesc Represents a FrostPlanningLog.
         * @implements IFrostPlanningLog
         * @constructor
         * @param {pb.IFrostPlanningLog=} [properties] Properties to set
         */
        function FrostPlanningLog(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostPlanningLog step.
         * @member {string} step
         * @memberof pb.FrostPlanningLog
         * @instance
         */
        FrostPlanningLog.prototype.step = "";

        /**
         * FrostPlanningLog status.
         * @member {string} status
         * @memberof pb.FrostPlanningLog
         * @instance
         */
        FrostPlanningLog.prototype.status = "";

        /**
         * FrostPlanningLog message.
         * @member {string} message
         * @memberof pb.FrostPlanningLog
         * @instance
         */
        FrostPlanningLog.prototype.message = "";

        /**
         * FrostPlanningLog timestamp.
         * @member {number|Long} timestamp
         * @memberof pb.FrostPlanningLog
         * @instance
         */
        FrostPlanningLog.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FrostPlanningLog instance using the specified properties.
         * @function create
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {pb.IFrostPlanningLog=} [properties] Properties to set
         * @returns {pb.FrostPlanningLog} FrostPlanningLog instance
         */
        FrostPlanningLog.create = function create(properties) {
            return new FrostPlanningLog(properties);
        };

        /**
         * Encodes the specified FrostPlanningLog message. Does not implicitly {@link pb.FrostPlanningLog.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {pb.IFrostPlanningLog} message FrostPlanningLog message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostPlanningLog.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.step);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.status);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.message);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.timestamp);
            return writer;
        };

        /**
         * Encodes the specified FrostPlanningLog message, length delimited. Does not implicitly {@link pb.FrostPlanningLog.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {pb.IFrostPlanningLog} message FrostPlanningLog message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostPlanningLog.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostPlanningLog message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostPlanningLog} FrostPlanningLog
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostPlanningLog.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostPlanningLog();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.step = reader.string();
                        break;
                    }
                case 2: {
                        message.status = reader.string();
                        break;
                    }
                case 3: {
                        message.message = reader.string();
                        break;
                    }
                case 4: {
                        message.timestamp = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostPlanningLog message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostPlanningLog} FrostPlanningLog
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostPlanningLog.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostPlanningLog message.
         * @function verify
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostPlanningLog.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isString(message.step))
                    return "step: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            return null;
        };

        /**
         * Creates a FrostPlanningLog message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostPlanningLog} FrostPlanningLog
         */
        FrostPlanningLog.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostPlanningLog)
                return object;
            let message = new $root.pb.FrostPlanningLog();
            if (object.step != null)
                message.step = String(object.step);
            if (object.status != null)
                message.status = String(object.status);
            if (object.message != null)
                message.message = String(object.message);
            if (object.timestamp != null)
                if ($util.Long)
                    (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = true;
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FrostPlanningLog message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {pb.FrostPlanningLog} message FrostPlanningLog
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostPlanningLog.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.step = "";
                object.status = "";
                object.message = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : 0;
            }
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            return object;
        };

        /**
         * Converts this FrostPlanningLog to JSON.
         * @function toJSON
         * @memberof pb.FrostPlanningLog
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostPlanningLog.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostPlanningLog
         * @function getTypeUrl
         * @memberof pb.FrostPlanningLog
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostPlanningLog.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostPlanningLog";
        };

        return FrostPlanningLog;
    })();

    pb.FrostWithdrawState = (function() {

        /**
         * Properties of a FrostWithdrawState.
         * @memberof pb
         * @interface IFrostWithdrawState
         * @property {string|null} [withdrawId] FrostWithdrawState withdrawId
         * @property {string|null} [txId] FrostWithdrawState txId
         * @property {number|Long|null} [seq] FrostWithdrawState seq
         * @property {string|null} [chain] FrostWithdrawState chain
         * @property {string|null} [asset] FrostWithdrawState asset
         * @property {string|null} [to] FrostWithdrawState to
         * @property {string|null} [amount] FrostWithdrawState amount
         * @property {number|Long|null} [requestHeight] FrostWithdrawState requestHeight
         * @property {string|null} [status] FrostWithdrawState status
         * @property {string|null} [jobId] FrostWithdrawState jobId
         * @property {number|null} [vaultId] FrostWithdrawState vaultId
         * @property {Array.<pb.IFrostPlanningLog>|null} [planningLogs] FrostWithdrawState planningLogs
         */

        /**
         * Constructs a new FrostWithdrawState.
         * @memberof pb
         * @classdesc Represents a FrostWithdrawState.
         * @implements IFrostWithdrawState
         * @constructor
         * @param {pb.IFrostWithdrawState=} [properties] Properties to set
         */
        function FrostWithdrawState(properties) {
            this.planningLogs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostWithdrawState withdrawId.
         * @member {string} withdrawId
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.withdrawId = "";

        /**
         * FrostWithdrawState txId.
         * @member {string} txId
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.txId = "";

        /**
         * FrostWithdrawState seq.
         * @member {number|Long} seq
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.seq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostWithdrawState chain.
         * @member {string} chain
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.chain = "";

        /**
         * FrostWithdrawState asset.
         * @member {string} asset
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.asset = "";

        /**
         * FrostWithdrawState to.
         * @member {string} to
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.to = "";

        /**
         * FrostWithdrawState amount.
         * @member {string} amount
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.amount = "";

        /**
         * FrostWithdrawState requestHeight.
         * @member {number|Long} requestHeight
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.requestHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostWithdrawState status.
         * @member {string} status
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.status = "";

        /**
         * FrostWithdrawState jobId.
         * @member {string} jobId
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.jobId = "";

        /**
         * FrostWithdrawState vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.vaultId = 0;

        /**
         * FrostWithdrawState planningLogs.
         * @member {Array.<pb.IFrostPlanningLog>} planningLogs
         * @memberof pb.FrostWithdrawState
         * @instance
         */
        FrostWithdrawState.prototype.planningLogs = $util.emptyArray;

        /**
         * Creates a new FrostWithdrawState instance using the specified properties.
         * @function create
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {pb.IFrostWithdrawState=} [properties] Properties to set
         * @returns {pb.FrostWithdrawState} FrostWithdrawState instance
         */
        FrostWithdrawState.create = function create(properties) {
            return new FrostWithdrawState(properties);
        };

        /**
         * Encodes the specified FrostWithdrawState message. Does not implicitly {@link pb.FrostWithdrawState.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {pb.IFrostWithdrawState} message FrostWithdrawState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.withdrawId != null && Object.hasOwnProperty.call(message, "withdrawId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.withdrawId);
            if (message.txId != null && Object.hasOwnProperty.call(message, "txId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.txId);
            if (message.seq != null && Object.hasOwnProperty.call(message, "seq"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.seq);
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.chain);
            if (message.asset != null && Object.hasOwnProperty.call(message, "asset"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.asset);
            if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.to);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.amount);
            if (message.requestHeight != null && Object.hasOwnProperty.call(message, "requestHeight"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.requestHeight);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.status);
            if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.jobId);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.vaultId);
            if (message.planningLogs != null && message.planningLogs.length)
                for (let i = 0; i < message.planningLogs.length; ++i)
                    $root.pb.FrostPlanningLog.encode(message.planningLogs[i], writer.uint32(/* id 12, wireType 2 =*/98).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FrostWithdrawState message, length delimited. Does not implicitly {@link pb.FrostWithdrawState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {pb.IFrostWithdrawState} message FrostWithdrawState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostWithdrawState message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostWithdrawState} FrostWithdrawState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawState.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostWithdrawState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.withdrawId = reader.string();
                        break;
                    }
                case 2: {
                        message.txId = reader.string();
                        break;
                    }
                case 3: {
                        message.seq = reader.uint64();
                        break;
                    }
                case 4: {
                        message.chain = reader.string();
                        break;
                    }
                case 5: {
                        message.asset = reader.string();
                        break;
                    }
                case 6: {
                        message.to = reader.string();
                        break;
                    }
                case 7: {
                        message.amount = reader.string();
                        break;
                    }
                case 8: {
                        message.requestHeight = reader.uint64();
                        break;
                    }
                case 9: {
                        message.status = reader.string();
                        break;
                    }
                case 10: {
                        message.jobId = reader.string();
                        break;
                    }
                case 11: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 12: {
                        if (!(message.planningLogs && message.planningLogs.length))
                            message.planningLogs = [];
                        message.planningLogs.push($root.pb.FrostPlanningLog.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostWithdrawState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostWithdrawState} FrostWithdrawState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostWithdrawState message.
         * @function verify
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostWithdrawState.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.withdrawId != null && message.hasOwnProperty("withdrawId"))
                if (!$util.isString(message.withdrawId))
                    return "withdrawId: string expected";
            if (message.txId != null && message.hasOwnProperty("txId"))
                if (!$util.isString(message.txId))
                    return "txId: string expected";
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (!$util.isInteger(message.seq) && !(message.seq && $util.isInteger(message.seq.low) && $util.isInteger(message.seq.high)))
                    return "seq: integer|Long expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.asset != null && message.hasOwnProperty("asset"))
                if (!$util.isString(message.asset))
                    return "asset: string expected";
            if (message.to != null && message.hasOwnProperty("to"))
                if (!$util.isString(message.to))
                    return "to: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.requestHeight != null && message.hasOwnProperty("requestHeight"))
                if (!$util.isInteger(message.requestHeight) && !(message.requestHeight && $util.isInteger(message.requestHeight.low) && $util.isInteger(message.requestHeight.high)))
                    return "requestHeight: integer|Long expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                if (!$util.isString(message.jobId))
                    return "jobId: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.planningLogs != null && message.hasOwnProperty("planningLogs")) {
                if (!Array.isArray(message.planningLogs))
                    return "planningLogs: array expected";
                for (let i = 0; i < message.planningLogs.length; ++i) {
                    let error = $root.pb.FrostPlanningLog.verify(message.planningLogs[i]);
                    if (error)
                        return "planningLogs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FrostWithdrawState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostWithdrawState} FrostWithdrawState
         */
        FrostWithdrawState.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostWithdrawState)
                return object;
            let message = new $root.pb.FrostWithdrawState();
            if (object.withdrawId != null)
                message.withdrawId = String(object.withdrawId);
            if (object.txId != null)
                message.txId = String(object.txId);
            if (object.seq != null)
                if ($util.Long)
                    (message.seq = $util.Long.fromValue(object.seq)).unsigned = true;
                else if (typeof object.seq === "string")
                    message.seq = parseInt(object.seq, 10);
                else if (typeof object.seq === "number")
                    message.seq = object.seq;
                else if (typeof object.seq === "object")
                    message.seq = new $util.LongBits(object.seq.low >>> 0, object.seq.high >>> 0).toNumber(true);
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.asset != null)
                message.asset = String(object.asset);
            if (object.to != null)
                message.to = String(object.to);
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.requestHeight != null)
                if ($util.Long)
                    (message.requestHeight = $util.Long.fromValue(object.requestHeight)).unsigned = true;
                else if (typeof object.requestHeight === "string")
                    message.requestHeight = parseInt(object.requestHeight, 10);
                else if (typeof object.requestHeight === "number")
                    message.requestHeight = object.requestHeight;
                else if (typeof object.requestHeight === "object")
                    message.requestHeight = new $util.LongBits(object.requestHeight.low >>> 0, object.requestHeight.high >>> 0).toNumber(true);
            if (object.status != null)
                message.status = String(object.status);
            if (object.jobId != null)
                message.jobId = String(object.jobId);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.planningLogs) {
                if (!Array.isArray(object.planningLogs))
                    throw TypeError(".pb.FrostWithdrawState.planningLogs: array expected");
                message.planningLogs = [];
                for (let i = 0; i < object.planningLogs.length; ++i) {
                    if (typeof object.planningLogs[i] !== "object")
                        throw TypeError(".pb.FrostWithdrawState.planningLogs: object expected");
                    message.planningLogs[i] = $root.pb.FrostPlanningLog.fromObject(object.planningLogs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FrostWithdrawState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {pb.FrostWithdrawState} message FrostWithdrawState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostWithdrawState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.planningLogs = [];
            if (options.defaults) {
                object.withdrawId = "";
                object.txId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.seq = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.seq = options.longs === String ? "0" : 0;
                object.chain = "";
                object.asset = "";
                object.to = "";
                object.amount = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.requestHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.requestHeight = options.longs === String ? "0" : 0;
                object.status = "";
                object.jobId = "";
                object.vaultId = 0;
            }
            if (message.withdrawId != null && message.hasOwnProperty("withdrawId"))
                object.withdrawId = message.withdrawId;
            if (message.txId != null && message.hasOwnProperty("txId"))
                object.txId = message.txId;
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (typeof message.seq === "number")
                    object.seq = options.longs === String ? String(message.seq) : message.seq;
                else
                    object.seq = options.longs === String ? $util.Long.prototype.toString.call(message.seq) : options.longs === Number ? new $util.LongBits(message.seq.low >>> 0, message.seq.high >>> 0).toNumber(true) : message.seq;
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.asset != null && message.hasOwnProperty("asset"))
                object.asset = message.asset;
            if (message.to != null && message.hasOwnProperty("to"))
                object.to = message.to;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.requestHeight != null && message.hasOwnProperty("requestHeight"))
                if (typeof message.requestHeight === "number")
                    object.requestHeight = options.longs === String ? String(message.requestHeight) : message.requestHeight;
                else
                    object.requestHeight = options.longs === String ? $util.Long.prototype.toString.call(message.requestHeight) : options.longs === Number ? new $util.LongBits(message.requestHeight.low >>> 0, message.requestHeight.high >>> 0).toNumber(true) : message.requestHeight;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                object.jobId = message.jobId;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.planningLogs && message.planningLogs.length) {
                object.planningLogs = [];
                for (let j = 0; j < message.planningLogs.length; ++j)
                    object.planningLogs[j] = $root.pb.FrostPlanningLog.toObject(message.planningLogs[j], options);
            }
            return object;
        };

        /**
         * Converts this FrostWithdrawState to JSON.
         * @function toJSON
         * @memberof pb.FrostWithdrawState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostWithdrawState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostWithdrawState
         * @function getTypeUrl
         * @memberof pb.FrostWithdrawState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostWithdrawState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostWithdrawState";
        };

        return FrostWithdrawState;
    })();

    pb.FrostWithdrawPlanningLogTx = (function() {

        /**
         * Properties of a FrostWithdrawPlanningLogTx.
         * @memberof pb
         * @interface IFrostWithdrawPlanningLogTx
         * @property {string|null} [reporter] FrostWithdrawPlanningLogTx reporter
         * @property {string|null} [withdrawId] FrostWithdrawPlanningLogTx withdrawId
         * @property {Array.<pb.IFrostPlanningLog>|null} [logs] FrostWithdrawPlanningLogTx logs
         */

        /**
         * Constructs a new FrostWithdrawPlanningLogTx.
         * @memberof pb
         * @classdesc Represents a FrostWithdrawPlanningLogTx.
         * @implements IFrostWithdrawPlanningLogTx
         * @constructor
         * @param {pb.IFrostWithdrawPlanningLogTx=} [properties] Properties to set
         */
        function FrostWithdrawPlanningLogTx(properties) {
            this.logs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostWithdrawPlanningLogTx reporter.
         * @member {string} reporter
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @instance
         */
        FrostWithdrawPlanningLogTx.prototype.reporter = "";

        /**
         * FrostWithdrawPlanningLogTx withdrawId.
         * @member {string} withdrawId
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @instance
         */
        FrostWithdrawPlanningLogTx.prototype.withdrawId = "";

        /**
         * FrostWithdrawPlanningLogTx logs.
         * @member {Array.<pb.IFrostPlanningLog>} logs
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @instance
         */
        FrostWithdrawPlanningLogTx.prototype.logs = $util.emptyArray;

        /**
         * Creates a new FrostWithdrawPlanningLogTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {pb.IFrostWithdrawPlanningLogTx=} [properties] Properties to set
         * @returns {pb.FrostWithdrawPlanningLogTx} FrostWithdrawPlanningLogTx instance
         */
        FrostWithdrawPlanningLogTx.create = function create(properties) {
            return new FrostWithdrawPlanningLogTx(properties);
        };

        /**
         * Encodes the specified FrostWithdrawPlanningLogTx message. Does not implicitly {@link pb.FrostWithdrawPlanningLogTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {pb.IFrostWithdrawPlanningLogTx} message FrostWithdrawPlanningLogTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawPlanningLogTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.reporter != null && Object.hasOwnProperty.call(message, "reporter"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.reporter);
            if (message.withdrawId != null && Object.hasOwnProperty.call(message, "withdrawId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.withdrawId);
            if (message.logs != null && message.logs.length)
                for (let i = 0; i < message.logs.length; ++i)
                    $root.pb.FrostPlanningLog.encode(message.logs[i], writer.uint32(/* id 3, wireType 2 =*/26).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified FrostWithdrawPlanningLogTx message, length delimited. Does not implicitly {@link pb.FrostWithdrawPlanningLogTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {pb.IFrostWithdrawPlanningLogTx} message FrostWithdrawPlanningLogTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostWithdrawPlanningLogTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostWithdrawPlanningLogTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostWithdrawPlanningLogTx} FrostWithdrawPlanningLogTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawPlanningLogTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostWithdrawPlanningLogTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.reporter = reader.string();
                        break;
                    }
                case 2: {
                        message.withdrawId = reader.string();
                        break;
                    }
                case 3: {
                        if (!(message.logs && message.logs.length))
                            message.logs = [];
                        message.logs.push($root.pb.FrostPlanningLog.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostWithdrawPlanningLogTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostWithdrawPlanningLogTx} FrostWithdrawPlanningLogTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostWithdrawPlanningLogTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostWithdrawPlanningLogTx message.
         * @function verify
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostWithdrawPlanningLogTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.reporter != null && message.hasOwnProperty("reporter"))
                if (!$util.isString(message.reporter))
                    return "reporter: string expected";
            if (message.withdrawId != null && message.hasOwnProperty("withdrawId"))
                if (!$util.isString(message.withdrawId))
                    return "withdrawId: string expected";
            if (message.logs != null && message.hasOwnProperty("logs")) {
                if (!Array.isArray(message.logs))
                    return "logs: array expected";
                for (let i = 0; i < message.logs.length; ++i) {
                    let error = $root.pb.FrostPlanningLog.verify(message.logs[i]);
                    if (error)
                        return "logs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a FrostWithdrawPlanningLogTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostWithdrawPlanningLogTx} FrostWithdrawPlanningLogTx
         */
        FrostWithdrawPlanningLogTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostWithdrawPlanningLogTx)
                return object;
            let message = new $root.pb.FrostWithdrawPlanningLogTx();
            if (object.reporter != null)
                message.reporter = String(object.reporter);
            if (object.withdrawId != null)
                message.withdrawId = String(object.withdrawId);
            if (object.logs) {
                if (!Array.isArray(object.logs))
                    throw TypeError(".pb.FrostWithdrawPlanningLogTx.logs: array expected");
                message.logs = [];
                for (let i = 0; i < object.logs.length; ++i) {
                    if (typeof object.logs[i] !== "object")
                        throw TypeError(".pb.FrostWithdrawPlanningLogTx.logs: object expected");
                    message.logs[i] = $root.pb.FrostPlanningLog.fromObject(object.logs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a FrostWithdrawPlanningLogTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {pb.FrostWithdrawPlanningLogTx} message FrostWithdrawPlanningLogTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostWithdrawPlanningLogTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.logs = [];
            if (options.defaults) {
                object.reporter = "";
                object.withdrawId = "";
            }
            if (message.reporter != null && message.hasOwnProperty("reporter"))
                object.reporter = message.reporter;
            if (message.withdrawId != null && message.hasOwnProperty("withdrawId"))
                object.withdrawId = message.withdrawId;
            if (message.logs && message.logs.length) {
                object.logs = [];
                for (let j = 0; j < message.logs.length; ++j)
                    object.logs[j] = $root.pb.FrostPlanningLog.toObject(message.logs[j], options);
            }
            return object;
        };

        /**
         * Converts this FrostWithdrawPlanningLogTx to JSON.
         * @function toJSON
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostWithdrawPlanningLogTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostWithdrawPlanningLogTx
         * @function getTypeUrl
         * @memberof pb.FrostWithdrawPlanningLogTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostWithdrawPlanningLogTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostWithdrawPlanningLogTx";
        };

        return FrostWithdrawPlanningLogTx;
    })();

    pb.FrostSignedPackage = (function() {

        /**
         * Properties of a FrostSignedPackage.
         * @memberof pb
         * @interface IFrostSignedPackage
         * @property {string|null} [jobId] FrostSignedPackage jobId
         * @property {number|Long|null} [idx] FrostSignedPackage idx
         * @property {Uint8Array|null} [signedPackageBytes] FrostSignedPackage signedPackageBytes
         * @property {number|Long|null} [submitHeight] FrostSignedPackage submitHeight
         * @property {string|null} [submitter] FrostSignedPackage submitter
         */

        /**
         * Constructs a new FrostSignedPackage.
         * @memberof pb
         * @classdesc Represents a FrostSignedPackage.
         * @implements IFrostSignedPackage
         * @constructor
         * @param {pb.IFrostSignedPackage=} [properties] Properties to set
         */
        function FrostSignedPackage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostSignedPackage jobId.
         * @member {string} jobId
         * @memberof pb.FrostSignedPackage
         * @instance
         */
        FrostSignedPackage.prototype.jobId = "";

        /**
         * FrostSignedPackage idx.
         * @member {number|Long} idx
         * @memberof pb.FrostSignedPackage
         * @instance
         */
        FrostSignedPackage.prototype.idx = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostSignedPackage signedPackageBytes.
         * @member {Uint8Array} signedPackageBytes
         * @memberof pb.FrostSignedPackage
         * @instance
         */
        FrostSignedPackage.prototype.signedPackageBytes = $util.newBuffer([]);

        /**
         * FrostSignedPackage submitHeight.
         * @member {number|Long} submitHeight
         * @memberof pb.FrostSignedPackage
         * @instance
         */
        FrostSignedPackage.prototype.submitHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostSignedPackage submitter.
         * @member {string} submitter
         * @memberof pb.FrostSignedPackage
         * @instance
         */
        FrostSignedPackage.prototype.submitter = "";

        /**
         * Creates a new FrostSignedPackage instance using the specified properties.
         * @function create
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {pb.IFrostSignedPackage=} [properties] Properties to set
         * @returns {pb.FrostSignedPackage} FrostSignedPackage instance
         */
        FrostSignedPackage.create = function create(properties) {
            return new FrostSignedPackage(properties);
        };

        /**
         * Encodes the specified FrostSignedPackage message. Does not implicitly {@link pb.FrostSignedPackage.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {pb.IFrostSignedPackage} message FrostSignedPackage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostSignedPackage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.jobId);
            if (message.idx != null && Object.hasOwnProperty.call(message, "idx"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.idx);
            if (message.signedPackageBytes != null && Object.hasOwnProperty.call(message, "signedPackageBytes"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.signedPackageBytes);
            if (message.submitHeight != null && Object.hasOwnProperty.call(message, "submitHeight"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.submitHeight);
            if (message.submitter != null && Object.hasOwnProperty.call(message, "submitter"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.submitter);
            return writer;
        };

        /**
         * Encodes the specified FrostSignedPackage message, length delimited. Does not implicitly {@link pb.FrostSignedPackage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {pb.IFrostSignedPackage} message FrostSignedPackage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostSignedPackage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostSignedPackage message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostSignedPackage} FrostSignedPackage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostSignedPackage.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostSignedPackage();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.jobId = reader.string();
                        break;
                    }
                case 2: {
                        message.idx = reader.uint64();
                        break;
                    }
                case 3: {
                        message.signedPackageBytes = reader.bytes();
                        break;
                    }
                case 4: {
                        message.submitHeight = reader.uint64();
                        break;
                    }
                case 5: {
                        message.submitter = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostSignedPackage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostSignedPackage} FrostSignedPackage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostSignedPackage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostSignedPackage message.
         * @function verify
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostSignedPackage.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                if (!$util.isString(message.jobId))
                    return "jobId: string expected";
            if (message.idx != null && message.hasOwnProperty("idx"))
                if (!$util.isInteger(message.idx) && !(message.idx && $util.isInteger(message.idx.low) && $util.isInteger(message.idx.high)))
                    return "idx: integer|Long expected";
            if (message.signedPackageBytes != null && message.hasOwnProperty("signedPackageBytes"))
                if (!(message.signedPackageBytes && typeof message.signedPackageBytes.length === "number" || $util.isString(message.signedPackageBytes)))
                    return "signedPackageBytes: buffer expected";
            if (message.submitHeight != null && message.hasOwnProperty("submitHeight"))
                if (!$util.isInteger(message.submitHeight) && !(message.submitHeight && $util.isInteger(message.submitHeight.low) && $util.isInteger(message.submitHeight.high)))
                    return "submitHeight: integer|Long expected";
            if (message.submitter != null && message.hasOwnProperty("submitter"))
                if (!$util.isString(message.submitter))
                    return "submitter: string expected";
            return null;
        };

        /**
         * Creates a FrostSignedPackage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostSignedPackage} FrostSignedPackage
         */
        FrostSignedPackage.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostSignedPackage)
                return object;
            let message = new $root.pb.FrostSignedPackage();
            if (object.jobId != null)
                message.jobId = String(object.jobId);
            if (object.idx != null)
                if ($util.Long)
                    (message.idx = $util.Long.fromValue(object.idx)).unsigned = true;
                else if (typeof object.idx === "string")
                    message.idx = parseInt(object.idx, 10);
                else if (typeof object.idx === "number")
                    message.idx = object.idx;
                else if (typeof object.idx === "object")
                    message.idx = new $util.LongBits(object.idx.low >>> 0, object.idx.high >>> 0).toNumber(true);
            if (object.signedPackageBytes != null)
                if (typeof object.signedPackageBytes === "string")
                    $util.base64.decode(object.signedPackageBytes, message.signedPackageBytes = $util.newBuffer($util.base64.length(object.signedPackageBytes)), 0);
                else if (object.signedPackageBytes.length >= 0)
                    message.signedPackageBytes = object.signedPackageBytes;
            if (object.submitHeight != null)
                if ($util.Long)
                    (message.submitHeight = $util.Long.fromValue(object.submitHeight)).unsigned = true;
                else if (typeof object.submitHeight === "string")
                    message.submitHeight = parseInt(object.submitHeight, 10);
                else if (typeof object.submitHeight === "number")
                    message.submitHeight = object.submitHeight;
                else if (typeof object.submitHeight === "object")
                    message.submitHeight = new $util.LongBits(object.submitHeight.low >>> 0, object.submitHeight.high >>> 0).toNumber(true);
            if (object.submitter != null)
                message.submitter = String(object.submitter);
            return message;
        };

        /**
         * Creates a plain object from a FrostSignedPackage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {pb.FrostSignedPackage} message FrostSignedPackage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostSignedPackage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.jobId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.idx = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.idx = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.signedPackageBytes = "";
                else {
                    object.signedPackageBytes = [];
                    if (options.bytes !== Array)
                        object.signedPackageBytes = $util.newBuffer(object.signedPackageBytes);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.submitHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.submitHeight = options.longs === String ? "0" : 0;
                object.submitter = "";
            }
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                object.jobId = message.jobId;
            if (message.idx != null && message.hasOwnProperty("idx"))
                if (typeof message.idx === "number")
                    object.idx = options.longs === String ? String(message.idx) : message.idx;
                else
                    object.idx = options.longs === String ? $util.Long.prototype.toString.call(message.idx) : options.longs === Number ? new $util.LongBits(message.idx.low >>> 0, message.idx.high >>> 0).toNumber(true) : message.idx;
            if (message.signedPackageBytes != null && message.hasOwnProperty("signedPackageBytes"))
                object.signedPackageBytes = options.bytes === String ? $util.base64.encode(message.signedPackageBytes, 0, message.signedPackageBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.signedPackageBytes) : message.signedPackageBytes;
            if (message.submitHeight != null && message.hasOwnProperty("submitHeight"))
                if (typeof message.submitHeight === "number")
                    object.submitHeight = options.longs === String ? String(message.submitHeight) : message.submitHeight;
                else
                    object.submitHeight = options.longs === String ? $util.Long.prototype.toString.call(message.submitHeight) : options.longs === Number ? new $util.LongBits(message.submitHeight.low >>> 0, message.submitHeight.high >>> 0).toNumber(true) : message.submitHeight;
            if (message.submitter != null && message.hasOwnProperty("submitter"))
                object.submitter = message.submitter;
            return object;
        };

        /**
         * Converts this FrostSignedPackage to JSON.
         * @function toJSON
         * @memberof pb.FrostSignedPackage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostSignedPackage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostSignedPackage
         * @function getTypeUrl
         * @memberof pb.FrostSignedPackage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostSignedPackage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostSignedPackage";
        };

        return FrostSignedPackage;
    })();

    pb.FrostVaultState = (function() {

        /**
         * Properties of a FrostVaultState.
         * @memberof pb
         * @interface IFrostVaultState
         * @property {number|null} [vaultId] FrostVaultState vaultId
         * @property {string|null} [chain] FrostVaultState chain
         * @property {number|Long|null} [keyEpoch] FrostVaultState keyEpoch
         * @property {Uint8Array|null} [groupPubkey] FrostVaultState groupPubkey
         * @property {pb.SignAlgo|null} [signAlgo] FrostVaultState signAlgo
         * @property {string|null} [status] FrostVaultState status
         * @property {number|Long|null} [activeSinceHeight] FrostVaultState activeSinceHeight
         * @property {Array.<string>|null} [committeeMembers] FrostVaultState committeeMembers
         */

        /**
         * Constructs a new FrostVaultState.
         * @memberof pb
         * @classdesc Represents a FrostVaultState.
         * @implements IFrostVaultState
         * @constructor
         * @param {pb.IFrostVaultState=} [properties] Properties to set
         */
        function FrostVaultState(properties) {
            this.committeeMembers = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultState vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.vaultId = 0;

        /**
         * FrostVaultState chain.
         * @member {string} chain
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.chain = "";

        /**
         * FrostVaultState keyEpoch.
         * @member {number|Long} keyEpoch
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.keyEpoch = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultState groupPubkey.
         * @member {Uint8Array} groupPubkey
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.groupPubkey = $util.newBuffer([]);

        /**
         * FrostVaultState signAlgo.
         * @member {pb.SignAlgo} signAlgo
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.signAlgo = 0;

        /**
         * FrostVaultState status.
         * @member {string} status
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.status = "";

        /**
         * FrostVaultState activeSinceHeight.
         * @member {number|Long} activeSinceHeight
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.activeSinceHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultState committeeMembers.
         * @member {Array.<string>} committeeMembers
         * @memberof pb.FrostVaultState
         * @instance
         */
        FrostVaultState.prototype.committeeMembers = $util.emptyArray;

        /**
         * Creates a new FrostVaultState instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultState
         * @static
         * @param {pb.IFrostVaultState=} [properties] Properties to set
         * @returns {pb.FrostVaultState} FrostVaultState instance
         */
        FrostVaultState.create = function create(properties) {
            return new FrostVaultState(properties);
        };

        /**
         * Encodes the specified FrostVaultState message. Does not implicitly {@link pb.FrostVaultState.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultState
         * @static
         * @param {pb.IFrostVaultState} message FrostVaultState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.vaultId);
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.keyEpoch != null && Object.hasOwnProperty.call(message, "keyEpoch"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.keyEpoch);
            if (message.groupPubkey != null && Object.hasOwnProperty.call(message, "groupPubkey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.groupPubkey);
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.signAlgo);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.status);
            if (message.activeSinceHeight != null && Object.hasOwnProperty.call(message, "activeSinceHeight"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.activeSinceHeight);
            if (message.committeeMembers != null && message.committeeMembers.length)
                for (let i = 0; i < message.committeeMembers.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.committeeMembers[i]);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultState message, length delimited. Does not implicitly {@link pb.FrostVaultState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultState
         * @static
         * @param {pb.IFrostVaultState} message FrostVaultState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultState message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultState} FrostVaultState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultState.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.keyEpoch = reader.uint64();
                        break;
                    }
                case 4: {
                        message.groupPubkey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.signAlgo = reader.int32();
                        break;
                    }
                case 6: {
                        message.status = reader.string();
                        break;
                    }
                case 7: {
                        message.activeSinceHeight = reader.uint64();
                        break;
                    }
                case 8: {
                        if (!(message.committeeMembers && message.committeeMembers.length))
                            message.committeeMembers = [];
                        message.committeeMembers.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultState} FrostVaultState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultState message.
         * @function verify
         * @memberof pb.FrostVaultState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultState.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.keyEpoch != null && message.hasOwnProperty("keyEpoch"))
                if (!$util.isInteger(message.keyEpoch) && !(message.keyEpoch && $util.isInteger(message.keyEpoch.low) && $util.isInteger(message.keyEpoch.high)))
                    return "keyEpoch: integer|Long expected";
            if (message.groupPubkey != null && message.hasOwnProperty("groupPubkey"))
                if (!(message.groupPubkey && typeof message.groupPubkey.length === "number" || $util.isString(message.groupPubkey)))
                    return "groupPubkey: buffer expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                switch (message.signAlgo) {
                default:
                    return "signAlgo: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.activeSinceHeight != null && message.hasOwnProperty("activeSinceHeight"))
                if (!$util.isInteger(message.activeSinceHeight) && !(message.activeSinceHeight && $util.isInteger(message.activeSinceHeight.low) && $util.isInteger(message.activeSinceHeight.high)))
                    return "activeSinceHeight: integer|Long expected";
            if (message.committeeMembers != null && message.hasOwnProperty("committeeMembers")) {
                if (!Array.isArray(message.committeeMembers))
                    return "committeeMembers: array expected";
                for (let i = 0; i < message.committeeMembers.length; ++i)
                    if (!$util.isString(message.committeeMembers[i]))
                        return "committeeMembers: string[] expected";
            }
            return null;
        };

        /**
         * Creates a FrostVaultState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultState} FrostVaultState
         */
        FrostVaultState.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultState)
                return object;
            let message = new $root.pb.FrostVaultState();
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.keyEpoch != null)
                if ($util.Long)
                    (message.keyEpoch = $util.Long.fromValue(object.keyEpoch)).unsigned = true;
                else if (typeof object.keyEpoch === "string")
                    message.keyEpoch = parseInt(object.keyEpoch, 10);
                else if (typeof object.keyEpoch === "number")
                    message.keyEpoch = object.keyEpoch;
                else if (typeof object.keyEpoch === "object")
                    message.keyEpoch = new $util.LongBits(object.keyEpoch.low >>> 0, object.keyEpoch.high >>> 0).toNumber(true);
            if (object.groupPubkey != null)
                if (typeof object.groupPubkey === "string")
                    $util.base64.decode(object.groupPubkey, message.groupPubkey = $util.newBuffer($util.base64.length(object.groupPubkey)), 0);
                else if (object.groupPubkey.length >= 0)
                    message.groupPubkey = object.groupPubkey;
            switch (object.signAlgo) {
            default:
                if (typeof object.signAlgo === "number") {
                    message.signAlgo = object.signAlgo;
                    break;
                }
                break;
            case "SIGN_ALGO_UNSPECIFIED":
            case 0:
                message.signAlgo = 0;
                break;
            case "SIGN_ALGO_ECDSA_P256":
            case 1:
                message.signAlgo = 1;
                break;
            case "SIGN_ALGO_BLS_BN256_G2":
            case 2:
                message.signAlgo = 2;
                break;
            case "SIGN_ALGO_SCHNORR_SECP256K1_BIP340":
            case 3:
                message.signAlgo = 3;
                break;
            case "SIGN_ALGO_SCHNORR_ALT_BN128":
            case 4:
                message.signAlgo = 4;
                break;
            case "SIGN_ALGO_ED25519":
            case 5:
                message.signAlgo = 5;
                break;
            case "SIGN_ALGO_ECDSA_SECP256K1":
            case 6:
                message.signAlgo = 6;
                break;
            }
            if (object.status != null)
                message.status = String(object.status);
            if (object.activeSinceHeight != null)
                if ($util.Long)
                    (message.activeSinceHeight = $util.Long.fromValue(object.activeSinceHeight)).unsigned = true;
                else if (typeof object.activeSinceHeight === "string")
                    message.activeSinceHeight = parseInt(object.activeSinceHeight, 10);
                else if (typeof object.activeSinceHeight === "number")
                    message.activeSinceHeight = object.activeSinceHeight;
                else if (typeof object.activeSinceHeight === "object")
                    message.activeSinceHeight = new $util.LongBits(object.activeSinceHeight.low >>> 0, object.activeSinceHeight.high >>> 0).toNumber(true);
            if (object.committeeMembers) {
                if (!Array.isArray(object.committeeMembers))
                    throw TypeError(".pb.FrostVaultState.committeeMembers: array expected");
                message.committeeMembers = [];
                for (let i = 0; i < object.committeeMembers.length; ++i)
                    message.committeeMembers[i] = String(object.committeeMembers[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultState
         * @static
         * @param {pb.FrostVaultState} message FrostVaultState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.committeeMembers = [];
            if (options.defaults) {
                object.vaultId = 0;
                object.chain = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.keyEpoch = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.keyEpoch = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.groupPubkey = "";
                else {
                    object.groupPubkey = [];
                    if (options.bytes !== Array)
                        object.groupPubkey = $util.newBuffer(object.groupPubkey);
                }
                object.signAlgo = options.enums === String ? "SIGN_ALGO_UNSPECIFIED" : 0;
                object.status = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.activeSinceHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.activeSinceHeight = options.longs === String ? "0" : 0;
            }
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.keyEpoch != null && message.hasOwnProperty("keyEpoch"))
                if (typeof message.keyEpoch === "number")
                    object.keyEpoch = options.longs === String ? String(message.keyEpoch) : message.keyEpoch;
                else
                    object.keyEpoch = options.longs === String ? $util.Long.prototype.toString.call(message.keyEpoch) : options.longs === Number ? new $util.LongBits(message.keyEpoch.low >>> 0, message.keyEpoch.high >>> 0).toNumber(true) : message.keyEpoch;
            if (message.groupPubkey != null && message.hasOwnProperty("groupPubkey"))
                object.groupPubkey = options.bytes === String ? $util.base64.encode(message.groupPubkey, 0, message.groupPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.groupPubkey) : message.groupPubkey;
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = options.enums === String ? $root.pb.SignAlgo[message.signAlgo] === undefined ? message.signAlgo : $root.pb.SignAlgo[message.signAlgo] : message.signAlgo;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.activeSinceHeight != null && message.hasOwnProperty("activeSinceHeight"))
                if (typeof message.activeSinceHeight === "number")
                    object.activeSinceHeight = options.longs === String ? String(message.activeSinceHeight) : message.activeSinceHeight;
                else
                    object.activeSinceHeight = options.longs === String ? $util.Long.prototype.toString.call(message.activeSinceHeight) : options.longs === Number ? new $util.LongBits(message.activeSinceHeight.low >>> 0, message.activeSinceHeight.high >>> 0).toNumber(true) : message.activeSinceHeight;
            if (message.committeeMembers && message.committeeMembers.length) {
                object.committeeMembers = [];
                for (let j = 0; j < message.committeeMembers.length; ++j)
                    object.committeeMembers[j] = message.committeeMembers[j];
            }
            return object;
        };

        /**
         * Converts this FrostVaultState to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultState
         * @function getTypeUrl
         * @memberof pb.FrostVaultState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultState";
        };

        return FrostVaultState;
    })();

    pb.VaultTransitionState = (function() {

        /**
         * Properties of a VaultTransitionState.
         * @memberof pb
         * @interface IVaultTransitionState
         * @property {string|null} [chain] VaultTransitionState chain
         * @property {number|null} [vaultId] VaultTransitionState vaultId
         * @property {number|Long|null} [epochId] VaultTransitionState epochId
         * @property {pb.SignAlgo|null} [signAlgo] VaultTransitionState signAlgo
         * @property {number|Long|null} [triggerHeight] VaultTransitionState triggerHeight
         * @property {Array.<string>|null} [oldCommitteeMembers] VaultTransitionState oldCommitteeMembers
         * @property {Array.<string>|null} [newCommitteeMembers] VaultTransitionState newCommitteeMembers
         * @property {string|null} [dkgStatus] VaultTransitionState dkgStatus
         * @property {string|null} [dkgSessionId] VaultTransitionState dkgSessionId
         * @property {number|null} [dkgThresholdT] VaultTransitionState dkgThresholdT
         * @property {number|null} [dkgN] VaultTransitionState dkgN
         * @property {number|Long|null} [dkgCommitDeadline] VaultTransitionState dkgCommitDeadline
         * @property {number|Long|null} [dkgDisputeDeadline] VaultTransitionState dkgDisputeDeadline
         * @property {Uint8Array|null} [oldGroupPubkey] VaultTransitionState oldGroupPubkey
         * @property {Uint8Array|null} [newGroupPubkey] VaultTransitionState newGroupPubkey
         * @property {string|null} [validationStatus] VaultTransitionState validationStatus
         * @property {Uint8Array|null} [validationMsgHash] VaultTransitionState validationMsgHash
         * @property {string|null} [lifecycle] VaultTransitionState lifecycle
         * @property {number|Long|null} [dkgSharingDeadline] VaultTransitionState dkgSharingDeadline
         */

        /**
         * Constructs a new VaultTransitionState.
         * @memberof pb
         * @classdesc Represents a VaultTransitionState.
         * @implements IVaultTransitionState
         * @constructor
         * @param {pb.IVaultTransitionState=} [properties] Properties to set
         */
        function VaultTransitionState(properties) {
            this.oldCommitteeMembers = [];
            this.newCommitteeMembers = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VaultTransitionState chain.
         * @member {string} chain
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.chain = "";

        /**
         * VaultTransitionState vaultId.
         * @member {number} vaultId
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.vaultId = 0;

        /**
         * VaultTransitionState epochId.
         * @member {number|Long} epochId
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * VaultTransitionState signAlgo.
         * @member {pb.SignAlgo} signAlgo
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.signAlgo = 0;

        /**
         * VaultTransitionState triggerHeight.
         * @member {number|Long} triggerHeight
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.triggerHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * VaultTransitionState oldCommitteeMembers.
         * @member {Array.<string>} oldCommitteeMembers
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.oldCommitteeMembers = $util.emptyArray;

        /**
         * VaultTransitionState newCommitteeMembers.
         * @member {Array.<string>} newCommitteeMembers
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.newCommitteeMembers = $util.emptyArray;

        /**
         * VaultTransitionState dkgStatus.
         * @member {string} dkgStatus
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.dkgStatus = "";

        /**
         * VaultTransitionState dkgSessionId.
         * @member {string} dkgSessionId
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.dkgSessionId = "";

        /**
         * VaultTransitionState dkgThresholdT.
         * @member {number} dkgThresholdT
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.dkgThresholdT = 0;

        /**
         * VaultTransitionState dkgN.
         * @member {number} dkgN
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.dkgN = 0;

        /**
         * VaultTransitionState dkgCommitDeadline.
         * @member {number|Long} dkgCommitDeadline
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.dkgCommitDeadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * VaultTransitionState dkgDisputeDeadline.
         * @member {number|Long} dkgDisputeDeadline
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.dkgDisputeDeadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * VaultTransitionState oldGroupPubkey.
         * @member {Uint8Array} oldGroupPubkey
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.oldGroupPubkey = $util.newBuffer([]);

        /**
         * VaultTransitionState newGroupPubkey.
         * @member {Uint8Array} newGroupPubkey
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.newGroupPubkey = $util.newBuffer([]);

        /**
         * VaultTransitionState validationStatus.
         * @member {string} validationStatus
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.validationStatus = "";

        /**
         * VaultTransitionState validationMsgHash.
         * @member {Uint8Array} validationMsgHash
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.validationMsgHash = $util.newBuffer([]);

        /**
         * VaultTransitionState lifecycle.
         * @member {string} lifecycle
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.lifecycle = "";

        /**
         * VaultTransitionState dkgSharingDeadline.
         * @member {number|Long} dkgSharingDeadline
         * @memberof pb.VaultTransitionState
         * @instance
         */
        VaultTransitionState.prototype.dkgSharingDeadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new VaultTransitionState instance using the specified properties.
         * @function create
         * @memberof pb.VaultTransitionState
         * @static
         * @param {pb.IVaultTransitionState=} [properties] Properties to set
         * @returns {pb.VaultTransitionState} VaultTransitionState instance
         */
        VaultTransitionState.create = function create(properties) {
            return new VaultTransitionState(properties);
        };

        /**
         * Encodes the specified VaultTransitionState message. Does not implicitly {@link pb.VaultTransitionState.verify|verify} messages.
         * @function encode
         * @memberof pb.VaultTransitionState
         * @static
         * @param {pb.IVaultTransitionState} message VaultTransitionState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultTransitionState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.epochId);
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.signAlgo);
            if (message.triggerHeight != null && Object.hasOwnProperty.call(message, "triggerHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.triggerHeight);
            if (message.oldCommitteeMembers != null && message.oldCommitteeMembers.length)
                for (let i = 0; i < message.oldCommitteeMembers.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.oldCommitteeMembers[i]);
            if (message.newCommitteeMembers != null && message.newCommitteeMembers.length)
                for (let i = 0; i < message.newCommitteeMembers.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.newCommitteeMembers[i]);
            if (message.dkgStatus != null && Object.hasOwnProperty.call(message, "dkgStatus"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.dkgStatus);
            if (message.dkgSessionId != null && Object.hasOwnProperty.call(message, "dkgSessionId"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.dkgSessionId);
            if (message.dkgThresholdT != null && Object.hasOwnProperty.call(message, "dkgThresholdT"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.dkgThresholdT);
            if (message.dkgN != null && Object.hasOwnProperty.call(message, "dkgN"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.dkgN);
            if (message.dkgCommitDeadline != null && Object.hasOwnProperty.call(message, "dkgCommitDeadline"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.dkgCommitDeadline);
            if (message.dkgDisputeDeadline != null && Object.hasOwnProperty.call(message, "dkgDisputeDeadline"))
                writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.dkgDisputeDeadline);
            if (message.oldGroupPubkey != null && Object.hasOwnProperty.call(message, "oldGroupPubkey"))
                writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.oldGroupPubkey);
            if (message.newGroupPubkey != null && Object.hasOwnProperty.call(message, "newGroupPubkey"))
                writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.newGroupPubkey);
            if (message.validationStatus != null && Object.hasOwnProperty.call(message, "validationStatus"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.validationStatus);
            if (message.validationMsgHash != null && Object.hasOwnProperty.call(message, "validationMsgHash"))
                writer.uint32(/* id 17, wireType 2 =*/138).bytes(message.validationMsgHash);
            if (message.lifecycle != null && Object.hasOwnProperty.call(message, "lifecycle"))
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.lifecycle);
            if (message.dkgSharingDeadline != null && Object.hasOwnProperty.call(message, "dkgSharingDeadline"))
                writer.uint32(/* id 19, wireType 0 =*/152).uint64(message.dkgSharingDeadline);
            return writer;
        };

        /**
         * Encodes the specified VaultTransitionState message, length delimited. Does not implicitly {@link pb.VaultTransitionState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.VaultTransitionState
         * @static
         * @param {pb.IVaultTransitionState} message VaultTransitionState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultTransitionState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a VaultTransitionState message from the specified reader or buffer.
         * @function decode
         * @memberof pb.VaultTransitionState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.VaultTransitionState} VaultTransitionState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultTransitionState.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.VaultTransitionState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.signAlgo = reader.int32();
                        break;
                    }
                case 5: {
                        message.triggerHeight = reader.uint64();
                        break;
                    }
                case 6: {
                        if (!(message.oldCommitteeMembers && message.oldCommitteeMembers.length))
                            message.oldCommitteeMembers = [];
                        message.oldCommitteeMembers.push(reader.string());
                        break;
                    }
                case 7: {
                        if (!(message.newCommitteeMembers && message.newCommitteeMembers.length))
                            message.newCommitteeMembers = [];
                        message.newCommitteeMembers.push(reader.string());
                        break;
                    }
                case 8: {
                        message.dkgStatus = reader.string();
                        break;
                    }
                case 9: {
                        message.dkgSessionId = reader.string();
                        break;
                    }
                case 10: {
                        message.dkgThresholdT = reader.uint32();
                        break;
                    }
                case 11: {
                        message.dkgN = reader.uint32();
                        break;
                    }
                case 12: {
                        message.dkgCommitDeadline = reader.uint64();
                        break;
                    }
                case 13: {
                        message.dkgDisputeDeadline = reader.uint64();
                        break;
                    }
                case 14: {
                        message.oldGroupPubkey = reader.bytes();
                        break;
                    }
                case 15: {
                        message.newGroupPubkey = reader.bytes();
                        break;
                    }
                case 16: {
                        message.validationStatus = reader.string();
                        break;
                    }
                case 17: {
                        message.validationMsgHash = reader.bytes();
                        break;
                    }
                case 18: {
                        message.lifecycle = reader.string();
                        break;
                    }
                case 19: {
                        message.dkgSharingDeadline = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VaultTransitionState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.VaultTransitionState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.VaultTransitionState} VaultTransitionState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultTransitionState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VaultTransitionState message.
         * @function verify
         * @memberof pb.VaultTransitionState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VaultTransitionState.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                switch (message.signAlgo) {
                default:
                    return "signAlgo: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.triggerHeight != null && message.hasOwnProperty("triggerHeight"))
                if (!$util.isInteger(message.triggerHeight) && !(message.triggerHeight && $util.isInteger(message.triggerHeight.low) && $util.isInteger(message.triggerHeight.high)))
                    return "triggerHeight: integer|Long expected";
            if (message.oldCommitteeMembers != null && message.hasOwnProperty("oldCommitteeMembers")) {
                if (!Array.isArray(message.oldCommitteeMembers))
                    return "oldCommitteeMembers: array expected";
                for (let i = 0; i < message.oldCommitteeMembers.length; ++i)
                    if (!$util.isString(message.oldCommitteeMembers[i]))
                        return "oldCommitteeMembers: string[] expected";
            }
            if (message.newCommitteeMembers != null && message.hasOwnProperty("newCommitteeMembers")) {
                if (!Array.isArray(message.newCommitteeMembers))
                    return "newCommitteeMembers: array expected";
                for (let i = 0; i < message.newCommitteeMembers.length; ++i)
                    if (!$util.isString(message.newCommitteeMembers[i]))
                        return "newCommitteeMembers: string[] expected";
            }
            if (message.dkgStatus != null && message.hasOwnProperty("dkgStatus"))
                if (!$util.isString(message.dkgStatus))
                    return "dkgStatus: string expected";
            if (message.dkgSessionId != null && message.hasOwnProperty("dkgSessionId"))
                if (!$util.isString(message.dkgSessionId))
                    return "dkgSessionId: string expected";
            if (message.dkgThresholdT != null && message.hasOwnProperty("dkgThresholdT"))
                if (!$util.isInteger(message.dkgThresholdT))
                    return "dkgThresholdT: integer expected";
            if (message.dkgN != null && message.hasOwnProperty("dkgN"))
                if (!$util.isInteger(message.dkgN))
                    return "dkgN: integer expected";
            if (message.dkgCommitDeadline != null && message.hasOwnProperty("dkgCommitDeadline"))
                if (!$util.isInteger(message.dkgCommitDeadline) && !(message.dkgCommitDeadline && $util.isInteger(message.dkgCommitDeadline.low) && $util.isInteger(message.dkgCommitDeadline.high)))
                    return "dkgCommitDeadline: integer|Long expected";
            if (message.dkgDisputeDeadline != null && message.hasOwnProperty("dkgDisputeDeadline"))
                if (!$util.isInteger(message.dkgDisputeDeadline) && !(message.dkgDisputeDeadline && $util.isInteger(message.dkgDisputeDeadline.low) && $util.isInteger(message.dkgDisputeDeadline.high)))
                    return "dkgDisputeDeadline: integer|Long expected";
            if (message.oldGroupPubkey != null && message.hasOwnProperty("oldGroupPubkey"))
                if (!(message.oldGroupPubkey && typeof message.oldGroupPubkey.length === "number" || $util.isString(message.oldGroupPubkey)))
                    return "oldGroupPubkey: buffer expected";
            if (message.newGroupPubkey != null && message.hasOwnProperty("newGroupPubkey"))
                if (!(message.newGroupPubkey && typeof message.newGroupPubkey.length === "number" || $util.isString(message.newGroupPubkey)))
                    return "newGroupPubkey: buffer expected";
            if (message.validationStatus != null && message.hasOwnProperty("validationStatus"))
                if (!$util.isString(message.validationStatus))
                    return "validationStatus: string expected";
            if (message.validationMsgHash != null && message.hasOwnProperty("validationMsgHash"))
                if (!(message.validationMsgHash && typeof message.validationMsgHash.length === "number" || $util.isString(message.validationMsgHash)))
                    return "validationMsgHash: buffer expected";
            if (message.lifecycle != null && message.hasOwnProperty("lifecycle"))
                if (!$util.isString(message.lifecycle))
                    return "lifecycle: string expected";
            if (message.dkgSharingDeadline != null && message.hasOwnProperty("dkgSharingDeadline"))
                if (!$util.isInteger(message.dkgSharingDeadline) && !(message.dkgSharingDeadline && $util.isInteger(message.dkgSharingDeadline.low) && $util.isInteger(message.dkgSharingDeadline.high)))
                    return "dkgSharingDeadline: integer|Long expected";
            return null;
        };

        /**
         * Creates a VaultTransitionState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.VaultTransitionState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.VaultTransitionState} VaultTransitionState
         */
        VaultTransitionState.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.VaultTransitionState)
                return object;
            let message = new $root.pb.VaultTransitionState();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            switch (object.signAlgo) {
            default:
                if (typeof object.signAlgo === "number") {
                    message.signAlgo = object.signAlgo;
                    break;
                }
                break;
            case "SIGN_ALGO_UNSPECIFIED":
            case 0:
                message.signAlgo = 0;
                break;
            case "SIGN_ALGO_ECDSA_P256":
            case 1:
                message.signAlgo = 1;
                break;
            case "SIGN_ALGO_BLS_BN256_G2":
            case 2:
                message.signAlgo = 2;
                break;
            case "SIGN_ALGO_SCHNORR_SECP256K1_BIP340":
            case 3:
                message.signAlgo = 3;
                break;
            case "SIGN_ALGO_SCHNORR_ALT_BN128":
            case 4:
                message.signAlgo = 4;
                break;
            case "SIGN_ALGO_ED25519":
            case 5:
                message.signAlgo = 5;
                break;
            case "SIGN_ALGO_ECDSA_SECP256K1":
            case 6:
                message.signAlgo = 6;
                break;
            }
            if (object.triggerHeight != null)
                if ($util.Long)
                    (message.triggerHeight = $util.Long.fromValue(object.triggerHeight)).unsigned = true;
                else if (typeof object.triggerHeight === "string")
                    message.triggerHeight = parseInt(object.triggerHeight, 10);
                else if (typeof object.triggerHeight === "number")
                    message.triggerHeight = object.triggerHeight;
                else if (typeof object.triggerHeight === "object")
                    message.triggerHeight = new $util.LongBits(object.triggerHeight.low >>> 0, object.triggerHeight.high >>> 0).toNumber(true);
            if (object.oldCommitteeMembers) {
                if (!Array.isArray(object.oldCommitteeMembers))
                    throw TypeError(".pb.VaultTransitionState.oldCommitteeMembers: array expected");
                message.oldCommitteeMembers = [];
                for (let i = 0; i < object.oldCommitteeMembers.length; ++i)
                    message.oldCommitteeMembers[i] = String(object.oldCommitteeMembers[i]);
            }
            if (object.newCommitteeMembers) {
                if (!Array.isArray(object.newCommitteeMembers))
                    throw TypeError(".pb.VaultTransitionState.newCommitteeMembers: array expected");
                message.newCommitteeMembers = [];
                for (let i = 0; i < object.newCommitteeMembers.length; ++i)
                    message.newCommitteeMembers[i] = String(object.newCommitteeMembers[i]);
            }
            if (object.dkgStatus != null)
                message.dkgStatus = String(object.dkgStatus);
            if (object.dkgSessionId != null)
                message.dkgSessionId = String(object.dkgSessionId);
            if (object.dkgThresholdT != null)
                message.dkgThresholdT = object.dkgThresholdT >>> 0;
            if (object.dkgN != null)
                message.dkgN = object.dkgN >>> 0;
            if (object.dkgCommitDeadline != null)
                if ($util.Long)
                    (message.dkgCommitDeadline = $util.Long.fromValue(object.dkgCommitDeadline)).unsigned = true;
                else if (typeof object.dkgCommitDeadline === "string")
                    message.dkgCommitDeadline = parseInt(object.dkgCommitDeadline, 10);
                else if (typeof object.dkgCommitDeadline === "number")
                    message.dkgCommitDeadline = object.dkgCommitDeadline;
                else if (typeof object.dkgCommitDeadline === "object")
                    message.dkgCommitDeadline = new $util.LongBits(object.dkgCommitDeadline.low >>> 0, object.dkgCommitDeadline.high >>> 0).toNumber(true);
            if (object.dkgDisputeDeadline != null)
                if ($util.Long)
                    (message.dkgDisputeDeadline = $util.Long.fromValue(object.dkgDisputeDeadline)).unsigned = true;
                else if (typeof object.dkgDisputeDeadline === "string")
                    message.dkgDisputeDeadline = parseInt(object.dkgDisputeDeadline, 10);
                else if (typeof object.dkgDisputeDeadline === "number")
                    message.dkgDisputeDeadline = object.dkgDisputeDeadline;
                else if (typeof object.dkgDisputeDeadline === "object")
                    message.dkgDisputeDeadline = new $util.LongBits(object.dkgDisputeDeadline.low >>> 0, object.dkgDisputeDeadline.high >>> 0).toNumber(true);
            if (object.oldGroupPubkey != null)
                if (typeof object.oldGroupPubkey === "string")
                    $util.base64.decode(object.oldGroupPubkey, message.oldGroupPubkey = $util.newBuffer($util.base64.length(object.oldGroupPubkey)), 0);
                else if (object.oldGroupPubkey.length >= 0)
                    message.oldGroupPubkey = object.oldGroupPubkey;
            if (object.newGroupPubkey != null)
                if (typeof object.newGroupPubkey === "string")
                    $util.base64.decode(object.newGroupPubkey, message.newGroupPubkey = $util.newBuffer($util.base64.length(object.newGroupPubkey)), 0);
                else if (object.newGroupPubkey.length >= 0)
                    message.newGroupPubkey = object.newGroupPubkey;
            if (object.validationStatus != null)
                message.validationStatus = String(object.validationStatus);
            if (object.validationMsgHash != null)
                if (typeof object.validationMsgHash === "string")
                    $util.base64.decode(object.validationMsgHash, message.validationMsgHash = $util.newBuffer($util.base64.length(object.validationMsgHash)), 0);
                else if (object.validationMsgHash.length >= 0)
                    message.validationMsgHash = object.validationMsgHash;
            if (object.lifecycle != null)
                message.lifecycle = String(object.lifecycle);
            if (object.dkgSharingDeadline != null)
                if ($util.Long)
                    (message.dkgSharingDeadline = $util.Long.fromValue(object.dkgSharingDeadline)).unsigned = true;
                else if (typeof object.dkgSharingDeadline === "string")
                    message.dkgSharingDeadline = parseInt(object.dkgSharingDeadline, 10);
                else if (typeof object.dkgSharingDeadline === "number")
                    message.dkgSharingDeadline = object.dkgSharingDeadline;
                else if (typeof object.dkgSharingDeadline === "object")
                    message.dkgSharingDeadline = new $util.LongBits(object.dkgSharingDeadline.low >>> 0, object.dkgSharingDeadline.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a VaultTransitionState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.VaultTransitionState
         * @static
         * @param {pb.VaultTransitionState} message VaultTransitionState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VaultTransitionState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.oldCommitteeMembers = [];
                object.newCommitteeMembers = [];
            }
            if (options.defaults) {
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.signAlgo = options.enums === String ? "SIGN_ALGO_UNSPECIFIED" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.triggerHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.triggerHeight = options.longs === String ? "0" : 0;
                object.dkgStatus = "";
                object.dkgSessionId = "";
                object.dkgThresholdT = 0;
                object.dkgN = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.dkgCommitDeadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.dkgCommitDeadline = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.dkgDisputeDeadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.dkgDisputeDeadline = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.oldGroupPubkey = "";
                else {
                    object.oldGroupPubkey = [];
                    if (options.bytes !== Array)
                        object.oldGroupPubkey = $util.newBuffer(object.oldGroupPubkey);
                }
                if (options.bytes === String)
                    object.newGroupPubkey = "";
                else {
                    object.newGroupPubkey = [];
                    if (options.bytes !== Array)
                        object.newGroupPubkey = $util.newBuffer(object.newGroupPubkey);
                }
                object.validationStatus = "";
                if (options.bytes === String)
                    object.validationMsgHash = "";
                else {
                    object.validationMsgHash = [];
                    if (options.bytes !== Array)
                        object.validationMsgHash = $util.newBuffer(object.validationMsgHash);
                }
                object.lifecycle = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.dkgSharingDeadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.dkgSharingDeadline = options.longs === String ? "0" : 0;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = options.enums === String ? $root.pb.SignAlgo[message.signAlgo] === undefined ? message.signAlgo : $root.pb.SignAlgo[message.signAlgo] : message.signAlgo;
            if (message.triggerHeight != null && message.hasOwnProperty("triggerHeight"))
                if (typeof message.triggerHeight === "number")
                    object.triggerHeight = options.longs === String ? String(message.triggerHeight) : message.triggerHeight;
                else
                    object.triggerHeight = options.longs === String ? $util.Long.prototype.toString.call(message.triggerHeight) : options.longs === Number ? new $util.LongBits(message.triggerHeight.low >>> 0, message.triggerHeight.high >>> 0).toNumber(true) : message.triggerHeight;
            if (message.oldCommitteeMembers && message.oldCommitteeMembers.length) {
                object.oldCommitteeMembers = [];
                for (let j = 0; j < message.oldCommitteeMembers.length; ++j)
                    object.oldCommitteeMembers[j] = message.oldCommitteeMembers[j];
            }
            if (message.newCommitteeMembers && message.newCommitteeMembers.length) {
                object.newCommitteeMembers = [];
                for (let j = 0; j < message.newCommitteeMembers.length; ++j)
                    object.newCommitteeMembers[j] = message.newCommitteeMembers[j];
            }
            if (message.dkgStatus != null && message.hasOwnProperty("dkgStatus"))
                object.dkgStatus = message.dkgStatus;
            if (message.dkgSessionId != null && message.hasOwnProperty("dkgSessionId"))
                object.dkgSessionId = message.dkgSessionId;
            if (message.dkgThresholdT != null && message.hasOwnProperty("dkgThresholdT"))
                object.dkgThresholdT = message.dkgThresholdT;
            if (message.dkgN != null && message.hasOwnProperty("dkgN"))
                object.dkgN = message.dkgN;
            if (message.dkgCommitDeadline != null && message.hasOwnProperty("dkgCommitDeadline"))
                if (typeof message.dkgCommitDeadline === "number")
                    object.dkgCommitDeadline = options.longs === String ? String(message.dkgCommitDeadline) : message.dkgCommitDeadline;
                else
                    object.dkgCommitDeadline = options.longs === String ? $util.Long.prototype.toString.call(message.dkgCommitDeadline) : options.longs === Number ? new $util.LongBits(message.dkgCommitDeadline.low >>> 0, message.dkgCommitDeadline.high >>> 0).toNumber(true) : message.dkgCommitDeadline;
            if (message.dkgDisputeDeadline != null && message.hasOwnProperty("dkgDisputeDeadline"))
                if (typeof message.dkgDisputeDeadline === "number")
                    object.dkgDisputeDeadline = options.longs === String ? String(message.dkgDisputeDeadline) : message.dkgDisputeDeadline;
                else
                    object.dkgDisputeDeadline = options.longs === String ? $util.Long.prototype.toString.call(message.dkgDisputeDeadline) : options.longs === Number ? new $util.LongBits(message.dkgDisputeDeadline.low >>> 0, message.dkgDisputeDeadline.high >>> 0).toNumber(true) : message.dkgDisputeDeadline;
            if (message.oldGroupPubkey != null && message.hasOwnProperty("oldGroupPubkey"))
                object.oldGroupPubkey = options.bytes === String ? $util.base64.encode(message.oldGroupPubkey, 0, message.oldGroupPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.oldGroupPubkey) : message.oldGroupPubkey;
            if (message.newGroupPubkey != null && message.hasOwnProperty("newGroupPubkey"))
                object.newGroupPubkey = options.bytes === String ? $util.base64.encode(message.newGroupPubkey, 0, message.newGroupPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.newGroupPubkey) : message.newGroupPubkey;
            if (message.validationStatus != null && message.hasOwnProperty("validationStatus"))
                object.validationStatus = message.validationStatus;
            if (message.validationMsgHash != null && message.hasOwnProperty("validationMsgHash"))
                object.validationMsgHash = options.bytes === String ? $util.base64.encode(message.validationMsgHash, 0, message.validationMsgHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.validationMsgHash) : message.validationMsgHash;
            if (message.lifecycle != null && message.hasOwnProperty("lifecycle"))
                object.lifecycle = message.lifecycle;
            if (message.dkgSharingDeadline != null && message.hasOwnProperty("dkgSharingDeadline"))
                if (typeof message.dkgSharingDeadline === "number")
                    object.dkgSharingDeadline = options.longs === String ? String(message.dkgSharingDeadline) : message.dkgSharingDeadline;
                else
                    object.dkgSharingDeadline = options.longs === String ? $util.Long.prototype.toString.call(message.dkgSharingDeadline) : options.longs === Number ? new $util.LongBits(message.dkgSharingDeadline.low >>> 0, message.dkgSharingDeadline.high >>> 0).toNumber(true) : message.dkgSharingDeadline;
            return object;
        };

        /**
         * Converts this VaultTransitionState to JSON.
         * @function toJSON
         * @memberof pb.VaultTransitionState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VaultTransitionState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VaultTransitionState
         * @function getTypeUrl
         * @memberof pb.VaultTransitionState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VaultTransitionState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.VaultTransitionState";
        };

        return VaultTransitionState;
    })();

    pb.FrostEnvelope = (function() {

        /**
         * Properties of a FrostEnvelope.
         * @memberof pb
         * @interface IFrostEnvelope
         * @property {string|null} [from] FrostEnvelope from
         * @property {string|null} [to] FrostEnvelope to
         * @property {pb.FrostEnvelopeKind|null} [kind] FrostEnvelope kind
         * @property {Uint8Array|null} [payload] FrostEnvelope payload
         * @property {Uint8Array|null} [sig] FrostEnvelope sig
         * @property {string|null} [jobId] FrostEnvelope jobId
         * @property {number|Long|null} [seq] FrostEnvelope seq
         */

        /**
         * Constructs a new FrostEnvelope.
         * @memberof pb
         * @classdesc Represents a FrostEnvelope.
         * @implements IFrostEnvelope
         * @constructor
         * @param {pb.IFrostEnvelope=} [properties] Properties to set
         */
        function FrostEnvelope(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostEnvelope from.
         * @member {string} from
         * @memberof pb.FrostEnvelope
         * @instance
         */
        FrostEnvelope.prototype.from = "";

        /**
         * FrostEnvelope to.
         * @member {string} to
         * @memberof pb.FrostEnvelope
         * @instance
         */
        FrostEnvelope.prototype.to = "";

        /**
         * FrostEnvelope kind.
         * @member {pb.FrostEnvelopeKind} kind
         * @memberof pb.FrostEnvelope
         * @instance
         */
        FrostEnvelope.prototype.kind = 0;

        /**
         * FrostEnvelope payload.
         * @member {Uint8Array} payload
         * @memberof pb.FrostEnvelope
         * @instance
         */
        FrostEnvelope.prototype.payload = $util.newBuffer([]);

        /**
         * FrostEnvelope sig.
         * @member {Uint8Array} sig
         * @memberof pb.FrostEnvelope
         * @instance
         */
        FrostEnvelope.prototype.sig = $util.newBuffer([]);

        /**
         * FrostEnvelope jobId.
         * @member {string} jobId
         * @memberof pb.FrostEnvelope
         * @instance
         */
        FrostEnvelope.prototype.jobId = "";

        /**
         * FrostEnvelope seq.
         * @member {number|Long} seq
         * @memberof pb.FrostEnvelope
         * @instance
         */
        FrostEnvelope.prototype.seq = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FrostEnvelope instance using the specified properties.
         * @function create
         * @memberof pb.FrostEnvelope
         * @static
         * @param {pb.IFrostEnvelope=} [properties] Properties to set
         * @returns {pb.FrostEnvelope} FrostEnvelope instance
         */
        FrostEnvelope.create = function create(properties) {
            return new FrostEnvelope(properties);
        };

        /**
         * Encodes the specified FrostEnvelope message. Does not implicitly {@link pb.FrostEnvelope.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostEnvelope
         * @static
         * @param {pb.IFrostEnvelope} message FrostEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostEnvelope.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.from != null && Object.hasOwnProperty.call(message, "from"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.from);
            if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.to);
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.kind);
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.payload);
            if (message.sig != null && Object.hasOwnProperty.call(message, "sig"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.sig);
            if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.jobId);
            if (message.seq != null && Object.hasOwnProperty.call(message, "seq"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.seq);
            return writer;
        };

        /**
         * Encodes the specified FrostEnvelope message, length delimited. Does not implicitly {@link pb.FrostEnvelope.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostEnvelope
         * @static
         * @param {pb.IFrostEnvelope} message FrostEnvelope message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostEnvelope.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostEnvelope message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostEnvelope} FrostEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostEnvelope.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostEnvelope();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.from = reader.string();
                        break;
                    }
                case 2: {
                        message.to = reader.string();
                        break;
                    }
                case 3: {
                        message.kind = reader.int32();
                        break;
                    }
                case 4: {
                        message.payload = reader.bytes();
                        break;
                    }
                case 5: {
                        message.sig = reader.bytes();
                        break;
                    }
                case 6: {
                        message.jobId = reader.string();
                        break;
                    }
                case 7: {
                        message.seq = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostEnvelope message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostEnvelope
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostEnvelope} FrostEnvelope
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostEnvelope.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostEnvelope message.
         * @function verify
         * @memberof pb.FrostEnvelope
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostEnvelope.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.from != null && message.hasOwnProperty("from"))
                if (!$util.isString(message.from))
                    return "from: string expected";
            if (message.to != null && message.hasOwnProperty("to"))
                if (!$util.isString(message.to))
                    return "to: string expected";
            if (message.kind != null && message.hasOwnProperty("kind"))
                switch (message.kind) {
                default:
                    return "kind: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.payload != null && message.hasOwnProperty("payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            if (message.sig != null && message.hasOwnProperty("sig"))
                if (!(message.sig && typeof message.sig.length === "number" || $util.isString(message.sig)))
                    return "sig: buffer expected";
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                if (!$util.isString(message.jobId))
                    return "jobId: string expected";
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (!$util.isInteger(message.seq) && !(message.seq && $util.isInteger(message.seq.low) && $util.isInteger(message.seq.high)))
                    return "seq: integer|Long expected";
            return null;
        };

        /**
         * Creates a FrostEnvelope message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostEnvelope
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostEnvelope} FrostEnvelope
         */
        FrostEnvelope.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostEnvelope)
                return object;
            let message = new $root.pb.FrostEnvelope();
            if (object.from != null)
                message.from = String(object.from);
            if (object.to != null)
                message.to = String(object.to);
            switch (object.kind) {
            default:
                if (typeof object.kind === "number") {
                    message.kind = object.kind;
                    break;
                }
                break;
            case "FROST_ENVELOPE_KIND_UNSPECIFIED":
            case 0:
                message.kind = 0;
                break;
            case "FROST_ENVELOPE_KIND_DKG_ROUND1":
            case 1:
                message.kind = 1;
                break;
            case "FROST_ENVELOPE_KIND_DKG_ROUND2":
            case 2:
                message.kind = 2;
                break;
            case "FROST_ENVELOPE_KIND_SIGN_NONCE":
            case 3:
                message.kind = 3;
                break;
            case "FROST_ENVELOPE_KIND_SIGN_SHARE":
            case 4:
                message.kind = 4;
                break;
            case "FROST_ENVELOPE_KIND_ROAST_REQUEST":
            case 5:
                message.kind = 5;
                break;
            case "FROST_ENVELOPE_KIND_ROAST_RESPONSE":
            case 6:
                message.kind = 6;
                break;
            }
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            if (object.sig != null)
                if (typeof object.sig === "string")
                    $util.base64.decode(object.sig, message.sig = $util.newBuffer($util.base64.length(object.sig)), 0);
                else if (object.sig.length >= 0)
                    message.sig = object.sig;
            if (object.jobId != null)
                message.jobId = String(object.jobId);
            if (object.seq != null)
                if ($util.Long)
                    (message.seq = $util.Long.fromValue(object.seq)).unsigned = true;
                else if (typeof object.seq === "string")
                    message.seq = parseInt(object.seq, 10);
                else if (typeof object.seq === "number")
                    message.seq = object.seq;
                else if (typeof object.seq === "object")
                    message.seq = new $util.LongBits(object.seq.low >>> 0, object.seq.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FrostEnvelope message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostEnvelope
         * @static
         * @param {pb.FrostEnvelope} message FrostEnvelope
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostEnvelope.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.from = "";
                object.to = "";
                object.kind = options.enums === String ? "FROST_ENVELOPE_KIND_UNSPECIFIED" : 0;
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
                if (options.bytes === String)
                    object.sig = "";
                else {
                    object.sig = [];
                    if (options.bytes !== Array)
                        object.sig = $util.newBuffer(object.sig);
                }
                object.jobId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.seq = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.seq = options.longs === String ? "0" : 0;
            }
            if (message.from != null && message.hasOwnProperty("from"))
                object.from = message.from;
            if (message.to != null && message.hasOwnProperty("to"))
                object.to = message.to;
            if (message.kind != null && message.hasOwnProperty("kind"))
                object.kind = options.enums === String ? $root.pb.FrostEnvelopeKind[message.kind] === undefined ? message.kind : $root.pb.FrostEnvelopeKind[message.kind] : message.kind;
            if (message.payload != null && message.hasOwnProperty("payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            if (message.sig != null && message.hasOwnProperty("sig"))
                object.sig = options.bytes === String ? $util.base64.encode(message.sig, 0, message.sig.length) : options.bytes === Array ? Array.prototype.slice.call(message.sig) : message.sig;
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                object.jobId = message.jobId;
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (typeof message.seq === "number")
                    object.seq = options.longs === String ? String(message.seq) : message.seq;
                else
                    object.seq = options.longs === String ? $util.Long.prototype.toString.call(message.seq) : options.longs === Number ? new $util.LongBits(message.seq.low >>> 0, message.seq.high >>> 0).toNumber(true) : message.seq;
            return object;
        };

        /**
         * Converts this FrostEnvelope to JSON.
         * @function toJSON
         * @memberof pb.FrostEnvelope
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostEnvelope.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostEnvelope
         * @function getTypeUrl
         * @memberof pb.FrostEnvelope
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostEnvelope.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostEnvelope";
        };

        return FrostEnvelope;
    })();

    /**
     * FrostEnvelopeKind enum.
     * @name pb.FrostEnvelopeKind
     * @enum {number}
     * @property {number} FROST_ENVELOPE_KIND_UNSPECIFIED=0 FROST_ENVELOPE_KIND_UNSPECIFIED value
     * @property {number} FROST_ENVELOPE_KIND_DKG_ROUND1=1 FROST_ENVELOPE_KIND_DKG_ROUND1 value
     * @property {number} FROST_ENVELOPE_KIND_DKG_ROUND2=2 FROST_ENVELOPE_KIND_DKG_ROUND2 value
     * @property {number} FROST_ENVELOPE_KIND_SIGN_NONCE=3 FROST_ENVELOPE_KIND_SIGN_NONCE value
     * @property {number} FROST_ENVELOPE_KIND_SIGN_SHARE=4 FROST_ENVELOPE_KIND_SIGN_SHARE value
     * @property {number} FROST_ENVELOPE_KIND_ROAST_REQUEST=5 FROST_ENVELOPE_KIND_ROAST_REQUEST value
     * @property {number} FROST_ENVELOPE_KIND_ROAST_RESPONSE=6 FROST_ENVELOPE_KIND_ROAST_RESPONSE value
     */
    pb.FrostEnvelopeKind = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "FROST_ENVELOPE_KIND_UNSPECIFIED"] = 0;
        values[valuesById[1] = "FROST_ENVELOPE_KIND_DKG_ROUND1"] = 1;
        values[valuesById[2] = "FROST_ENVELOPE_KIND_DKG_ROUND2"] = 2;
        values[valuesById[3] = "FROST_ENVELOPE_KIND_SIGN_NONCE"] = 3;
        values[valuesById[4] = "FROST_ENVELOPE_KIND_SIGN_SHARE"] = 4;
        values[valuesById[5] = "FROST_ENVELOPE_KIND_ROAST_REQUEST"] = 5;
        values[valuesById[6] = "FROST_ENVELOPE_KIND_ROAST_RESPONSE"] = 6;
        return values;
    })();

    pb.FrostVaultDkgCommitTx = (function() {

        /**
         * Properties of a FrostVaultDkgCommitTx.
         * @memberof pb
         * @interface IFrostVaultDkgCommitTx
         * @property {pb.IBaseMessage|null} [base] FrostVaultDkgCommitTx base
         * @property {string|null} [chain] FrostVaultDkgCommitTx chain
         * @property {number|null} [vaultId] FrostVaultDkgCommitTx vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgCommitTx epochId
         * @property {pb.SignAlgo|null} [signAlgo] FrostVaultDkgCommitTx signAlgo
         * @property {Array.<Uint8Array>|null} [commitmentPoints] FrostVaultDkgCommitTx commitmentPoints
         * @property {Uint8Array|null} [aI0] FrostVaultDkgCommitTx aI0
         */

        /**
         * Constructs a new FrostVaultDkgCommitTx.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgCommitTx.
         * @implements IFrostVaultDkgCommitTx
         * @constructor
         * @param {pb.IFrostVaultDkgCommitTx=} [properties] Properties to set
         */
        function FrostVaultDkgCommitTx(properties) {
            this.commitmentPoints = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgCommitTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         */
        FrostVaultDkgCommitTx.prototype.base = null;

        /**
         * FrostVaultDkgCommitTx chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         */
        FrostVaultDkgCommitTx.prototype.chain = "";

        /**
         * FrostVaultDkgCommitTx vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         */
        FrostVaultDkgCommitTx.prototype.vaultId = 0;

        /**
         * FrostVaultDkgCommitTx epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         */
        FrostVaultDkgCommitTx.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgCommitTx signAlgo.
         * @member {pb.SignAlgo} signAlgo
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         */
        FrostVaultDkgCommitTx.prototype.signAlgo = 0;

        /**
         * FrostVaultDkgCommitTx commitmentPoints.
         * @member {Array.<Uint8Array>} commitmentPoints
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         */
        FrostVaultDkgCommitTx.prototype.commitmentPoints = $util.emptyArray;

        /**
         * FrostVaultDkgCommitTx aI0.
         * @member {Uint8Array} aI0
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         */
        FrostVaultDkgCommitTx.prototype.aI0 = $util.newBuffer([]);

        /**
         * Creates a new FrostVaultDkgCommitTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {pb.IFrostVaultDkgCommitTx=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgCommitTx} FrostVaultDkgCommitTx instance
         */
        FrostVaultDkgCommitTx.create = function create(properties) {
            return new FrostVaultDkgCommitTx(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgCommitTx message. Does not implicitly {@link pb.FrostVaultDkgCommitTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {pb.IFrostVaultDkgCommitTx} message FrostVaultDkgCommitTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgCommitTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.epochId);
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.signAlgo);
            if (message.commitmentPoints != null && message.commitmentPoints.length)
                for (let i = 0; i < message.commitmentPoints.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.commitmentPoints[i]);
            if (message.aI0 != null && Object.hasOwnProperty.call(message, "aI0"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.aI0);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgCommitTx message, length delimited. Does not implicitly {@link pb.FrostVaultDkgCommitTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {pb.IFrostVaultDkgCommitTx} message FrostVaultDkgCommitTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgCommitTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgCommitTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgCommitTx} FrostVaultDkgCommitTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgCommitTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgCommitTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 5: {
                        message.signAlgo = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.commitmentPoints && message.commitmentPoints.length))
                            message.commitmentPoints = [];
                        message.commitmentPoints.push(reader.bytes());
                        break;
                    }
                case 7: {
                        message.aI0 = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgCommitTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgCommitTx} FrostVaultDkgCommitTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgCommitTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgCommitTx message.
         * @function verify
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgCommitTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                switch (message.signAlgo) {
                default:
                    return "signAlgo: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.commitmentPoints != null && message.hasOwnProperty("commitmentPoints")) {
                if (!Array.isArray(message.commitmentPoints))
                    return "commitmentPoints: array expected";
                for (let i = 0; i < message.commitmentPoints.length; ++i)
                    if (!(message.commitmentPoints[i] && typeof message.commitmentPoints[i].length === "number" || $util.isString(message.commitmentPoints[i])))
                        return "commitmentPoints: buffer[] expected";
            }
            if (message.aI0 != null && message.hasOwnProperty("aI0"))
                if (!(message.aI0 && typeof message.aI0.length === "number" || $util.isString(message.aI0)))
                    return "aI0: buffer expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgCommitTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgCommitTx} FrostVaultDkgCommitTx
         */
        FrostVaultDkgCommitTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgCommitTx)
                return object;
            let message = new $root.pb.FrostVaultDkgCommitTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostVaultDkgCommitTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            switch (object.signAlgo) {
            default:
                if (typeof object.signAlgo === "number") {
                    message.signAlgo = object.signAlgo;
                    break;
                }
                break;
            case "SIGN_ALGO_UNSPECIFIED":
            case 0:
                message.signAlgo = 0;
                break;
            case "SIGN_ALGO_ECDSA_P256":
            case 1:
                message.signAlgo = 1;
                break;
            case "SIGN_ALGO_BLS_BN256_G2":
            case 2:
                message.signAlgo = 2;
                break;
            case "SIGN_ALGO_SCHNORR_SECP256K1_BIP340":
            case 3:
                message.signAlgo = 3;
                break;
            case "SIGN_ALGO_SCHNORR_ALT_BN128":
            case 4:
                message.signAlgo = 4;
                break;
            case "SIGN_ALGO_ED25519":
            case 5:
                message.signAlgo = 5;
                break;
            case "SIGN_ALGO_ECDSA_SECP256K1":
            case 6:
                message.signAlgo = 6;
                break;
            }
            if (object.commitmentPoints) {
                if (!Array.isArray(object.commitmentPoints))
                    throw TypeError(".pb.FrostVaultDkgCommitTx.commitmentPoints: array expected");
                message.commitmentPoints = [];
                for (let i = 0; i < object.commitmentPoints.length; ++i)
                    if (typeof object.commitmentPoints[i] === "string")
                        $util.base64.decode(object.commitmentPoints[i], message.commitmentPoints[i] = $util.newBuffer($util.base64.length(object.commitmentPoints[i])), 0);
                    else if (object.commitmentPoints[i].length >= 0)
                        message.commitmentPoints[i] = object.commitmentPoints[i];
            }
            if (object.aI0 != null)
                if (typeof object.aI0 === "string")
                    $util.base64.decode(object.aI0, message.aI0 = $util.newBuffer($util.base64.length(object.aI0)), 0);
                else if (object.aI0.length >= 0)
                    message.aI0 = object.aI0;
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgCommitTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {pb.FrostVaultDkgCommitTx} message FrostVaultDkgCommitTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgCommitTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.commitmentPoints = [];
            if (options.defaults) {
                object.base = null;
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.signAlgo = options.enums === String ? "SIGN_ALGO_UNSPECIFIED" : 0;
                if (options.bytes === String)
                    object.aI0 = "";
                else {
                    object.aI0 = [];
                    if (options.bytes !== Array)
                        object.aI0 = $util.newBuffer(object.aI0);
                }
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = options.enums === String ? $root.pb.SignAlgo[message.signAlgo] === undefined ? message.signAlgo : $root.pb.SignAlgo[message.signAlgo] : message.signAlgo;
            if (message.commitmentPoints && message.commitmentPoints.length) {
                object.commitmentPoints = [];
                for (let j = 0; j < message.commitmentPoints.length; ++j)
                    object.commitmentPoints[j] = options.bytes === String ? $util.base64.encode(message.commitmentPoints[j], 0, message.commitmentPoints[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.commitmentPoints[j]) : message.commitmentPoints[j];
            }
            if (message.aI0 != null && message.hasOwnProperty("aI0"))
                object.aI0 = options.bytes === String ? $util.base64.encode(message.aI0, 0, message.aI0.length) : options.bytes === Array ? Array.prototype.slice.call(message.aI0) : message.aI0;
            return object;
        };

        /**
         * Converts this FrostVaultDkgCommitTx to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgCommitTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgCommitTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgCommitTx
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgCommitTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgCommitTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgCommitTx";
        };

        return FrostVaultDkgCommitTx;
    })();

    pb.FrostVaultDkgShareTx = (function() {

        /**
         * Properties of a FrostVaultDkgShareTx.
         * @memberof pb
         * @interface IFrostVaultDkgShareTx
         * @property {pb.IBaseMessage|null} [base] FrostVaultDkgShareTx base
         * @property {string|null} [chain] FrostVaultDkgShareTx chain
         * @property {number|null} [vaultId] FrostVaultDkgShareTx vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgShareTx epochId
         * @property {string|null} [dealerId] FrostVaultDkgShareTx dealerId
         * @property {string|null} [receiverId] FrostVaultDkgShareTx receiverId
         * @property {Uint8Array|null} [ciphertext] FrostVaultDkgShareTx ciphertext
         */

        /**
         * Constructs a new FrostVaultDkgShareTx.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgShareTx.
         * @implements IFrostVaultDkgShareTx
         * @constructor
         * @param {pb.IFrostVaultDkgShareTx=} [properties] Properties to set
         */
        function FrostVaultDkgShareTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgShareTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         */
        FrostVaultDkgShareTx.prototype.base = null;

        /**
         * FrostVaultDkgShareTx chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         */
        FrostVaultDkgShareTx.prototype.chain = "";

        /**
         * FrostVaultDkgShareTx vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         */
        FrostVaultDkgShareTx.prototype.vaultId = 0;

        /**
         * FrostVaultDkgShareTx epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         */
        FrostVaultDkgShareTx.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgShareTx dealerId.
         * @member {string} dealerId
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         */
        FrostVaultDkgShareTx.prototype.dealerId = "";

        /**
         * FrostVaultDkgShareTx receiverId.
         * @member {string} receiverId
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         */
        FrostVaultDkgShareTx.prototype.receiverId = "";

        /**
         * FrostVaultDkgShareTx ciphertext.
         * @member {Uint8Array} ciphertext
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         */
        FrostVaultDkgShareTx.prototype.ciphertext = $util.newBuffer([]);

        /**
         * Creates a new FrostVaultDkgShareTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {pb.IFrostVaultDkgShareTx=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgShareTx} FrostVaultDkgShareTx instance
         */
        FrostVaultDkgShareTx.create = function create(properties) {
            return new FrostVaultDkgShareTx(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgShareTx message. Does not implicitly {@link pb.FrostVaultDkgShareTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {pb.IFrostVaultDkgShareTx} message FrostVaultDkgShareTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgShareTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.epochId);
            if (message.dealerId != null && Object.hasOwnProperty.call(message, "dealerId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.dealerId);
            if (message.receiverId != null && Object.hasOwnProperty.call(message, "receiverId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.receiverId);
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.ciphertext);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgShareTx message, length delimited. Does not implicitly {@link pb.FrostVaultDkgShareTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {pb.IFrostVaultDkgShareTx} message FrostVaultDkgShareTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgShareTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgShareTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgShareTx} FrostVaultDkgShareTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgShareTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgShareTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 5: {
                        message.dealerId = reader.string();
                        break;
                    }
                case 6: {
                        message.receiverId = reader.string();
                        break;
                    }
                case 7: {
                        message.ciphertext = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgShareTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgShareTx} FrostVaultDkgShareTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgShareTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgShareTx message.
         * @function verify
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgShareTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                if (!$util.isString(message.dealerId))
                    return "dealerId: string expected";
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                if (!$util.isString(message.receiverId))
                    return "receiverId: string expected";
            if (message.ciphertext != null && message.hasOwnProperty("ciphertext"))
                if (!(message.ciphertext && typeof message.ciphertext.length === "number" || $util.isString(message.ciphertext)))
                    return "ciphertext: buffer expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgShareTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgShareTx} FrostVaultDkgShareTx
         */
        FrostVaultDkgShareTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgShareTx)
                return object;
            let message = new $root.pb.FrostVaultDkgShareTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostVaultDkgShareTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.dealerId != null)
                message.dealerId = String(object.dealerId);
            if (object.receiverId != null)
                message.receiverId = String(object.receiverId);
            if (object.ciphertext != null)
                if (typeof object.ciphertext === "string")
                    $util.base64.decode(object.ciphertext, message.ciphertext = $util.newBuffer($util.base64.length(object.ciphertext)), 0);
                else if (object.ciphertext.length >= 0)
                    message.ciphertext = object.ciphertext;
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgShareTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {pb.FrostVaultDkgShareTx} message FrostVaultDkgShareTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgShareTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.dealerId = "";
                object.receiverId = "";
                if (options.bytes === String)
                    object.ciphertext = "";
                else {
                    object.ciphertext = [];
                    if (options.bytes !== Array)
                        object.ciphertext = $util.newBuffer(object.ciphertext);
                }
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                object.dealerId = message.dealerId;
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                object.receiverId = message.receiverId;
            if (message.ciphertext != null && message.hasOwnProperty("ciphertext"))
                object.ciphertext = options.bytes === String ? $util.base64.encode(message.ciphertext, 0, message.ciphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.ciphertext) : message.ciphertext;
            return object;
        };

        /**
         * Converts this FrostVaultDkgShareTx to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgShareTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgShareTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgShareTx
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgShareTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgShareTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgShareTx";
        };

        return FrostVaultDkgShareTx;
    })();

    pb.FrostVaultDkgCommitment = (function() {

        /**
         * Properties of a FrostVaultDkgCommitment.
         * @memberof pb
         * @interface IFrostVaultDkgCommitment
         * @property {string|null} [chain] FrostVaultDkgCommitment chain
         * @property {number|null} [vaultId] FrostVaultDkgCommitment vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgCommitment epochId
         * @property {string|null} [minerAddress] FrostVaultDkgCommitment minerAddress
         * @property {pb.SignAlgo|null} [signAlgo] FrostVaultDkgCommitment signAlgo
         * @property {Array.<Uint8Array>|null} [commitmentPoints] FrostVaultDkgCommitment commitmentPoints
         * @property {Uint8Array|null} [aI0] FrostVaultDkgCommitment aI0
         * @property {number|Long|null} [commitHeight] FrostVaultDkgCommitment commitHeight
         */

        /**
         * Constructs a new FrostVaultDkgCommitment.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgCommitment.
         * @implements IFrostVaultDkgCommitment
         * @constructor
         * @param {pb.IFrostVaultDkgCommitment=} [properties] Properties to set
         */
        function FrostVaultDkgCommitment(properties) {
            this.commitmentPoints = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgCommitment chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.chain = "";

        /**
         * FrostVaultDkgCommitment vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.vaultId = 0;

        /**
         * FrostVaultDkgCommitment epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgCommitment minerAddress.
         * @member {string} minerAddress
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.minerAddress = "";

        /**
         * FrostVaultDkgCommitment signAlgo.
         * @member {pb.SignAlgo} signAlgo
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.signAlgo = 0;

        /**
         * FrostVaultDkgCommitment commitmentPoints.
         * @member {Array.<Uint8Array>} commitmentPoints
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.commitmentPoints = $util.emptyArray;

        /**
         * FrostVaultDkgCommitment aI0.
         * @member {Uint8Array} aI0
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.aI0 = $util.newBuffer([]);

        /**
         * FrostVaultDkgCommitment commitHeight.
         * @member {number|Long} commitHeight
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         */
        FrostVaultDkgCommitment.prototype.commitHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FrostVaultDkgCommitment instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {pb.IFrostVaultDkgCommitment=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgCommitment} FrostVaultDkgCommitment instance
         */
        FrostVaultDkgCommitment.create = function create(properties) {
            return new FrostVaultDkgCommitment(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgCommitment message. Does not implicitly {@link pb.FrostVaultDkgCommitment.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {pb.IFrostVaultDkgCommitment} message FrostVaultDkgCommitment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgCommitment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.epochId);
            if (message.minerAddress != null && Object.hasOwnProperty.call(message, "minerAddress"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.minerAddress);
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.signAlgo);
            if (message.commitmentPoints != null && message.commitmentPoints.length)
                for (let i = 0; i < message.commitmentPoints.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.commitmentPoints[i]);
            if (message.aI0 != null && Object.hasOwnProperty.call(message, "aI0"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.aI0);
            if (message.commitHeight != null && Object.hasOwnProperty.call(message, "commitHeight"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.commitHeight);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgCommitment message, length delimited. Does not implicitly {@link pb.FrostVaultDkgCommitment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {pb.IFrostVaultDkgCommitment} message FrostVaultDkgCommitment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgCommitment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgCommitment message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgCommitment} FrostVaultDkgCommitment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgCommitment.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgCommitment();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.minerAddress = reader.string();
                        break;
                    }
                case 5: {
                        message.signAlgo = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.commitmentPoints && message.commitmentPoints.length))
                            message.commitmentPoints = [];
                        message.commitmentPoints.push(reader.bytes());
                        break;
                    }
                case 7: {
                        message.aI0 = reader.bytes();
                        break;
                    }
                case 8: {
                        message.commitHeight = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgCommitment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgCommitment} FrostVaultDkgCommitment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgCommitment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgCommitment message.
         * @function verify
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgCommitment.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.minerAddress != null && message.hasOwnProperty("minerAddress"))
                if (!$util.isString(message.minerAddress))
                    return "minerAddress: string expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                switch (message.signAlgo) {
                default:
                    return "signAlgo: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.commitmentPoints != null && message.hasOwnProperty("commitmentPoints")) {
                if (!Array.isArray(message.commitmentPoints))
                    return "commitmentPoints: array expected";
                for (let i = 0; i < message.commitmentPoints.length; ++i)
                    if (!(message.commitmentPoints[i] && typeof message.commitmentPoints[i].length === "number" || $util.isString(message.commitmentPoints[i])))
                        return "commitmentPoints: buffer[] expected";
            }
            if (message.aI0 != null && message.hasOwnProperty("aI0"))
                if (!(message.aI0 && typeof message.aI0.length === "number" || $util.isString(message.aI0)))
                    return "aI0: buffer expected";
            if (message.commitHeight != null && message.hasOwnProperty("commitHeight"))
                if (!$util.isInteger(message.commitHeight) && !(message.commitHeight && $util.isInteger(message.commitHeight.low) && $util.isInteger(message.commitHeight.high)))
                    return "commitHeight: integer|Long expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgCommitment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgCommitment} FrostVaultDkgCommitment
         */
        FrostVaultDkgCommitment.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgCommitment)
                return object;
            let message = new $root.pb.FrostVaultDkgCommitment();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.minerAddress != null)
                message.minerAddress = String(object.minerAddress);
            switch (object.signAlgo) {
            default:
                if (typeof object.signAlgo === "number") {
                    message.signAlgo = object.signAlgo;
                    break;
                }
                break;
            case "SIGN_ALGO_UNSPECIFIED":
            case 0:
                message.signAlgo = 0;
                break;
            case "SIGN_ALGO_ECDSA_P256":
            case 1:
                message.signAlgo = 1;
                break;
            case "SIGN_ALGO_BLS_BN256_G2":
            case 2:
                message.signAlgo = 2;
                break;
            case "SIGN_ALGO_SCHNORR_SECP256K1_BIP340":
            case 3:
                message.signAlgo = 3;
                break;
            case "SIGN_ALGO_SCHNORR_ALT_BN128":
            case 4:
                message.signAlgo = 4;
                break;
            case "SIGN_ALGO_ED25519":
            case 5:
                message.signAlgo = 5;
                break;
            case "SIGN_ALGO_ECDSA_SECP256K1":
            case 6:
                message.signAlgo = 6;
                break;
            }
            if (object.commitmentPoints) {
                if (!Array.isArray(object.commitmentPoints))
                    throw TypeError(".pb.FrostVaultDkgCommitment.commitmentPoints: array expected");
                message.commitmentPoints = [];
                for (let i = 0; i < object.commitmentPoints.length; ++i)
                    if (typeof object.commitmentPoints[i] === "string")
                        $util.base64.decode(object.commitmentPoints[i], message.commitmentPoints[i] = $util.newBuffer($util.base64.length(object.commitmentPoints[i])), 0);
                    else if (object.commitmentPoints[i].length >= 0)
                        message.commitmentPoints[i] = object.commitmentPoints[i];
            }
            if (object.aI0 != null)
                if (typeof object.aI0 === "string")
                    $util.base64.decode(object.aI0, message.aI0 = $util.newBuffer($util.base64.length(object.aI0)), 0);
                else if (object.aI0.length >= 0)
                    message.aI0 = object.aI0;
            if (object.commitHeight != null)
                if ($util.Long)
                    (message.commitHeight = $util.Long.fromValue(object.commitHeight)).unsigned = true;
                else if (typeof object.commitHeight === "string")
                    message.commitHeight = parseInt(object.commitHeight, 10);
                else if (typeof object.commitHeight === "number")
                    message.commitHeight = object.commitHeight;
                else if (typeof object.commitHeight === "object")
                    message.commitHeight = new $util.LongBits(object.commitHeight.low >>> 0, object.commitHeight.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgCommitment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {pb.FrostVaultDkgCommitment} message FrostVaultDkgCommitment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgCommitment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.commitmentPoints = [];
            if (options.defaults) {
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.minerAddress = "";
                object.signAlgo = options.enums === String ? "SIGN_ALGO_UNSPECIFIED" : 0;
                if (options.bytes === String)
                    object.aI0 = "";
                else {
                    object.aI0 = [];
                    if (options.bytes !== Array)
                        object.aI0 = $util.newBuffer(object.aI0);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.commitHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.commitHeight = options.longs === String ? "0" : 0;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.minerAddress != null && message.hasOwnProperty("minerAddress"))
                object.minerAddress = message.minerAddress;
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = options.enums === String ? $root.pb.SignAlgo[message.signAlgo] === undefined ? message.signAlgo : $root.pb.SignAlgo[message.signAlgo] : message.signAlgo;
            if (message.commitmentPoints && message.commitmentPoints.length) {
                object.commitmentPoints = [];
                for (let j = 0; j < message.commitmentPoints.length; ++j)
                    object.commitmentPoints[j] = options.bytes === String ? $util.base64.encode(message.commitmentPoints[j], 0, message.commitmentPoints[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.commitmentPoints[j]) : message.commitmentPoints[j];
            }
            if (message.aI0 != null && message.hasOwnProperty("aI0"))
                object.aI0 = options.bytes === String ? $util.base64.encode(message.aI0, 0, message.aI0.length) : options.bytes === Array ? Array.prototype.slice.call(message.aI0) : message.aI0;
            if (message.commitHeight != null && message.hasOwnProperty("commitHeight"))
                if (typeof message.commitHeight === "number")
                    object.commitHeight = options.longs === String ? String(message.commitHeight) : message.commitHeight;
                else
                    object.commitHeight = options.longs === String ? $util.Long.prototype.toString.call(message.commitHeight) : options.longs === Number ? new $util.LongBits(message.commitHeight.low >>> 0, message.commitHeight.high >>> 0).toNumber(true) : message.commitHeight;
            return object;
        };

        /**
         * Converts this FrostVaultDkgCommitment to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgCommitment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgCommitment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgCommitment
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgCommitment
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgCommitment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgCommitment";
        };

        return FrostVaultDkgCommitment;
    })();

    pb.FrostVaultDkgShare = (function() {

        /**
         * Properties of a FrostVaultDkgShare.
         * @memberof pb
         * @interface IFrostVaultDkgShare
         * @property {string|null} [chain] FrostVaultDkgShare chain
         * @property {number|null} [vaultId] FrostVaultDkgShare vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgShare epochId
         * @property {string|null} [dealerId] FrostVaultDkgShare dealerId
         * @property {string|null} [receiverId] FrostVaultDkgShare receiverId
         * @property {Uint8Array|null} [ciphertext] FrostVaultDkgShare ciphertext
         * @property {number|Long|null} [shareHeight] FrostVaultDkgShare shareHeight
         */

        /**
         * Constructs a new FrostVaultDkgShare.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgShare.
         * @implements IFrostVaultDkgShare
         * @constructor
         * @param {pb.IFrostVaultDkgShare=} [properties] Properties to set
         */
        function FrostVaultDkgShare(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgShare chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgShare
         * @instance
         */
        FrostVaultDkgShare.prototype.chain = "";

        /**
         * FrostVaultDkgShare vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgShare
         * @instance
         */
        FrostVaultDkgShare.prototype.vaultId = 0;

        /**
         * FrostVaultDkgShare epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgShare
         * @instance
         */
        FrostVaultDkgShare.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgShare dealerId.
         * @member {string} dealerId
         * @memberof pb.FrostVaultDkgShare
         * @instance
         */
        FrostVaultDkgShare.prototype.dealerId = "";

        /**
         * FrostVaultDkgShare receiverId.
         * @member {string} receiverId
         * @memberof pb.FrostVaultDkgShare
         * @instance
         */
        FrostVaultDkgShare.prototype.receiverId = "";

        /**
         * FrostVaultDkgShare ciphertext.
         * @member {Uint8Array} ciphertext
         * @memberof pb.FrostVaultDkgShare
         * @instance
         */
        FrostVaultDkgShare.prototype.ciphertext = $util.newBuffer([]);

        /**
         * FrostVaultDkgShare shareHeight.
         * @member {number|Long} shareHeight
         * @memberof pb.FrostVaultDkgShare
         * @instance
         */
        FrostVaultDkgShare.prototype.shareHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FrostVaultDkgShare instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {pb.IFrostVaultDkgShare=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgShare} FrostVaultDkgShare instance
         */
        FrostVaultDkgShare.create = function create(properties) {
            return new FrostVaultDkgShare(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgShare message. Does not implicitly {@link pb.FrostVaultDkgShare.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {pb.IFrostVaultDkgShare} message FrostVaultDkgShare message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgShare.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.epochId);
            if (message.dealerId != null && Object.hasOwnProperty.call(message, "dealerId"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.dealerId);
            if (message.receiverId != null && Object.hasOwnProperty.call(message, "receiverId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.receiverId);
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.ciphertext);
            if (message.shareHeight != null && Object.hasOwnProperty.call(message, "shareHeight"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.shareHeight);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgShare message, length delimited. Does not implicitly {@link pb.FrostVaultDkgShare.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {pb.IFrostVaultDkgShare} message FrostVaultDkgShare message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgShare.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgShare message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgShare} FrostVaultDkgShare
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgShare.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgShare();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.dealerId = reader.string();
                        break;
                    }
                case 5: {
                        message.receiverId = reader.string();
                        break;
                    }
                case 6: {
                        message.ciphertext = reader.bytes();
                        break;
                    }
                case 7: {
                        message.shareHeight = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgShare message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgShare} FrostVaultDkgShare
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgShare.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgShare message.
         * @function verify
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgShare.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                if (!$util.isString(message.dealerId))
                    return "dealerId: string expected";
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                if (!$util.isString(message.receiverId))
                    return "receiverId: string expected";
            if (message.ciphertext != null && message.hasOwnProperty("ciphertext"))
                if (!(message.ciphertext && typeof message.ciphertext.length === "number" || $util.isString(message.ciphertext)))
                    return "ciphertext: buffer expected";
            if (message.shareHeight != null && message.hasOwnProperty("shareHeight"))
                if (!$util.isInteger(message.shareHeight) && !(message.shareHeight && $util.isInteger(message.shareHeight.low) && $util.isInteger(message.shareHeight.high)))
                    return "shareHeight: integer|Long expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgShare message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgShare} FrostVaultDkgShare
         */
        FrostVaultDkgShare.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgShare)
                return object;
            let message = new $root.pb.FrostVaultDkgShare();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.dealerId != null)
                message.dealerId = String(object.dealerId);
            if (object.receiverId != null)
                message.receiverId = String(object.receiverId);
            if (object.ciphertext != null)
                if (typeof object.ciphertext === "string")
                    $util.base64.decode(object.ciphertext, message.ciphertext = $util.newBuffer($util.base64.length(object.ciphertext)), 0);
                else if (object.ciphertext.length >= 0)
                    message.ciphertext = object.ciphertext;
            if (object.shareHeight != null)
                if ($util.Long)
                    (message.shareHeight = $util.Long.fromValue(object.shareHeight)).unsigned = true;
                else if (typeof object.shareHeight === "string")
                    message.shareHeight = parseInt(object.shareHeight, 10);
                else if (typeof object.shareHeight === "number")
                    message.shareHeight = object.shareHeight;
                else if (typeof object.shareHeight === "object")
                    message.shareHeight = new $util.LongBits(object.shareHeight.low >>> 0, object.shareHeight.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgShare message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {pb.FrostVaultDkgShare} message FrostVaultDkgShare
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgShare.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.dealerId = "";
                object.receiverId = "";
                if (options.bytes === String)
                    object.ciphertext = "";
                else {
                    object.ciphertext = [];
                    if (options.bytes !== Array)
                        object.ciphertext = $util.newBuffer(object.ciphertext);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.shareHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.shareHeight = options.longs === String ? "0" : 0;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                object.dealerId = message.dealerId;
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                object.receiverId = message.receiverId;
            if (message.ciphertext != null && message.hasOwnProperty("ciphertext"))
                object.ciphertext = options.bytes === String ? $util.base64.encode(message.ciphertext, 0, message.ciphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.ciphertext) : message.ciphertext;
            if (message.shareHeight != null && message.hasOwnProperty("shareHeight"))
                if (typeof message.shareHeight === "number")
                    object.shareHeight = options.longs === String ? String(message.shareHeight) : message.shareHeight;
                else
                    object.shareHeight = options.longs === String ? $util.Long.prototype.toString.call(message.shareHeight) : options.longs === Number ? new $util.LongBits(message.shareHeight.low >>> 0, message.shareHeight.high >>> 0).toNumber(true) : message.shareHeight;
            return object;
        };

        /**
         * Converts this FrostVaultDkgShare to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgShare
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgShare.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgShare
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgShare
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgShare";
        };

        return FrostVaultDkgShare;
    })();

    pb.FrostVaultDkgComplaintTx = (function() {

        /**
         * Properties of a FrostVaultDkgComplaintTx.
         * @memberof pb
         * @interface IFrostVaultDkgComplaintTx
         * @property {pb.IBaseMessage|null} [base] FrostVaultDkgComplaintTx base
         * @property {string|null} [chain] FrostVaultDkgComplaintTx chain
         * @property {number|null} [vaultId] FrostVaultDkgComplaintTx vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgComplaintTx epochId
         * @property {string|null} [dealerId] FrostVaultDkgComplaintTx dealerId
         * @property {string|null} [receiverId] FrostVaultDkgComplaintTx receiverId
         * @property {string|null} [bond] FrostVaultDkgComplaintTx bond
         */

        /**
         * Constructs a new FrostVaultDkgComplaintTx.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgComplaintTx.
         * @implements IFrostVaultDkgComplaintTx
         * @constructor
         * @param {pb.IFrostVaultDkgComplaintTx=} [properties] Properties to set
         */
        function FrostVaultDkgComplaintTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgComplaintTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         */
        FrostVaultDkgComplaintTx.prototype.base = null;

        /**
         * FrostVaultDkgComplaintTx chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         */
        FrostVaultDkgComplaintTx.prototype.chain = "";

        /**
         * FrostVaultDkgComplaintTx vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         */
        FrostVaultDkgComplaintTx.prototype.vaultId = 0;

        /**
         * FrostVaultDkgComplaintTx epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         */
        FrostVaultDkgComplaintTx.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgComplaintTx dealerId.
         * @member {string} dealerId
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         */
        FrostVaultDkgComplaintTx.prototype.dealerId = "";

        /**
         * FrostVaultDkgComplaintTx receiverId.
         * @member {string} receiverId
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         */
        FrostVaultDkgComplaintTx.prototype.receiverId = "";

        /**
         * FrostVaultDkgComplaintTx bond.
         * @member {string} bond
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         */
        FrostVaultDkgComplaintTx.prototype.bond = "";

        /**
         * Creates a new FrostVaultDkgComplaintTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {pb.IFrostVaultDkgComplaintTx=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgComplaintTx} FrostVaultDkgComplaintTx instance
         */
        FrostVaultDkgComplaintTx.create = function create(properties) {
            return new FrostVaultDkgComplaintTx(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgComplaintTx message. Does not implicitly {@link pb.FrostVaultDkgComplaintTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {pb.IFrostVaultDkgComplaintTx} message FrostVaultDkgComplaintTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgComplaintTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.epochId);
            if (message.dealerId != null && Object.hasOwnProperty.call(message, "dealerId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.dealerId);
            if (message.receiverId != null && Object.hasOwnProperty.call(message, "receiverId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.receiverId);
            if (message.bond != null && Object.hasOwnProperty.call(message, "bond"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.bond);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgComplaintTx message, length delimited. Does not implicitly {@link pb.FrostVaultDkgComplaintTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {pb.IFrostVaultDkgComplaintTx} message FrostVaultDkgComplaintTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgComplaintTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgComplaintTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgComplaintTx} FrostVaultDkgComplaintTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgComplaintTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgComplaintTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 5: {
                        message.dealerId = reader.string();
                        break;
                    }
                case 6: {
                        message.receiverId = reader.string();
                        break;
                    }
                case 7: {
                        message.bond = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgComplaintTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgComplaintTx} FrostVaultDkgComplaintTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgComplaintTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgComplaintTx message.
         * @function verify
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgComplaintTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                if (!$util.isString(message.dealerId))
                    return "dealerId: string expected";
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                if (!$util.isString(message.receiverId))
                    return "receiverId: string expected";
            if (message.bond != null && message.hasOwnProperty("bond"))
                if (!$util.isString(message.bond))
                    return "bond: string expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgComplaintTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgComplaintTx} FrostVaultDkgComplaintTx
         */
        FrostVaultDkgComplaintTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgComplaintTx)
                return object;
            let message = new $root.pb.FrostVaultDkgComplaintTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostVaultDkgComplaintTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.dealerId != null)
                message.dealerId = String(object.dealerId);
            if (object.receiverId != null)
                message.receiverId = String(object.receiverId);
            if (object.bond != null)
                message.bond = String(object.bond);
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgComplaintTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {pb.FrostVaultDkgComplaintTx} message FrostVaultDkgComplaintTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgComplaintTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.dealerId = "";
                object.receiverId = "";
                object.bond = "";
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                object.dealerId = message.dealerId;
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                object.receiverId = message.receiverId;
            if (message.bond != null && message.hasOwnProperty("bond"))
                object.bond = message.bond;
            return object;
        };

        /**
         * Converts this FrostVaultDkgComplaintTx to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgComplaintTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgComplaintTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgComplaintTx
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgComplaintTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgComplaintTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgComplaintTx";
        };

        return FrostVaultDkgComplaintTx;
    })();

    pb.FrostVaultDkgRevealTx = (function() {

        /**
         * Properties of a FrostVaultDkgRevealTx.
         * @memberof pb
         * @interface IFrostVaultDkgRevealTx
         * @property {pb.IBaseMessage|null} [base] FrostVaultDkgRevealTx base
         * @property {string|null} [chain] FrostVaultDkgRevealTx chain
         * @property {number|null} [vaultId] FrostVaultDkgRevealTx vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgRevealTx epochId
         * @property {string|null} [dealerId] FrostVaultDkgRevealTx dealerId
         * @property {string|null} [receiverId] FrostVaultDkgRevealTx receiverId
         * @property {Uint8Array|null} [share] FrostVaultDkgRevealTx share
         * @property {Uint8Array|null} [encRand] FrostVaultDkgRevealTx encRand
         */

        /**
         * Constructs a new FrostVaultDkgRevealTx.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgRevealTx.
         * @implements IFrostVaultDkgRevealTx
         * @constructor
         * @param {pb.IFrostVaultDkgRevealTx=} [properties] Properties to set
         */
        function FrostVaultDkgRevealTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgRevealTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.base = null;

        /**
         * FrostVaultDkgRevealTx chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.chain = "";

        /**
         * FrostVaultDkgRevealTx vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.vaultId = 0;

        /**
         * FrostVaultDkgRevealTx epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgRevealTx dealerId.
         * @member {string} dealerId
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.dealerId = "";

        /**
         * FrostVaultDkgRevealTx receiverId.
         * @member {string} receiverId
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.receiverId = "";

        /**
         * FrostVaultDkgRevealTx share.
         * @member {Uint8Array} share
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.share = $util.newBuffer([]);

        /**
         * FrostVaultDkgRevealTx encRand.
         * @member {Uint8Array} encRand
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         */
        FrostVaultDkgRevealTx.prototype.encRand = $util.newBuffer([]);

        /**
         * Creates a new FrostVaultDkgRevealTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {pb.IFrostVaultDkgRevealTx=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgRevealTx} FrostVaultDkgRevealTx instance
         */
        FrostVaultDkgRevealTx.create = function create(properties) {
            return new FrostVaultDkgRevealTx(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgRevealTx message. Does not implicitly {@link pb.FrostVaultDkgRevealTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {pb.IFrostVaultDkgRevealTx} message FrostVaultDkgRevealTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgRevealTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.epochId);
            if (message.dealerId != null && Object.hasOwnProperty.call(message, "dealerId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.dealerId);
            if (message.receiverId != null && Object.hasOwnProperty.call(message, "receiverId"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.receiverId);
            if (message.share != null && Object.hasOwnProperty.call(message, "share"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.share);
            if (message.encRand != null && Object.hasOwnProperty.call(message, "encRand"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.encRand);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgRevealTx message, length delimited. Does not implicitly {@link pb.FrostVaultDkgRevealTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {pb.IFrostVaultDkgRevealTx} message FrostVaultDkgRevealTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgRevealTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgRevealTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgRevealTx} FrostVaultDkgRevealTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgRevealTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgRevealTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 5: {
                        message.dealerId = reader.string();
                        break;
                    }
                case 6: {
                        message.receiverId = reader.string();
                        break;
                    }
                case 7: {
                        message.share = reader.bytes();
                        break;
                    }
                case 8: {
                        message.encRand = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgRevealTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgRevealTx} FrostVaultDkgRevealTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgRevealTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgRevealTx message.
         * @function verify
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgRevealTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                if (!$util.isString(message.dealerId))
                    return "dealerId: string expected";
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                if (!$util.isString(message.receiverId))
                    return "receiverId: string expected";
            if (message.share != null && message.hasOwnProperty("share"))
                if (!(message.share && typeof message.share.length === "number" || $util.isString(message.share)))
                    return "share: buffer expected";
            if (message.encRand != null && message.hasOwnProperty("encRand"))
                if (!(message.encRand && typeof message.encRand.length === "number" || $util.isString(message.encRand)))
                    return "encRand: buffer expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgRevealTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgRevealTx} FrostVaultDkgRevealTx
         */
        FrostVaultDkgRevealTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgRevealTx)
                return object;
            let message = new $root.pb.FrostVaultDkgRevealTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostVaultDkgRevealTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.dealerId != null)
                message.dealerId = String(object.dealerId);
            if (object.receiverId != null)
                message.receiverId = String(object.receiverId);
            if (object.share != null)
                if (typeof object.share === "string")
                    $util.base64.decode(object.share, message.share = $util.newBuffer($util.base64.length(object.share)), 0);
                else if (object.share.length >= 0)
                    message.share = object.share;
            if (object.encRand != null)
                if (typeof object.encRand === "string")
                    $util.base64.decode(object.encRand, message.encRand = $util.newBuffer($util.base64.length(object.encRand)), 0);
                else if (object.encRand.length >= 0)
                    message.encRand = object.encRand;
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgRevealTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {pb.FrostVaultDkgRevealTx} message FrostVaultDkgRevealTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgRevealTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.dealerId = "";
                object.receiverId = "";
                if (options.bytes === String)
                    object.share = "";
                else {
                    object.share = [];
                    if (options.bytes !== Array)
                        object.share = $util.newBuffer(object.share);
                }
                if (options.bytes === String)
                    object.encRand = "";
                else {
                    object.encRand = [];
                    if (options.bytes !== Array)
                        object.encRand = $util.newBuffer(object.encRand);
                }
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                object.dealerId = message.dealerId;
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                object.receiverId = message.receiverId;
            if (message.share != null && message.hasOwnProperty("share"))
                object.share = options.bytes === String ? $util.base64.encode(message.share, 0, message.share.length) : options.bytes === Array ? Array.prototype.slice.call(message.share) : message.share;
            if (message.encRand != null && message.hasOwnProperty("encRand"))
                object.encRand = options.bytes === String ? $util.base64.encode(message.encRand, 0, message.encRand.length) : options.bytes === Array ? Array.prototype.slice.call(message.encRand) : message.encRand;
            return object;
        };

        /**
         * Converts this FrostVaultDkgRevealTx to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgRevealTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgRevealTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgRevealTx
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgRevealTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgRevealTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgRevealTx";
        };

        return FrostVaultDkgRevealTx;
    })();

    pb.FrostVaultDkgComplaint = (function() {

        /**
         * Properties of a FrostVaultDkgComplaint.
         * @memberof pb
         * @interface IFrostVaultDkgComplaint
         * @property {string|null} [chain] FrostVaultDkgComplaint chain
         * @property {number|null} [vaultId] FrostVaultDkgComplaint vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgComplaint epochId
         * @property {string|null} [dealerId] FrostVaultDkgComplaint dealerId
         * @property {string|null} [receiverId] FrostVaultDkgComplaint receiverId
         * @property {string|null} [bond] FrostVaultDkgComplaint bond
         * @property {number|Long|null} [complaintHeight] FrostVaultDkgComplaint complaintHeight
         * @property {number|Long|null} [revealDeadline] FrostVaultDkgComplaint revealDeadline
         * @property {string|null} [status] FrostVaultDkgComplaint status
         */

        /**
         * Constructs a new FrostVaultDkgComplaint.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgComplaint.
         * @implements IFrostVaultDkgComplaint
         * @constructor
         * @param {pb.IFrostVaultDkgComplaint=} [properties] Properties to set
         */
        function FrostVaultDkgComplaint(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgComplaint chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.chain = "";

        /**
         * FrostVaultDkgComplaint vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.vaultId = 0;

        /**
         * FrostVaultDkgComplaint epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgComplaint dealerId.
         * @member {string} dealerId
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.dealerId = "";

        /**
         * FrostVaultDkgComplaint receiverId.
         * @member {string} receiverId
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.receiverId = "";

        /**
         * FrostVaultDkgComplaint bond.
         * @member {string} bond
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.bond = "";

        /**
         * FrostVaultDkgComplaint complaintHeight.
         * @member {number|Long} complaintHeight
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.complaintHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgComplaint revealDeadline.
         * @member {number|Long} revealDeadline
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.revealDeadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgComplaint status.
         * @member {string} status
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         */
        FrostVaultDkgComplaint.prototype.status = "";

        /**
         * Creates a new FrostVaultDkgComplaint instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {pb.IFrostVaultDkgComplaint=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgComplaint} FrostVaultDkgComplaint instance
         */
        FrostVaultDkgComplaint.create = function create(properties) {
            return new FrostVaultDkgComplaint(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgComplaint message. Does not implicitly {@link pb.FrostVaultDkgComplaint.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {pb.IFrostVaultDkgComplaint} message FrostVaultDkgComplaint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgComplaint.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.epochId);
            if (message.dealerId != null && Object.hasOwnProperty.call(message, "dealerId"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.dealerId);
            if (message.receiverId != null && Object.hasOwnProperty.call(message, "receiverId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.receiverId);
            if (message.bond != null && Object.hasOwnProperty.call(message, "bond"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.bond);
            if (message.complaintHeight != null && Object.hasOwnProperty.call(message, "complaintHeight"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.complaintHeight);
            if (message.revealDeadline != null && Object.hasOwnProperty.call(message, "revealDeadline"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.revealDeadline);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.status);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgComplaint message, length delimited. Does not implicitly {@link pb.FrostVaultDkgComplaint.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {pb.IFrostVaultDkgComplaint} message FrostVaultDkgComplaint message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgComplaint.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgComplaint message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgComplaint} FrostVaultDkgComplaint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgComplaint.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgComplaint();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.dealerId = reader.string();
                        break;
                    }
                case 5: {
                        message.receiverId = reader.string();
                        break;
                    }
                case 6: {
                        message.bond = reader.string();
                        break;
                    }
                case 7: {
                        message.complaintHeight = reader.uint64();
                        break;
                    }
                case 8: {
                        message.revealDeadline = reader.uint64();
                        break;
                    }
                case 9: {
                        message.status = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgComplaint message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgComplaint} FrostVaultDkgComplaint
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgComplaint.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgComplaint message.
         * @function verify
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgComplaint.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                if (!$util.isString(message.dealerId))
                    return "dealerId: string expected";
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                if (!$util.isString(message.receiverId))
                    return "receiverId: string expected";
            if (message.bond != null && message.hasOwnProperty("bond"))
                if (!$util.isString(message.bond))
                    return "bond: string expected";
            if (message.complaintHeight != null && message.hasOwnProperty("complaintHeight"))
                if (!$util.isInteger(message.complaintHeight) && !(message.complaintHeight && $util.isInteger(message.complaintHeight.low) && $util.isInteger(message.complaintHeight.high)))
                    return "complaintHeight: integer|Long expected";
            if (message.revealDeadline != null && message.hasOwnProperty("revealDeadline"))
                if (!$util.isInteger(message.revealDeadline) && !(message.revealDeadline && $util.isInteger(message.revealDeadline.low) && $util.isInteger(message.revealDeadline.high)))
                    return "revealDeadline: integer|Long expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgComplaint message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgComplaint} FrostVaultDkgComplaint
         */
        FrostVaultDkgComplaint.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgComplaint)
                return object;
            let message = new $root.pb.FrostVaultDkgComplaint();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.dealerId != null)
                message.dealerId = String(object.dealerId);
            if (object.receiverId != null)
                message.receiverId = String(object.receiverId);
            if (object.bond != null)
                message.bond = String(object.bond);
            if (object.complaintHeight != null)
                if ($util.Long)
                    (message.complaintHeight = $util.Long.fromValue(object.complaintHeight)).unsigned = true;
                else if (typeof object.complaintHeight === "string")
                    message.complaintHeight = parseInt(object.complaintHeight, 10);
                else if (typeof object.complaintHeight === "number")
                    message.complaintHeight = object.complaintHeight;
                else if (typeof object.complaintHeight === "object")
                    message.complaintHeight = new $util.LongBits(object.complaintHeight.low >>> 0, object.complaintHeight.high >>> 0).toNumber(true);
            if (object.revealDeadline != null)
                if ($util.Long)
                    (message.revealDeadline = $util.Long.fromValue(object.revealDeadline)).unsigned = true;
                else if (typeof object.revealDeadline === "string")
                    message.revealDeadline = parseInt(object.revealDeadline, 10);
                else if (typeof object.revealDeadline === "number")
                    message.revealDeadline = object.revealDeadline;
                else if (typeof object.revealDeadline === "object")
                    message.revealDeadline = new $util.LongBits(object.revealDeadline.low >>> 0, object.revealDeadline.high >>> 0).toNumber(true);
            if (object.status != null)
                message.status = String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgComplaint message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {pb.FrostVaultDkgComplaint} message FrostVaultDkgComplaint
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgComplaint.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.dealerId = "";
                object.receiverId = "";
                object.bond = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.complaintHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.complaintHeight = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.revealDeadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.revealDeadline = options.longs === String ? "0" : 0;
                object.status = "";
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.dealerId != null && message.hasOwnProperty("dealerId"))
                object.dealerId = message.dealerId;
            if (message.receiverId != null && message.hasOwnProperty("receiverId"))
                object.receiverId = message.receiverId;
            if (message.bond != null && message.hasOwnProperty("bond"))
                object.bond = message.bond;
            if (message.complaintHeight != null && message.hasOwnProperty("complaintHeight"))
                if (typeof message.complaintHeight === "number")
                    object.complaintHeight = options.longs === String ? String(message.complaintHeight) : message.complaintHeight;
                else
                    object.complaintHeight = options.longs === String ? $util.Long.prototype.toString.call(message.complaintHeight) : options.longs === Number ? new $util.LongBits(message.complaintHeight.low >>> 0, message.complaintHeight.high >>> 0).toNumber(true) : message.complaintHeight;
            if (message.revealDeadline != null && message.hasOwnProperty("revealDeadline"))
                if (typeof message.revealDeadline === "number")
                    object.revealDeadline = options.longs === String ? String(message.revealDeadline) : message.revealDeadline;
                else
                    object.revealDeadline = options.longs === String ? $util.Long.prototype.toString.call(message.revealDeadline) : options.longs === Number ? new $util.LongBits(message.revealDeadline.low >>> 0, message.revealDeadline.high >>> 0).toNumber(true) : message.revealDeadline;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this FrostVaultDkgComplaint to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgComplaint
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgComplaint.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgComplaint
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgComplaint
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgComplaint.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgComplaint";
        };

        return FrostVaultDkgComplaint;
    })();

    pb.FrostVaultDkgValidationSignedTx = (function() {

        /**
         * Properties of a FrostVaultDkgValidationSignedTx.
         * @memberof pb
         * @interface IFrostVaultDkgValidationSignedTx
         * @property {pb.IBaseMessage|null} [base] FrostVaultDkgValidationSignedTx base
         * @property {string|null} [chain] FrostVaultDkgValidationSignedTx chain
         * @property {number|null} [vaultId] FrostVaultDkgValidationSignedTx vaultId
         * @property {number|Long|null} [epochId] FrostVaultDkgValidationSignedTx epochId
         * @property {Uint8Array|null} [msgHash] FrostVaultDkgValidationSignedTx msgHash
         * @property {Uint8Array|null} [newGroupPubkey] FrostVaultDkgValidationSignedTx newGroupPubkey
         * @property {Uint8Array|null} [signature] FrostVaultDkgValidationSignedTx signature
         */

        /**
         * Constructs a new FrostVaultDkgValidationSignedTx.
         * @memberof pb
         * @classdesc Represents a FrostVaultDkgValidationSignedTx.
         * @implements IFrostVaultDkgValidationSignedTx
         * @constructor
         * @param {pb.IFrostVaultDkgValidationSignedTx=} [properties] Properties to set
         */
        function FrostVaultDkgValidationSignedTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultDkgValidationSignedTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         */
        FrostVaultDkgValidationSignedTx.prototype.base = null;

        /**
         * FrostVaultDkgValidationSignedTx chain.
         * @member {string} chain
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         */
        FrostVaultDkgValidationSignedTx.prototype.chain = "";

        /**
         * FrostVaultDkgValidationSignedTx vaultId.
         * @member {number} vaultId
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         */
        FrostVaultDkgValidationSignedTx.prototype.vaultId = 0;

        /**
         * FrostVaultDkgValidationSignedTx epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         */
        FrostVaultDkgValidationSignedTx.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultDkgValidationSignedTx msgHash.
         * @member {Uint8Array} msgHash
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         */
        FrostVaultDkgValidationSignedTx.prototype.msgHash = $util.newBuffer([]);

        /**
         * FrostVaultDkgValidationSignedTx newGroupPubkey.
         * @member {Uint8Array} newGroupPubkey
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         */
        FrostVaultDkgValidationSignedTx.prototype.newGroupPubkey = $util.newBuffer([]);

        /**
         * FrostVaultDkgValidationSignedTx signature.
         * @member {Uint8Array} signature
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         */
        FrostVaultDkgValidationSignedTx.prototype.signature = $util.newBuffer([]);

        /**
         * Creates a new FrostVaultDkgValidationSignedTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {pb.IFrostVaultDkgValidationSignedTx=} [properties] Properties to set
         * @returns {pb.FrostVaultDkgValidationSignedTx} FrostVaultDkgValidationSignedTx instance
         */
        FrostVaultDkgValidationSignedTx.create = function create(properties) {
            return new FrostVaultDkgValidationSignedTx(properties);
        };

        /**
         * Encodes the specified FrostVaultDkgValidationSignedTx message. Does not implicitly {@link pb.FrostVaultDkgValidationSignedTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {pb.IFrostVaultDkgValidationSignedTx} message FrostVaultDkgValidationSignedTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgValidationSignedTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.epochId);
            if (message.msgHash != null && Object.hasOwnProperty.call(message, "msgHash"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.msgHash);
            if (message.newGroupPubkey != null && Object.hasOwnProperty.call(message, "newGroupPubkey"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.newGroupPubkey);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.signature);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultDkgValidationSignedTx message, length delimited. Does not implicitly {@link pb.FrostVaultDkgValidationSignedTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {pb.IFrostVaultDkgValidationSignedTx} message FrostVaultDkgValidationSignedTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultDkgValidationSignedTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultDkgValidationSignedTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultDkgValidationSignedTx} FrostVaultDkgValidationSignedTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgValidationSignedTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultDkgValidationSignedTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 5: {
                        message.msgHash = reader.bytes();
                        break;
                    }
                case 6: {
                        message.newGroupPubkey = reader.bytes();
                        break;
                    }
                case 7: {
                        message.signature = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultDkgValidationSignedTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultDkgValidationSignedTx} FrostVaultDkgValidationSignedTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultDkgValidationSignedTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultDkgValidationSignedTx message.
         * @function verify
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultDkgValidationSignedTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.msgHash != null && message.hasOwnProperty("msgHash"))
                if (!(message.msgHash && typeof message.msgHash.length === "number" || $util.isString(message.msgHash)))
                    return "msgHash: buffer expected";
            if (message.newGroupPubkey != null && message.hasOwnProperty("newGroupPubkey"))
                if (!(message.newGroupPubkey && typeof message.newGroupPubkey.length === "number" || $util.isString(message.newGroupPubkey)))
                    return "newGroupPubkey: buffer expected";
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            return null;
        };

        /**
         * Creates a FrostVaultDkgValidationSignedTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultDkgValidationSignedTx} FrostVaultDkgValidationSignedTx
         */
        FrostVaultDkgValidationSignedTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultDkgValidationSignedTx)
                return object;
            let message = new $root.pb.FrostVaultDkgValidationSignedTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostVaultDkgValidationSignedTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.msgHash != null)
                if (typeof object.msgHash === "string")
                    $util.base64.decode(object.msgHash, message.msgHash = $util.newBuffer($util.base64.length(object.msgHash)), 0);
                else if (object.msgHash.length >= 0)
                    message.msgHash = object.msgHash;
            if (object.newGroupPubkey != null)
                if (typeof object.newGroupPubkey === "string")
                    $util.base64.decode(object.newGroupPubkey, message.newGroupPubkey = $util.newBuffer($util.base64.length(object.newGroupPubkey)), 0);
                else if (object.newGroupPubkey.length >= 0)
                    message.newGroupPubkey = object.newGroupPubkey;
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultDkgValidationSignedTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {pb.FrostVaultDkgValidationSignedTx} message FrostVaultDkgValidationSignedTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultDkgValidationSignedTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.msgHash = "";
                else {
                    object.msgHash = [];
                    if (options.bytes !== Array)
                        object.msgHash = $util.newBuffer(object.msgHash);
                }
                if (options.bytes === String)
                    object.newGroupPubkey = "";
                else {
                    object.newGroupPubkey = [];
                    if (options.bytes !== Array)
                        object.newGroupPubkey = $util.newBuffer(object.newGroupPubkey);
                }
                if (options.bytes === String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== Array)
                        object.signature = $util.newBuffer(object.signature);
                }
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.msgHash != null && message.hasOwnProperty("msgHash"))
                object.msgHash = options.bytes === String ? $util.base64.encode(message.msgHash, 0, message.msgHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.msgHash) : message.msgHash;
            if (message.newGroupPubkey != null && message.hasOwnProperty("newGroupPubkey"))
                object.newGroupPubkey = options.bytes === String ? $util.base64.encode(message.newGroupPubkey, 0, message.newGroupPubkey.length) : options.bytes === Array ? Array.prototype.slice.call(message.newGroupPubkey) : message.newGroupPubkey;
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            return object;
        };

        /**
         * Converts this FrostVaultDkgValidationSignedTx to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultDkgValidationSignedTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultDkgValidationSignedTx
         * @function getTypeUrl
         * @memberof pb.FrostVaultDkgValidationSignedTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultDkgValidationSignedTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultDkgValidationSignedTx";
        };

        return FrostVaultDkgValidationSignedTx;
    })();

    pb.FrostVaultTransitionSignedTx = (function() {

        /**
         * Properties of a FrostVaultTransitionSignedTx.
         * @memberof pb
         * @interface IFrostVaultTransitionSignedTx
         * @property {pb.IBaseMessage|null} [base] FrostVaultTransitionSignedTx base
         * @property {string|null} [chain] FrostVaultTransitionSignedTx chain
         * @property {number|null} [oldVaultId] FrostVaultTransitionSignedTx oldVaultId
         * @property {number|null} [newVaultId] FrostVaultTransitionSignedTx newVaultId
         * @property {number|Long|null} [epochId] FrostVaultTransitionSignedTx epochId
         * @property {Uint8Array|null} [msgHash] FrostVaultTransitionSignedTx msgHash
         * @property {Uint8Array|null} [signature] FrostVaultTransitionSignedTx signature
         */

        /**
         * Constructs a new FrostVaultTransitionSignedTx.
         * @memberof pb
         * @classdesc Represents a FrostVaultTransitionSignedTx.
         * @implements IFrostVaultTransitionSignedTx
         * @constructor
         * @param {pb.IFrostVaultTransitionSignedTx=} [properties] Properties to set
         */
        function FrostVaultTransitionSignedTx(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultTransitionSignedTx base.
         * @member {pb.IBaseMessage|null|undefined} base
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         */
        FrostVaultTransitionSignedTx.prototype.base = null;

        /**
         * FrostVaultTransitionSignedTx chain.
         * @member {string} chain
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         */
        FrostVaultTransitionSignedTx.prototype.chain = "";

        /**
         * FrostVaultTransitionSignedTx oldVaultId.
         * @member {number} oldVaultId
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         */
        FrostVaultTransitionSignedTx.prototype.oldVaultId = 0;

        /**
         * FrostVaultTransitionSignedTx newVaultId.
         * @member {number} newVaultId
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         */
        FrostVaultTransitionSignedTx.prototype.newVaultId = 0;

        /**
         * FrostVaultTransitionSignedTx epochId.
         * @member {number|Long} epochId
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         */
        FrostVaultTransitionSignedTx.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostVaultTransitionSignedTx msgHash.
         * @member {Uint8Array} msgHash
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         */
        FrostVaultTransitionSignedTx.prototype.msgHash = $util.newBuffer([]);

        /**
         * FrostVaultTransitionSignedTx signature.
         * @member {Uint8Array} signature
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         */
        FrostVaultTransitionSignedTx.prototype.signature = $util.newBuffer([]);

        /**
         * Creates a new FrostVaultTransitionSignedTx instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {pb.IFrostVaultTransitionSignedTx=} [properties] Properties to set
         * @returns {pb.FrostVaultTransitionSignedTx} FrostVaultTransitionSignedTx instance
         */
        FrostVaultTransitionSignedTx.create = function create(properties) {
            return new FrostVaultTransitionSignedTx(properties);
        };

        /**
         * Encodes the specified FrostVaultTransitionSignedTx message. Does not implicitly {@link pb.FrostVaultTransitionSignedTx.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {pb.IFrostVaultTransitionSignedTx} message FrostVaultTransitionSignedTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultTransitionSignedTx.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.pb.BaseMessage.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.oldVaultId != null && Object.hasOwnProperty.call(message, "oldVaultId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.oldVaultId);
            if (message.newVaultId != null && Object.hasOwnProperty.call(message, "newVaultId"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.newVaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.epochId);
            if (message.msgHash != null && Object.hasOwnProperty.call(message, "msgHash"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.msgHash);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.signature);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultTransitionSignedTx message, length delimited. Does not implicitly {@link pb.FrostVaultTransitionSignedTx.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {pb.IFrostVaultTransitionSignedTx} message FrostVaultTransitionSignedTx message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultTransitionSignedTx.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultTransitionSignedTx message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultTransitionSignedTx} FrostVaultTransitionSignedTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultTransitionSignedTx.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultTransitionSignedTx();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.base = $root.pb.BaseMessage.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.oldVaultId = reader.uint32();
                        break;
                    }
                case 4: {
                        message.newVaultId = reader.uint32();
                        break;
                    }
                case 5: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 6: {
                        message.msgHash = reader.bytes();
                        break;
                    }
                case 7: {
                        message.signature = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultTransitionSignedTx message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultTransitionSignedTx} FrostVaultTransitionSignedTx
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultTransitionSignedTx.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultTransitionSignedTx message.
         * @function verify
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultTransitionSignedTx.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.pb.BaseMessage.verify(message.base);
                if (error)
                    return "base." + error;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.oldVaultId != null && message.hasOwnProperty("oldVaultId"))
                if (!$util.isInteger(message.oldVaultId))
                    return "oldVaultId: integer expected";
            if (message.newVaultId != null && message.hasOwnProperty("newVaultId"))
                if (!$util.isInteger(message.newVaultId))
                    return "newVaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.msgHash != null && message.hasOwnProperty("msgHash"))
                if (!(message.msgHash && typeof message.msgHash.length === "number" || $util.isString(message.msgHash)))
                    return "msgHash: buffer expected";
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            return null;
        };

        /**
         * Creates a FrostVaultTransitionSignedTx message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultTransitionSignedTx} FrostVaultTransitionSignedTx
         */
        FrostVaultTransitionSignedTx.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultTransitionSignedTx)
                return object;
            let message = new $root.pb.FrostVaultTransitionSignedTx();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".pb.FrostVaultTransitionSignedTx.base: object expected");
                message.base = $root.pb.BaseMessage.fromObject(object.base);
            }
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.oldVaultId != null)
                message.oldVaultId = object.oldVaultId >>> 0;
            if (object.newVaultId != null)
                message.newVaultId = object.newVaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.msgHash != null)
                if (typeof object.msgHash === "string")
                    $util.base64.decode(object.msgHash, message.msgHash = $util.newBuffer($util.base64.length(object.msgHash)), 0);
                else if (object.msgHash.length >= 0)
                    message.msgHash = object.msgHash;
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultTransitionSignedTx message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {pb.FrostVaultTransitionSignedTx} message FrostVaultTransitionSignedTx
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultTransitionSignedTx.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.chain = "";
                object.oldVaultId = 0;
                object.newVaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                if (options.bytes === String)
                    object.msgHash = "";
                else {
                    object.msgHash = [];
                    if (options.bytes !== Array)
                        object.msgHash = $util.newBuffer(object.msgHash);
                }
                if (options.bytes === String)
                    object.signature = "";
                else {
                    object.signature = [];
                    if (options.bytes !== Array)
                        object.signature = $util.newBuffer(object.signature);
                }
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.pb.BaseMessage.toObject(message.base, options);
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.oldVaultId != null && message.hasOwnProperty("oldVaultId"))
                object.oldVaultId = message.oldVaultId;
            if (message.newVaultId != null && message.hasOwnProperty("newVaultId"))
                object.newVaultId = message.newVaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.msgHash != null && message.hasOwnProperty("msgHash"))
                object.msgHash = options.bytes === String ? $util.base64.encode(message.msgHash, 0, message.msgHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.msgHash) : message.msgHash;
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            return object;
        };

        /**
         * Converts this FrostVaultTransitionSignedTx to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultTransitionSignedTx
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultTransitionSignedTx.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultTransitionSignedTx
         * @function getTypeUrl
         * @memberof pb.FrostVaultTransitionSignedTx
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultTransitionSignedTx.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultTransitionSignedTx";
        };

        return FrostVaultTransitionSignedTx;
    })();

    pb.FrostTop10000 = (function() {

        /**
         * Properties of a FrostTop10000.
         * @memberof pb
         * @interface IFrostTop10000
         * @property {number|Long|null} [height] FrostTop10000 height
         * @property {Array.<number|Long>|null} [indices] FrostTop10000 indices
         * @property {Array.<string>|null} [addresses] FrostTop10000 addresses
         * @property {Array.<Uint8Array>|null} [publicKeys] FrostTop10000 publicKeys
         * @property {number|Long|null} [updateHeight] FrostTop10000 updateHeight
         */

        /**
         * Constructs a new FrostTop10000.
         * @memberof pb
         * @classdesc Represents a FrostTop10000.
         * @implements IFrostTop10000
         * @constructor
         * @param {pb.IFrostTop10000=} [properties] Properties to set
         */
        function FrostTop10000(properties) {
            this.indices = [];
            this.addresses = [];
            this.publicKeys = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostTop10000 height.
         * @member {number|Long} height
         * @memberof pb.FrostTop10000
         * @instance
         */
        FrostTop10000.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * FrostTop10000 indices.
         * @member {Array.<number|Long>} indices
         * @memberof pb.FrostTop10000
         * @instance
         */
        FrostTop10000.prototype.indices = $util.emptyArray;

        /**
         * FrostTop10000 addresses.
         * @member {Array.<string>} addresses
         * @memberof pb.FrostTop10000
         * @instance
         */
        FrostTop10000.prototype.addresses = $util.emptyArray;

        /**
         * FrostTop10000 publicKeys.
         * @member {Array.<Uint8Array>} publicKeys
         * @memberof pb.FrostTop10000
         * @instance
         */
        FrostTop10000.prototype.publicKeys = $util.emptyArray;

        /**
         * FrostTop10000 updateHeight.
         * @member {number|Long} updateHeight
         * @memberof pb.FrostTop10000
         * @instance
         */
        FrostTop10000.prototype.updateHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new FrostTop10000 instance using the specified properties.
         * @function create
         * @memberof pb.FrostTop10000
         * @static
         * @param {pb.IFrostTop10000=} [properties] Properties to set
         * @returns {pb.FrostTop10000} FrostTop10000 instance
         */
        FrostTop10000.create = function create(properties) {
            return new FrostTop10000(properties);
        };

        /**
         * Encodes the specified FrostTop10000 message. Does not implicitly {@link pb.FrostTop10000.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostTop10000
         * @static
         * @param {pb.IFrostTop10000} message FrostTop10000 message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostTop10000.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.height);
            if (message.indices != null && message.indices.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.indices.length; ++i)
                    writer.uint64(message.indices[i]);
                writer.ldelim();
            }
            if (message.addresses != null && message.addresses.length)
                for (let i = 0; i < message.addresses.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.addresses[i]);
            if (message.publicKeys != null && message.publicKeys.length)
                for (let i = 0; i < message.publicKeys.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.publicKeys[i]);
            if (message.updateHeight != null && Object.hasOwnProperty.call(message, "updateHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.updateHeight);
            return writer;
        };

        /**
         * Encodes the specified FrostTop10000 message, length delimited. Does not implicitly {@link pb.FrostTop10000.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostTop10000
         * @static
         * @param {pb.IFrostTop10000} message FrostTop10000 message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostTop10000.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostTop10000 message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostTop10000
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostTop10000} FrostTop10000
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostTop10000.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostTop10000();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.height = reader.uint64();
                        break;
                    }
                case 2: {
                        if (!(message.indices && message.indices.length))
                            message.indices = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.indices.push(reader.uint64());
                        } else
                            message.indices.push(reader.uint64());
                        break;
                    }
                case 3: {
                        if (!(message.addresses && message.addresses.length))
                            message.addresses = [];
                        message.addresses.push(reader.string());
                        break;
                    }
                case 4: {
                        if (!(message.publicKeys && message.publicKeys.length))
                            message.publicKeys = [];
                        message.publicKeys.push(reader.bytes());
                        break;
                    }
                case 5: {
                        message.updateHeight = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostTop10000 message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostTop10000
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostTop10000} FrostTop10000
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostTop10000.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostTop10000 message.
         * @function verify
         * @memberof pb.FrostTop10000
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostTop10000.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.indices != null && message.hasOwnProperty("indices")) {
                if (!Array.isArray(message.indices))
                    return "indices: array expected";
                for (let i = 0; i < message.indices.length; ++i)
                    if (!$util.isInteger(message.indices[i]) && !(message.indices[i] && $util.isInteger(message.indices[i].low) && $util.isInteger(message.indices[i].high)))
                        return "indices: integer|Long[] expected";
            }
            if (message.addresses != null && message.hasOwnProperty("addresses")) {
                if (!Array.isArray(message.addresses))
                    return "addresses: array expected";
                for (let i = 0; i < message.addresses.length; ++i)
                    if (!$util.isString(message.addresses[i]))
                        return "addresses: string[] expected";
            }
            if (message.publicKeys != null && message.hasOwnProperty("publicKeys")) {
                if (!Array.isArray(message.publicKeys))
                    return "publicKeys: array expected";
                for (let i = 0; i < message.publicKeys.length; ++i)
                    if (!(message.publicKeys[i] && typeof message.publicKeys[i].length === "number" || $util.isString(message.publicKeys[i])))
                        return "publicKeys: buffer[] expected";
            }
            if (message.updateHeight != null && message.hasOwnProperty("updateHeight"))
                if (!$util.isInteger(message.updateHeight) && !(message.updateHeight && $util.isInteger(message.updateHeight.low) && $util.isInteger(message.updateHeight.high)))
                    return "updateHeight: integer|Long expected";
            return null;
        };

        /**
         * Creates a FrostTop10000 message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostTop10000
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostTop10000} FrostTop10000
         */
        FrostTop10000.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostTop10000)
                return object;
            let message = new $root.pb.FrostTop10000();
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.indices) {
                if (!Array.isArray(object.indices))
                    throw TypeError(".pb.FrostTop10000.indices: array expected");
                message.indices = [];
                for (let i = 0; i < object.indices.length; ++i)
                    if ($util.Long)
                        (message.indices[i] = $util.Long.fromValue(object.indices[i])).unsigned = true;
                    else if (typeof object.indices[i] === "string")
                        message.indices[i] = parseInt(object.indices[i], 10);
                    else if (typeof object.indices[i] === "number")
                        message.indices[i] = object.indices[i];
                    else if (typeof object.indices[i] === "object")
                        message.indices[i] = new $util.LongBits(object.indices[i].low >>> 0, object.indices[i].high >>> 0).toNumber(true);
            }
            if (object.addresses) {
                if (!Array.isArray(object.addresses))
                    throw TypeError(".pb.FrostTop10000.addresses: array expected");
                message.addresses = [];
                for (let i = 0; i < object.addresses.length; ++i)
                    message.addresses[i] = String(object.addresses[i]);
            }
            if (object.publicKeys) {
                if (!Array.isArray(object.publicKeys))
                    throw TypeError(".pb.FrostTop10000.publicKeys: array expected");
                message.publicKeys = [];
                for (let i = 0; i < object.publicKeys.length; ++i)
                    if (typeof object.publicKeys[i] === "string")
                        $util.base64.decode(object.publicKeys[i], message.publicKeys[i] = $util.newBuffer($util.base64.length(object.publicKeys[i])), 0);
                    else if (object.publicKeys[i].length >= 0)
                        message.publicKeys[i] = object.publicKeys[i];
            }
            if (object.updateHeight != null)
                if ($util.Long)
                    (message.updateHeight = $util.Long.fromValue(object.updateHeight)).unsigned = true;
                else if (typeof object.updateHeight === "string")
                    message.updateHeight = parseInt(object.updateHeight, 10);
                else if (typeof object.updateHeight === "number")
                    message.updateHeight = object.updateHeight;
                else if (typeof object.updateHeight === "object")
                    message.updateHeight = new $util.LongBits(object.updateHeight.low >>> 0, object.updateHeight.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a FrostTop10000 message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostTop10000
         * @static
         * @param {pb.FrostTop10000} message FrostTop10000
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostTop10000.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.indices = [];
                object.addresses = [];
                object.publicKeys = [];
            }
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.updateHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.updateHeight = options.longs === String ? "0" : 0;
            }
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.indices && message.indices.length) {
                object.indices = [];
                for (let j = 0; j < message.indices.length; ++j)
                    if (typeof message.indices[j] === "number")
                        object.indices[j] = options.longs === String ? String(message.indices[j]) : message.indices[j];
                    else
                        object.indices[j] = options.longs === String ? $util.Long.prototype.toString.call(message.indices[j]) : options.longs === Number ? new $util.LongBits(message.indices[j].low >>> 0, message.indices[j].high >>> 0).toNumber(true) : message.indices[j];
            }
            if (message.addresses && message.addresses.length) {
                object.addresses = [];
                for (let j = 0; j < message.addresses.length; ++j)
                    object.addresses[j] = message.addresses[j];
            }
            if (message.publicKeys && message.publicKeys.length) {
                object.publicKeys = [];
                for (let j = 0; j < message.publicKeys.length; ++j)
                    object.publicKeys[j] = options.bytes === String ? $util.base64.encode(message.publicKeys[j], 0, message.publicKeys[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKeys[j]) : message.publicKeys[j];
            }
            if (message.updateHeight != null && message.hasOwnProperty("updateHeight"))
                if (typeof message.updateHeight === "number")
                    object.updateHeight = options.longs === String ? String(message.updateHeight) : message.updateHeight;
                else
                    object.updateHeight = options.longs === String ? $util.Long.prototype.toString.call(message.updateHeight) : options.longs === Number ? new $util.LongBits(message.updateHeight.low >>> 0, message.updateHeight.high >>> 0).toNumber(true) : message.updateHeight;
            return object;
        };

        /**
         * Converts this FrostTop10000 to JSON.
         * @function toJSON
         * @memberof pb.FrostTop10000
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostTop10000.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostTop10000
         * @function getTypeUrl
         * @memberof pb.FrostTop10000
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostTop10000.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostTop10000";
        };

        return FrostTop10000;
    })();

    pb.FrostVaultConfig = (function() {

        /**
         * Properties of a FrostVaultConfig.
         * @memberof pb
         * @interface IFrostVaultConfig
         * @property {string|null} [chain] FrostVaultConfig chain
         * @property {pb.SignAlgo|null} [signAlgo] FrostVaultConfig signAlgo
         * @property {number|null} [vaultCount] FrostVaultConfig vaultCount
         * @property {number|null} [committeeSize] FrostVaultConfig committeeSize
         * @property {number|null} [thresholdRatio] FrostVaultConfig thresholdRatio
         * @property {string|null} [selectionSeedRule] FrostVaultConfig selectionSeedRule
         * @property {Array.<string>|null} [vaultRefs] FrostVaultConfig vaultRefs
         * @property {string|null} [depositAllocationRule] FrostVaultConfig depositAllocationRule
         */

        /**
         * Constructs a new FrostVaultConfig.
         * @memberof pb
         * @classdesc Represents a FrostVaultConfig.
         * @implements IFrostVaultConfig
         * @constructor
         * @param {pb.IFrostVaultConfig=} [properties] Properties to set
         */
        function FrostVaultConfig(properties) {
            this.vaultRefs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FrostVaultConfig chain.
         * @member {string} chain
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.chain = "";

        /**
         * FrostVaultConfig signAlgo.
         * @member {pb.SignAlgo} signAlgo
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.signAlgo = 0;

        /**
         * FrostVaultConfig vaultCount.
         * @member {number} vaultCount
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.vaultCount = 0;

        /**
         * FrostVaultConfig committeeSize.
         * @member {number} committeeSize
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.committeeSize = 0;

        /**
         * FrostVaultConfig thresholdRatio.
         * @member {number} thresholdRatio
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.thresholdRatio = 0;

        /**
         * FrostVaultConfig selectionSeedRule.
         * @member {string} selectionSeedRule
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.selectionSeedRule = "";

        /**
         * FrostVaultConfig vaultRefs.
         * @member {Array.<string>} vaultRefs
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.vaultRefs = $util.emptyArray;

        /**
         * FrostVaultConfig depositAllocationRule.
         * @member {string} depositAllocationRule
         * @memberof pb.FrostVaultConfig
         * @instance
         */
        FrostVaultConfig.prototype.depositAllocationRule = "";

        /**
         * Creates a new FrostVaultConfig instance using the specified properties.
         * @function create
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {pb.IFrostVaultConfig=} [properties] Properties to set
         * @returns {pb.FrostVaultConfig} FrostVaultConfig instance
         */
        FrostVaultConfig.create = function create(properties) {
            return new FrostVaultConfig(properties);
        };

        /**
         * Encodes the specified FrostVaultConfig message. Does not implicitly {@link pb.FrostVaultConfig.verify|verify} messages.
         * @function encode
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {pb.IFrostVaultConfig} message FrostVaultConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultConfig.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.signAlgo);
            if (message.vaultCount != null && Object.hasOwnProperty.call(message, "vaultCount"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.vaultCount);
            if (message.committeeSize != null && Object.hasOwnProperty.call(message, "committeeSize"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.committeeSize);
            if (message.thresholdRatio != null && Object.hasOwnProperty.call(message, "thresholdRatio"))
                writer.uint32(/* id 5, wireType 5 =*/45).float(message.thresholdRatio);
            if (message.selectionSeedRule != null && Object.hasOwnProperty.call(message, "selectionSeedRule"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.selectionSeedRule);
            if (message.vaultRefs != null && message.vaultRefs.length)
                for (let i = 0; i < message.vaultRefs.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.vaultRefs[i]);
            if (message.depositAllocationRule != null && Object.hasOwnProperty.call(message, "depositAllocationRule"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.depositAllocationRule);
            return writer;
        };

        /**
         * Encodes the specified FrostVaultConfig message, length delimited. Does not implicitly {@link pb.FrostVaultConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {pb.IFrostVaultConfig} message FrostVaultConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FrostVaultConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a FrostVaultConfig message from the specified reader or buffer.
         * @function decode
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.FrostVaultConfig} FrostVaultConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultConfig.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.FrostVaultConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.signAlgo = reader.int32();
                        break;
                    }
                case 3: {
                        message.vaultCount = reader.uint32();
                        break;
                    }
                case 4: {
                        message.committeeSize = reader.uint32();
                        break;
                    }
                case 5: {
                        message.thresholdRatio = reader.float();
                        break;
                    }
                case 6: {
                        message.selectionSeedRule = reader.string();
                        break;
                    }
                case 7: {
                        if (!(message.vaultRefs && message.vaultRefs.length))
                            message.vaultRefs = [];
                        message.vaultRefs.push(reader.string());
                        break;
                    }
                case 8: {
                        message.depositAllocationRule = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FrostVaultConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.FrostVaultConfig} FrostVaultConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FrostVaultConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FrostVaultConfig message.
         * @function verify
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FrostVaultConfig.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                switch (message.signAlgo) {
                default:
                    return "signAlgo: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.vaultCount != null && message.hasOwnProperty("vaultCount"))
                if (!$util.isInteger(message.vaultCount))
                    return "vaultCount: integer expected";
            if (message.committeeSize != null && message.hasOwnProperty("committeeSize"))
                if (!$util.isInteger(message.committeeSize))
                    return "committeeSize: integer expected";
            if (message.thresholdRatio != null && message.hasOwnProperty("thresholdRatio"))
                if (typeof message.thresholdRatio !== "number")
                    return "thresholdRatio: number expected";
            if (message.selectionSeedRule != null && message.hasOwnProperty("selectionSeedRule"))
                if (!$util.isString(message.selectionSeedRule))
                    return "selectionSeedRule: string expected";
            if (message.vaultRefs != null && message.hasOwnProperty("vaultRefs")) {
                if (!Array.isArray(message.vaultRefs))
                    return "vaultRefs: array expected";
                for (let i = 0; i < message.vaultRefs.length; ++i)
                    if (!$util.isString(message.vaultRefs[i]))
                        return "vaultRefs: string[] expected";
            }
            if (message.depositAllocationRule != null && message.hasOwnProperty("depositAllocationRule"))
                if (!$util.isString(message.depositAllocationRule))
                    return "depositAllocationRule: string expected";
            return null;
        };

        /**
         * Creates a FrostVaultConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.FrostVaultConfig} FrostVaultConfig
         */
        FrostVaultConfig.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.FrostVaultConfig)
                return object;
            let message = new $root.pb.FrostVaultConfig();
            if (object.chain != null)
                message.chain = String(object.chain);
            switch (object.signAlgo) {
            default:
                if (typeof object.signAlgo === "number") {
                    message.signAlgo = object.signAlgo;
                    break;
                }
                break;
            case "SIGN_ALGO_UNSPECIFIED":
            case 0:
                message.signAlgo = 0;
                break;
            case "SIGN_ALGO_ECDSA_P256":
            case 1:
                message.signAlgo = 1;
                break;
            case "SIGN_ALGO_BLS_BN256_G2":
            case 2:
                message.signAlgo = 2;
                break;
            case "SIGN_ALGO_SCHNORR_SECP256K1_BIP340":
            case 3:
                message.signAlgo = 3;
                break;
            case "SIGN_ALGO_SCHNORR_ALT_BN128":
            case 4:
                message.signAlgo = 4;
                break;
            case "SIGN_ALGO_ED25519":
            case 5:
                message.signAlgo = 5;
                break;
            case "SIGN_ALGO_ECDSA_SECP256K1":
            case 6:
                message.signAlgo = 6;
                break;
            }
            if (object.vaultCount != null)
                message.vaultCount = object.vaultCount >>> 0;
            if (object.committeeSize != null)
                message.committeeSize = object.committeeSize >>> 0;
            if (object.thresholdRatio != null)
                message.thresholdRatio = Number(object.thresholdRatio);
            if (object.selectionSeedRule != null)
                message.selectionSeedRule = String(object.selectionSeedRule);
            if (object.vaultRefs) {
                if (!Array.isArray(object.vaultRefs))
                    throw TypeError(".pb.FrostVaultConfig.vaultRefs: array expected");
                message.vaultRefs = [];
                for (let i = 0; i < object.vaultRefs.length; ++i)
                    message.vaultRefs[i] = String(object.vaultRefs[i]);
            }
            if (object.depositAllocationRule != null)
                message.depositAllocationRule = String(object.depositAllocationRule);
            return message;
        };

        /**
         * Creates a plain object from a FrostVaultConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {pb.FrostVaultConfig} message FrostVaultConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FrostVaultConfig.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.vaultRefs = [];
            if (options.defaults) {
                object.chain = "";
                object.signAlgo = options.enums === String ? "SIGN_ALGO_UNSPECIFIED" : 0;
                object.vaultCount = 0;
                object.committeeSize = 0;
                object.thresholdRatio = 0;
                object.selectionSeedRule = "";
                object.depositAllocationRule = "";
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = options.enums === String ? $root.pb.SignAlgo[message.signAlgo] === undefined ? message.signAlgo : $root.pb.SignAlgo[message.signAlgo] : message.signAlgo;
            if (message.vaultCount != null && message.hasOwnProperty("vaultCount"))
                object.vaultCount = message.vaultCount;
            if (message.committeeSize != null && message.hasOwnProperty("committeeSize"))
                object.committeeSize = message.committeeSize;
            if (message.thresholdRatio != null && message.hasOwnProperty("thresholdRatio"))
                object.thresholdRatio = options.json && !isFinite(message.thresholdRatio) ? String(message.thresholdRatio) : message.thresholdRatio;
            if (message.selectionSeedRule != null && message.hasOwnProperty("selectionSeedRule"))
                object.selectionSeedRule = message.selectionSeedRule;
            if (message.vaultRefs && message.vaultRefs.length) {
                object.vaultRefs = [];
                for (let j = 0; j < message.vaultRefs.length; ++j)
                    object.vaultRefs[j] = message.vaultRefs[j];
            }
            if (message.depositAllocationRule != null && message.hasOwnProperty("depositAllocationRule"))
                object.depositAllocationRule = message.depositAllocationRule;
            return object;
        };

        /**
         * Converts this FrostVaultConfig to JSON.
         * @function toJSON
         * @memberof pb.FrostVaultConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FrostVaultConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FrostVaultConfig
         * @function getTypeUrl
         * @memberof pb.FrostVaultConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FrostVaultConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.FrostVaultConfig";
        };

        return FrostVaultConfig;
    })();

    pb.ChainCfg = (function() {

        /**
         * Properties of a ChainCfg.
         * @memberof pb
         * @interface IChainCfg
         * @property {string|null} [signAlgo] ChainCfg signAlgo
         * @property {string|null} [frostVariant] ChainCfg frostVariant
         * @property {number|null} [vaultsPerChain] ChainCfg vaultsPerChain
         */

        /**
         * Constructs a new ChainCfg.
         * @memberof pb
         * @classdesc Represents a ChainCfg.
         * @implements IChainCfg
         * @constructor
         * @param {pb.IChainCfg=} [properties] Properties to set
         */
        function ChainCfg(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChainCfg signAlgo.
         * @member {string} signAlgo
         * @memberof pb.ChainCfg
         * @instance
         */
        ChainCfg.prototype.signAlgo = "";

        /**
         * ChainCfg frostVariant.
         * @member {string} frostVariant
         * @memberof pb.ChainCfg
         * @instance
         */
        ChainCfg.prototype.frostVariant = "";

        /**
         * ChainCfg vaultsPerChain.
         * @member {number} vaultsPerChain
         * @memberof pb.ChainCfg
         * @instance
         */
        ChainCfg.prototype.vaultsPerChain = 0;

        /**
         * Creates a new ChainCfg instance using the specified properties.
         * @function create
         * @memberof pb.ChainCfg
         * @static
         * @param {pb.IChainCfg=} [properties] Properties to set
         * @returns {pb.ChainCfg} ChainCfg instance
         */
        ChainCfg.create = function create(properties) {
            return new ChainCfg(properties);
        };

        /**
         * Encodes the specified ChainCfg message. Does not implicitly {@link pb.ChainCfg.verify|verify} messages.
         * @function encode
         * @memberof pb.ChainCfg
         * @static
         * @param {pb.IChainCfg} message ChainCfg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChainCfg.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.signAlgo);
            if (message.frostVariant != null && Object.hasOwnProperty.call(message, "frostVariant"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.frostVariant);
            if (message.vaultsPerChain != null && Object.hasOwnProperty.call(message, "vaultsPerChain"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.vaultsPerChain);
            return writer;
        };

        /**
         * Encodes the specified ChainCfg message, length delimited. Does not implicitly {@link pb.ChainCfg.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ChainCfg
         * @static
         * @param {pb.IChainCfg} message ChainCfg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChainCfg.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChainCfg message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ChainCfg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ChainCfg} ChainCfg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChainCfg.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ChainCfg();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.signAlgo = reader.string();
                        break;
                    }
                case 2: {
                        message.frostVariant = reader.string();
                        break;
                    }
                case 3: {
                        message.vaultsPerChain = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChainCfg message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ChainCfg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ChainCfg} ChainCfg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChainCfg.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChainCfg message.
         * @function verify
         * @memberof pb.ChainCfg
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChainCfg.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                if (!$util.isString(message.signAlgo))
                    return "signAlgo: string expected";
            if (message.frostVariant != null && message.hasOwnProperty("frostVariant"))
                if (!$util.isString(message.frostVariant))
                    return "frostVariant: string expected";
            if (message.vaultsPerChain != null && message.hasOwnProperty("vaultsPerChain"))
                if (!$util.isInteger(message.vaultsPerChain))
                    return "vaultsPerChain: integer expected";
            return null;
        };

        /**
         * Creates a ChainCfg message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ChainCfg
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ChainCfg} ChainCfg
         */
        ChainCfg.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ChainCfg)
                return object;
            let message = new $root.pb.ChainCfg();
            if (object.signAlgo != null)
                message.signAlgo = String(object.signAlgo);
            if (object.frostVariant != null)
                message.frostVariant = String(object.frostVariant);
            if (object.vaultsPerChain != null)
                message.vaultsPerChain = object.vaultsPerChain | 0;
            return message;
        };

        /**
         * Creates a plain object from a ChainCfg message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ChainCfg
         * @static
         * @param {pb.ChainCfg} message ChainCfg
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChainCfg.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.signAlgo = "";
                object.frostVariant = "";
                object.vaultsPerChain = 0;
            }
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = message.signAlgo;
            if (message.frostVariant != null && message.hasOwnProperty("frostVariant"))
                object.frostVariant = message.frostVariant;
            if (message.vaultsPerChain != null && message.hasOwnProperty("vaultsPerChain"))
                object.vaultsPerChain = message.vaultsPerChain;
            return object;
        };

        /**
         * Converts this ChainCfg to JSON.
         * @function toJSON
         * @memberof pb.ChainCfg
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChainCfg.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChainCfg
         * @function getTypeUrl
         * @memberof pb.ChainCfg
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChainCfg.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ChainCfg";
        };

        return ChainCfg;
    })();

    pb.GetFrostConfigResponse = (function() {

        /**
         * Properties of a GetFrostConfigResponse.
         * @memberof pb
         * @interface IGetFrostConfigResponse
         * @property {boolean|null} [enabled] GetFrostConfigResponse enabled
         * @property {Array.<string>|null} [supportedChains] GetFrostConfigResponse supportedChains
         * @property {number|null} [defaultThreshold] GetFrostConfigResponse defaultThreshold
         * @property {number|null} [vaultCount] GetFrostConfigResponse vaultCount
         * @property {number|null} [committeeSize] GetFrostConfigResponse committeeSize
         * @property {string|null} [minWithdraw] GetFrostConfigResponse minWithdraw
         * @property {Object.<string,pb.IChainCfg>|null} [chains] GetFrostConfigResponse chains
         */

        /**
         * Constructs a new GetFrostConfigResponse.
         * @memberof pb
         * @classdesc Represents a GetFrostConfigResponse.
         * @implements IGetFrostConfigResponse
         * @constructor
         * @param {pb.IGetFrostConfigResponse=} [properties] Properties to set
         */
        function GetFrostConfigResponse(properties) {
            this.supportedChains = [];
            this.chains = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetFrostConfigResponse enabled.
         * @member {boolean} enabled
         * @memberof pb.GetFrostConfigResponse
         * @instance
         */
        GetFrostConfigResponse.prototype.enabled = false;

        /**
         * GetFrostConfigResponse supportedChains.
         * @member {Array.<string>} supportedChains
         * @memberof pb.GetFrostConfigResponse
         * @instance
         */
        GetFrostConfigResponse.prototype.supportedChains = $util.emptyArray;

        /**
         * GetFrostConfigResponse defaultThreshold.
         * @member {number} defaultThreshold
         * @memberof pb.GetFrostConfigResponse
         * @instance
         */
        GetFrostConfigResponse.prototype.defaultThreshold = 0;

        /**
         * GetFrostConfigResponse vaultCount.
         * @member {number} vaultCount
         * @memberof pb.GetFrostConfigResponse
         * @instance
         */
        GetFrostConfigResponse.prototype.vaultCount = 0;

        /**
         * GetFrostConfigResponse committeeSize.
         * @member {number} committeeSize
         * @memberof pb.GetFrostConfigResponse
         * @instance
         */
        GetFrostConfigResponse.prototype.committeeSize = 0;

        /**
         * GetFrostConfigResponse minWithdraw.
         * @member {string} minWithdraw
         * @memberof pb.GetFrostConfigResponse
         * @instance
         */
        GetFrostConfigResponse.prototype.minWithdraw = "";

        /**
         * GetFrostConfigResponse chains.
         * @member {Object.<string,pb.IChainCfg>} chains
         * @memberof pb.GetFrostConfigResponse
         * @instance
         */
        GetFrostConfigResponse.prototype.chains = $util.emptyObject;

        /**
         * Creates a new GetFrostConfigResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {pb.IGetFrostConfigResponse=} [properties] Properties to set
         * @returns {pb.GetFrostConfigResponse} GetFrostConfigResponse instance
         */
        GetFrostConfigResponse.create = function create(properties) {
            return new GetFrostConfigResponse(properties);
        };

        /**
         * Encodes the specified GetFrostConfigResponse message. Does not implicitly {@link pb.GetFrostConfigResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {pb.IGetFrostConfigResponse} message GetFrostConfigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetFrostConfigResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.enabled);
            if (message.supportedChains != null && message.supportedChains.length)
                for (let i = 0; i < message.supportedChains.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.supportedChains[i]);
            if (message.defaultThreshold != null && Object.hasOwnProperty.call(message, "defaultThreshold"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.defaultThreshold);
            if (message.vaultCount != null && Object.hasOwnProperty.call(message, "vaultCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.vaultCount);
            if (message.committeeSize != null && Object.hasOwnProperty.call(message, "committeeSize"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.committeeSize);
            if (message.minWithdraw != null && Object.hasOwnProperty.call(message, "minWithdraw"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.minWithdraw);
            if (message.chains != null && Object.hasOwnProperty.call(message, "chains"))
                for (let keys = Object.keys(message.chains), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 7, wireType 2 =*/58).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.pb.ChainCfg.encode(message.chains[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified GetFrostConfigResponse message, length delimited. Does not implicitly {@link pb.GetFrostConfigResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {pb.IGetFrostConfigResponse} message GetFrostConfigResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetFrostConfigResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetFrostConfigResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetFrostConfigResponse} GetFrostConfigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetFrostConfigResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetFrostConfigResponse(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enabled = reader.bool();
                        break;
                    }
                case 2: {
                        if (!(message.supportedChains && message.supportedChains.length))
                            message.supportedChains = [];
                        message.supportedChains.push(reader.string());
                        break;
                    }
                case 3: {
                        message.defaultThreshold = reader.double();
                        break;
                    }
                case 4: {
                        message.vaultCount = reader.int32();
                        break;
                    }
                case 5: {
                        message.committeeSize = reader.int32();
                        break;
                    }
                case 6: {
                        message.minWithdraw = reader.string();
                        break;
                    }
                case 7: {
                        if (message.chains === $util.emptyObject)
                            message.chains = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = $root.pb.ChainCfg.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.chains[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetFrostConfigResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetFrostConfigResponse} GetFrostConfigResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetFrostConfigResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetFrostConfigResponse message.
         * @function verify
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetFrostConfigResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                if (typeof message.enabled !== "boolean")
                    return "enabled: boolean expected";
            if (message.supportedChains != null && message.hasOwnProperty("supportedChains")) {
                if (!Array.isArray(message.supportedChains))
                    return "supportedChains: array expected";
                for (let i = 0; i < message.supportedChains.length; ++i)
                    if (!$util.isString(message.supportedChains[i]))
                        return "supportedChains: string[] expected";
            }
            if (message.defaultThreshold != null && message.hasOwnProperty("defaultThreshold"))
                if (typeof message.defaultThreshold !== "number")
                    return "defaultThreshold: number expected";
            if (message.vaultCount != null && message.hasOwnProperty("vaultCount"))
                if (!$util.isInteger(message.vaultCount))
                    return "vaultCount: integer expected";
            if (message.committeeSize != null && message.hasOwnProperty("committeeSize"))
                if (!$util.isInteger(message.committeeSize))
                    return "committeeSize: integer expected";
            if (message.minWithdraw != null && message.hasOwnProperty("minWithdraw"))
                if (!$util.isString(message.minWithdraw))
                    return "minWithdraw: string expected";
            if (message.chains != null && message.hasOwnProperty("chains")) {
                if (!$util.isObject(message.chains))
                    return "chains: object expected";
                let key = Object.keys(message.chains);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.pb.ChainCfg.verify(message.chains[key[i]]);
                    if (error)
                        return "chains." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GetFrostConfigResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetFrostConfigResponse} GetFrostConfigResponse
         */
        GetFrostConfigResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetFrostConfigResponse)
                return object;
            let message = new $root.pb.GetFrostConfigResponse();
            if (object.enabled != null)
                message.enabled = Boolean(object.enabled);
            if (object.supportedChains) {
                if (!Array.isArray(object.supportedChains))
                    throw TypeError(".pb.GetFrostConfigResponse.supportedChains: array expected");
                message.supportedChains = [];
                for (let i = 0; i < object.supportedChains.length; ++i)
                    message.supportedChains[i] = String(object.supportedChains[i]);
            }
            if (object.defaultThreshold != null)
                message.defaultThreshold = Number(object.defaultThreshold);
            if (object.vaultCount != null)
                message.vaultCount = object.vaultCount | 0;
            if (object.committeeSize != null)
                message.committeeSize = object.committeeSize | 0;
            if (object.minWithdraw != null)
                message.minWithdraw = String(object.minWithdraw);
            if (object.chains) {
                if (typeof object.chains !== "object")
                    throw TypeError(".pb.GetFrostConfigResponse.chains: object expected");
                message.chains = {};
                for (let keys = Object.keys(object.chains), i = 0; i < keys.length; ++i) {
                    if (typeof object.chains[keys[i]] !== "object")
                        throw TypeError(".pb.GetFrostConfigResponse.chains: object expected");
                    message.chains[keys[i]] = $root.pb.ChainCfg.fromObject(object.chains[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetFrostConfigResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {pb.GetFrostConfigResponse} message GetFrostConfigResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetFrostConfigResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.supportedChains = [];
            if (options.objects || options.defaults)
                object.chains = {};
            if (options.defaults) {
                object.enabled = false;
                object.defaultThreshold = 0;
                object.vaultCount = 0;
                object.committeeSize = 0;
                object.minWithdraw = "";
            }
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                object.enabled = message.enabled;
            if (message.supportedChains && message.supportedChains.length) {
                object.supportedChains = [];
                for (let j = 0; j < message.supportedChains.length; ++j)
                    object.supportedChains[j] = message.supportedChains[j];
            }
            if (message.defaultThreshold != null && message.hasOwnProperty("defaultThreshold"))
                object.defaultThreshold = options.json && !isFinite(message.defaultThreshold) ? String(message.defaultThreshold) : message.defaultThreshold;
            if (message.vaultCount != null && message.hasOwnProperty("vaultCount"))
                object.vaultCount = message.vaultCount;
            if (message.committeeSize != null && message.hasOwnProperty("committeeSize"))
                object.committeeSize = message.committeeSize;
            if (message.minWithdraw != null && message.hasOwnProperty("minWithdraw"))
                object.minWithdraw = message.minWithdraw;
            let keys2;
            if (message.chains && (keys2 = Object.keys(message.chains)).length) {
                object.chains = {};
                for (let j = 0; j < keys2.length; ++j)
                    object.chains[keys2[j]] = $root.pb.ChainCfg.toObject(message.chains[keys2[j]], options);
            }
            return object;
        };

        /**
         * Converts this GetFrostConfigResponse to JSON.
         * @function toJSON
         * @memberof pb.GetFrostConfigResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetFrostConfigResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetFrostConfigResponse
         * @function getTypeUrl
         * @memberof pb.GetFrostConfigResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetFrostConfigResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetFrostConfigResponse";
        };

        return GetFrostConfigResponse;
    })();

    pb.GetWithdrawStatusResponse = (function() {

        /**
         * Properties of a GetWithdrawStatusResponse.
         * @memberof pb
         * @interface IGetWithdrawStatusResponse
         * @property {string|null} [lotId] GetWithdrawStatusResponse lotId
         * @property {string|null} [chain] GetWithdrawStatusResponse chain
         * @property {string|null} [status] GetWithdrawStatusResponse status
         * @property {string|null} [amount] GetWithdrawStatusResponse amount
         * @property {string|null} [receiver] GetWithdrawStatusResponse receiver
         * @property {number|Long|null} [height] GetWithdrawStatusResponse height
         * @property {string|null} [txHash] GetWithdrawStatusResponse txHash
         * @property {Array.<pb.IFrostPlanningLog>|null} [planningLogs] GetWithdrawStatusResponse planningLogs
         */

        /**
         * Constructs a new GetWithdrawStatusResponse.
         * @memberof pb
         * @classdesc Represents a GetWithdrawStatusResponse.
         * @implements IGetWithdrawStatusResponse
         * @constructor
         * @param {pb.IGetWithdrawStatusResponse=} [properties] Properties to set
         */
        function GetWithdrawStatusResponse(properties) {
            this.planningLogs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetWithdrawStatusResponse lotId.
         * @member {string} lotId
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.lotId = "";

        /**
         * GetWithdrawStatusResponse chain.
         * @member {string} chain
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.chain = "";

        /**
         * GetWithdrawStatusResponse status.
         * @member {string} status
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.status = "";

        /**
         * GetWithdrawStatusResponse amount.
         * @member {string} amount
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.amount = "";

        /**
         * GetWithdrawStatusResponse receiver.
         * @member {string} receiver
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.receiver = "";

        /**
         * GetWithdrawStatusResponse height.
         * @member {number|Long} height
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * GetWithdrawStatusResponse txHash.
         * @member {string} txHash
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.txHash = "";

        /**
         * GetWithdrawStatusResponse planningLogs.
         * @member {Array.<pb.IFrostPlanningLog>} planningLogs
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         */
        GetWithdrawStatusResponse.prototype.planningLogs = $util.emptyArray;

        /**
         * Creates a new GetWithdrawStatusResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {pb.IGetWithdrawStatusResponse=} [properties] Properties to set
         * @returns {pb.GetWithdrawStatusResponse} GetWithdrawStatusResponse instance
         */
        GetWithdrawStatusResponse.create = function create(properties) {
            return new GetWithdrawStatusResponse(properties);
        };

        /**
         * Encodes the specified GetWithdrawStatusResponse message. Does not implicitly {@link pb.GetWithdrawStatusResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {pb.IGetWithdrawStatusResponse} message GetWithdrawStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetWithdrawStatusResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lotId != null && Object.hasOwnProperty.call(message, "lotId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.lotId);
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.chain);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.status);
            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.amount);
            if (message.receiver != null && Object.hasOwnProperty.call(message, "receiver"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.receiver);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.height);
            if (message.txHash != null && Object.hasOwnProperty.call(message, "txHash"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.txHash);
            if (message.planningLogs != null && message.planningLogs.length)
                for (let i = 0; i < message.planningLogs.length; ++i)
                    $root.pb.FrostPlanningLog.encode(message.planningLogs[i], writer.uint32(/* id 8, wireType 2 =*/66).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GetWithdrawStatusResponse message, length delimited. Does not implicitly {@link pb.GetWithdrawStatusResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {pb.IGetWithdrawStatusResponse} message GetWithdrawStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetWithdrawStatusResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetWithdrawStatusResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetWithdrawStatusResponse} GetWithdrawStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetWithdrawStatusResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetWithdrawStatusResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.lotId = reader.string();
                        break;
                    }
                case 2: {
                        message.chain = reader.string();
                        break;
                    }
                case 3: {
                        message.status = reader.string();
                        break;
                    }
                case 4: {
                        message.amount = reader.string();
                        break;
                    }
                case 5: {
                        message.receiver = reader.string();
                        break;
                    }
                case 6: {
                        message.height = reader.uint64();
                        break;
                    }
                case 7: {
                        message.txHash = reader.string();
                        break;
                    }
                case 8: {
                        if (!(message.planningLogs && message.planningLogs.length))
                            message.planningLogs = [];
                        message.planningLogs.push($root.pb.FrostPlanningLog.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetWithdrawStatusResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetWithdrawStatusResponse} GetWithdrawStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetWithdrawStatusResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetWithdrawStatusResponse message.
         * @function verify
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetWithdrawStatusResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.lotId != null && message.hasOwnProperty("lotId"))
                if (!$util.isString(message.lotId))
                    return "lotId: string expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.amount != null && message.hasOwnProperty("amount"))
                if (!$util.isString(message.amount))
                    return "amount: string expected";
            if (message.receiver != null && message.hasOwnProperty("receiver"))
                if (!$util.isString(message.receiver))
                    return "receiver: string expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.txHash != null && message.hasOwnProperty("txHash"))
                if (!$util.isString(message.txHash))
                    return "txHash: string expected";
            if (message.planningLogs != null && message.hasOwnProperty("planningLogs")) {
                if (!Array.isArray(message.planningLogs))
                    return "planningLogs: array expected";
                for (let i = 0; i < message.planningLogs.length; ++i) {
                    let error = $root.pb.FrostPlanningLog.verify(message.planningLogs[i]);
                    if (error)
                        return "planningLogs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GetWithdrawStatusResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetWithdrawStatusResponse} GetWithdrawStatusResponse
         */
        GetWithdrawStatusResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetWithdrawStatusResponse)
                return object;
            let message = new $root.pb.GetWithdrawStatusResponse();
            if (object.lotId != null)
                message.lotId = String(object.lotId);
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.status != null)
                message.status = String(object.status);
            if (object.amount != null)
                message.amount = String(object.amount);
            if (object.receiver != null)
                message.receiver = String(object.receiver);
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.txHash != null)
                message.txHash = String(object.txHash);
            if (object.planningLogs) {
                if (!Array.isArray(object.planningLogs))
                    throw TypeError(".pb.GetWithdrawStatusResponse.planningLogs: array expected");
                message.planningLogs = [];
                for (let i = 0; i < object.planningLogs.length; ++i) {
                    if (typeof object.planningLogs[i] !== "object")
                        throw TypeError(".pb.GetWithdrawStatusResponse.planningLogs: object expected");
                    message.planningLogs[i] = $root.pb.FrostPlanningLog.fromObject(object.planningLogs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetWithdrawStatusResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {pb.GetWithdrawStatusResponse} message GetWithdrawStatusResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetWithdrawStatusResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.planningLogs = [];
            if (options.defaults) {
                object.lotId = "";
                object.chain = "";
                object.status = "";
                object.amount = "";
                object.receiver = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.txHash = "";
            }
            if (message.lotId != null && message.hasOwnProperty("lotId"))
                object.lotId = message.lotId;
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.amount != null && message.hasOwnProperty("amount"))
                object.amount = message.amount;
            if (message.receiver != null && message.hasOwnProperty("receiver"))
                object.receiver = message.receiver;
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.txHash != null && message.hasOwnProperty("txHash"))
                object.txHash = message.txHash;
            if (message.planningLogs && message.planningLogs.length) {
                object.planningLogs = [];
                for (let j = 0; j < message.planningLogs.length; ++j)
                    object.planningLogs[j] = $root.pb.FrostPlanningLog.toObject(message.planningLogs[j], options);
            }
            return object;
        };

        /**
         * Converts this GetWithdrawStatusResponse to JSON.
         * @function toJSON
         * @memberof pb.GetWithdrawStatusResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetWithdrawStatusResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetWithdrawStatusResponse
         * @function getTypeUrl
         * @memberof pb.GetWithdrawStatusResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetWithdrawStatusResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetWithdrawStatusResponse";
        };

        return GetWithdrawStatusResponse;
    })();

    pb.ListWithdrawsResponse = (function() {

        /**
         * Properties of a ListWithdrawsResponse.
         * @memberof pb
         * @interface IListWithdrawsResponse
         * @property {Array.<pb.IGetWithdrawStatusResponse>|null} [withdraws] ListWithdrawsResponse withdraws
         * @property {number|null} [total] ListWithdrawsResponse total
         */

        /**
         * Constructs a new ListWithdrawsResponse.
         * @memberof pb
         * @classdesc Represents a ListWithdrawsResponse.
         * @implements IListWithdrawsResponse
         * @constructor
         * @param {pb.IListWithdrawsResponse=} [properties] Properties to set
         */
        function ListWithdrawsResponse(properties) {
            this.withdraws = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ListWithdrawsResponse withdraws.
         * @member {Array.<pb.IGetWithdrawStatusResponse>} withdraws
         * @memberof pb.ListWithdrawsResponse
         * @instance
         */
        ListWithdrawsResponse.prototype.withdraws = $util.emptyArray;

        /**
         * ListWithdrawsResponse total.
         * @member {number} total
         * @memberof pb.ListWithdrawsResponse
         * @instance
         */
        ListWithdrawsResponse.prototype.total = 0;

        /**
         * Creates a new ListWithdrawsResponse instance using the specified properties.
         * @function create
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {pb.IListWithdrawsResponse=} [properties] Properties to set
         * @returns {pb.ListWithdrawsResponse} ListWithdrawsResponse instance
         */
        ListWithdrawsResponse.create = function create(properties) {
            return new ListWithdrawsResponse(properties);
        };

        /**
         * Encodes the specified ListWithdrawsResponse message. Does not implicitly {@link pb.ListWithdrawsResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {pb.IListWithdrawsResponse} message ListWithdrawsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListWithdrawsResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.withdraws != null && message.withdraws.length)
                for (let i = 0; i < message.withdraws.length; ++i)
                    $root.pb.GetWithdrawStatusResponse.encode(message.withdraws[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.total != null && Object.hasOwnProperty.call(message, "total"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.total);
            return writer;
        };

        /**
         * Encodes the specified ListWithdrawsResponse message, length delimited. Does not implicitly {@link pb.ListWithdrawsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {pb.IListWithdrawsResponse} message ListWithdrawsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListWithdrawsResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ListWithdrawsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ListWithdrawsResponse} ListWithdrawsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListWithdrawsResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ListWithdrawsResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.withdraws && message.withdraws.length))
                            message.withdraws = [];
                        message.withdraws.push($root.pb.GetWithdrawStatusResponse.decode(reader, reader.uint32()));
                        break;
                    }
                case 2: {
                        message.total = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ListWithdrawsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ListWithdrawsResponse} ListWithdrawsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListWithdrawsResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListWithdrawsResponse message.
         * @function verify
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListWithdrawsResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.withdraws != null && message.hasOwnProperty("withdraws")) {
                if (!Array.isArray(message.withdraws))
                    return "withdraws: array expected";
                for (let i = 0; i < message.withdraws.length; ++i) {
                    let error = $root.pb.GetWithdrawStatusResponse.verify(message.withdraws[i]);
                    if (error)
                        return "withdraws." + error;
                }
            }
            if (message.total != null && message.hasOwnProperty("total"))
                if (!$util.isInteger(message.total))
                    return "total: integer expected";
            return null;
        };

        /**
         * Creates a ListWithdrawsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ListWithdrawsResponse} ListWithdrawsResponse
         */
        ListWithdrawsResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ListWithdrawsResponse)
                return object;
            let message = new $root.pb.ListWithdrawsResponse();
            if (object.withdraws) {
                if (!Array.isArray(object.withdraws))
                    throw TypeError(".pb.ListWithdrawsResponse.withdraws: array expected");
                message.withdraws = [];
                for (let i = 0; i < object.withdraws.length; ++i) {
                    if (typeof object.withdraws[i] !== "object")
                        throw TypeError(".pb.ListWithdrawsResponse.withdraws: object expected");
                    message.withdraws[i] = $root.pb.GetWithdrawStatusResponse.fromObject(object.withdraws[i]);
                }
            }
            if (object.total != null)
                message.total = object.total | 0;
            return message;
        };

        /**
         * Creates a plain object from a ListWithdrawsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {pb.ListWithdrawsResponse} message ListWithdrawsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListWithdrawsResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.withdraws = [];
            if (options.defaults)
                object.total = 0;
            if (message.withdraws && message.withdraws.length) {
                object.withdraws = [];
                for (let j = 0; j < message.withdraws.length; ++j)
                    object.withdraws[j] = $root.pb.GetWithdrawStatusResponse.toObject(message.withdraws[j], options);
            }
            if (message.total != null && message.hasOwnProperty("total"))
                object.total = message.total;
            return object;
        };

        /**
         * Converts this ListWithdrawsResponse to JSON.
         * @function toJSON
         * @memberof pb.ListWithdrawsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListWithdrawsResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ListWithdrawsResponse
         * @function getTypeUrl
         * @memberof pb.ListWithdrawsResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ListWithdrawsResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ListWithdrawsResponse";
        };

        return ListWithdrawsResponse;
    })();

    pb.GetVaultGroupPubKeyResponse = (function() {

        /**
         * Properties of a GetVaultGroupPubKeyResponse.
         * @memberof pb
         * @interface IGetVaultGroupPubKeyResponse
         * @property {string|null} [chain] GetVaultGroupPubKeyResponse chain
         * @property {number|null} [vaultId] GetVaultGroupPubKeyResponse vaultId
         * @property {number|Long|null} [keyEpoch] GetVaultGroupPubKeyResponse keyEpoch
         * @property {string|null} [groupPubkey] GetVaultGroupPubKeyResponse groupPubkey
         * @property {string|null} [signAlgo] GetVaultGroupPubKeyResponse signAlgo
         * @property {string|null} [status] GetVaultGroupPubKeyResponse status
         */

        /**
         * Constructs a new GetVaultGroupPubKeyResponse.
         * @memberof pb
         * @classdesc Represents a GetVaultGroupPubKeyResponse.
         * @implements IGetVaultGroupPubKeyResponse
         * @constructor
         * @param {pb.IGetVaultGroupPubKeyResponse=} [properties] Properties to set
         */
        function GetVaultGroupPubKeyResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetVaultGroupPubKeyResponse chain.
         * @member {string} chain
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @instance
         */
        GetVaultGroupPubKeyResponse.prototype.chain = "";

        /**
         * GetVaultGroupPubKeyResponse vaultId.
         * @member {number} vaultId
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @instance
         */
        GetVaultGroupPubKeyResponse.prototype.vaultId = 0;

        /**
         * GetVaultGroupPubKeyResponse keyEpoch.
         * @member {number|Long} keyEpoch
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @instance
         */
        GetVaultGroupPubKeyResponse.prototype.keyEpoch = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * GetVaultGroupPubKeyResponse groupPubkey.
         * @member {string} groupPubkey
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @instance
         */
        GetVaultGroupPubKeyResponse.prototype.groupPubkey = "";

        /**
         * GetVaultGroupPubKeyResponse signAlgo.
         * @member {string} signAlgo
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @instance
         */
        GetVaultGroupPubKeyResponse.prototype.signAlgo = "";

        /**
         * GetVaultGroupPubKeyResponse status.
         * @member {string} status
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @instance
         */
        GetVaultGroupPubKeyResponse.prototype.status = "";

        /**
         * Creates a new GetVaultGroupPubKeyResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {pb.IGetVaultGroupPubKeyResponse=} [properties] Properties to set
         * @returns {pb.GetVaultGroupPubKeyResponse} GetVaultGroupPubKeyResponse instance
         */
        GetVaultGroupPubKeyResponse.create = function create(properties) {
            return new GetVaultGroupPubKeyResponse(properties);
        };

        /**
         * Encodes the specified GetVaultGroupPubKeyResponse message. Does not implicitly {@link pb.GetVaultGroupPubKeyResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {pb.IGetVaultGroupPubKeyResponse} message GetVaultGroupPubKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVaultGroupPubKeyResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vaultId);
            if (message.keyEpoch != null && Object.hasOwnProperty.call(message, "keyEpoch"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.keyEpoch);
            if (message.groupPubkey != null && Object.hasOwnProperty.call(message, "groupPubkey"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.groupPubkey);
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.signAlgo);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.status);
            return writer;
        };

        /**
         * Encodes the specified GetVaultGroupPubKeyResponse message, length delimited. Does not implicitly {@link pb.GetVaultGroupPubKeyResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {pb.IGetVaultGroupPubKeyResponse} message GetVaultGroupPubKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVaultGroupPubKeyResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetVaultGroupPubKeyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetVaultGroupPubKeyResponse} GetVaultGroupPubKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVaultGroupPubKeyResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetVaultGroupPubKeyResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.keyEpoch = reader.uint64();
                        break;
                    }
                case 4: {
                        message.groupPubkey = reader.string();
                        break;
                    }
                case 5: {
                        message.signAlgo = reader.string();
                        break;
                    }
                case 6: {
                        message.status = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetVaultGroupPubKeyResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetVaultGroupPubKeyResponse} GetVaultGroupPubKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVaultGroupPubKeyResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetVaultGroupPubKeyResponse message.
         * @function verify
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetVaultGroupPubKeyResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.keyEpoch != null && message.hasOwnProperty("keyEpoch"))
                if (!$util.isInteger(message.keyEpoch) && !(message.keyEpoch && $util.isInteger(message.keyEpoch.low) && $util.isInteger(message.keyEpoch.high)))
                    return "keyEpoch: integer|Long expected";
            if (message.groupPubkey != null && message.hasOwnProperty("groupPubkey"))
                if (!$util.isString(message.groupPubkey))
                    return "groupPubkey: string expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                if (!$util.isString(message.signAlgo))
                    return "signAlgo: string expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            return null;
        };

        /**
         * Creates a GetVaultGroupPubKeyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetVaultGroupPubKeyResponse} GetVaultGroupPubKeyResponse
         */
        GetVaultGroupPubKeyResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetVaultGroupPubKeyResponse)
                return object;
            let message = new $root.pb.GetVaultGroupPubKeyResponse();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.keyEpoch != null)
                if ($util.Long)
                    (message.keyEpoch = $util.Long.fromValue(object.keyEpoch)).unsigned = true;
                else if (typeof object.keyEpoch === "string")
                    message.keyEpoch = parseInt(object.keyEpoch, 10);
                else if (typeof object.keyEpoch === "number")
                    message.keyEpoch = object.keyEpoch;
                else if (typeof object.keyEpoch === "object")
                    message.keyEpoch = new $util.LongBits(object.keyEpoch.low >>> 0, object.keyEpoch.high >>> 0).toNumber(true);
            if (object.groupPubkey != null)
                message.groupPubkey = String(object.groupPubkey);
            if (object.signAlgo != null)
                message.signAlgo = String(object.signAlgo);
            if (object.status != null)
                message.status = String(object.status);
            return message;
        };

        /**
         * Creates a plain object from a GetVaultGroupPubKeyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {pb.GetVaultGroupPubKeyResponse} message GetVaultGroupPubKeyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetVaultGroupPubKeyResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.keyEpoch = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.keyEpoch = options.longs === String ? "0" : 0;
                object.groupPubkey = "";
                object.signAlgo = "";
                object.status = "";
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.keyEpoch != null && message.hasOwnProperty("keyEpoch"))
                if (typeof message.keyEpoch === "number")
                    object.keyEpoch = options.longs === String ? String(message.keyEpoch) : message.keyEpoch;
                else
                    object.keyEpoch = options.longs === String ? $util.Long.prototype.toString.call(message.keyEpoch) : options.longs === Number ? new $util.LongBits(message.keyEpoch.low >>> 0, message.keyEpoch.high >>> 0).toNumber(true) : message.keyEpoch;
            if (message.groupPubkey != null && message.hasOwnProperty("groupPubkey"))
                object.groupPubkey = message.groupPubkey;
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = message.signAlgo;
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this GetVaultGroupPubKeyResponse to JSON.
         * @function toJSON
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetVaultGroupPubKeyResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetVaultGroupPubKeyResponse
         * @function getTypeUrl
         * @memberof pb.GetVaultGroupPubKeyResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetVaultGroupPubKeyResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetVaultGroupPubKeyResponse";
        };

        return GetVaultGroupPubKeyResponse;
    })();

    pb.GetVaultTransitionStatusResponse = (function() {

        /**
         * Properties of a GetVaultTransitionStatusResponse.
         * @memberof pb
         * @interface IGetVaultTransitionStatusResponse
         * @property {string|null} [chain] GetVaultTransitionStatusResponse chain
         * @property {number|null} [vaultId] GetVaultTransitionStatusResponse vaultId
         * @property {number|Long|null} [epochId] GetVaultTransitionStatusResponse epochId
         * @property {string|null} [signAlgo] GetVaultTransitionStatusResponse signAlgo
         * @property {number|Long|null} [triggerHeight] GetVaultTransitionStatusResponse triggerHeight
         * @property {Array.<string>|null} [oldCommitteeMembers] GetVaultTransitionStatusResponse oldCommitteeMembers
         * @property {Array.<string>|null} [newCommitteeMembers] GetVaultTransitionStatusResponse newCommitteeMembers
         * @property {string|null} [dkgStatus] GetVaultTransitionStatusResponse dkgStatus
         * @property {string|null} [dkgSessionId] GetVaultTransitionStatusResponse dkgSessionId
         * @property {number|null} [dkgThresholdT] GetVaultTransitionStatusResponse dkgThresholdT
         * @property {number|null} [dkgN] GetVaultTransitionStatusResponse dkgN
         * @property {number|Long|null} [dkgCommitDeadline] GetVaultTransitionStatusResponse dkgCommitDeadline
         * @property {number|Long|null} [dkgDisputeDeadline] GetVaultTransitionStatusResponse dkgDisputeDeadline
         * @property {string|null} [oldGroupPubkey] GetVaultTransitionStatusResponse oldGroupPubkey
         * @property {string|null} [newGroupPubkey] GetVaultTransitionStatusResponse newGroupPubkey
         * @property {string|null} [validationStatus] GetVaultTransitionStatusResponse validationStatus
         * @property {string|null} [lifecycle] GetVaultTransitionStatusResponse lifecycle
         */

        /**
         * Constructs a new GetVaultTransitionStatusResponse.
         * @memberof pb
         * @classdesc Represents a GetVaultTransitionStatusResponse.
         * @implements IGetVaultTransitionStatusResponse
         * @constructor
         * @param {pb.IGetVaultTransitionStatusResponse=} [properties] Properties to set
         */
        function GetVaultTransitionStatusResponse(properties) {
            this.oldCommitteeMembers = [];
            this.newCommitteeMembers = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetVaultTransitionStatusResponse chain.
         * @member {string} chain
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.chain = "";

        /**
         * GetVaultTransitionStatusResponse vaultId.
         * @member {number} vaultId
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.vaultId = 0;

        /**
         * GetVaultTransitionStatusResponse epochId.
         * @member {number|Long} epochId
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * GetVaultTransitionStatusResponse signAlgo.
         * @member {string} signAlgo
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.signAlgo = "";

        /**
         * GetVaultTransitionStatusResponse triggerHeight.
         * @member {number|Long} triggerHeight
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.triggerHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * GetVaultTransitionStatusResponse oldCommitteeMembers.
         * @member {Array.<string>} oldCommitteeMembers
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.oldCommitteeMembers = $util.emptyArray;

        /**
         * GetVaultTransitionStatusResponse newCommitteeMembers.
         * @member {Array.<string>} newCommitteeMembers
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.newCommitteeMembers = $util.emptyArray;

        /**
         * GetVaultTransitionStatusResponse dkgStatus.
         * @member {string} dkgStatus
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.dkgStatus = "";

        /**
         * GetVaultTransitionStatusResponse dkgSessionId.
         * @member {string} dkgSessionId
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.dkgSessionId = "";

        /**
         * GetVaultTransitionStatusResponse dkgThresholdT.
         * @member {number} dkgThresholdT
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.dkgThresholdT = 0;

        /**
         * GetVaultTransitionStatusResponse dkgN.
         * @member {number} dkgN
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.dkgN = 0;

        /**
         * GetVaultTransitionStatusResponse dkgCommitDeadline.
         * @member {number|Long} dkgCommitDeadline
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.dkgCommitDeadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * GetVaultTransitionStatusResponse dkgDisputeDeadline.
         * @member {number|Long} dkgDisputeDeadline
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.dkgDisputeDeadline = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * GetVaultTransitionStatusResponse oldGroupPubkey.
         * @member {string} oldGroupPubkey
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.oldGroupPubkey = "";

        /**
         * GetVaultTransitionStatusResponse newGroupPubkey.
         * @member {string} newGroupPubkey
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.newGroupPubkey = "";

        /**
         * GetVaultTransitionStatusResponse validationStatus.
         * @member {string} validationStatus
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.validationStatus = "";

        /**
         * GetVaultTransitionStatusResponse lifecycle.
         * @member {string} lifecycle
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         */
        GetVaultTransitionStatusResponse.prototype.lifecycle = "";

        /**
         * Creates a new GetVaultTransitionStatusResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {pb.IGetVaultTransitionStatusResponse=} [properties] Properties to set
         * @returns {pb.GetVaultTransitionStatusResponse} GetVaultTransitionStatusResponse instance
         */
        GetVaultTransitionStatusResponse.create = function create(properties) {
            return new GetVaultTransitionStatusResponse(properties);
        };

        /**
         * Encodes the specified GetVaultTransitionStatusResponse message. Does not implicitly {@link pb.GetVaultTransitionStatusResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {pb.IGetVaultTransitionStatusResponse} message GetVaultTransitionStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVaultTransitionStatusResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.epochId);
            if (message.signAlgo != null && Object.hasOwnProperty.call(message, "signAlgo"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.signAlgo);
            if (message.triggerHeight != null && Object.hasOwnProperty.call(message, "triggerHeight"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.triggerHeight);
            if (message.oldCommitteeMembers != null && message.oldCommitteeMembers.length)
                for (let i = 0; i < message.oldCommitteeMembers.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.oldCommitteeMembers[i]);
            if (message.newCommitteeMembers != null && message.newCommitteeMembers.length)
                for (let i = 0; i < message.newCommitteeMembers.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.newCommitteeMembers[i]);
            if (message.dkgStatus != null && Object.hasOwnProperty.call(message, "dkgStatus"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.dkgStatus);
            if (message.dkgSessionId != null && Object.hasOwnProperty.call(message, "dkgSessionId"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.dkgSessionId);
            if (message.dkgThresholdT != null && Object.hasOwnProperty.call(message, "dkgThresholdT"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.dkgThresholdT);
            if (message.dkgN != null && Object.hasOwnProperty.call(message, "dkgN"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.dkgN);
            if (message.dkgCommitDeadline != null && Object.hasOwnProperty.call(message, "dkgCommitDeadline"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.dkgCommitDeadline);
            if (message.dkgDisputeDeadline != null && Object.hasOwnProperty.call(message, "dkgDisputeDeadline"))
                writer.uint32(/* id 13, wireType 0 =*/104).uint64(message.dkgDisputeDeadline);
            if (message.oldGroupPubkey != null && Object.hasOwnProperty.call(message, "oldGroupPubkey"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.oldGroupPubkey);
            if (message.newGroupPubkey != null && Object.hasOwnProperty.call(message, "newGroupPubkey"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.newGroupPubkey);
            if (message.validationStatus != null && Object.hasOwnProperty.call(message, "validationStatus"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.validationStatus);
            if (message.lifecycle != null && Object.hasOwnProperty.call(message, "lifecycle"))
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.lifecycle);
            return writer;
        };

        /**
         * Encodes the specified GetVaultTransitionStatusResponse message, length delimited. Does not implicitly {@link pb.GetVaultTransitionStatusResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {pb.IGetVaultTransitionStatusResponse} message GetVaultTransitionStatusResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVaultTransitionStatusResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetVaultTransitionStatusResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetVaultTransitionStatusResponse} GetVaultTransitionStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVaultTransitionStatusResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetVaultTransitionStatusResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 4: {
                        message.signAlgo = reader.string();
                        break;
                    }
                case 5: {
                        message.triggerHeight = reader.uint64();
                        break;
                    }
                case 6: {
                        if (!(message.oldCommitteeMembers && message.oldCommitteeMembers.length))
                            message.oldCommitteeMembers = [];
                        message.oldCommitteeMembers.push(reader.string());
                        break;
                    }
                case 7: {
                        if (!(message.newCommitteeMembers && message.newCommitteeMembers.length))
                            message.newCommitteeMembers = [];
                        message.newCommitteeMembers.push(reader.string());
                        break;
                    }
                case 8: {
                        message.dkgStatus = reader.string();
                        break;
                    }
                case 9: {
                        message.dkgSessionId = reader.string();
                        break;
                    }
                case 10: {
                        message.dkgThresholdT = reader.uint32();
                        break;
                    }
                case 11: {
                        message.dkgN = reader.uint32();
                        break;
                    }
                case 12: {
                        message.dkgCommitDeadline = reader.uint64();
                        break;
                    }
                case 13: {
                        message.dkgDisputeDeadline = reader.uint64();
                        break;
                    }
                case 14: {
                        message.oldGroupPubkey = reader.string();
                        break;
                    }
                case 15: {
                        message.newGroupPubkey = reader.string();
                        break;
                    }
                case 16: {
                        message.validationStatus = reader.string();
                        break;
                    }
                case 17: {
                        message.lifecycle = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetVaultTransitionStatusResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetVaultTransitionStatusResponse} GetVaultTransitionStatusResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVaultTransitionStatusResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetVaultTransitionStatusResponse message.
         * @function verify
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetVaultTransitionStatusResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                if (!$util.isString(message.signAlgo))
                    return "signAlgo: string expected";
            if (message.triggerHeight != null && message.hasOwnProperty("triggerHeight"))
                if (!$util.isInteger(message.triggerHeight) && !(message.triggerHeight && $util.isInteger(message.triggerHeight.low) && $util.isInteger(message.triggerHeight.high)))
                    return "triggerHeight: integer|Long expected";
            if (message.oldCommitteeMembers != null && message.hasOwnProperty("oldCommitteeMembers")) {
                if (!Array.isArray(message.oldCommitteeMembers))
                    return "oldCommitteeMembers: array expected";
                for (let i = 0; i < message.oldCommitteeMembers.length; ++i)
                    if (!$util.isString(message.oldCommitteeMembers[i]))
                        return "oldCommitteeMembers: string[] expected";
            }
            if (message.newCommitteeMembers != null && message.hasOwnProperty("newCommitteeMembers")) {
                if (!Array.isArray(message.newCommitteeMembers))
                    return "newCommitteeMembers: array expected";
                for (let i = 0; i < message.newCommitteeMembers.length; ++i)
                    if (!$util.isString(message.newCommitteeMembers[i]))
                        return "newCommitteeMembers: string[] expected";
            }
            if (message.dkgStatus != null && message.hasOwnProperty("dkgStatus"))
                if (!$util.isString(message.dkgStatus))
                    return "dkgStatus: string expected";
            if (message.dkgSessionId != null && message.hasOwnProperty("dkgSessionId"))
                if (!$util.isString(message.dkgSessionId))
                    return "dkgSessionId: string expected";
            if (message.dkgThresholdT != null && message.hasOwnProperty("dkgThresholdT"))
                if (!$util.isInteger(message.dkgThresholdT))
                    return "dkgThresholdT: integer expected";
            if (message.dkgN != null && message.hasOwnProperty("dkgN"))
                if (!$util.isInteger(message.dkgN))
                    return "dkgN: integer expected";
            if (message.dkgCommitDeadline != null && message.hasOwnProperty("dkgCommitDeadline"))
                if (!$util.isInteger(message.dkgCommitDeadline) && !(message.dkgCommitDeadline && $util.isInteger(message.dkgCommitDeadline.low) && $util.isInteger(message.dkgCommitDeadline.high)))
                    return "dkgCommitDeadline: integer|Long expected";
            if (message.dkgDisputeDeadline != null && message.hasOwnProperty("dkgDisputeDeadline"))
                if (!$util.isInteger(message.dkgDisputeDeadline) && !(message.dkgDisputeDeadline && $util.isInteger(message.dkgDisputeDeadline.low) && $util.isInteger(message.dkgDisputeDeadline.high)))
                    return "dkgDisputeDeadline: integer|Long expected";
            if (message.oldGroupPubkey != null && message.hasOwnProperty("oldGroupPubkey"))
                if (!$util.isString(message.oldGroupPubkey))
                    return "oldGroupPubkey: string expected";
            if (message.newGroupPubkey != null && message.hasOwnProperty("newGroupPubkey"))
                if (!$util.isString(message.newGroupPubkey))
                    return "newGroupPubkey: string expected";
            if (message.validationStatus != null && message.hasOwnProperty("validationStatus"))
                if (!$util.isString(message.validationStatus))
                    return "validationStatus: string expected";
            if (message.lifecycle != null && message.hasOwnProperty("lifecycle"))
                if (!$util.isString(message.lifecycle))
                    return "lifecycle: string expected";
            return null;
        };

        /**
         * Creates a GetVaultTransitionStatusResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetVaultTransitionStatusResponse} GetVaultTransitionStatusResponse
         */
        GetVaultTransitionStatusResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetVaultTransitionStatusResponse)
                return object;
            let message = new $root.pb.GetVaultTransitionStatusResponse();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.signAlgo != null)
                message.signAlgo = String(object.signAlgo);
            if (object.triggerHeight != null)
                if ($util.Long)
                    (message.triggerHeight = $util.Long.fromValue(object.triggerHeight)).unsigned = true;
                else if (typeof object.triggerHeight === "string")
                    message.triggerHeight = parseInt(object.triggerHeight, 10);
                else if (typeof object.triggerHeight === "number")
                    message.triggerHeight = object.triggerHeight;
                else if (typeof object.triggerHeight === "object")
                    message.triggerHeight = new $util.LongBits(object.triggerHeight.low >>> 0, object.triggerHeight.high >>> 0).toNumber(true);
            if (object.oldCommitteeMembers) {
                if (!Array.isArray(object.oldCommitteeMembers))
                    throw TypeError(".pb.GetVaultTransitionStatusResponse.oldCommitteeMembers: array expected");
                message.oldCommitteeMembers = [];
                for (let i = 0; i < object.oldCommitteeMembers.length; ++i)
                    message.oldCommitteeMembers[i] = String(object.oldCommitteeMembers[i]);
            }
            if (object.newCommitteeMembers) {
                if (!Array.isArray(object.newCommitteeMembers))
                    throw TypeError(".pb.GetVaultTransitionStatusResponse.newCommitteeMembers: array expected");
                message.newCommitteeMembers = [];
                for (let i = 0; i < object.newCommitteeMembers.length; ++i)
                    message.newCommitteeMembers[i] = String(object.newCommitteeMembers[i]);
            }
            if (object.dkgStatus != null)
                message.dkgStatus = String(object.dkgStatus);
            if (object.dkgSessionId != null)
                message.dkgSessionId = String(object.dkgSessionId);
            if (object.dkgThresholdT != null)
                message.dkgThresholdT = object.dkgThresholdT >>> 0;
            if (object.dkgN != null)
                message.dkgN = object.dkgN >>> 0;
            if (object.dkgCommitDeadline != null)
                if ($util.Long)
                    (message.dkgCommitDeadline = $util.Long.fromValue(object.dkgCommitDeadline)).unsigned = true;
                else if (typeof object.dkgCommitDeadline === "string")
                    message.dkgCommitDeadline = parseInt(object.dkgCommitDeadline, 10);
                else if (typeof object.dkgCommitDeadline === "number")
                    message.dkgCommitDeadline = object.dkgCommitDeadline;
                else if (typeof object.dkgCommitDeadline === "object")
                    message.dkgCommitDeadline = new $util.LongBits(object.dkgCommitDeadline.low >>> 0, object.dkgCommitDeadline.high >>> 0).toNumber(true);
            if (object.dkgDisputeDeadline != null)
                if ($util.Long)
                    (message.dkgDisputeDeadline = $util.Long.fromValue(object.dkgDisputeDeadline)).unsigned = true;
                else if (typeof object.dkgDisputeDeadline === "string")
                    message.dkgDisputeDeadline = parseInt(object.dkgDisputeDeadline, 10);
                else if (typeof object.dkgDisputeDeadline === "number")
                    message.dkgDisputeDeadline = object.dkgDisputeDeadline;
                else if (typeof object.dkgDisputeDeadline === "object")
                    message.dkgDisputeDeadline = new $util.LongBits(object.dkgDisputeDeadline.low >>> 0, object.dkgDisputeDeadline.high >>> 0).toNumber(true);
            if (object.oldGroupPubkey != null)
                message.oldGroupPubkey = String(object.oldGroupPubkey);
            if (object.newGroupPubkey != null)
                message.newGroupPubkey = String(object.newGroupPubkey);
            if (object.validationStatus != null)
                message.validationStatus = String(object.validationStatus);
            if (object.lifecycle != null)
                message.lifecycle = String(object.lifecycle);
            return message;
        };

        /**
         * Creates a plain object from a GetVaultTransitionStatusResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {pb.GetVaultTransitionStatusResponse} message GetVaultTransitionStatusResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetVaultTransitionStatusResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.oldCommitteeMembers = [];
                object.newCommitteeMembers = [];
            }
            if (options.defaults) {
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.signAlgo = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.triggerHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.triggerHeight = options.longs === String ? "0" : 0;
                object.dkgStatus = "";
                object.dkgSessionId = "";
                object.dkgThresholdT = 0;
                object.dkgN = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.dkgCommitDeadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.dkgCommitDeadline = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.dkgDisputeDeadline = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.dkgDisputeDeadline = options.longs === String ? "0" : 0;
                object.oldGroupPubkey = "";
                object.newGroupPubkey = "";
                object.validationStatus = "";
                object.lifecycle = "";
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.signAlgo != null && message.hasOwnProperty("signAlgo"))
                object.signAlgo = message.signAlgo;
            if (message.triggerHeight != null && message.hasOwnProperty("triggerHeight"))
                if (typeof message.triggerHeight === "number")
                    object.triggerHeight = options.longs === String ? String(message.triggerHeight) : message.triggerHeight;
                else
                    object.triggerHeight = options.longs === String ? $util.Long.prototype.toString.call(message.triggerHeight) : options.longs === Number ? new $util.LongBits(message.triggerHeight.low >>> 0, message.triggerHeight.high >>> 0).toNumber(true) : message.triggerHeight;
            if (message.oldCommitteeMembers && message.oldCommitteeMembers.length) {
                object.oldCommitteeMembers = [];
                for (let j = 0; j < message.oldCommitteeMembers.length; ++j)
                    object.oldCommitteeMembers[j] = message.oldCommitteeMembers[j];
            }
            if (message.newCommitteeMembers && message.newCommitteeMembers.length) {
                object.newCommitteeMembers = [];
                for (let j = 0; j < message.newCommitteeMembers.length; ++j)
                    object.newCommitteeMembers[j] = message.newCommitteeMembers[j];
            }
            if (message.dkgStatus != null && message.hasOwnProperty("dkgStatus"))
                object.dkgStatus = message.dkgStatus;
            if (message.dkgSessionId != null && message.hasOwnProperty("dkgSessionId"))
                object.dkgSessionId = message.dkgSessionId;
            if (message.dkgThresholdT != null && message.hasOwnProperty("dkgThresholdT"))
                object.dkgThresholdT = message.dkgThresholdT;
            if (message.dkgN != null && message.hasOwnProperty("dkgN"))
                object.dkgN = message.dkgN;
            if (message.dkgCommitDeadline != null && message.hasOwnProperty("dkgCommitDeadline"))
                if (typeof message.dkgCommitDeadline === "number")
                    object.dkgCommitDeadline = options.longs === String ? String(message.dkgCommitDeadline) : message.dkgCommitDeadline;
                else
                    object.dkgCommitDeadline = options.longs === String ? $util.Long.prototype.toString.call(message.dkgCommitDeadline) : options.longs === Number ? new $util.LongBits(message.dkgCommitDeadline.low >>> 0, message.dkgCommitDeadline.high >>> 0).toNumber(true) : message.dkgCommitDeadline;
            if (message.dkgDisputeDeadline != null && message.hasOwnProperty("dkgDisputeDeadline"))
                if (typeof message.dkgDisputeDeadline === "number")
                    object.dkgDisputeDeadline = options.longs === String ? String(message.dkgDisputeDeadline) : message.dkgDisputeDeadline;
                else
                    object.dkgDisputeDeadline = options.longs === String ? $util.Long.prototype.toString.call(message.dkgDisputeDeadline) : options.longs === Number ? new $util.LongBits(message.dkgDisputeDeadline.low >>> 0, message.dkgDisputeDeadline.high >>> 0).toNumber(true) : message.dkgDisputeDeadline;
            if (message.oldGroupPubkey != null && message.hasOwnProperty("oldGroupPubkey"))
                object.oldGroupPubkey = message.oldGroupPubkey;
            if (message.newGroupPubkey != null && message.hasOwnProperty("newGroupPubkey"))
                object.newGroupPubkey = message.newGroupPubkey;
            if (message.validationStatus != null && message.hasOwnProperty("validationStatus"))
                object.validationStatus = message.validationStatus;
            if (message.lifecycle != null && message.hasOwnProperty("lifecycle"))
                object.lifecycle = message.lifecycle;
            return object;
        };

        /**
         * Converts this GetVaultTransitionStatusResponse to JSON.
         * @function toJSON
         * @memberof pb.GetVaultTransitionStatusResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetVaultTransitionStatusResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetVaultTransitionStatusResponse
         * @function getTypeUrl
         * @memberof pb.GetVaultTransitionStatusResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetVaultTransitionStatusResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetVaultTransitionStatusResponse";
        };

        return GetVaultTransitionStatusResponse;
    })();

    pb.DkgCommitmentInfo = (function() {

        /**
         * Properties of a DkgCommitmentInfo.
         * @memberof pb
         * @interface IDkgCommitmentInfo
         * @property {string|null} [participant] DkgCommitmentInfo participant
         * @property {Array.<string>|null} [commitmentPoints] DkgCommitmentInfo commitmentPoints
         * @property {number|Long|null} [commitHeight] DkgCommitmentInfo commitHeight
         */

        /**
         * Constructs a new DkgCommitmentInfo.
         * @memberof pb
         * @classdesc Represents a DkgCommitmentInfo.
         * @implements IDkgCommitmentInfo
         * @constructor
         * @param {pb.IDkgCommitmentInfo=} [properties] Properties to set
         */
        function DkgCommitmentInfo(properties) {
            this.commitmentPoints = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DkgCommitmentInfo participant.
         * @member {string} participant
         * @memberof pb.DkgCommitmentInfo
         * @instance
         */
        DkgCommitmentInfo.prototype.participant = "";

        /**
         * DkgCommitmentInfo commitmentPoints.
         * @member {Array.<string>} commitmentPoints
         * @memberof pb.DkgCommitmentInfo
         * @instance
         */
        DkgCommitmentInfo.prototype.commitmentPoints = $util.emptyArray;

        /**
         * DkgCommitmentInfo commitHeight.
         * @member {number|Long} commitHeight
         * @memberof pb.DkgCommitmentInfo
         * @instance
         */
        DkgCommitmentInfo.prototype.commitHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new DkgCommitmentInfo instance using the specified properties.
         * @function create
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {pb.IDkgCommitmentInfo=} [properties] Properties to set
         * @returns {pb.DkgCommitmentInfo} DkgCommitmentInfo instance
         */
        DkgCommitmentInfo.create = function create(properties) {
            return new DkgCommitmentInfo(properties);
        };

        /**
         * Encodes the specified DkgCommitmentInfo message. Does not implicitly {@link pb.DkgCommitmentInfo.verify|verify} messages.
         * @function encode
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {pb.IDkgCommitmentInfo} message DkgCommitmentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DkgCommitmentInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.participant);
            if (message.commitmentPoints != null && message.commitmentPoints.length)
                for (let i = 0; i < message.commitmentPoints.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.commitmentPoints[i]);
            if (message.commitHeight != null && Object.hasOwnProperty.call(message, "commitHeight"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.commitHeight);
            return writer;
        };

        /**
         * Encodes the specified DkgCommitmentInfo message, length delimited. Does not implicitly {@link pb.DkgCommitmentInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {pb.IDkgCommitmentInfo} message DkgCommitmentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DkgCommitmentInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DkgCommitmentInfo message from the specified reader or buffer.
         * @function decode
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.DkgCommitmentInfo} DkgCommitmentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DkgCommitmentInfo.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.DkgCommitmentInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.participant = reader.string();
                        break;
                    }
                case 2: {
                        if (!(message.commitmentPoints && message.commitmentPoints.length))
                            message.commitmentPoints = [];
                        message.commitmentPoints.push(reader.string());
                        break;
                    }
                case 3: {
                        message.commitHeight = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DkgCommitmentInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.DkgCommitmentInfo} DkgCommitmentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DkgCommitmentInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DkgCommitmentInfo message.
         * @function verify
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DkgCommitmentInfo.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.participant != null && message.hasOwnProperty("participant"))
                if (!$util.isString(message.participant))
                    return "participant: string expected";
            if (message.commitmentPoints != null && message.hasOwnProperty("commitmentPoints")) {
                if (!Array.isArray(message.commitmentPoints))
                    return "commitmentPoints: array expected";
                for (let i = 0; i < message.commitmentPoints.length; ++i)
                    if (!$util.isString(message.commitmentPoints[i]))
                        return "commitmentPoints: string[] expected";
            }
            if (message.commitHeight != null && message.hasOwnProperty("commitHeight"))
                if (!$util.isInteger(message.commitHeight) && !(message.commitHeight && $util.isInteger(message.commitHeight.low) && $util.isInteger(message.commitHeight.high)))
                    return "commitHeight: integer|Long expected";
            return null;
        };

        /**
         * Creates a DkgCommitmentInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.DkgCommitmentInfo} DkgCommitmentInfo
         */
        DkgCommitmentInfo.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.DkgCommitmentInfo)
                return object;
            let message = new $root.pb.DkgCommitmentInfo();
            if (object.participant != null)
                message.participant = String(object.participant);
            if (object.commitmentPoints) {
                if (!Array.isArray(object.commitmentPoints))
                    throw TypeError(".pb.DkgCommitmentInfo.commitmentPoints: array expected");
                message.commitmentPoints = [];
                for (let i = 0; i < object.commitmentPoints.length; ++i)
                    message.commitmentPoints[i] = String(object.commitmentPoints[i]);
            }
            if (object.commitHeight != null)
                if ($util.Long)
                    (message.commitHeight = $util.Long.fromValue(object.commitHeight)).unsigned = true;
                else if (typeof object.commitHeight === "string")
                    message.commitHeight = parseInt(object.commitHeight, 10);
                else if (typeof object.commitHeight === "number")
                    message.commitHeight = object.commitHeight;
                else if (typeof object.commitHeight === "object")
                    message.commitHeight = new $util.LongBits(object.commitHeight.low >>> 0, object.commitHeight.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a DkgCommitmentInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {pb.DkgCommitmentInfo} message DkgCommitmentInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DkgCommitmentInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.commitmentPoints = [];
            if (options.defaults) {
                object.participant = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.commitHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.commitHeight = options.longs === String ? "0" : 0;
            }
            if (message.participant != null && message.hasOwnProperty("participant"))
                object.participant = message.participant;
            if (message.commitmentPoints && message.commitmentPoints.length) {
                object.commitmentPoints = [];
                for (let j = 0; j < message.commitmentPoints.length; ++j)
                    object.commitmentPoints[j] = message.commitmentPoints[j];
            }
            if (message.commitHeight != null && message.hasOwnProperty("commitHeight"))
                if (typeof message.commitHeight === "number")
                    object.commitHeight = options.longs === String ? String(message.commitHeight) : message.commitHeight;
                else
                    object.commitHeight = options.longs === String ? $util.Long.prototype.toString.call(message.commitHeight) : options.longs === Number ? new $util.LongBits(message.commitHeight.low >>> 0, message.commitHeight.high >>> 0).toNumber(true) : message.commitHeight;
            return object;
        };

        /**
         * Converts this DkgCommitmentInfo to JSON.
         * @function toJSON
         * @memberof pb.DkgCommitmentInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DkgCommitmentInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DkgCommitmentInfo
         * @function getTypeUrl
         * @memberof pb.DkgCommitmentInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DkgCommitmentInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.DkgCommitmentInfo";
        };

        return DkgCommitmentInfo;
    })();

    pb.GetVaultDkgCommitmentsResponse = (function() {

        /**
         * Properties of a GetVaultDkgCommitmentsResponse.
         * @memberof pb
         * @interface IGetVaultDkgCommitmentsResponse
         * @property {string|null} [chain] GetVaultDkgCommitmentsResponse chain
         * @property {number|null} [vaultId] GetVaultDkgCommitmentsResponse vaultId
         * @property {number|Long|null} [epochId] GetVaultDkgCommitmentsResponse epochId
         * @property {Array.<pb.IDkgCommitmentInfo>|null} [commitments] GetVaultDkgCommitmentsResponse commitments
         * @property {number|null} [total] GetVaultDkgCommitmentsResponse total
         */

        /**
         * Constructs a new GetVaultDkgCommitmentsResponse.
         * @memberof pb
         * @classdesc Represents a GetVaultDkgCommitmentsResponse.
         * @implements IGetVaultDkgCommitmentsResponse
         * @constructor
         * @param {pb.IGetVaultDkgCommitmentsResponse=} [properties] Properties to set
         */
        function GetVaultDkgCommitmentsResponse(properties) {
            this.commitments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetVaultDkgCommitmentsResponse chain.
         * @member {string} chain
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @instance
         */
        GetVaultDkgCommitmentsResponse.prototype.chain = "";

        /**
         * GetVaultDkgCommitmentsResponse vaultId.
         * @member {number} vaultId
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @instance
         */
        GetVaultDkgCommitmentsResponse.prototype.vaultId = 0;

        /**
         * GetVaultDkgCommitmentsResponse epochId.
         * @member {number|Long} epochId
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @instance
         */
        GetVaultDkgCommitmentsResponse.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * GetVaultDkgCommitmentsResponse commitments.
         * @member {Array.<pb.IDkgCommitmentInfo>} commitments
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @instance
         */
        GetVaultDkgCommitmentsResponse.prototype.commitments = $util.emptyArray;

        /**
         * GetVaultDkgCommitmentsResponse total.
         * @member {number} total
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @instance
         */
        GetVaultDkgCommitmentsResponse.prototype.total = 0;

        /**
         * Creates a new GetVaultDkgCommitmentsResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {pb.IGetVaultDkgCommitmentsResponse=} [properties] Properties to set
         * @returns {pb.GetVaultDkgCommitmentsResponse} GetVaultDkgCommitmentsResponse instance
         */
        GetVaultDkgCommitmentsResponse.create = function create(properties) {
            return new GetVaultDkgCommitmentsResponse(properties);
        };

        /**
         * Encodes the specified GetVaultDkgCommitmentsResponse message. Does not implicitly {@link pb.GetVaultDkgCommitmentsResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {pb.IGetVaultDkgCommitmentsResponse} message GetVaultDkgCommitmentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVaultDkgCommitmentsResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.vaultId != null && Object.hasOwnProperty.call(message, "vaultId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.vaultId);
            if (message.epochId != null && Object.hasOwnProperty.call(message, "epochId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.epochId);
            if (message.commitments != null && message.commitments.length)
                for (let i = 0; i < message.commitments.length; ++i)
                    $root.pb.DkgCommitmentInfo.encode(message.commitments[i], writer.uint32(/* id 4, wireType 2 =*/34).fork()).ldelim();
            if (message.total != null && Object.hasOwnProperty.call(message, "total"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.total);
            return writer;
        };

        /**
         * Encodes the specified GetVaultDkgCommitmentsResponse message, length delimited. Does not implicitly {@link pb.GetVaultDkgCommitmentsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {pb.IGetVaultDkgCommitmentsResponse} message GetVaultDkgCommitmentsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVaultDkgCommitmentsResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetVaultDkgCommitmentsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetVaultDkgCommitmentsResponse} GetVaultDkgCommitmentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVaultDkgCommitmentsResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetVaultDkgCommitmentsResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.vaultId = reader.uint32();
                        break;
                    }
                case 3: {
                        message.epochId = reader.uint64();
                        break;
                    }
                case 4: {
                        if (!(message.commitments && message.commitments.length))
                            message.commitments = [];
                        message.commitments.push($root.pb.DkgCommitmentInfo.decode(reader, reader.uint32()));
                        break;
                    }
                case 5: {
                        message.total = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetVaultDkgCommitmentsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetVaultDkgCommitmentsResponse} GetVaultDkgCommitmentsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVaultDkgCommitmentsResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetVaultDkgCommitmentsResponse message.
         * @function verify
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetVaultDkgCommitmentsResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                if (!$util.isInteger(message.vaultId))
                    return "vaultId: integer expected";
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (!$util.isInteger(message.epochId) && !(message.epochId && $util.isInteger(message.epochId.low) && $util.isInteger(message.epochId.high)))
                    return "epochId: integer|Long expected";
            if (message.commitments != null && message.hasOwnProperty("commitments")) {
                if (!Array.isArray(message.commitments))
                    return "commitments: array expected";
                for (let i = 0; i < message.commitments.length; ++i) {
                    let error = $root.pb.DkgCommitmentInfo.verify(message.commitments[i]);
                    if (error)
                        return "commitments." + error;
                }
            }
            if (message.total != null && message.hasOwnProperty("total"))
                if (!$util.isInteger(message.total))
                    return "total: integer expected";
            return null;
        };

        /**
         * Creates a GetVaultDkgCommitmentsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetVaultDkgCommitmentsResponse} GetVaultDkgCommitmentsResponse
         */
        GetVaultDkgCommitmentsResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetVaultDkgCommitmentsResponse)
                return object;
            let message = new $root.pb.GetVaultDkgCommitmentsResponse();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.vaultId != null)
                message.vaultId = object.vaultId >>> 0;
            if (object.epochId != null)
                if ($util.Long)
                    (message.epochId = $util.Long.fromValue(object.epochId)).unsigned = true;
                else if (typeof object.epochId === "string")
                    message.epochId = parseInt(object.epochId, 10);
                else if (typeof object.epochId === "number")
                    message.epochId = object.epochId;
                else if (typeof object.epochId === "object")
                    message.epochId = new $util.LongBits(object.epochId.low >>> 0, object.epochId.high >>> 0).toNumber(true);
            if (object.commitments) {
                if (!Array.isArray(object.commitments))
                    throw TypeError(".pb.GetVaultDkgCommitmentsResponse.commitments: array expected");
                message.commitments = [];
                for (let i = 0; i < object.commitments.length; ++i) {
                    if (typeof object.commitments[i] !== "object")
                        throw TypeError(".pb.GetVaultDkgCommitmentsResponse.commitments: object expected");
                    message.commitments[i] = $root.pb.DkgCommitmentInfo.fromObject(object.commitments[i]);
                }
            }
            if (object.total != null)
                message.total = object.total | 0;
            return message;
        };

        /**
         * Creates a plain object from a GetVaultDkgCommitmentsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {pb.GetVaultDkgCommitmentsResponse} message GetVaultDkgCommitmentsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetVaultDkgCommitmentsResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.commitments = [];
            if (options.defaults) {
                object.chain = "";
                object.vaultId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.epochId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.epochId = options.longs === String ? "0" : 0;
                object.total = 0;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.vaultId != null && message.hasOwnProperty("vaultId"))
                object.vaultId = message.vaultId;
            if (message.epochId != null && message.hasOwnProperty("epochId"))
                if (typeof message.epochId === "number")
                    object.epochId = options.longs === String ? String(message.epochId) : message.epochId;
                else
                    object.epochId = options.longs === String ? $util.Long.prototype.toString.call(message.epochId) : options.longs === Number ? new $util.LongBits(message.epochId.low >>> 0, message.epochId.high >>> 0).toNumber(true) : message.epochId;
            if (message.commitments && message.commitments.length) {
                object.commitments = [];
                for (let j = 0; j < message.commitments.length; ++j)
                    object.commitments[j] = $root.pb.DkgCommitmentInfo.toObject(message.commitments[j], options);
            }
            if (message.total != null && message.hasOwnProperty("total"))
                object.total = message.total;
            return object;
        };

        /**
         * Converts this GetVaultDkgCommitmentsResponse to JSON.
         * @function toJSON
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetVaultDkgCommitmentsResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetVaultDkgCommitmentsResponse
         * @function getTypeUrl
         * @memberof pb.GetVaultDkgCommitmentsResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetVaultDkgCommitmentsResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetVaultDkgCommitmentsResponse";
        };

        return GetVaultDkgCommitmentsResponse;
    })();

    pb.DownloadSignedPackageResponse = (function() {

        /**
         * Properties of a DownloadSignedPackageResponse.
         * @memberof pb
         * @interface IDownloadSignedPackageResponse
         * @property {string|null} [jobId] DownloadSignedPackageResponse jobId
         * @property {number|Long|null} [idx] DownloadSignedPackageResponse idx
         * @property {string|null} [signedPackageBytes] DownloadSignedPackageResponse signedPackageBytes
         * @property {number|Long|null} [submitHeight] DownloadSignedPackageResponse submitHeight
         * @property {string|null} [submitter] DownloadSignedPackageResponse submitter
         */

        /**
         * Constructs a new DownloadSignedPackageResponse.
         * @memberof pb
         * @classdesc Represents a DownloadSignedPackageResponse.
         * @implements IDownloadSignedPackageResponse
         * @constructor
         * @param {pb.IDownloadSignedPackageResponse=} [properties] Properties to set
         */
        function DownloadSignedPackageResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DownloadSignedPackageResponse jobId.
         * @member {string} jobId
         * @memberof pb.DownloadSignedPackageResponse
         * @instance
         */
        DownloadSignedPackageResponse.prototype.jobId = "";

        /**
         * DownloadSignedPackageResponse idx.
         * @member {number|Long} idx
         * @memberof pb.DownloadSignedPackageResponse
         * @instance
         */
        DownloadSignedPackageResponse.prototype.idx = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * DownloadSignedPackageResponse signedPackageBytes.
         * @member {string} signedPackageBytes
         * @memberof pb.DownloadSignedPackageResponse
         * @instance
         */
        DownloadSignedPackageResponse.prototype.signedPackageBytes = "";

        /**
         * DownloadSignedPackageResponse submitHeight.
         * @member {number|Long} submitHeight
         * @memberof pb.DownloadSignedPackageResponse
         * @instance
         */
        DownloadSignedPackageResponse.prototype.submitHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * DownloadSignedPackageResponse submitter.
         * @member {string} submitter
         * @memberof pb.DownloadSignedPackageResponse
         * @instance
         */
        DownloadSignedPackageResponse.prototype.submitter = "";

        /**
         * Creates a new DownloadSignedPackageResponse instance using the specified properties.
         * @function create
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {pb.IDownloadSignedPackageResponse=} [properties] Properties to set
         * @returns {pb.DownloadSignedPackageResponse} DownloadSignedPackageResponse instance
         */
        DownloadSignedPackageResponse.create = function create(properties) {
            return new DownloadSignedPackageResponse(properties);
        };

        /**
         * Encodes the specified DownloadSignedPackageResponse message. Does not implicitly {@link pb.DownloadSignedPackageResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {pb.IDownloadSignedPackageResponse} message DownloadSignedPackageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DownloadSignedPackageResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.jobId);
            if (message.idx != null && Object.hasOwnProperty.call(message, "idx"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.idx);
            if (message.signedPackageBytes != null && Object.hasOwnProperty.call(message, "signedPackageBytes"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.signedPackageBytes);
            if (message.submitHeight != null && Object.hasOwnProperty.call(message, "submitHeight"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.submitHeight);
            if (message.submitter != null && Object.hasOwnProperty.call(message, "submitter"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.submitter);
            return writer;
        };

        /**
         * Encodes the specified DownloadSignedPackageResponse message, length delimited. Does not implicitly {@link pb.DownloadSignedPackageResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {pb.IDownloadSignedPackageResponse} message DownloadSignedPackageResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DownloadSignedPackageResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a DownloadSignedPackageResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.DownloadSignedPackageResponse} DownloadSignedPackageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DownloadSignedPackageResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.DownloadSignedPackageResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.jobId = reader.string();
                        break;
                    }
                case 2: {
                        message.idx = reader.uint64();
                        break;
                    }
                case 3: {
                        message.signedPackageBytes = reader.string();
                        break;
                    }
                case 4: {
                        message.submitHeight = reader.uint64();
                        break;
                    }
                case 5: {
                        message.submitter = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DownloadSignedPackageResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.DownloadSignedPackageResponse} DownloadSignedPackageResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DownloadSignedPackageResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DownloadSignedPackageResponse message.
         * @function verify
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DownloadSignedPackageResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                if (!$util.isString(message.jobId))
                    return "jobId: string expected";
            if (message.idx != null && message.hasOwnProperty("idx"))
                if (!$util.isInteger(message.idx) && !(message.idx && $util.isInteger(message.idx.low) && $util.isInteger(message.idx.high)))
                    return "idx: integer|Long expected";
            if (message.signedPackageBytes != null && message.hasOwnProperty("signedPackageBytes"))
                if (!$util.isString(message.signedPackageBytes))
                    return "signedPackageBytes: string expected";
            if (message.submitHeight != null && message.hasOwnProperty("submitHeight"))
                if (!$util.isInteger(message.submitHeight) && !(message.submitHeight && $util.isInteger(message.submitHeight.low) && $util.isInteger(message.submitHeight.high)))
                    return "submitHeight: integer|Long expected";
            if (message.submitter != null && message.hasOwnProperty("submitter"))
                if (!$util.isString(message.submitter))
                    return "submitter: string expected";
            return null;
        };

        /**
         * Creates a DownloadSignedPackageResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.DownloadSignedPackageResponse} DownloadSignedPackageResponse
         */
        DownloadSignedPackageResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.DownloadSignedPackageResponse)
                return object;
            let message = new $root.pb.DownloadSignedPackageResponse();
            if (object.jobId != null)
                message.jobId = String(object.jobId);
            if (object.idx != null)
                if ($util.Long)
                    (message.idx = $util.Long.fromValue(object.idx)).unsigned = true;
                else if (typeof object.idx === "string")
                    message.idx = parseInt(object.idx, 10);
                else if (typeof object.idx === "number")
                    message.idx = object.idx;
                else if (typeof object.idx === "object")
                    message.idx = new $util.LongBits(object.idx.low >>> 0, object.idx.high >>> 0).toNumber(true);
            if (object.signedPackageBytes != null)
                message.signedPackageBytes = String(object.signedPackageBytes);
            if (object.submitHeight != null)
                if ($util.Long)
                    (message.submitHeight = $util.Long.fromValue(object.submitHeight)).unsigned = true;
                else if (typeof object.submitHeight === "string")
                    message.submitHeight = parseInt(object.submitHeight, 10);
                else if (typeof object.submitHeight === "number")
                    message.submitHeight = object.submitHeight;
                else if (typeof object.submitHeight === "object")
                    message.submitHeight = new $util.LongBits(object.submitHeight.low >>> 0, object.submitHeight.high >>> 0).toNumber(true);
            if (object.submitter != null)
                message.submitter = String(object.submitter);
            return message;
        };

        /**
         * Creates a plain object from a DownloadSignedPackageResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {pb.DownloadSignedPackageResponse} message DownloadSignedPackageResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DownloadSignedPackageResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.jobId = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.idx = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.idx = options.longs === String ? "0" : 0;
                object.signedPackageBytes = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.submitHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.submitHeight = options.longs === String ? "0" : 0;
                object.submitter = "";
            }
            if (message.jobId != null && message.hasOwnProperty("jobId"))
                object.jobId = message.jobId;
            if (message.idx != null && message.hasOwnProperty("idx"))
                if (typeof message.idx === "number")
                    object.idx = options.longs === String ? String(message.idx) : message.idx;
                else
                    object.idx = options.longs === String ? $util.Long.prototype.toString.call(message.idx) : options.longs === Number ? new $util.LongBits(message.idx.low >>> 0, message.idx.high >>> 0).toNumber(true) : message.idx;
            if (message.signedPackageBytes != null && message.hasOwnProperty("signedPackageBytes"))
                object.signedPackageBytes = message.signedPackageBytes;
            if (message.submitHeight != null && message.hasOwnProperty("submitHeight"))
                if (typeof message.submitHeight === "number")
                    object.submitHeight = options.longs === String ? String(message.submitHeight) : message.submitHeight;
                else
                    object.submitHeight = options.longs === String ? $util.Long.prototype.toString.call(message.submitHeight) : options.longs === Number ? new $util.LongBits(message.submitHeight.low >>> 0, message.submitHeight.high >>> 0).toNumber(true) : message.submitHeight;
            if (message.submitter != null && message.hasOwnProperty("submitter"))
                object.submitter = message.submitter;
            return object;
        };

        /**
         * Converts this DownloadSignedPackageResponse to JSON.
         * @function toJSON
         * @memberof pb.DownloadSignedPackageResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DownloadSignedPackageResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DownloadSignedPackageResponse
         * @function getTypeUrl
         * @memberof pb.DownloadSignedPackageResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DownloadSignedPackageResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.DownloadSignedPackageResponse";
        };

        return DownloadSignedPackageResponse;
    })();

    pb.HealthResponse = (function() {

        /**
         * Properties of a HealthResponse.
         * @memberof pb
         * @interface IHealthResponse
         * @property {string|null} [status] HealthResponse status
         * @property {string|null} [uptime] HealthResponse uptime
         * @property {string|null} [version] HealthResponse version
         * @property {string|null} [goVersion] HealthResponse goVersion
         * @property {number|null} [numCpu] HealthResponse numCpu
         */

        /**
         * Constructs a new HealthResponse.
         * @memberof pb
         * @classdesc Represents a HealthResponse.
         * @implements IHealthResponse
         * @constructor
         * @param {pb.IHealthResponse=} [properties] Properties to set
         */
        function HealthResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HealthResponse status.
         * @member {string} status
         * @memberof pb.HealthResponse
         * @instance
         */
        HealthResponse.prototype.status = "";

        /**
         * HealthResponse uptime.
         * @member {string} uptime
         * @memberof pb.HealthResponse
         * @instance
         */
        HealthResponse.prototype.uptime = "";

        /**
         * HealthResponse version.
         * @member {string} version
         * @memberof pb.HealthResponse
         * @instance
         */
        HealthResponse.prototype.version = "";

        /**
         * HealthResponse goVersion.
         * @member {string} goVersion
         * @memberof pb.HealthResponse
         * @instance
         */
        HealthResponse.prototype.goVersion = "";

        /**
         * HealthResponse numCpu.
         * @member {number} numCpu
         * @memberof pb.HealthResponse
         * @instance
         */
        HealthResponse.prototype.numCpu = 0;

        /**
         * Creates a new HealthResponse instance using the specified properties.
         * @function create
         * @memberof pb.HealthResponse
         * @static
         * @param {pb.IHealthResponse=} [properties] Properties to set
         * @returns {pb.HealthResponse} HealthResponse instance
         */
        HealthResponse.create = function create(properties) {
            return new HealthResponse(properties);
        };

        /**
         * Encodes the specified HealthResponse message. Does not implicitly {@link pb.HealthResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.HealthResponse
         * @static
         * @param {pb.IHealthResponse} message HealthResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HealthResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.status);
            if (message.uptime != null && Object.hasOwnProperty.call(message, "uptime"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.uptime);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.version);
            if (message.goVersion != null && Object.hasOwnProperty.call(message, "goVersion"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.goVersion);
            if (message.numCpu != null && Object.hasOwnProperty.call(message, "numCpu"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.numCpu);
            return writer;
        };

        /**
         * Encodes the specified HealthResponse message, length delimited. Does not implicitly {@link pb.HealthResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.HealthResponse
         * @static
         * @param {pb.IHealthResponse} message HealthResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HealthResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a HealthResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.HealthResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.HealthResponse} HealthResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HealthResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.HealthResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.string();
                        break;
                    }
                case 2: {
                        message.uptime = reader.string();
                        break;
                    }
                case 3: {
                        message.version = reader.string();
                        break;
                    }
                case 4: {
                        message.goVersion = reader.string();
                        break;
                    }
                case 5: {
                        message.numCpu = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a HealthResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.HealthResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.HealthResponse} HealthResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HealthResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HealthResponse message.
         * @function verify
         * @memberof pb.HealthResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HealthResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.status != null && message.hasOwnProperty("status"))
                if (!$util.isString(message.status))
                    return "status: string expected";
            if (message.uptime != null && message.hasOwnProperty("uptime"))
                if (!$util.isString(message.uptime))
                    return "uptime: string expected";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isString(message.version))
                    return "version: string expected";
            if (message.goVersion != null && message.hasOwnProperty("goVersion"))
                if (!$util.isString(message.goVersion))
                    return "goVersion: string expected";
            if (message.numCpu != null && message.hasOwnProperty("numCpu"))
                if (!$util.isInteger(message.numCpu))
                    return "numCpu: integer expected";
            return null;
        };

        /**
         * Creates a HealthResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.HealthResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.HealthResponse} HealthResponse
         */
        HealthResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.HealthResponse)
                return object;
            let message = new $root.pb.HealthResponse();
            if (object.status != null)
                message.status = String(object.status);
            if (object.uptime != null)
                message.uptime = String(object.uptime);
            if (object.version != null)
                message.version = String(object.version);
            if (object.goVersion != null)
                message.goVersion = String(object.goVersion);
            if (object.numCpu != null)
                message.numCpu = object.numCpu | 0;
            return message;
        };

        /**
         * Creates a plain object from a HealthResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.HealthResponse
         * @static
         * @param {pb.HealthResponse} message HealthResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HealthResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.status = "";
                object.uptime = "";
                object.version = "";
                object.goVersion = "";
                object.numCpu = 0;
            }
            if (message.status != null && message.hasOwnProperty("status"))
                object.status = message.status;
            if (message.uptime != null && message.hasOwnProperty("uptime"))
                object.uptime = message.uptime;
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.goVersion != null && message.hasOwnProperty("goVersion"))
                object.goVersion = message.goVersion;
            if (message.numCpu != null && message.hasOwnProperty("numCpu"))
                object.numCpu = message.numCpu;
            return object;
        };

        /**
         * Converts this HealthResponse to JSON.
         * @function toJSON
         * @memberof pb.HealthResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HealthResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HealthResponse
         * @function getTypeUrl
         * @memberof pb.HealthResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HealthResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.HealthResponse";
        };

        return HealthResponse;
    })();

    pb.ChannelStat = (function() {

        /**
         * Properties of a ChannelStat.
         * @memberof pb
         * @interface IChannelStat
         * @property {string|null} [name] ChannelStat name
         * @property {string|null} [module] ChannelStat module
         * @property {number|null} [len] ChannelStat len
         * @property {number|null} [cap] ChannelStat cap
         * @property {number|null} [usage] ChannelStat usage
         */

        /**
         * Constructs a new ChannelStat.
         * @memberof pb
         * @classdesc Represents a ChannelStat.
         * @implements IChannelStat
         * @constructor
         * @param {pb.IChannelStat=} [properties] Properties to set
         */
        function ChannelStat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChannelStat name.
         * @member {string} name
         * @memberof pb.ChannelStat
         * @instance
         */
        ChannelStat.prototype.name = "";

        /**
         * ChannelStat module.
         * @member {string} module
         * @memberof pb.ChannelStat
         * @instance
         */
        ChannelStat.prototype.module = "";

        /**
         * ChannelStat len.
         * @member {number} len
         * @memberof pb.ChannelStat
         * @instance
         */
        ChannelStat.prototype.len = 0;

        /**
         * ChannelStat cap.
         * @member {number} cap
         * @memberof pb.ChannelStat
         * @instance
         */
        ChannelStat.prototype.cap = 0;

        /**
         * ChannelStat usage.
         * @member {number} usage
         * @memberof pb.ChannelStat
         * @instance
         */
        ChannelStat.prototype.usage = 0;

        /**
         * Creates a new ChannelStat instance using the specified properties.
         * @function create
         * @memberof pb.ChannelStat
         * @static
         * @param {pb.IChannelStat=} [properties] Properties to set
         * @returns {pb.ChannelStat} ChannelStat instance
         */
        ChannelStat.create = function create(properties) {
            return new ChannelStat(properties);
        };

        /**
         * Encodes the specified ChannelStat message. Does not implicitly {@link pb.ChannelStat.verify|verify} messages.
         * @function encode
         * @memberof pb.ChannelStat
         * @static
         * @param {pb.IChannelStat} message ChannelStat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelStat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.module != null && Object.hasOwnProperty.call(message, "module"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.module);
            if (message.len != null && Object.hasOwnProperty.call(message, "len"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.len);
            if (message.cap != null && Object.hasOwnProperty.call(message, "cap"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.cap);
            if (message.usage != null && Object.hasOwnProperty.call(message, "usage"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.usage);
            return writer;
        };

        /**
         * Encodes the specified ChannelStat message, length delimited. Does not implicitly {@link pb.ChannelStat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ChannelStat
         * @static
         * @param {pb.IChannelStat} message ChannelStat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChannelStat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ChannelStat message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ChannelStat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ChannelStat} ChannelStat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelStat.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ChannelStat();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.module = reader.string();
                        break;
                    }
                case 3: {
                        message.len = reader.int32();
                        break;
                    }
                case 4: {
                        message.cap = reader.int32();
                        break;
                    }
                case 5: {
                        message.usage = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChannelStat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ChannelStat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ChannelStat} ChannelStat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChannelStat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChannelStat message.
         * @function verify
         * @memberof pb.ChannelStat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChannelStat.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.module != null && message.hasOwnProperty("module"))
                if (!$util.isString(message.module))
                    return "module: string expected";
            if (message.len != null && message.hasOwnProperty("len"))
                if (!$util.isInteger(message.len))
                    return "len: integer expected";
            if (message.cap != null && message.hasOwnProperty("cap"))
                if (!$util.isInteger(message.cap))
                    return "cap: integer expected";
            if (message.usage != null && message.hasOwnProperty("usage"))
                if (typeof message.usage !== "number")
                    return "usage: number expected";
            return null;
        };

        /**
         * Creates a ChannelStat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ChannelStat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ChannelStat} ChannelStat
         */
        ChannelStat.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ChannelStat)
                return object;
            let message = new $root.pb.ChannelStat();
            if (object.name != null)
                message.name = String(object.name);
            if (object.module != null)
                message.module = String(object.module);
            if (object.len != null)
                message.len = object.len | 0;
            if (object.cap != null)
                message.cap = object.cap | 0;
            if (object.usage != null)
                message.usage = Number(object.usage);
            return message;
        };

        /**
         * Creates a plain object from a ChannelStat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ChannelStat
         * @static
         * @param {pb.ChannelStat} message ChannelStat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChannelStat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.module = "";
                object.len = 0;
                object.cap = 0;
                object.usage = 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.module != null && message.hasOwnProperty("module"))
                object.module = message.module;
            if (message.len != null && message.hasOwnProperty("len"))
                object.len = message.len;
            if (message.cap != null && message.hasOwnProperty("cap"))
                object.cap = message.cap;
            if (message.usage != null && message.hasOwnProperty("usage"))
                object.usage = options.json && !isFinite(message.usage) ? String(message.usage) : message.usage;
            return object;
        };

        /**
         * Converts this ChannelStat to JSON.
         * @function toJSON
         * @memberof pb.ChannelStat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChannelStat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChannelStat
         * @function getTypeUrl
         * @memberof pb.ChannelStat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChannelStat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ChannelStat";
        };

        return ChannelStat;
    })();

    pb.MetricsResponse = (function() {

        /**
         * Properties of a MetricsResponse.
         * @memberof pb
         * @interface IMetricsResponse
         * @property {number|Long|null} [heapAlloc] MetricsResponse heapAlloc
         * @property {number|Long|null} [heapSys] MetricsResponse heapSys
         * @property {number|null} [numGoroutine] MetricsResponse numGoroutine
         * @property {number|null} [frostJobs] MetricsResponse frostJobs
         * @property {number|null} [frostWithdraws] MetricsResponse frostWithdraws
         * @property {Object.<string,number|Long>|null} [apiCallStats] MetricsResponse apiCallStats
         * @property {Array.<pb.IChannelStat>|null} [channelStats] MetricsResponse channelStats
         */

        /**
         * Constructs a new MetricsResponse.
         * @memberof pb
         * @classdesc Represents a MetricsResponse.
         * @implements IMetricsResponse
         * @constructor
         * @param {pb.IMetricsResponse=} [properties] Properties to set
         */
        function MetricsResponse(properties) {
            this.apiCallStats = {};
            this.channelStats = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MetricsResponse heapAlloc.
         * @member {number|Long} heapAlloc
         * @memberof pb.MetricsResponse
         * @instance
         */
        MetricsResponse.prototype.heapAlloc = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * MetricsResponse heapSys.
         * @member {number|Long} heapSys
         * @memberof pb.MetricsResponse
         * @instance
         */
        MetricsResponse.prototype.heapSys = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * MetricsResponse numGoroutine.
         * @member {number} numGoroutine
         * @memberof pb.MetricsResponse
         * @instance
         */
        MetricsResponse.prototype.numGoroutine = 0;

        /**
         * MetricsResponse frostJobs.
         * @member {number} frostJobs
         * @memberof pb.MetricsResponse
         * @instance
         */
        MetricsResponse.prototype.frostJobs = 0;

        /**
         * MetricsResponse frostWithdraws.
         * @member {number} frostWithdraws
         * @memberof pb.MetricsResponse
         * @instance
         */
        MetricsResponse.prototype.frostWithdraws = 0;

        /**
         * MetricsResponse apiCallStats.
         * @member {Object.<string,number|Long>} apiCallStats
         * @memberof pb.MetricsResponse
         * @instance
         */
        MetricsResponse.prototype.apiCallStats = $util.emptyObject;

        /**
         * MetricsResponse channelStats.
         * @member {Array.<pb.IChannelStat>} channelStats
         * @memberof pb.MetricsResponse
         * @instance
         */
        MetricsResponse.prototype.channelStats = $util.emptyArray;

        /**
         * Creates a new MetricsResponse instance using the specified properties.
         * @function create
         * @memberof pb.MetricsResponse
         * @static
         * @param {pb.IMetricsResponse=} [properties] Properties to set
         * @returns {pb.MetricsResponse} MetricsResponse instance
         */
        MetricsResponse.create = function create(properties) {
            return new MetricsResponse(properties);
        };

        /**
         * Encodes the specified MetricsResponse message. Does not implicitly {@link pb.MetricsResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.MetricsResponse
         * @static
         * @param {pb.IMetricsResponse} message MetricsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MetricsResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.heapAlloc != null && Object.hasOwnProperty.call(message, "heapAlloc"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.heapAlloc);
            if (message.heapSys != null && Object.hasOwnProperty.call(message, "heapSys"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.heapSys);
            if (message.numGoroutine != null && Object.hasOwnProperty.call(message, "numGoroutine"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.numGoroutine);
            if (message.frostJobs != null && Object.hasOwnProperty.call(message, "frostJobs"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.frostJobs);
            if (message.frostWithdraws != null && Object.hasOwnProperty.call(message, "frostWithdraws"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.frostWithdraws);
            if (message.apiCallStats != null && Object.hasOwnProperty.call(message, "apiCallStats"))
                for (let keys = Object.keys(message.apiCallStats), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).uint64(message.apiCallStats[keys[i]]).ldelim();
            if (message.channelStats != null && message.channelStats.length)
                for (let i = 0; i < message.channelStats.length; ++i)
                    $root.pb.ChannelStat.encode(message.channelStats[i], writer.uint32(/* id 7, wireType 2 =*/58).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified MetricsResponse message, length delimited. Does not implicitly {@link pb.MetricsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.MetricsResponse
         * @static
         * @param {pb.IMetricsResponse} message MetricsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MetricsResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a MetricsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.MetricsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.MetricsResponse} MetricsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MetricsResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.MetricsResponse(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.heapAlloc = reader.uint64();
                        break;
                    }
                case 2: {
                        message.heapSys = reader.uint64();
                        break;
                    }
                case 3: {
                        message.numGoroutine = reader.int32();
                        break;
                    }
                case 4: {
                        message.frostJobs = reader.int32();
                        break;
                    }
                case 5: {
                        message.frostWithdraws = reader.int32();
                        break;
                    }
                case 6: {
                        if (message.apiCallStats === $util.emptyObject)
                            message.apiCallStats = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = 0;
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.uint64();
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.apiCallStats[key] = value;
                        break;
                    }
                case 7: {
                        if (!(message.channelStats && message.channelStats.length))
                            message.channelStats = [];
                        message.channelStats.push($root.pb.ChannelStat.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MetricsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.MetricsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.MetricsResponse} MetricsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MetricsResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MetricsResponse message.
         * @function verify
         * @memberof pb.MetricsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MetricsResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.heapAlloc != null && message.hasOwnProperty("heapAlloc"))
                if (!$util.isInteger(message.heapAlloc) && !(message.heapAlloc && $util.isInteger(message.heapAlloc.low) && $util.isInteger(message.heapAlloc.high)))
                    return "heapAlloc: integer|Long expected";
            if (message.heapSys != null && message.hasOwnProperty("heapSys"))
                if (!$util.isInteger(message.heapSys) && !(message.heapSys && $util.isInteger(message.heapSys.low) && $util.isInteger(message.heapSys.high)))
                    return "heapSys: integer|Long expected";
            if (message.numGoroutine != null && message.hasOwnProperty("numGoroutine"))
                if (!$util.isInteger(message.numGoroutine))
                    return "numGoroutine: integer expected";
            if (message.frostJobs != null && message.hasOwnProperty("frostJobs"))
                if (!$util.isInteger(message.frostJobs))
                    return "frostJobs: integer expected";
            if (message.frostWithdraws != null && message.hasOwnProperty("frostWithdraws"))
                if (!$util.isInteger(message.frostWithdraws))
                    return "frostWithdraws: integer expected";
            if (message.apiCallStats != null && message.hasOwnProperty("apiCallStats")) {
                if (!$util.isObject(message.apiCallStats))
                    return "apiCallStats: object expected";
                let key = Object.keys(message.apiCallStats);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isInteger(message.apiCallStats[key[i]]) && !(message.apiCallStats[key[i]] && $util.isInteger(message.apiCallStats[key[i]].low) && $util.isInteger(message.apiCallStats[key[i]].high)))
                        return "apiCallStats: integer|Long{k:string} expected";
            }
            if (message.channelStats != null && message.hasOwnProperty("channelStats")) {
                if (!Array.isArray(message.channelStats))
                    return "channelStats: array expected";
                for (let i = 0; i < message.channelStats.length; ++i) {
                    let error = $root.pb.ChannelStat.verify(message.channelStats[i]);
                    if (error)
                        return "channelStats." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MetricsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.MetricsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.MetricsResponse} MetricsResponse
         */
        MetricsResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.MetricsResponse)
                return object;
            let message = new $root.pb.MetricsResponse();
            if (object.heapAlloc != null)
                if ($util.Long)
                    (message.heapAlloc = $util.Long.fromValue(object.heapAlloc)).unsigned = true;
                else if (typeof object.heapAlloc === "string")
                    message.heapAlloc = parseInt(object.heapAlloc, 10);
                else if (typeof object.heapAlloc === "number")
                    message.heapAlloc = object.heapAlloc;
                else if (typeof object.heapAlloc === "object")
                    message.heapAlloc = new $util.LongBits(object.heapAlloc.low >>> 0, object.heapAlloc.high >>> 0).toNumber(true);
            if (object.heapSys != null)
                if ($util.Long)
                    (message.heapSys = $util.Long.fromValue(object.heapSys)).unsigned = true;
                else if (typeof object.heapSys === "string")
                    message.heapSys = parseInt(object.heapSys, 10);
                else if (typeof object.heapSys === "number")
                    message.heapSys = object.heapSys;
                else if (typeof object.heapSys === "object")
                    message.heapSys = new $util.LongBits(object.heapSys.low >>> 0, object.heapSys.high >>> 0).toNumber(true);
            if (object.numGoroutine != null)
                message.numGoroutine = object.numGoroutine | 0;
            if (object.frostJobs != null)
                message.frostJobs = object.frostJobs | 0;
            if (object.frostWithdraws != null)
                message.frostWithdraws = object.frostWithdraws | 0;
            if (object.apiCallStats) {
                if (typeof object.apiCallStats !== "object")
                    throw TypeError(".pb.MetricsResponse.apiCallStats: object expected");
                message.apiCallStats = {};
                for (let keys = Object.keys(object.apiCallStats), i = 0; i < keys.length; ++i)
                    if ($util.Long)
                        (message.apiCallStats[keys[i]] = $util.Long.fromValue(object.apiCallStats[keys[i]])).unsigned = true;
                    else if (typeof object.apiCallStats[keys[i]] === "string")
                        message.apiCallStats[keys[i]] = parseInt(object.apiCallStats[keys[i]], 10);
                    else if (typeof object.apiCallStats[keys[i]] === "number")
                        message.apiCallStats[keys[i]] = object.apiCallStats[keys[i]];
                    else if (typeof object.apiCallStats[keys[i]] === "object")
                        message.apiCallStats[keys[i]] = new $util.LongBits(object.apiCallStats[keys[i]].low >>> 0, object.apiCallStats[keys[i]].high >>> 0).toNumber(true);
            }
            if (object.channelStats) {
                if (!Array.isArray(object.channelStats))
                    throw TypeError(".pb.MetricsResponse.channelStats: array expected");
                message.channelStats = [];
                for (let i = 0; i < object.channelStats.length; ++i) {
                    if (typeof object.channelStats[i] !== "object")
                        throw TypeError(".pb.MetricsResponse.channelStats: object expected");
                    message.channelStats[i] = $root.pb.ChannelStat.fromObject(object.channelStats[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a MetricsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.MetricsResponse
         * @static
         * @param {pb.MetricsResponse} message MetricsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MetricsResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.channelStats = [];
            if (options.objects || options.defaults)
                object.apiCallStats = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.heapAlloc = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.heapAlloc = options.longs === String ? "0" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.heapSys = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.heapSys = options.longs === String ? "0" : 0;
                object.numGoroutine = 0;
                object.frostJobs = 0;
                object.frostWithdraws = 0;
            }
            if (message.heapAlloc != null && message.hasOwnProperty("heapAlloc"))
                if (typeof message.heapAlloc === "number")
                    object.heapAlloc = options.longs === String ? String(message.heapAlloc) : message.heapAlloc;
                else
                    object.heapAlloc = options.longs === String ? $util.Long.prototype.toString.call(message.heapAlloc) : options.longs === Number ? new $util.LongBits(message.heapAlloc.low >>> 0, message.heapAlloc.high >>> 0).toNumber(true) : message.heapAlloc;
            if (message.heapSys != null && message.hasOwnProperty("heapSys"))
                if (typeof message.heapSys === "number")
                    object.heapSys = options.longs === String ? String(message.heapSys) : message.heapSys;
                else
                    object.heapSys = options.longs === String ? $util.Long.prototype.toString.call(message.heapSys) : options.longs === Number ? new $util.LongBits(message.heapSys.low >>> 0, message.heapSys.high >>> 0).toNumber(true) : message.heapSys;
            if (message.numGoroutine != null && message.hasOwnProperty("numGoroutine"))
                object.numGoroutine = message.numGoroutine;
            if (message.frostJobs != null && message.hasOwnProperty("frostJobs"))
                object.frostJobs = message.frostJobs;
            if (message.frostWithdraws != null && message.hasOwnProperty("frostWithdraws"))
                object.frostWithdraws = message.frostWithdraws;
            let keys2;
            if (message.apiCallStats && (keys2 = Object.keys(message.apiCallStats)).length) {
                object.apiCallStats = {};
                for (let j = 0; j < keys2.length; ++j)
                    if (typeof message.apiCallStats[keys2[j]] === "number")
                        object.apiCallStats[keys2[j]] = options.longs === String ? String(message.apiCallStats[keys2[j]]) : message.apiCallStats[keys2[j]];
                    else
                        object.apiCallStats[keys2[j]] = options.longs === String ? $util.Long.prototype.toString.call(message.apiCallStats[keys2[j]]) : options.longs === Number ? new $util.LongBits(message.apiCallStats[keys2[j]].low >>> 0, message.apiCallStats[keys2[j]].high >>> 0).toNumber(true) : message.apiCallStats[keys2[j]];
            }
            if (message.channelStats && message.channelStats.length) {
                object.channelStats = [];
                for (let j = 0; j < message.channelStats.length; ++j)
                    object.channelStats[j] = $root.pb.ChannelStat.toObject(message.channelStats[j], options);
            }
            return object;
        };

        /**
         * Converts this MetricsResponse to JSON.
         * @function toJSON
         * @memberof pb.MetricsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MetricsResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MetricsResponse
         * @function getTypeUrl
         * @memberof pb.MetricsResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MetricsResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.MetricsResponse";
        };

        return MetricsResponse;
    })();

    pb.ForceRescanRequest = (function() {

        /**
         * Properties of a ForceRescanRequest.
         * @memberof pb
         * @interface IForceRescanRequest
         * @property {string|null} [chain] ForceRescanRequest chain
         * @property {number|Long|null} [fromHeight] ForceRescanRequest fromHeight
         */

        /**
         * Constructs a new ForceRescanRequest.
         * @memberof pb
         * @classdesc Represents a ForceRescanRequest.
         * @implements IForceRescanRequest
         * @constructor
         * @param {pb.IForceRescanRequest=} [properties] Properties to set
         */
        function ForceRescanRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ForceRescanRequest chain.
         * @member {string} chain
         * @memberof pb.ForceRescanRequest
         * @instance
         */
        ForceRescanRequest.prototype.chain = "";

        /**
         * ForceRescanRequest fromHeight.
         * @member {number|Long} fromHeight
         * @memberof pb.ForceRescanRequest
         * @instance
         */
        ForceRescanRequest.prototype.fromHeight = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * Creates a new ForceRescanRequest instance using the specified properties.
         * @function create
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {pb.IForceRescanRequest=} [properties] Properties to set
         * @returns {pb.ForceRescanRequest} ForceRescanRequest instance
         */
        ForceRescanRequest.create = function create(properties) {
            return new ForceRescanRequest(properties);
        };

        /**
         * Encodes the specified ForceRescanRequest message. Does not implicitly {@link pb.ForceRescanRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {pb.IForceRescanRequest} message ForceRescanRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForceRescanRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chain != null && Object.hasOwnProperty.call(message, "chain"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.chain);
            if (message.fromHeight != null && Object.hasOwnProperty.call(message, "fromHeight"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.fromHeight);
            return writer;
        };

        /**
         * Encodes the specified ForceRescanRequest message, length delimited. Does not implicitly {@link pb.ForceRescanRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {pb.IForceRescanRequest} message ForceRescanRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForceRescanRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ForceRescanRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ForceRescanRequest} ForceRescanRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForceRescanRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ForceRescanRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.chain = reader.string();
                        break;
                    }
                case 2: {
                        message.fromHeight = reader.uint64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ForceRescanRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ForceRescanRequest} ForceRescanRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForceRescanRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForceRescanRequest message.
         * @function verify
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForceRescanRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.chain != null && message.hasOwnProperty("chain"))
                if (!$util.isString(message.chain))
                    return "chain: string expected";
            if (message.fromHeight != null && message.hasOwnProperty("fromHeight"))
                if (!$util.isInteger(message.fromHeight) && !(message.fromHeight && $util.isInteger(message.fromHeight.low) && $util.isInteger(message.fromHeight.high)))
                    return "fromHeight: integer|Long expected";
            return null;
        };

        /**
         * Creates a ForceRescanRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ForceRescanRequest} ForceRescanRequest
         */
        ForceRescanRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ForceRescanRequest)
                return object;
            let message = new $root.pb.ForceRescanRequest();
            if (object.chain != null)
                message.chain = String(object.chain);
            if (object.fromHeight != null)
                if ($util.Long)
                    (message.fromHeight = $util.Long.fromValue(object.fromHeight)).unsigned = true;
                else if (typeof object.fromHeight === "string")
                    message.fromHeight = parseInt(object.fromHeight, 10);
                else if (typeof object.fromHeight === "number")
                    message.fromHeight = object.fromHeight;
                else if (typeof object.fromHeight === "object")
                    message.fromHeight = new $util.LongBits(object.fromHeight.low >>> 0, object.fromHeight.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ForceRescanRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {pb.ForceRescanRequest} message ForceRescanRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForceRescanRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chain = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.fromHeight = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.fromHeight = options.longs === String ? "0" : 0;
            }
            if (message.chain != null && message.hasOwnProperty("chain"))
                object.chain = message.chain;
            if (message.fromHeight != null && message.hasOwnProperty("fromHeight"))
                if (typeof message.fromHeight === "number")
                    object.fromHeight = options.longs === String ? String(message.fromHeight) : message.fromHeight;
                else
                    object.fromHeight = options.longs === String ? $util.Long.prototype.toString.call(message.fromHeight) : options.longs === Number ? new $util.LongBits(message.fromHeight.low >>> 0, message.fromHeight.high >>> 0).toNumber(true) : message.fromHeight;
            return object;
        };

        /**
         * Converts this ForceRescanRequest to JSON.
         * @function toJSON
         * @memberof pb.ForceRescanRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForceRescanRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ForceRescanRequest
         * @function getTypeUrl
         * @memberof pb.ForceRescanRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ForceRescanRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ForceRescanRequest";
        };

        return ForceRescanRequest;
    })();

    pb.ForceRescanResponse = (function() {

        /**
         * Properties of a ForceRescanResponse.
         * @memberof pb
         * @interface IForceRescanResponse
         * @property {boolean|null} [success] ForceRescanResponse success
         * @property {string|null} [message] ForceRescanResponse message
         */

        /**
         * Constructs a new ForceRescanResponse.
         * @memberof pb
         * @classdesc Represents a ForceRescanResponse.
         * @implements IForceRescanResponse
         * @constructor
         * @param {pb.IForceRescanResponse=} [properties] Properties to set
         */
        function ForceRescanResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ForceRescanResponse success.
         * @member {boolean} success
         * @memberof pb.ForceRescanResponse
         * @instance
         */
        ForceRescanResponse.prototype.success = false;

        /**
         * ForceRescanResponse message.
         * @member {string} message
         * @memberof pb.ForceRescanResponse
         * @instance
         */
        ForceRescanResponse.prototype.message = "";

        /**
         * Creates a new ForceRescanResponse instance using the specified properties.
         * @function create
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {pb.IForceRescanResponse=} [properties] Properties to set
         * @returns {pb.ForceRescanResponse} ForceRescanResponse instance
         */
        ForceRescanResponse.create = function create(properties) {
            return new ForceRescanResponse(properties);
        };

        /**
         * Encodes the specified ForceRescanResponse message. Does not implicitly {@link pb.ForceRescanResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {pb.IForceRescanResponse} message ForceRescanResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForceRescanResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            return writer;
        };

        /**
         * Encodes the specified ForceRescanResponse message, length delimited. Does not implicitly {@link pb.ForceRescanResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {pb.IForceRescanResponse} message ForceRescanResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForceRescanResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a ForceRescanResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.ForceRescanResponse} ForceRescanResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForceRescanResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.ForceRescanResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.success = reader.bool();
                        break;
                    }
                case 2: {
                        message.message = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ForceRescanResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.ForceRescanResponse} ForceRescanResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForceRescanResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForceRescanResponse message.
         * @function verify
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForceRescanResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.success != null && message.hasOwnProperty("success"))
                if (typeof message.success !== "boolean")
                    return "success: boolean expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates a ForceRescanResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.ForceRescanResponse} ForceRescanResponse
         */
        ForceRescanResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.ForceRescanResponse)
                return object;
            let message = new $root.pb.ForceRescanResponse();
            if (object.success != null)
                message.success = Boolean(object.success);
            if (object.message != null)
                message.message = String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a ForceRescanResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {pb.ForceRescanResponse} message ForceRescanResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForceRescanResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.success = false;
                object.message = "";
            }
            if (message.success != null && message.hasOwnProperty("success"))
                object.success = message.success;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this ForceRescanResponse to JSON.
         * @function toJSON
         * @memberof pb.ForceRescanResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForceRescanResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ForceRescanResponse
         * @function getTypeUrl
         * @memberof pb.ForceRescanResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ForceRescanResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.ForceRescanResponse";
        };

        return ForceRescanResponse;
    })();

    pb.LogsRequest = (function() {

        /**
         * Properties of a LogsRequest.
         * @memberof pb
         * @interface ILogsRequest
         * @property {number|null} [maxLines] LogsRequest maxLines
         */

        /**
         * Constructs a new LogsRequest.
         * @memberof pb
         * @classdesc Represents a LogsRequest.
         * @implements ILogsRequest
         * @constructor
         * @param {pb.ILogsRequest=} [properties] Properties to set
         */
        function LogsRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LogsRequest maxLines.
         * @member {number} maxLines
         * @memberof pb.LogsRequest
         * @instance
         */
        LogsRequest.prototype.maxLines = 0;

        /**
         * Creates a new LogsRequest instance using the specified properties.
         * @function create
         * @memberof pb.LogsRequest
         * @static
         * @param {pb.ILogsRequest=} [properties] Properties to set
         * @returns {pb.LogsRequest} LogsRequest instance
         */
        LogsRequest.create = function create(properties) {
            return new LogsRequest(properties);
        };

        /**
         * Encodes the specified LogsRequest message. Does not implicitly {@link pb.LogsRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.LogsRequest
         * @static
         * @param {pb.ILogsRequest} message LogsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogsRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.maxLines != null && Object.hasOwnProperty.call(message, "maxLines"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.maxLines);
            return writer;
        };

        /**
         * Encodes the specified LogsRequest message, length delimited. Does not implicitly {@link pb.LogsRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.LogsRequest
         * @static
         * @param {pb.ILogsRequest} message LogsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogsRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LogsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.LogsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.LogsRequest} LogsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogsRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.LogsRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.maxLines = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LogsRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.LogsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.LogsRequest} LogsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogsRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LogsRequest message.
         * @function verify
         * @memberof pb.LogsRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LogsRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.maxLines != null && message.hasOwnProperty("maxLines"))
                if (!$util.isInteger(message.maxLines))
                    return "maxLines: integer expected";
            return null;
        };

        /**
         * Creates a LogsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.LogsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.LogsRequest} LogsRequest
         */
        LogsRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.LogsRequest)
                return object;
            let message = new $root.pb.LogsRequest();
            if (object.maxLines != null)
                message.maxLines = object.maxLines | 0;
            return message;
        };

        /**
         * Creates a plain object from a LogsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.LogsRequest
         * @static
         * @param {pb.LogsRequest} message LogsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LogsRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.maxLines = 0;
            if (message.maxLines != null && message.hasOwnProperty("maxLines"))
                object.maxLines = message.maxLines;
            return object;
        };

        /**
         * Converts this LogsRequest to JSON.
         * @function toJSON
         * @memberof pb.LogsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LogsRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LogsRequest
         * @function getTypeUrl
         * @memberof pb.LogsRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LogsRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.LogsRequest";
        };

        return LogsRequest;
    })();

    pb.LogLine = (function() {

        /**
         * Properties of a LogLine.
         * @memberof pb
         * @interface ILogLine
         * @property {string|null} [timestamp] LogLine timestamp
         * @property {string|null} [level] LogLine level
         * @property {string|null} [message] LogLine message
         */

        /**
         * Constructs a new LogLine.
         * @memberof pb
         * @classdesc Represents a LogLine.
         * @implements ILogLine
         * @constructor
         * @param {pb.ILogLine=} [properties] Properties to set
         */
        function LogLine(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LogLine timestamp.
         * @member {string} timestamp
         * @memberof pb.LogLine
         * @instance
         */
        LogLine.prototype.timestamp = "";

        /**
         * LogLine level.
         * @member {string} level
         * @memberof pb.LogLine
         * @instance
         */
        LogLine.prototype.level = "";

        /**
         * LogLine message.
         * @member {string} message
         * @memberof pb.LogLine
         * @instance
         */
        LogLine.prototype.message = "";

        /**
         * Creates a new LogLine instance using the specified properties.
         * @function create
         * @memberof pb.LogLine
         * @static
         * @param {pb.ILogLine=} [properties] Properties to set
         * @returns {pb.LogLine} LogLine instance
         */
        LogLine.create = function create(properties) {
            return new LogLine(properties);
        };

        /**
         * Encodes the specified LogLine message. Does not implicitly {@link pb.LogLine.verify|verify} messages.
         * @function encode
         * @memberof pb.LogLine
         * @static
         * @param {pb.ILogLine} message LogLine message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogLine.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.timestamp);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.level);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.message);
            return writer;
        };

        /**
         * Encodes the specified LogLine message, length delimited. Does not implicitly {@link pb.LogLine.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.LogLine
         * @static
         * @param {pb.ILogLine} message LogLine message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogLine.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LogLine message from the specified reader or buffer.
         * @function decode
         * @memberof pb.LogLine
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.LogLine} LogLine
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogLine.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.LogLine();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.timestamp = reader.string();
                        break;
                    }
                case 2: {
                        message.level = reader.string();
                        break;
                    }
                case 3: {
                        message.message = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LogLine message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.LogLine
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.LogLine} LogLine
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogLine.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LogLine message.
         * @function verify
         * @memberof pb.LogLine
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LogLine.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isString(message.timestamp))
                    return "timestamp: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isString(message.level))
                    return "level: string expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        /**
         * Creates a LogLine message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.LogLine
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.LogLine} LogLine
         */
        LogLine.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.LogLine)
                return object;
            let message = new $root.pb.LogLine();
            if (object.timestamp != null)
                message.timestamp = String(object.timestamp);
            if (object.level != null)
                message.level = String(object.level);
            if (object.message != null)
                message.message = String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a LogLine message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.LogLine
         * @static
         * @param {pb.LogLine} message LogLine
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LogLine.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.timestamp = "";
                object.level = "";
                object.message = "";
            }
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                object.timestamp = message.timestamp;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this LogLine to JSON.
         * @function toJSON
         * @memberof pb.LogLine
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LogLine.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LogLine
         * @function getTypeUrl
         * @memberof pb.LogLine
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LogLine.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.LogLine";
        };

        return LogLine;
    })();

    pb.LogsResponse = (function() {

        /**
         * Properties of a LogsResponse.
         * @memberof pb
         * @interface ILogsResponse
         * @property {Array.<pb.ILogLine>|null} [logs] LogsResponse logs
         */

        /**
         * Constructs a new LogsResponse.
         * @memberof pb
         * @classdesc Represents a LogsResponse.
         * @implements ILogsResponse
         * @constructor
         * @param {pb.ILogsResponse=} [properties] Properties to set
         */
        function LogsResponse(properties) {
            this.logs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LogsResponse logs.
         * @member {Array.<pb.ILogLine>} logs
         * @memberof pb.LogsResponse
         * @instance
         */
        LogsResponse.prototype.logs = $util.emptyArray;

        /**
         * Creates a new LogsResponse instance using the specified properties.
         * @function create
         * @memberof pb.LogsResponse
         * @static
         * @param {pb.ILogsResponse=} [properties] Properties to set
         * @returns {pb.LogsResponse} LogsResponse instance
         */
        LogsResponse.create = function create(properties) {
            return new LogsResponse(properties);
        };

        /**
         * Encodes the specified LogsResponse message. Does not implicitly {@link pb.LogsResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.LogsResponse
         * @static
         * @param {pb.ILogsResponse} message LogsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogsResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.logs != null && message.logs.length)
                for (let i = 0; i < message.logs.length; ++i)
                    $root.pb.LogLine.encode(message.logs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified LogsResponse message, length delimited. Does not implicitly {@link pb.LogsResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.LogsResponse
         * @static
         * @param {pb.ILogsResponse} message LogsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LogsResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a LogsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.LogsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.LogsResponse} LogsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogsResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.LogsResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.logs && message.logs.length))
                            message.logs = [];
                        message.logs.push($root.pb.LogLine.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LogsResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.LogsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.LogsResponse} LogsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LogsResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LogsResponse message.
         * @function verify
         * @memberof pb.LogsResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LogsResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.logs != null && message.hasOwnProperty("logs")) {
                if (!Array.isArray(message.logs))
                    return "logs: array expected";
                for (let i = 0; i < message.logs.length; ++i) {
                    let error = $root.pb.LogLine.verify(message.logs[i]);
                    if (error)
                        return "logs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a LogsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.LogsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.LogsResponse} LogsResponse
         */
        LogsResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.LogsResponse)
                return object;
            let message = new $root.pb.LogsResponse();
            if (object.logs) {
                if (!Array.isArray(object.logs))
                    throw TypeError(".pb.LogsResponse.logs: array expected");
                message.logs = [];
                for (let i = 0; i < object.logs.length; ++i) {
                    if (typeof object.logs[i] !== "object")
                        throw TypeError(".pb.LogsResponse.logs: object expected");
                    message.logs[i] = $root.pb.LogLine.fromObject(object.logs[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a LogsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.LogsResponse
         * @static
         * @param {pb.LogsResponse} message LogsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LogsResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.logs = [];
            if (message.logs && message.logs.length) {
                object.logs = [];
                for (let j = 0; j < message.logs.length; ++j)
                    object.logs[j] = $root.pb.LogLine.toObject(message.logs[j], options);
            }
            return object;
        };

        /**
         * Converts this LogsResponse to JSON.
         * @function toJSON
         * @memberof pb.LogsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LogsResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LogsResponse
         * @function getTypeUrl
         * @memberof pb.LogsResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LogsResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.LogsResponse";
        };

        return LogsResponse;
    })();

    pb.BlockSummary = (function() {

        /**
         * Properties of a BlockSummary.
         * @memberof pb
         * @interface IBlockSummary
         * @property {number|Long|null} [height] BlockSummary height
         * @property {string|null} [blockHash] BlockSummary blockHash
         * @property {string|null} [txsHash] BlockSummary txsHash
         * @property {string|null} [miner] BlockSummary miner
         * @property {number|null} [txCount] BlockSummary txCount
         * @property {string|null} [accumulatedReward] BlockSummary accumulatedReward
         * @property {number|null} [window] BlockSummary window
         * @property {string|null} [stateRoot] BlockSummary stateRoot
         */

        /**
         * Constructs a new BlockSummary.
         * @memberof pb
         * @classdesc Represents a BlockSummary.
         * @implements IBlockSummary
         * @constructor
         * @param {pb.IBlockSummary=} [properties] Properties to set
         */
        function BlockSummary(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BlockSummary height.
         * @member {number|Long} height
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.height = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BlockSummary blockHash.
         * @member {string} blockHash
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.blockHash = "";

        /**
         * BlockSummary txsHash.
         * @member {string} txsHash
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.txsHash = "";

        /**
         * BlockSummary miner.
         * @member {string} miner
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.miner = "";

        /**
         * BlockSummary txCount.
         * @member {number} txCount
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.txCount = 0;

        /**
         * BlockSummary accumulatedReward.
         * @member {string} accumulatedReward
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.accumulatedReward = "";

        /**
         * BlockSummary window.
         * @member {number} window
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.window = 0;

        /**
         * BlockSummary stateRoot.
         * @member {string} stateRoot
         * @memberof pb.BlockSummary
         * @instance
         */
        BlockSummary.prototype.stateRoot = "";

        /**
         * Creates a new BlockSummary instance using the specified properties.
         * @function create
         * @memberof pb.BlockSummary
         * @static
         * @param {pb.IBlockSummary=} [properties] Properties to set
         * @returns {pb.BlockSummary} BlockSummary instance
         */
        BlockSummary.create = function create(properties) {
            return new BlockSummary(properties);
        };

        /**
         * Encodes the specified BlockSummary message. Does not implicitly {@link pb.BlockSummary.verify|verify} messages.
         * @function encode
         * @memberof pb.BlockSummary
         * @static
         * @param {pb.IBlockSummary} message BlockSummary message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockSummary.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.height);
            if (message.blockHash != null && Object.hasOwnProperty.call(message, "blockHash"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.blockHash);
            if (message.txsHash != null && Object.hasOwnProperty.call(message, "txsHash"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.txsHash);
            if (message.miner != null && Object.hasOwnProperty.call(message, "miner"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.miner);
            if (message.txCount != null && Object.hasOwnProperty.call(message, "txCount"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.txCount);
            if (message.accumulatedReward != null && Object.hasOwnProperty.call(message, "accumulatedReward"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.accumulatedReward);
            if (message.window != null && Object.hasOwnProperty.call(message, "window"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.window);
            if (message.stateRoot != null && Object.hasOwnProperty.call(message, "stateRoot"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.stateRoot);
            return writer;
        };

        /**
         * Encodes the specified BlockSummary message, length delimited. Does not implicitly {@link pb.BlockSummary.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.BlockSummary
         * @static
         * @param {pb.IBlockSummary} message BlockSummary message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BlockSummary.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a BlockSummary message from the specified reader or buffer.
         * @function decode
         * @memberof pb.BlockSummary
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.BlockSummary} BlockSummary
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockSummary.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.BlockSummary();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.height = reader.uint64();
                        break;
                    }
                case 2: {
                        message.blockHash = reader.string();
                        break;
                    }
                case 3: {
                        message.txsHash = reader.string();
                        break;
                    }
                case 4: {
                        message.miner = reader.string();
                        break;
                    }
                case 5: {
                        message.txCount = reader.int32();
                        break;
                    }
                case 6: {
                        message.accumulatedReward = reader.string();
                        break;
                    }
                case 7: {
                        message.window = reader.int32();
                        break;
                    }
                case 8: {
                        message.stateRoot = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BlockSummary message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.BlockSummary
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.BlockSummary} BlockSummary
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BlockSummary.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BlockSummary message.
         * @function verify
         * @memberof pb.BlockSummary
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BlockSummary.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.height != null && message.hasOwnProperty("height"))
                if (!$util.isInteger(message.height) && !(message.height && $util.isInteger(message.height.low) && $util.isInteger(message.height.high)))
                    return "height: integer|Long expected";
            if (message.blockHash != null && message.hasOwnProperty("blockHash"))
                if (!$util.isString(message.blockHash))
                    return "blockHash: string expected";
            if (message.txsHash != null && message.hasOwnProperty("txsHash"))
                if (!$util.isString(message.txsHash))
                    return "txsHash: string expected";
            if (message.miner != null && message.hasOwnProperty("miner"))
                if (!$util.isString(message.miner))
                    return "miner: string expected";
            if (message.txCount != null && message.hasOwnProperty("txCount"))
                if (!$util.isInteger(message.txCount))
                    return "txCount: integer expected";
            if (message.accumulatedReward != null && message.hasOwnProperty("accumulatedReward"))
                if (!$util.isString(message.accumulatedReward))
                    return "accumulatedReward: string expected";
            if (message.window != null && message.hasOwnProperty("window"))
                if (!$util.isInteger(message.window))
                    return "window: integer expected";
            if (message.stateRoot != null && message.hasOwnProperty("stateRoot"))
                if (!$util.isString(message.stateRoot))
                    return "stateRoot: string expected";
            return null;
        };

        /**
         * Creates a BlockSummary message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.BlockSummary
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.BlockSummary} BlockSummary
         */
        BlockSummary.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.BlockSummary)
                return object;
            let message = new $root.pb.BlockSummary();
            if (object.height != null)
                if ($util.Long)
                    (message.height = $util.Long.fromValue(object.height)).unsigned = true;
                else if (typeof object.height === "string")
                    message.height = parseInt(object.height, 10);
                else if (typeof object.height === "number")
                    message.height = object.height;
                else if (typeof object.height === "object")
                    message.height = new $util.LongBits(object.height.low >>> 0, object.height.high >>> 0).toNumber(true);
            if (object.blockHash != null)
                message.blockHash = String(object.blockHash);
            if (object.txsHash != null)
                message.txsHash = String(object.txsHash);
            if (object.miner != null)
                message.miner = String(object.miner);
            if (object.txCount != null)
                message.txCount = object.txCount | 0;
            if (object.accumulatedReward != null)
                message.accumulatedReward = String(object.accumulatedReward);
            if (object.window != null)
                message.window = object.window | 0;
            if (object.stateRoot != null)
                message.stateRoot = String(object.stateRoot);
            return message;
        };

        /**
         * Creates a plain object from a BlockSummary message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.BlockSummary
         * @static
         * @param {pb.BlockSummary} message BlockSummary
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BlockSummary.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.height = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : long;
                } else
                    object.height = options.longs === String ? "0" : 0;
                object.blockHash = "";
                object.txsHash = "";
                object.miner = "";
                object.txCount = 0;
                object.accumulatedReward = "";
                object.window = 0;
                object.stateRoot = "";
            }
            if (message.height != null && message.hasOwnProperty("height"))
                if (typeof message.height === "number")
                    object.height = options.longs === String ? String(message.height) : message.height;
                else
                    object.height = options.longs === String ? $util.Long.prototype.toString.call(message.height) : options.longs === Number ? new $util.LongBits(message.height.low >>> 0, message.height.high >>> 0).toNumber(true) : message.height;
            if (message.blockHash != null && message.hasOwnProperty("blockHash"))
                object.blockHash = message.blockHash;
            if (message.txsHash != null && message.hasOwnProperty("txsHash"))
                object.txsHash = message.txsHash;
            if (message.miner != null && message.hasOwnProperty("miner"))
                object.miner = message.miner;
            if (message.txCount != null && message.hasOwnProperty("txCount"))
                object.txCount = message.txCount;
            if (message.accumulatedReward != null && message.hasOwnProperty("accumulatedReward"))
                object.accumulatedReward = message.accumulatedReward;
            if (message.window != null && message.hasOwnProperty("window"))
                object.window = message.window;
            if (message.stateRoot != null && message.hasOwnProperty("stateRoot"))
                object.stateRoot = message.stateRoot;
            return object;
        };

        /**
         * Converts this BlockSummary to JSON.
         * @function toJSON
         * @memberof pb.BlockSummary
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BlockSummary.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BlockSummary
         * @function getTypeUrl
         * @memberof pb.BlockSummary
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BlockSummary.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.BlockSummary";
        };

        return BlockSummary;
    })();

    pb.GetRecentBlocksRequest = (function() {

        /**
         * Properties of a GetRecentBlocksRequest.
         * @memberof pb
         * @interface IGetRecentBlocksRequest
         * @property {number|null} [count] GetRecentBlocksRequest count
         */

        /**
         * Constructs a new GetRecentBlocksRequest.
         * @memberof pb
         * @classdesc Represents a GetRecentBlocksRequest.
         * @implements IGetRecentBlocksRequest
         * @constructor
         * @param {pb.IGetRecentBlocksRequest=} [properties] Properties to set
         */
        function GetRecentBlocksRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetRecentBlocksRequest count.
         * @member {number} count
         * @memberof pb.GetRecentBlocksRequest
         * @instance
         */
        GetRecentBlocksRequest.prototype.count = 0;

        /**
         * Creates a new GetRecentBlocksRequest instance using the specified properties.
         * @function create
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {pb.IGetRecentBlocksRequest=} [properties] Properties to set
         * @returns {pb.GetRecentBlocksRequest} GetRecentBlocksRequest instance
         */
        GetRecentBlocksRequest.create = function create(properties) {
            return new GetRecentBlocksRequest(properties);
        };

        /**
         * Encodes the specified GetRecentBlocksRequest message. Does not implicitly {@link pb.GetRecentBlocksRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {pb.IGetRecentBlocksRequest} message GetRecentBlocksRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecentBlocksRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.count);
            return writer;
        };

        /**
         * Encodes the specified GetRecentBlocksRequest message, length delimited. Does not implicitly {@link pb.GetRecentBlocksRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {pb.IGetRecentBlocksRequest} message GetRecentBlocksRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecentBlocksRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetRecentBlocksRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetRecentBlocksRequest} GetRecentBlocksRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecentBlocksRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetRecentBlocksRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.count = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetRecentBlocksRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetRecentBlocksRequest} GetRecentBlocksRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecentBlocksRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetRecentBlocksRequest message.
         * @function verify
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetRecentBlocksRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        /**
         * Creates a GetRecentBlocksRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetRecentBlocksRequest} GetRecentBlocksRequest
         */
        GetRecentBlocksRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetRecentBlocksRequest)
                return object;
            let message = new $root.pb.GetRecentBlocksRequest();
            if (object.count != null)
                message.count = object.count | 0;
            return message;
        };

        /**
         * Creates a plain object from a GetRecentBlocksRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {pb.GetRecentBlocksRequest} message GetRecentBlocksRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetRecentBlocksRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.count = 0;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        /**
         * Converts this GetRecentBlocksRequest to JSON.
         * @function toJSON
         * @memberof pb.GetRecentBlocksRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetRecentBlocksRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetRecentBlocksRequest
         * @function getTypeUrl
         * @memberof pb.GetRecentBlocksRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetRecentBlocksRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetRecentBlocksRequest";
        };

        return GetRecentBlocksRequest;
    })();

    pb.GetRecentBlocksResponse = (function() {

        /**
         * Properties of a GetRecentBlocksResponse.
         * @memberof pb
         * @interface IGetRecentBlocksResponse
         * @property {Array.<pb.IBlockSummary>|null} [blocks] GetRecentBlocksResponse blocks
         */

        /**
         * Constructs a new GetRecentBlocksResponse.
         * @memberof pb
         * @classdesc Represents a GetRecentBlocksResponse.
         * @implements IGetRecentBlocksResponse
         * @constructor
         * @param {pb.IGetRecentBlocksResponse=} [properties] Properties to set
         */
        function GetRecentBlocksResponse(properties) {
            this.blocks = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetRecentBlocksResponse blocks.
         * @member {Array.<pb.IBlockSummary>} blocks
         * @memberof pb.GetRecentBlocksResponse
         * @instance
         */
        GetRecentBlocksResponse.prototype.blocks = $util.emptyArray;

        /**
         * Creates a new GetRecentBlocksResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {pb.IGetRecentBlocksResponse=} [properties] Properties to set
         * @returns {pb.GetRecentBlocksResponse} GetRecentBlocksResponse instance
         */
        GetRecentBlocksResponse.create = function create(properties) {
            return new GetRecentBlocksResponse(properties);
        };

        /**
         * Encodes the specified GetRecentBlocksResponse message. Does not implicitly {@link pb.GetRecentBlocksResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {pb.IGetRecentBlocksResponse} message GetRecentBlocksResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecentBlocksResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.blocks != null && message.blocks.length)
                for (let i = 0; i < message.blocks.length; ++i)
                    $root.pb.BlockSummary.encode(message.blocks[i], writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GetRecentBlocksResponse message, length delimited. Does not implicitly {@link pb.GetRecentBlocksResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {pb.IGetRecentBlocksResponse} message GetRecentBlocksResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetRecentBlocksResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetRecentBlocksResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetRecentBlocksResponse} GetRecentBlocksResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecentBlocksResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetRecentBlocksResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.blocks && message.blocks.length))
                            message.blocks = [];
                        message.blocks.push($root.pb.BlockSummary.decode(reader, reader.uint32()));
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetRecentBlocksResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetRecentBlocksResponse} GetRecentBlocksResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetRecentBlocksResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetRecentBlocksResponse message.
         * @function verify
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetRecentBlocksResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.blocks != null && message.hasOwnProperty("blocks")) {
                if (!Array.isArray(message.blocks))
                    return "blocks: array expected";
                for (let i = 0; i < message.blocks.length; ++i) {
                    let error = $root.pb.BlockSummary.verify(message.blocks[i]);
                    if (error)
                        return "blocks." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GetRecentBlocksResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetRecentBlocksResponse} GetRecentBlocksResponse
         */
        GetRecentBlocksResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetRecentBlocksResponse)
                return object;
            let message = new $root.pb.GetRecentBlocksResponse();
            if (object.blocks) {
                if (!Array.isArray(object.blocks))
                    throw TypeError(".pb.GetRecentBlocksResponse.blocks: array expected");
                message.blocks = [];
                for (let i = 0; i < object.blocks.length; ++i) {
                    if (typeof object.blocks[i] !== "object")
                        throw TypeError(".pb.GetRecentBlocksResponse.blocks: object expected");
                    message.blocks[i] = $root.pb.BlockSummary.fromObject(object.blocks[i]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetRecentBlocksResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {pb.GetRecentBlocksResponse} message GetRecentBlocksResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetRecentBlocksResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.blocks = [];
            if (message.blocks && message.blocks.length) {
                object.blocks = [];
                for (let j = 0; j < message.blocks.length; ++j)
                    object.blocks[j] = $root.pb.BlockSummary.toObject(message.blocks[j], options);
            }
            return object;
        };

        /**
         * Converts this GetRecentBlocksResponse to JSON.
         * @function toJSON
         * @memberof pb.GetRecentBlocksResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetRecentBlocksResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetRecentBlocksResponse
         * @function getTypeUrl
         * @memberof pb.GetRecentBlocksResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetRecentBlocksResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetRecentBlocksResponse";
        };

        return GetRecentBlocksResponse;
    })();

    pb.GetAccountRequest = (function() {

        /**
         * Properties of a GetAccountRequest.
         * @memberof pb
         * @interface IGetAccountRequest
         * @property {string|null} [address] GetAccountRequest address
         */

        /**
         * Constructs a new GetAccountRequest.
         * @memberof pb
         * @classdesc Represents a GetAccountRequest.
         * @implements IGetAccountRequest
         * @constructor
         * @param {pb.IGetAccountRequest=} [properties] Properties to set
         */
        function GetAccountRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetAccountRequest address.
         * @member {string} address
         * @memberof pb.GetAccountRequest
         * @instance
         */
        GetAccountRequest.prototype.address = "";

        /**
         * Creates a new GetAccountRequest instance using the specified properties.
         * @function create
         * @memberof pb.GetAccountRequest
         * @static
         * @param {pb.IGetAccountRequest=} [properties] Properties to set
         * @returns {pb.GetAccountRequest} GetAccountRequest instance
         */
        GetAccountRequest.create = function create(properties) {
            return new GetAccountRequest(properties);
        };

        /**
         * Encodes the specified GetAccountRequest message. Does not implicitly {@link pb.GetAccountRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.GetAccountRequest
         * @static
         * @param {pb.IGetAccountRequest} message GetAccountRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            return writer;
        };

        /**
         * Encodes the specified GetAccountRequest message, length delimited. Does not implicitly {@link pb.GetAccountRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetAccountRequest
         * @static
         * @param {pb.IGetAccountRequest} message GetAccountRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetAccountRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetAccountRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetAccountRequest} GetAccountRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetAccountRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetAccountRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetAccountRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetAccountRequest} GetAccountRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetAccountRequest message.
         * @function verify
         * @memberof pb.GetAccountRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetAccountRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates a GetAccountRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetAccountRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetAccountRequest} GetAccountRequest
         */
        GetAccountRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetAccountRequest)
                return object;
            let message = new $root.pb.GetAccountRequest();
            if (object.address != null)
                message.address = String(object.address);
            return message;
        };

        /**
         * Creates a plain object from a GetAccountRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetAccountRequest
         * @static
         * @param {pb.GetAccountRequest} message GetAccountRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAccountRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.address = "";
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this GetAccountRequest to JSON.
         * @function toJSON
         * @memberof pb.GetAccountRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAccountRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetAccountRequest
         * @function getTypeUrl
         * @memberof pb.GetAccountRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetAccountRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetAccountRequest";
        };

        return GetAccountRequest;
    })();

    pb.GetAccountResponse = (function() {

        /**
         * Properties of a GetAccountResponse.
         * @memberof pb
         * @interface IGetAccountResponse
         * @property {pb.IAccount|null} [account] GetAccountResponse account
         * @property {string|null} [error] GetAccountResponse error
         * @property {Object.<string,pb.ITokenBalance>|null} [balances] GetAccountResponse balances
         */

        /**
         * Constructs a new GetAccountResponse.
         * @memberof pb
         * @classdesc Represents a GetAccountResponse.
         * @implements IGetAccountResponse
         * @constructor
         * @param {pb.IGetAccountResponse=} [properties] Properties to set
         */
        function GetAccountResponse(properties) {
            this.balances = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetAccountResponse account.
         * @member {pb.IAccount|null|undefined} account
         * @memberof pb.GetAccountResponse
         * @instance
         */
        GetAccountResponse.prototype.account = null;

        /**
         * GetAccountResponse error.
         * @member {string} error
         * @memberof pb.GetAccountResponse
         * @instance
         */
        GetAccountResponse.prototype.error = "";

        /**
         * GetAccountResponse balances.
         * @member {Object.<string,pb.ITokenBalance>} balances
         * @memberof pb.GetAccountResponse
         * @instance
         */
        GetAccountResponse.prototype.balances = $util.emptyObject;

        /**
         * Creates a new GetAccountResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetAccountResponse
         * @static
         * @param {pb.IGetAccountResponse=} [properties] Properties to set
         * @returns {pb.GetAccountResponse} GetAccountResponse instance
         */
        GetAccountResponse.create = function create(properties) {
            return new GetAccountResponse(properties);
        };

        /**
         * Encodes the specified GetAccountResponse message. Does not implicitly {@link pb.GetAccountResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetAccountResponse
         * @static
         * @param {pb.IGetAccountResponse} message GetAccountResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account != null && Object.hasOwnProperty.call(message, "account"))
                $root.pb.Account.encode(message.account, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.error);
            if (message.balances != null && Object.hasOwnProperty.call(message, "balances"))
                for (let keys = Object.keys(message.balances), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.pb.TokenBalance.encode(message.balances[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            return writer;
        };

        /**
         * Encodes the specified GetAccountResponse message, length delimited. Does not implicitly {@link pb.GetAccountResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetAccountResponse
         * @static
         * @param {pb.IGetAccountResponse} message GetAccountResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetAccountResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetAccountResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetAccountResponse} GetAccountResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetAccountResponse(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.account = $root.pb.Account.decode(reader, reader.uint32());
                        break;
                    }
                case 2: {
                        message.error = reader.string();
                        break;
                    }
                case 3: {
                        if (message.balances === $util.emptyObject)
                            message.balances = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = $root.pb.TokenBalance.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.balances[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetAccountResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetAccountResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetAccountResponse} GetAccountResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetAccountResponse message.
         * @function verify
         * @memberof pb.GetAccountResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetAccountResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.account != null && message.hasOwnProperty("account")) {
                let error = $root.pb.Account.verify(message.account);
                if (error)
                    return "account." + error;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            if (message.balances != null && message.hasOwnProperty("balances")) {
                if (!$util.isObject(message.balances))
                    return "balances: object expected";
                let key = Object.keys(message.balances);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.pb.TokenBalance.verify(message.balances[key[i]]);
                    if (error)
                        return "balances." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GetAccountResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetAccountResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetAccountResponse} GetAccountResponse
         */
        GetAccountResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetAccountResponse)
                return object;
            let message = new $root.pb.GetAccountResponse();
            if (object.account != null) {
                if (typeof object.account !== "object")
                    throw TypeError(".pb.GetAccountResponse.account: object expected");
                message.account = $root.pb.Account.fromObject(object.account);
            }
            if (object.error != null)
                message.error = String(object.error);
            if (object.balances) {
                if (typeof object.balances !== "object")
                    throw TypeError(".pb.GetAccountResponse.balances: object expected");
                message.balances = {};
                for (let keys = Object.keys(object.balances), i = 0; i < keys.length; ++i) {
                    if (typeof object.balances[keys[i]] !== "object")
                        throw TypeError(".pb.GetAccountResponse.balances: object expected");
                    message.balances[keys[i]] = $root.pb.TokenBalance.fromObject(object.balances[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetAccountResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetAccountResponse
         * @static
         * @param {pb.GetAccountResponse} message GetAccountResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAccountResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.objects || options.defaults)
                object.balances = {};
            if (options.defaults) {
                object.account = null;
                object.error = "";
            }
            if (message.account != null && message.hasOwnProperty("account"))
                object.account = $root.pb.Account.toObject(message.account, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            let keys2;
            if (message.balances && (keys2 = Object.keys(message.balances)).length) {
                object.balances = {};
                for (let j = 0; j < keys2.length; ++j)
                    object.balances[keys2[j]] = $root.pb.TokenBalance.toObject(message.balances[keys2[j]], options);
            }
            return object;
        };

        /**
         * Converts this GetAccountResponse to JSON.
         * @function toJSON
         * @memberof pb.GetAccountResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAccountResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetAccountResponse
         * @function getTypeUrl
         * @memberof pb.GetAccountResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetAccountResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetAccountResponse";
        };

        return GetAccountResponse;
    })();

    pb.GetAccountBalancesRequest = (function() {

        /**
         * Properties of a GetAccountBalancesRequest.
         * @memberof pb
         * @interface IGetAccountBalancesRequest
         * @property {string|null} [address] GetAccountBalancesRequest address
         */

        /**
         * Constructs a new GetAccountBalancesRequest.
         * @memberof pb
         * @classdesc Represents a GetAccountBalancesRequest.
         * @implements IGetAccountBalancesRequest
         * @constructor
         * @param {pb.IGetAccountBalancesRequest=} [properties] Properties to set
         */
        function GetAccountBalancesRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetAccountBalancesRequest address.
         * @member {string} address
         * @memberof pb.GetAccountBalancesRequest
         * @instance
         */
        GetAccountBalancesRequest.prototype.address = "";

        /**
         * Creates a new GetAccountBalancesRequest instance using the specified properties.
         * @function create
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {pb.IGetAccountBalancesRequest=} [properties] Properties to set
         * @returns {pb.GetAccountBalancesRequest} GetAccountBalancesRequest instance
         */
        GetAccountBalancesRequest.create = function create(properties) {
            return new GetAccountBalancesRequest(properties);
        };

        /**
         * Encodes the specified GetAccountBalancesRequest message. Does not implicitly {@link pb.GetAccountBalancesRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {pb.IGetAccountBalancesRequest} message GetAccountBalancesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountBalancesRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.address);
            return writer;
        };

        /**
         * Encodes the specified GetAccountBalancesRequest message, length delimited. Does not implicitly {@link pb.GetAccountBalancesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {pb.IGetAccountBalancesRequest} message GetAccountBalancesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountBalancesRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetAccountBalancesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetAccountBalancesRequest} GetAccountBalancesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountBalancesRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetAccountBalancesRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.address = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetAccountBalancesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetAccountBalancesRequest} GetAccountBalancesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountBalancesRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetAccountBalancesRequest message.
         * @function verify
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetAccountBalancesRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            return null;
        };

        /**
         * Creates a GetAccountBalancesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetAccountBalancesRequest} GetAccountBalancesRequest
         */
        GetAccountBalancesRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetAccountBalancesRequest)
                return object;
            let message = new $root.pb.GetAccountBalancesRequest();
            if (object.address != null)
                message.address = String(object.address);
            return message;
        };

        /**
         * Creates a plain object from a GetAccountBalancesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {pb.GetAccountBalancesRequest} message GetAccountBalancesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAccountBalancesRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.address = "";
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            return object;
        };

        /**
         * Converts this GetAccountBalancesRequest to JSON.
         * @function toJSON
         * @memberof pb.GetAccountBalancesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAccountBalancesRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetAccountBalancesRequest
         * @function getTypeUrl
         * @memberof pb.GetAccountBalancesRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetAccountBalancesRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetAccountBalancesRequest";
        };

        return GetAccountBalancesRequest;
    })();

    pb.GetAccountBalancesResponse = (function() {

        /**
         * Properties of a GetAccountBalancesResponse.
         * @memberof pb
         * @interface IGetAccountBalancesResponse
         * @property {Object.<string,pb.ITokenBalance>|null} [balances] GetAccountBalancesResponse balances
         * @property {string|null} [error] GetAccountBalancesResponse error
         */

        /**
         * Constructs a new GetAccountBalancesResponse.
         * @memberof pb
         * @classdesc Represents a GetAccountBalancesResponse.
         * @implements IGetAccountBalancesResponse
         * @constructor
         * @param {pb.IGetAccountBalancesResponse=} [properties] Properties to set
         */
        function GetAccountBalancesResponse(properties) {
            this.balances = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetAccountBalancesResponse balances.
         * @member {Object.<string,pb.ITokenBalance>} balances
         * @memberof pb.GetAccountBalancesResponse
         * @instance
         */
        GetAccountBalancesResponse.prototype.balances = $util.emptyObject;

        /**
         * GetAccountBalancesResponse error.
         * @member {string} error
         * @memberof pb.GetAccountBalancesResponse
         * @instance
         */
        GetAccountBalancesResponse.prototype.error = "";

        /**
         * Creates a new GetAccountBalancesResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {pb.IGetAccountBalancesResponse=} [properties] Properties to set
         * @returns {pb.GetAccountBalancesResponse} GetAccountBalancesResponse instance
         */
        GetAccountBalancesResponse.create = function create(properties) {
            return new GetAccountBalancesResponse(properties);
        };

        /**
         * Encodes the specified GetAccountBalancesResponse message. Does not implicitly {@link pb.GetAccountBalancesResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {pb.IGetAccountBalancesResponse} message GetAccountBalancesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountBalancesResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.balances != null && Object.hasOwnProperty.call(message, "balances"))
                for (let keys = Object.keys(message.balances), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                    $root.pb.TokenBalance.encode(message.balances[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork()).ldelim().ldelim();
                }
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.error);
            return writer;
        };

        /**
         * Encodes the specified GetAccountBalancesResponse message, length delimited. Does not implicitly {@link pb.GetAccountBalancesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {pb.IGetAccountBalancesResponse} message GetAccountBalancesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAccountBalancesResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetAccountBalancesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetAccountBalancesResponse} GetAccountBalancesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountBalancesResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetAccountBalancesResponse(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (message.balances === $util.emptyObject)
                            message.balances = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = null;
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = $root.pb.TokenBalance.decode(reader, reader.uint32());
                                break;
                            default:
                                reader.skipType(tag2 & 7);
                                break;
                            }
                        }
                        message.balances[key] = value;
                        break;
                    }
                case 2: {
                        message.error = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetAccountBalancesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetAccountBalancesResponse} GetAccountBalancesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAccountBalancesResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetAccountBalancesResponse message.
         * @function verify
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetAccountBalancesResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.balances != null && message.hasOwnProperty("balances")) {
                if (!$util.isObject(message.balances))
                    return "balances: object expected";
                let key = Object.keys(message.balances);
                for (let i = 0; i < key.length; ++i) {
                    let error = $root.pb.TokenBalance.verify(message.balances[key[i]]);
                    if (error)
                        return "balances." + error;
                }
            }
            if (message.error != null && message.hasOwnProperty("error"))
                if (!$util.isString(message.error))
                    return "error: string expected";
            return null;
        };

        /**
         * Creates a GetAccountBalancesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetAccountBalancesResponse} GetAccountBalancesResponse
         */
        GetAccountBalancesResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetAccountBalancesResponse)
                return object;
            let message = new $root.pb.GetAccountBalancesResponse();
            if (object.balances) {
                if (typeof object.balances !== "object")
                    throw TypeError(".pb.GetAccountBalancesResponse.balances: object expected");
                message.balances = {};
                for (let keys = Object.keys(object.balances), i = 0; i < keys.length; ++i) {
                    if (typeof object.balances[keys[i]] !== "object")
                        throw TypeError(".pb.GetAccountBalancesResponse.balances: object expected");
                    message.balances[keys[i]] = $root.pb.TokenBalance.fromObject(object.balances[keys[i]]);
                }
            }
            if (object.error != null)
                message.error = String(object.error);
            return message;
        };

        /**
         * Creates a plain object from a GetAccountBalancesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {pb.GetAccountBalancesResponse} message GetAccountBalancesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAccountBalancesResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.objects || options.defaults)
                object.balances = {};
            if (options.defaults)
                object.error = "";
            let keys2;
            if (message.balances && (keys2 = Object.keys(message.balances)).length) {
                object.balances = {};
                for (let j = 0; j < keys2.length; ++j)
                    object.balances[keys2[j]] = $root.pb.TokenBalance.toObject(message.balances[keys2[j]], options);
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = message.error;
            return object;
        };

        /**
         * Converts this GetAccountBalancesResponse to JSON.
         * @function toJSON
         * @memberof pb.GetAccountBalancesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAccountBalancesResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetAccountBalancesResponse
         * @function getTypeUrl
         * @memberof pb.GetAccountBalancesResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetAccountBalancesResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetAccountBalancesResponse";
        };

        return GetAccountBalancesResponse;
    })();

    pb.GetTokenRequest = (function() {

        /**
         * Properties of a GetTokenRequest.
         * @memberof pb
         * @interface IGetTokenRequest
         * @property {string|null} [tokenAddress] GetTokenRequest tokenAddress
         */

        /**
         * Constructs a new GetTokenRequest.
         * @memberof pb
         * @classdesc Represents a GetTokenRequest.
         * @implements IGetTokenRequest
         * @constructor
         * @param {pb.IGetTokenRequest=} [properties] Properties to set
         */
        function GetTokenRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetTokenRequest tokenAddress.
         * @member {string} tokenAddress
         * @memberof pb.GetTokenRequest
         * @instance
         */
        GetTokenRequest.prototype.tokenAddress = "";

        /**
         * Creates a new GetTokenRequest instance using the specified properties.
         * @function create
         * @memberof pb.GetTokenRequest
         * @static
         * @param {pb.IGetTokenRequest=} [properties] Properties to set
         * @returns {pb.GetTokenRequest} GetTokenRequest instance
         */
        GetTokenRequest.create = function create(properties) {
            return new GetTokenRequest(properties);
        };

        /**
         * Encodes the specified GetTokenRequest message. Does not implicitly {@link pb.GetTokenRequest.verify|verify} messages.
         * @function encode
         * @memberof pb.GetTokenRequest
         * @static
         * @param {pb.IGetTokenRequest} message GetTokenRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTokenRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tokenAddress != null && Object.hasOwnProperty.call(message, "tokenAddress"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.tokenAddress);
            return writer;
        };

        /**
         * Encodes the specified GetTokenRequest message, length delimited. Does not implicitly {@link pb.GetTokenRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetTokenRequest
         * @static
         * @param {pb.IGetTokenRequest} message GetTokenRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTokenRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetTokenRequest message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetTokenRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetTokenRequest} GetTokenRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTokenRequest.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetTokenRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.tokenAddress = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetTokenRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetTokenRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetTokenRequest} GetTokenRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTokenRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetTokenRequest message.
         * @function verify
         * @memberof pb.GetTokenRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetTokenRequest.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                if (!$util.isString(message.tokenAddress))
                    return "tokenAddress: string expected";
            return null;
        };

        /**
         * Creates a GetTokenRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetTokenRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetTokenRequest} GetTokenRequest
         */
        GetTokenRequest.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetTokenRequest)
                return object;
            let message = new $root.pb.GetTokenRequest();
            if (object.tokenAddress != null)
                message.tokenAddress = String(object.tokenAddress);
            return message;
        };

        /**
         * Creates a plain object from a GetTokenRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetTokenRequest
         * @static
         * @param {pb.GetTokenRequest} message GetTokenRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetTokenRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.tokenAddress = "";
            if (message.tokenAddress != null && message.hasOwnProperty("tokenAddress"))
                object.tokenAddress = message.tokenAddress;
            return object;
        };

        /**
         * Converts this GetTokenRequest to JSON.
         * @function toJSON
         * @memberof pb.GetTokenRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetTokenRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetTokenRequest
         * @function getTypeUrl
         * @memberof pb.GetTokenRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetTokenRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetTokenRequest";
        };

        return GetTokenRequest;
    })();

    pb.GetTokenResponse = (function() {

        /**
         * Properties of a GetTokenResponse.
         * @memberof pb
         * @interface IGetTokenResponse
         * @property {pb.IToken|null} [token] GetTokenResponse token
         */

        /**
         * Constructs a new GetTokenResponse.
         * @memberof pb
         * @classdesc Represents a GetTokenResponse.
         * @implements IGetTokenResponse
         * @constructor
         * @param {pb.IGetTokenResponse=} [properties] Properties to set
         */
        function GetTokenResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetTokenResponse token.
         * @member {pb.IToken|null|undefined} token
         * @memberof pb.GetTokenResponse
         * @instance
         */
        GetTokenResponse.prototype.token = null;

        /**
         * Creates a new GetTokenResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetTokenResponse
         * @static
         * @param {pb.IGetTokenResponse=} [properties] Properties to set
         * @returns {pb.GetTokenResponse} GetTokenResponse instance
         */
        GetTokenResponse.create = function create(properties) {
            return new GetTokenResponse(properties);
        };

        /**
         * Encodes the specified GetTokenResponse message. Does not implicitly {@link pb.GetTokenResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetTokenResponse
         * @static
         * @param {pb.IGetTokenResponse} message GetTokenResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTokenResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                $root.pb.Token.encode(message.token, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GetTokenResponse message, length delimited. Does not implicitly {@link pb.GetTokenResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetTokenResponse
         * @static
         * @param {pb.IGetTokenResponse} message GetTokenResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTokenResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetTokenResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetTokenResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetTokenResponse} GetTokenResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTokenResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetTokenResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.token = $root.pb.Token.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetTokenResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetTokenResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetTokenResponse} GetTokenResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTokenResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetTokenResponse message.
         * @function verify
         * @memberof pb.GetTokenResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetTokenResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.token != null && message.hasOwnProperty("token")) {
                let error = $root.pb.Token.verify(message.token);
                if (error)
                    return "token." + error;
            }
            return null;
        };

        /**
         * Creates a GetTokenResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetTokenResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetTokenResponse} GetTokenResponse
         */
        GetTokenResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetTokenResponse)
                return object;
            let message = new $root.pb.GetTokenResponse();
            if (object.token != null) {
                if (typeof object.token !== "object")
                    throw TypeError(".pb.GetTokenResponse.token: object expected");
                message.token = $root.pb.Token.fromObject(object.token);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetTokenResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetTokenResponse
         * @static
         * @param {pb.GetTokenResponse} message GetTokenResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetTokenResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.token = null;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = $root.pb.Token.toObject(message.token, options);
            return object;
        };

        /**
         * Converts this GetTokenResponse to JSON.
         * @function toJSON
         * @memberof pb.GetTokenResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetTokenResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetTokenResponse
         * @function getTypeUrl
         * @memberof pb.GetTokenResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetTokenResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetTokenResponse";
        };

        return GetTokenResponse;
    })();

    pb.GetTokenRegistryResponse = (function() {

        /**
         * Properties of a GetTokenRegistryResponse.
         * @memberof pb
         * @interface IGetTokenRegistryResponse
         * @property {pb.ITokenRegistry|null} [registry] GetTokenRegistryResponse registry
         */

        /**
         * Constructs a new GetTokenRegistryResponse.
         * @memberof pb
         * @classdesc Represents a GetTokenRegistryResponse.
         * @implements IGetTokenRegistryResponse
         * @constructor
         * @param {pb.IGetTokenRegistryResponse=} [properties] Properties to set
         */
        function GetTokenRegistryResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null)
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetTokenRegistryResponse registry.
         * @member {pb.ITokenRegistry|null|undefined} registry
         * @memberof pb.GetTokenRegistryResponse
         * @instance
         */
        GetTokenRegistryResponse.prototype.registry = null;

        /**
         * Creates a new GetTokenRegistryResponse instance using the specified properties.
         * @function create
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {pb.IGetTokenRegistryResponse=} [properties] Properties to set
         * @returns {pb.GetTokenRegistryResponse} GetTokenRegistryResponse instance
         */
        GetTokenRegistryResponse.create = function create(properties) {
            return new GetTokenRegistryResponse(properties);
        };

        /**
         * Encodes the specified GetTokenRegistryResponse message. Does not implicitly {@link pb.GetTokenRegistryResponse.verify|verify} messages.
         * @function encode
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {pb.IGetTokenRegistryResponse} message GetTokenRegistryResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTokenRegistryResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.registry != null && Object.hasOwnProperty.call(message, "registry"))
                $root.pb.TokenRegistry.encode(message.registry, writer.uint32(/* id 1, wireType 2 =*/10).fork()).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GetTokenRegistryResponse message, length delimited. Does not implicitly {@link pb.GetTokenRegistryResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {pb.IGetTokenRegistryResponse} message GetTokenRegistryResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTokenRegistryResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        /**
         * Decodes a GetTokenRegistryResponse message from the specified reader or buffer.
         * @function decode
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {pb.GetTokenRegistryResponse} GetTokenRegistryResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTokenRegistryResponse.decode = function decode(reader, length, error) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.pb.GetTokenRegistryResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.registry = $root.pb.TokenRegistry.decode(reader, reader.uint32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetTokenRegistryResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {pb.GetTokenRegistryResponse} GetTokenRegistryResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTokenRegistryResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetTokenRegistryResponse message.
         * @function verify
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetTokenRegistryResponse.verify = function verify(message) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (message.registry != null && message.hasOwnProperty("registry")) {
                let error = $root.pb.TokenRegistry.verify(message.registry);
                if (error)
                    return "registry." + error;
            }
            return null;
        };

        /**
         * Creates a GetTokenRegistryResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {pb.GetTokenRegistryResponse} GetTokenRegistryResponse
         */
        GetTokenRegistryResponse.fromObject = function fromObject(object) {
            if (object instanceof $root.pb.GetTokenRegistryResponse)
                return object;
            let message = new $root.pb.GetTokenRegistryResponse();
            if (object.registry != null) {
                if (typeof object.registry !== "object")
                    throw TypeError(".pb.GetTokenRegistryResponse.registry: object expected");
                message.registry = $root.pb.TokenRegistry.fromObject(object.registry);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetTokenRegistryResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {pb.GetTokenRegistryResponse} message GetTokenRegistryResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetTokenRegistryResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.registry = null;
            if (message.registry != null && message.hasOwnProperty("registry"))
                object.registry = $root.pb.TokenRegistry.toObject(message.registry, options);
            return object;
        };

        /**
         * Converts this GetTokenRegistryResponse to JSON.
         * @function toJSON
         * @memberof pb.GetTokenRegistryResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetTokenRegistryResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetTokenRegistryResponse
         * @function getTypeUrl
         * @memberof pb.GetTokenRegistryResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetTokenRegistryResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/pb.GetTokenRegistryResponse";
        };

        return GetTokenRegistryResponse;
    })();

    return pb;
})();

export { $root as default };
